# Game Mechanics Audit — every economic & governance variable across both systems

> Date: 2026-07-31. Produced by a seven-lens parallel sweep over
> **game-amora** (`C:/Users/taren/Desktop/Amora/game-amora`) and
> **regen-civics** (`C:/Users/taren/Downloads/regen-civics-clean`):
> registry inventory, hardcoded economics, governance structure, and
> non-registry config planes, per repo. 261 file reads.
> This is the FIRST-STEP deliverable for the Game Mechanics initiative:
> the raw material for (a) the admin editing surface and (b) the public
> Game Mechanics page, per system.

## Headline numbers

| | game-amora | regen-civics |
|---|---|---|
| Already a registered, admin-tunable mechanic | **77** (full registry below) | **36** in the public snapshot + 48 tunable elsewhere |
| Should become a variable (currently hardcoded) | **13** | **33** |
| Mechanics living in OTHER config planes (module config, app docs) | **19** | — |
| Tunable but founder-only (not for open governance) | **9** | **11** |
| Deliberately fixed (operational/safety, not game rules) | **58** | **13** |
| Never-tunable invariants identified | 96 (merged, both systems) | |

**The asymmetry that defines the work:** game-amora has the better *registry*
(typed, bounded, delta-only, admin-editable — but invisible to members);
regen-civics has the better *surface* (a public GameMechanics page with
sliders, diff view, shareable state, and a "copy proposed changes" generator —
but its registry is a loose key/value table with fallbacks). The initiative is
each system adopting the other's strength, plus the Hypha loop on top.

## The three rings (proposed vocabulary for every value below)

- **Ring 0 — Constitutional.** Never tunable by anyone: conservation, one
  gate, fiat-in-only, witness rules' EXISTENCE. Enforced in code, listed
  publicly on the mechanics page as "the laws", not sliders.
- **Ring 1 — Founder-config.** Tunable, but not by open governance: legal
  posture (trading card version, legal acks), infrastructure (RPC URLs,
  secrets), abuse guards. Admin section only.
- **Ring 2 — Open mechanics.** The community-governable dials: budgets,
  pools, caps, multipliers, thresholds, cadences. These appear on the public
  Game Mechanics page and are the domain of the Hypha proposal loop.

Every "already-mechanic" and "make-variable" row below is implicitly Ring 2
unless marked founder-config (Ring 1). The registry's min/max bounds stay
Ring 0 — governance can move a value WITHIN its bounds, never the bounds.

---

# Part A — game-amora

## A1. The registered variable registry — 77 mechanics, admin-editable today, **no public page**

> Exact count: 77 VariableDefs in shared/gameVariables.ts (verified by grep on key lines), across 21 categories: Gratitude (6), Ledger (1), Quests (5), Governance (3), Tokens (4), Hypha (5), Tools (2), Accounts & sessions (2), Abuse guards (5), Data lifecycle (2), Village map (7), Forum (1), Feed (5), Stays (9), Exchange (5), Library (8), Payments (3), Village (1), Health (1), Badges (1), Platform (1). All are admin-editable today via one endpoint; none are publicly visible. STORAGE CONTRACT (server/lib/variables.ts): the game_variables table stores DELTAS ONLY — setVariable writes an override row only when the value differs from the platform default, and setting a value back to its default DELETES the row, so un-opinionated villages inherit future platform defaults on upgrade (explicitly contrasted in-file with regen-civics' seed-every-row approach, which needs a migration to change a default). Overrides are cached in memory, loaded fail-loud at boot (loadVariables), read synchronously on hot paths; the single async write lives in the admin endpoint. value_type is always written as literal 'text' regardless of def.type — the def, not the row, is the type authority. READY-MADE API FOR THE PUBLIC PAGE: allVariables() (variables.ts:61-73) already returns every def with value, parsed value, and isDefault flag — a public Game Mechanics page can be a filtered projection of exactly this, but it MUST filter: tokens.base_rpc_url, hypha.link_* overrides, and the five abuse.* rate limits are operational/security knobs whose public display is either noise or mildly attacker-useful; retention.* and auth.session_days belong on a privacy page more than a game page. Values are stored as strings and parsed by type (integer/decimal/percentage/boolean/choice/text); text validation includes 255-char cap, 0x-address regex for *_address keys, and https-only (loopback-exempt) for *_url keys. Two knobs are declared-ahead-of-enforcement policy contracts (stay.credit_expiry_days, library.dispute_deadline_days auto-settle) — the public page should not present them as enforced. platform.feedback_relay is an integer 0/1 masquerading as a boolean. current_value in findings = the platform default; live effective values require reading the game_variables table (deltas) and were not inspected (READ-ONLY audit, no DB access).

| key / name | label | current value | affects | where | why it is a mechanic |
|---|---|---|---|---|---|
| gratitude.base_budget | Base sending budget per cycle | 100 (integer, min 0, max 100000, unit Gratitude) | economics | shared/gameVariables.ts:60 | Sets each member's per-lunar-cycle Gratitude giving budget before stage multiplier. |
| gratitude.pool_per_cycle | Value pool distributed at each cycle close | 1000 (integer, min 0, max 1000000, unit tokens) | economics | shared/gameVariables.ts:72 | Size of the token pool split pro-rata by recognition received when a cycle closes; 0 = signal-only. |
| gratitude.pool_token | Which token the pool pays | "credits" (text) | economics | shared/gameVariables.ts:84 | Registry slug of the platform token the cycle pool pays; must never be the recognition token itself (signal/value separation). |
| ledger.admin_mint_cycle_cap | Admin mint cap per lunar cycle | 10000 (integer, min 0, max 10000000, unit tokens) | both | shared/gameVariables.ts:93 | Aggregate ceiling on manual admin minting per token per cycle; bounds admin power over issuance; 0 disables minting. |
| gratitude.max_per_recipient_per_cycle | Maximum sends to the same person per cycle | 1 (integer, min 1, max 100, unit sends) | economics | shared/gameVariables.ts:105 | Anti-clique cap on written acknowledgments to one recipient per cycle. |
| gratitude.require_message | Require a message with every acknowledgment | true (boolean) | governance | shared/gameVariables.ts:117 | Norm-setting toggle: Gratitude cannot be sent silently when on. |
| gratitude.cycle_mode | Cycle rhythm | "lunar" (choice: lunar \| month) | economics | shared/gameVariables.ts:126 | Chooses whether budgets/pools reset on new moons or calendar months — the economy's clock. |
| quest.consent_cap_mode | How much can be released at consent | "posted" (choice: posted \| capped \| unlimited) | both | shared/gameVariables.ts:141 | Governs whether admins can pay above a quest's posted amount — the quest board's honesty contract. |
| quest.consent_cap_multiplier | Bonus ceiling multiplier | 2 (decimal, min 1, max 100, unit x posted) | economics | shared/gameVariables.ts:155 | Max award as a multiple of posted amount when cap mode is 'capped'. |
| quest.require_submission_before_consent | Require submitted work before consent | true (boolean) | both | shared/gameVariables.ts:167 | When off, admins can credit un-filed work, breaking the credit-follows-shown-work promise. |
| quest.allow_zero_consent | Allow consenting at zero | false (boolean) | both | shared/gameVariables.ts:176 | Lets a claim complete with 0 recognition ('acknowledged, no recognition') while stay-credit rewards still release. |
| quest.self_consent_until_members | Founder may self-consent below this many members | 6 (integer, min 0, max 1000, unit members) | both | shared/gameVariables.ts:185 | Village size below which the witness rule is waived for founders/admins; 0 = self-consent never allowed. |
| governance.voice_weighting | How sensing is weighted | "equal" (choice: equal \| hypha-mirror) | governance | shared/gameVariables.ts:199 | Whether informal sensing displays one-person-one-voice or mirrors Hypha Voice holdings; never computes binding weight. |
| governance.hypha_threshold | In-app tokens needed to take a proposal to Hypha | 1000 (integer, min 0, max 10000000, unit Gratitude) | both | shared/gameVariables.ts:209 | Earned-recognition threshold that qualifies a member to propose on Hypha; 0 = anyone may propose. |
| governance.sensing_days | How long a topic stays open for sensing | 7 (integer, min 1, max 90, unit days) | governance | shared/gameVariables.ts:221 | Days a proposal collects perspectives before moving to decision. |
| tokens.equity_address | Equity token contract address on Base | "" (text, blank = hidden; validated as 0x+40 hex) | economics | shared/gameVariables.ts:235 | ERC-20 address whose balance is read-only displayed; blank hides the surface. |
| tokens.voice_address | Governance token contract address on Base | "" (text, blank = hidden; validated as 0x+40 hex) | governance | shared/gameVariables.ts:244 | ERC-20 address of the governance-weight token, read-only display. |
| tokens.show_economics_section | Show the economics section | false (boolean) | economics | shared/gameVariables.ts:253 | Toggles token balances and Gratitude flows on member profiles. |
| tokens.base_rpc_url | Base RPC endpoint | "https://mainnet.base.org" (text, https enforced except loopback) | economics | shared/gameVariables.ts:262 | Where on-chain balances are read from; failure shows nothing rather than a wrong number — infrastructure, weak candidate for the public page. |
| hypha.org_url | Your Hypha DHO address | "" (text, blank hides every Hypha surface) | governance | shared/gameVariables.ts:275 | Single home for the DHO URL; all governance deep links derive from it. |
| hypha.link_governance | Override: governance link | "" (text, blank derives from org URL) | governance | shared/gameVariables.ts:284 | Optional override when the DHO governance page is not at the org root. |
| hypha.link_proposals | Override: proposals link | "" (text, blank derives from org URL /agreements) | governance | shared/gameVariables.ts:292 | Optional override for the DHO proposals deep link. |
| hypha.link_treasury | Override: treasury link | "" (text, blank derives from org URL /treasury) | governance | shared/gameVariables.ts:300 | Optional override for the DHO treasury deep link. |
| hypha.link_members | Override: members link | "" (text, blank derives from org URL /members) | governance | shared/gameVariables.ts:308 | Optional override for the DHO members deep link. |
| tools.click_tracking | Count tool opens | true (boolean) | safety | shared/gameVariables.ts:318 | Village-level privacy choice: whether tool opens record click rows. |
| tools.link_check_days | Days between automatic link checks | 0 (integer, min 0, max 90, unit days; 0 = manual only) | safety | shared/gameVariables.ts:327 | Operational cadence for tool link checks; not a game rule, marginal for the public page. |
| auth.session_days | Signed-in session length | 30 (integer, min 1, max 365, unit days) | safety | shared/gameVariables.ts:343 | Session lifetime for new sign-ins; a security posture knob, not an economic rule. |
| notify.daily_email_cap | Most emails one member receives per day | 20 (integer, min 1, max 200, unit per day) | safety | shared/gameVariables.ts:355 | Rolling-24h email ceiling; notifications above it stay in-app only. |
| abuse.register_per_ip_hourly | Registrations per IP per hour | 30 (integer, min 1, max 1000, unit per hour) | safety | shared/gameVariables.ts:371 | Abuse throttle on registrations; publishing exact limits on a public page mildly aids attackers — consider omitting the abuse.* group from the public view. |
| abuse.login_ip_per_quarter_hour | Failed logins per IP per 15 minutes | 30 (integer, min 1, max 1000, unit per 15 min) | safety | shared/gameVariables.ts:383 | Bulk-abuse throttle on failed sign-ins from one address; same public-disclosure caveat as the other abuse guards. |
| abuse.login_account_per_quarter_hour | Failed logins per account per 15 minutes | 10 (integer, min 1, max 100, unit per 15 min) | safety | shared/gameVariables.ts:395 | The real brute-force bound (all IPs combined per account); too low enables deliberate lockouts. |
| abuse.password_reset_per_ip_hourly | Password-reset requests per IP per hour | 10 (integer, min 1, max 200, unit per hour) | safety | shared/gameVariables.ts:407 | Bounds using the reset flow as a mail cannon. |
| abuse.investor_docs_per_ip_hourly | Investor-packet requests per IP per hour | 3 (integer, min 1, max 100, unit per hour) | safety | shared/gameVariables.ts:419 | Throttles the lead-storing, email-sending investor docs endpoint. |
| retention.submissions_days | Keep handled form submissions for | 365 (integer, min 0, max 3650, unit days; 0 = forever) | safety | shared/gameVariables.ts:432 | Data-lifecycle retention for handled submissions carrying personal details (Costa Rica Law 8968 posture); unhandled ones never sweep. |
| retention.notifications_days | Keep read notifications for | 90 (integer, min 0, max 3650, unit days; 0 = forever) | safety | shared/gameVariables.ts:444 | Retention for read notifications; unread ones stay. |
| map.public_structure | Show the map's structure to visitors | true (boolean) | safety | shared/gameVariables.ts:458 | Privacy/transparency toggle: anonymous visitors see circles and seat counts, never names or faces. |
| map.concierge_enabled | Show the coordination concierge | true (boolean) | governance | shared/gameVariables.ts:467 | Toggles the routing bar that matches members to circles, roles and quests. |
| map.contact_daily_cap | Contact requests a member may send per day | 5 (integer, min 0, max 50, unit messages; 0 disables relay) | safety | shared/gameVariables.ts:476 | Outbound brake on the contact relay. |
| map.contact_recipient_daily_cap | Contact requests one person receives per day | 3 (integer, min 1, max 20, unit messages) | safety | shared/gameVariables.ts:487 | Inbound protection for busy role holders; overflow senders are pointed at open quests. |
| map.show_quests | Show open quests on the map | true (boolean) | governance | shared/gameVariables.ts:499 | Display toggle for quest satellites on the village map. |
| map.vacant_highlight | Highlight vacant seats | true (boolean) | governance | shared/gameVariables.ts:507 | Whether vacant roles pulse as an open call or render as plain rings. |
| map.contact_retention_days | Keep contact message bodies for | 180 (integer, min 0, max 3650, unit days; 0 = forever) | safety | shared/gameVariables.ts:515 | Retention for relay message bodies (personal correspondence); the contact event itself is kept. |
| forum.report_hide_threshold | Soft reports that auto-hide a post | 3 (integer, min 2, max 10, unit reports) | governance | shared/gameVariables.ts:529 | Community-moderation threshold: this many distinct soft reports auto-hide a thread pending moderation. |
| feed.category_slug | Which forum category the feed shows | "village-life" (text) | governance | shared/gameVariables.ts:543 | Points the feed lens at one forum category; the feed is not a second content store. |
| feed.show_system_events | Weave the village's own milestones into the feed | true (boolean) | governance | shared/gameVariables.ts:552 | Mixes system events (quests consented, arrivals) into the feed alongside posts. |
| feed.max_post_length | How much of a long post the feed shows | 600 (integer, min 120, max 4000, unit characters) | governance | shared/gameVariables.ts:563 | Feed truncation length; pure display, weak candidate for the public mechanics page. |
| feed.heart_amount | Recognition each heart sends | 1 (integer, min 1, max 5, unit recognition) | economics | shared/gameVariables.ts:576 | A heart is a real send from the cycle budget; bounded 1-5 so hearts stay a tap, not a transfer instrument. |
| feed.max_hearts_per_recipient_per_cycle | Hearts one person may send another per cycle | 5 (integer, min 1, max 20, unit hearts) | economics | shared/gameVariables.ts:588 | Per-recipient ceiling on the heart channel, separate from written acknowledgments, same budget. |
| stay.guest_booking_enabled | Guests can request stays | true (boolean) | economics | shared/gameVariables.ts:602 | Whether non-members may request accommodation or must join first. |
| stay.autopay_default | New stays autopay by default | true (boolean) | economics | shared/gameVariables.ts:611 | Whether a newly activated stay burns one credit per night automatically. |
| stay.autopay_post_hour | Hour nightly credits post (UTC) | 10 (integer, min 0, max 23, unit h UTC) | economics | shared/gameVariables.ts:620 | Scheduler posting hour for nightly stay charges; operational timing more than a game rule. |
| stay.low_balance_warn_nights | Low-balance warning threshold | 2 (integer, min 0, max 30, unit nights) | economics | shared/gameVariables.ts:632 | Notifies a guest when remaining credits cover this many nights or fewer. |
| stay.grace_nights | Grace nights below zero | 2 (integer, min 0, max 14, unit nights) | economics | shared/gameVariables.ts:644 | Nights a stay may keep posting after zero balance before autopay refuses; the debt is a visible negative balance. |
| stay.max_purchase_nights | Most nights purchasable at once | 60 (integer, min 1, max 365, unit nights) | economics | shared/gameVariables.ts:656 | Ceiling on a single stay-credit purchase, counted in nights at the posted rate. |
| stay.credit_expiry_days | Credit expiry (0 = never) | 0 (integer, min 0, max 3650, unit days) | economics | shared/gameVariables.ts:667 | Policy contract shipped ahead of enforcement — v1 does not yet sweep expired credits; flag as declared-but-unenforced on the public page. |
| stay.credits_transferable | Members can gift credits to each other | false (boolean) | economics | shared/gameVariables.ts:679 | Transferability switch for stay credits; the surface is future work, the token stays non-transferable until then. |
| stay.work_exchange_tag | Work-exchange quest tag | "work-exchange" (text) | economics | shared/gameVariables.ts:688 | Quests with this tag surface on the Stay page as ways to earn credits. |
| exchange.price_change_max_pct | Largest single price change | 20 (integer, min 0, max 1000, unit %; 0 removes the bound) | economics | shared/gameVariables.ts:699 | Bounds how far one posted exchange price may move from the previous, keeping price history a story not a cliff. |
| library.intake_award_pct | Intake award, % of appraisal | 75 (integer, min 0, max 100, unit %) | economics | shared/gameVariables.ts:713 | Donor's credit award as a share of appraised value; capped at 100 so the mint's front door never pays more than the shelf gained. |
| library.intake_member_cycle_cap | Intake credits per member per cycle | 500 (integer, min 0, max 100000, unit credits; 0 disables the cap) | economics | shared/gameVariables.ts:725 | Per-person throttle on the library intake mint per lunation. NOTE: here 0 disables the cap, unlike the fail-closed caps elsewhere. |
| library.intake_dual_signoff_over | Dual sign-off above (appraisal) | 200 (integer, min 0, max 100000, unit credits; 0 turns second signature off) | both | shared/gameVariables.ts:737 | Appraisal value above which a second steward must approve before credits mint. |
| library.escrow_pct | Borrowing escrow, % of value | 25 (integer, min 0, max 200, unit %) | economics | shared/gameVariables.ts:749 | Deposit locked while an item is out, returned at settle minus wear and damage. |
| library.usage_fee_pct | Usage fee per loan, % of value | 5 (integer, min 0, max 100, unit %) | economics | shared/gameVariables.ts:761 | Default wear fee into the library pool; also the dispute default outcome after deadline. |
| library.loan_days_default | Loan length | 14 (integer, min 1, max 365, unit days) | economics | shared/gameVariables.ts:773 | Days from pickup to due date. |
| library.dispute_deadline_days | Dispute deadline | 14 (integer, min 1, max 90, unit days) | both | shared/gameVariables.ts:784 | Days before a disputed return settles to the default outcome; v1 surfaces but does not auto-settle — another declared-ahead-of-enforcement contract. |
| exchange.swap_spread_bps | The village's share of each swap | 0 (integer, min 0, max 2000, unit bps) | economics | shared/gameVariables.ts:797 | Swap spread in basis points; a policy dial, not the safety mechanism — round-up on the give side guarantees no member profit even at 0. |
| exchange.order_expiry_hours | Abandoned card checkouts are released after | 48 (integer, min 0, max 720, unit hours; 0 disables release) | economics | shared/gameVariables.ts:809 | Pending-order release delay; must stay above 24h because a Stripe checkout stays payable that long, and pending orders block exchange-off and member exit. |
| exchange.swap_fiat_hold_days | Card-bought tokens settle before they can be swapped | 45 (integer, min 0, max 180, unit days; 0 disables the hold) | economics | shared/gameVariables.ts:821 | Chargeback-window freeze on swapping card-bought tokens. |
| exchange.swap_max_receive_per_order | Most a member can receive in one swap | 500 (integer, min 1, max 1000000, unit tokens) | economics | shared/gameVariables.ts:833 | Per-swap receive ceiling regardless of cycle caps. |
| payments.purchase_limit_per_order_usd | Largest single purchase (USD) | 1000 (integer, min 0, max 100000, unit USD; 0 disables the check) | economics | shared/gameVariables.ts:846 | Per-order fiat ceiling shared by ALL fiat modules; a compliance guardrail — consider whether open governance should be able to raise it, or whether it belongs founder-config-only. |
| payments.purchase_limit_30d_usd | 30-day purchase limit per member (USD) | 3000 (integer, min 0, max 1000000, unit USD; 0 disables the check) | economics | shared/gameVariables.ts:858 | Rolling 30-day fiat ceiling per member across every module; same compliance-guardrail caveat. |
| payments.purchase_limit_annual_usd | Annual purchase limit per member (USD) | 10000 (integer, min 0, max 10000000, unit USD; 0 disables the check) | economics | shared/gameVariables.ts:870 | Rolling 365-day fiat ceiling per member; same compliance-guardrail caveat. |
| village.pulse_max_entries | Village Pulse length | 30 (integer, min 10, max 200, unit entries) | governance | shared/gameVariables.ts:884 | How many recent happenings the public activity feed shows; the in-file comment records it was once a dead knob (route served hard-coded 30) and the default was reset to match reality when wired up. |
| health.alert_change_pct | Alert when a metric moves this much | 40 (integer, min 0, max 500, unit %; 0 turns alerts off) | governance | shared/gameVariables.ts:903 | Lunation-over-lunation metric-swing threshold that flags stewards, direction-agnostic. |
| library.intake_stall_days | Intake stall alarm | 7 (integer, min 1, max 60, unit days) | economics | shared/gameVariables.ts:917 | Days an item may await its second sign-off before surfacing in the stewards' digest — the donor is owed credits. |
| badges.max_featured | Featured badges per member | 3 (integer, min 0, max 10, unit badges; 0 turns featured chips off) | governance | shared/gameVariables.ts:931 | How many badges a member may pin to their byline across forum and map surfaces. |
| platform.feedback_relay | Share bug reports and ideas with the platform team | 1 (integer 0/1, min 0, max 1, unit on/off) | safety | shared/gameVariables.ts:945 | Privacy toggle for the feedback relay (content only, never identity); oddly typed as integer 0/1 rather than boolean — worth normalizing before public display. |

## A2. Hardcoded values that should BECOME variables — 13

| key / name | label | current value | affects | where | why it is a mechanic |
|---|---|---|---|---|---|
| brand.currency_symbol | Quote currency symbol default | "$" | economics | server/lib/exchange.ts:516 | Display identity that must match what Stripe actually charges; forks are told to override it but there is no config home — it belongs in the brand overlay beside the deployment currency. |
| Gratitude stage multipliers |  | visitor 0, guest/immersant/participant 1, member..initiate 2, co-creator/role-holder 3, guide 4, sage 5 | economics | shared/gameConfig.ts:216-227 (read via server/index.ts:1671) | These multiply gratitude.base_budget and are pure economics, yet live in the identity plane where only a fork edit changes them — a Game Mechanics page must show them and admins should be able to tune them per stage. |
| payments.donation_max_usd | Anonymous donation checkout ceiling | 5,000,000 minor units ($50,000) | economics | server/index.ts:5774 | The only economic bound on anonymous chooser-amount payments (assertCanPurchase applies to signed-in members only); a legitimate village-tunable ceiling that currently needs a deploy to change. |
| stay.request_daily_cap | Member stay-request daily cap | 5 per member per 24h | economics | server/index.ts:5161 | Caps a member's economic activity in a funds-bearing module while its siblings (map.contact_daily_cap, abuse.*) are registered variables — inconsistent plane for the same kind of rule. |
| library.reserve_daily_cap | Member library-reserve daily cap | 10 per member per 24h | economics | server/index.ts:7074 | Bounds how fast one member can lock shelf items and escrow — a library-economy rule a village might tune, hardcoded unlike every library.* sibling. |
| STAGE_UNLOCKS: which stage unlocks each capability |  | forum.post→member, quest.propose→contributor, proposal.open→co-creator, map.viewPeople→guest, map.contact→member, stay.member_rate→member, exchange.buy→member, exchange.swap→member | both | shared/capabilities.ts:65-77 | This IS the constitution's parameter table — which rung of the ladder opens speech, proposing, and buying — and a community should be able to move rungs (as choice variables constrained to valid stage ids); the exchange.* floors are safe to tune because safety lives in the deployment-level trading switch and fail-closed caps, per the file's own comment. |
| progression.quests_for_contributor / progression.quests_for_quest_seeker | Quest counts that earn stages (contributor=1, quest-seeker=3) | min: 1 and min: 3 | both | shared/gameConfig.ts:221-222 | These two integers are pure game-mechanics thresholds buried inside the identity file; a village tuning how fast people climb should not need a deploy. |
| progression.stage_multipliers (per-stage table) | Per-stage gratitudeMultiplier ladder | 0,1,1,1,2,2,2,2,3,3,4,5 (visitor→sage) | both | shared/gameConfig.ts:216-227 | Multiplies each member's cycle sending budget (server/index.ts:1671) — a first-order economic AND status parameter that is currently only editable by editing code. |
| Role capability sets and minStage floors (seeded roles) |  | founders-circle: proposal.open+decide, quest.consent, forum.moderate, quest.propose, forum.post (minStage null); steward-circle: quest.consent+propose, forum.post (member); treasury: proposal.open, forum.post (member); practitioners: forum.post (participant) | governance | server/seeds/roles-seed.json:6,14,22,30 (minStage :7,:15,:23,:31) | Role definitions are already data (seeded once into the roles table), but the only admin edit surface (PUT /api/admin/roles/:id, server/index.ts:4638) touches circleId and seats — capabilities, minStage, and role creation/deletion have NO edit path, so who-may-consent/moderate/decide is frozen at seed time; a validated role editor (capabilities restricted to ALL_CAPABILITIES) closes the gap. |
| gratitude.proposal_accept_award | Work With Us acceptance award (acceptGratitude) | 100 Gratitude, minted from the recognition faucet when a proposal is accepted (idempotent per submission) | economics | server/index.ts:467 (default), minted at :2000-2016, saved unvalidated via PUT /api/admin/work-with-us-config at :9073-9076 | This is a recognition-issuance amount hiding in a content document with no min/max validation; it belongs in the variables registry beside gratitude.* so it gets bounds, admin visibility, and a mechanics-page row. |
| progression.stage_multipliers | Stage gratitude multipliers (the ladder's economic weights) | visitor 0, guest/immersant/participant 1, member→initiate 2, co-creator/role-holder 3, guide 4, sage 5 | economics | shared/gameConfig.ts:216-227 (gratitudeMultiplier per stage), applied at server/index.ts:1671 | The per-stage multiplier on the cycle sending budget is a core economic rule that today requires a code deploy to change — it sits in the identity file while the base budget it multiplies is already a registered variable. |
| progression.stage_quest_thresholds | Stage advancement quest thresholds | contributor = 1 consented quest, quest-seeker = 3 | governance | shared/gameConfig.ts:221 (contributor: quests min 1), :222 (quest-seeker: quests min 3) | How many consented quests advance a member is a progression rule villages will want to tune, and it is hardcoded in the identity file. |
| progression.capability_unlocks | Capability stage unlocks (STAGE_UNLOCKS) | forum.post@member, quest.propose@contributor, proposal.open@co-creator, map.viewPeople@guest, map.contact@member, stay.member_rate@member, exchange.buy@member, exchange.swap@member | governance | shared/capabilities.ts:65-77 | Which stage unlocks which permission is the heart of the progression game and pure governance; it is code-only today and is exactly what a public mechanics page must display. |

## A3. Mechanics living in other config planes — 19

These are real game rules a member should be able to SEE (and governance
possibly move), but they live in module-config JSON, app_config documents, or
structural config rather than the variables registry. The public page needs to
read them from their own planes (or they migrate into the registry).

| key / name | label | current value | affects | where | why it is a mechanic |
|---|---|---|---|---|---|
| Trading opt-in + legal acknowledgment |  | moduleConfig("exchange").tradingEnabled + legalAck.cardVersion match | both | server/index.ts:7853-7857 | Per-deployment opt-in already admin-manageable; belongs on the public Game Mechanics page as the market's open/closed state. |
| Per-token swap caps (per-cycle and per-member) |  | token_exchange_settings.max_swap_out_per_cycle / _per_member_per_cycle, default 0 | economics | server/lib/exchange.ts:51-56 (schema), server/index.ts:8060-8106 (enforcement) | Admin-editable per token via PUT /api/admin/exchange/tokens/:slug; the 0=ZERO (never unlimited) convention is the invariant, the numbers are the mechanic. |
| Exit notice period |  | voluntary.noticePeriodDays = 30, in the exit-policy app_config document | governance | server/index.ts:663-692 (DEFAULT_EXIT_POLICY, used at 6703/6729) | Admin-editable via PUT /api/admin/exit-policy and published at /api/exit-policy; the mechanics page should read the same document rather than duplicating it. |
| Badge definitions and rules |  | per-badge kind, capabilities, denies, rule {metric, threshold, stackable, maxStack} | governance | server/lib/badges.ts:80-126 (badges table) | Fully admin-editable with a shared write/boot validator; the mechanics page should render active badges and their thresholds from this table. |
| Exit policy document (notice period, valuation, involuntary process, restorative intake role) |  | placeholder:true; voluntary.noticePeriodDays:30; involuntary.decidingDomainId/appealDomainId:''; restorative.intakeContactRole:'' | governance | server/index.ts:663-691 (edit surface :6649-6662) | Ships as an explicit placeholder because the TERMS are a community decision (Rye #8); admin-editable and published at /api/exit-policy — surface noticePeriodDays and the deciding/appeal domains on the Game Mechanics page, sourced from the doc, not the registry. |
| Per-module lifecycle settings |  | per-deployment; everything non-core ships off | governance | module_settings table (read via server/lib/modules.ts:42-71) | Which parts of the game exist for whom is already a deliberate, recorded admin act — belongs on the Game Mechanics page as the deployment's module posture. |
| Forum category taxonomy |  | village-life, projects, governance, questions | governance | shared/modules.ts:148-155 (defaultConfig, validated :156-169) | Per-module config JSON, validated and admin-editable — where the village talks and where decisions get filed is already data. |
| Module lifecycle posture (off/preview/members/public per module) |  | Every non-core module ships off (absent row = off); core quests/gratitude/progression/profiles always public | governance | shared/modules.ts:13 (ranks at :16; stored in module_settings, served via server/lib/modules.ts) | Which parts of the game exist and for whom is the top-level governance posture; already admin-editable via PUT /api/admin/modules/:id/lifecycle and should headline the public mechanics page. |
| Forum categories (where threads and decisions live) |  | village-life, projects, governance, questions (id/label/sortOrder; lowercase-slug validated, unique ids) | governance | shared/modules.ts:148-155 (defaultConfig), validated at :156-169, read at server/index.ts:4081 | The category list defines the village's deliberation structure (including the governance category the decision primitive lives in); admin-editable via module config and worth listing on the mechanics page. |
| Work With Us reciprocity option menu |  | Financial-Cash, Tokens, Joint Venture, Memorandum of Understanding (value/title/desc each) | economics | server/index.ts:460-465, fed to the AI guide at :8988 | The menu of exchange forms the village offers proposers is an economic-mechanics statement; already admin-editable in its doc and worth mirroring on the mechanics page. |
| Village dues (amount, period, currency, offset note) |  | amount: "" (blank = 'to be confirmed'), period: "month", currency: "$", note says dues can be offset through Gratitude | economics | server/index.ts:473-480 (DEFAULT_SETTINGS), public at GET /api/settings :9559, saved unvalidated at :9568-9574 | Dues are a headline economic rule already published via the public settings endpoint; the mechanics page should render this doc (note the PUT accepts arbitrary JSON with no validation). |
| Exit policy: voluntary notice period |  | noticePeriodDays: 30 (ships as placeholder: true until an admin writes real terms) | governance | server/index.ts:666 (default 30), enforced at :6703 and :6729 (noticeDays on createExit), editable at :6649, published at GET /api/exit-policy :6645 | A mechanically-enforced governance timer already published on /exit-policy; the mechanics page should link or embed it rather than duplicate it. |
| Exit policy: deciding/appeal domains and restorative intake role |  | All "" (unset) — process text says 'to be decided by the community' | governance | server/index.ts:678-679 (involuntary.decidingDomainId, appealDomainId), :684 (restorative.intakeContactRole, validated against roles at :6655) | Who decides an involuntary exit and who receives restorative intake are governance routing choices, admin-editable and published with the policy. |
| Season calendar (seasons list, goals, cadence, timezone) |  | 2 seeded seasons (Foundations 2026-06-21→09-22, Rooting 09-22→12-21), cadence solstice-equinox, timezone America/Costa_Rica | governance | server/index.ts:655 (season doc, default from shared/gameConfig.ts:245-275), computed at :1509 (seasonState), admin PUT at :9666, public at GET /api/season :9648 | Seasons carry the village's goal-setting rhythm and drive banners and launch checks ('season-seeded'); already admin-editable and public. |
| Per-quest advertised reward ranges |  | Ranges like 40–80 / 50–100 / 100–200 per quest; consent must land inside the range (posted mode) or under range.max × quest.consent_cap_multiplier (capped mode) | economics | server/seeds/quests-seed.json:9 (e.g. "gratitude": "50–100"), parsed by shared/questRewards.ts:36, enforced at server/index.ts:9943-9979 | The board is the contract: posted ranges are per-quest data, admin-edited and publicly displayed; the mechanics page should explain the range/cap-mode rule, with the cap knobs already in the registry. |
| Accommodation posted prices (per audience, per token) |  | Per-accommodation rows: {stay-credit, usd} × {guest, member}, active-flagged; missing member row means one price for all | economics | server/lib/stays.ts:101-107 (accommodation_prices read), :128-135 (priceFor with member→guest fallback) | Room pricing is admin-posted entity data already public on the stays surface; the mechanics page should state the pricing rules (audience fallback, 1 credit = 1 night) rather than the rows. |
| Exchange posted token prices (currency_prices) |  | Admin-posted price_minor per token slug, append-only history with note and set_by | economics | server/lib/exchange.ts:334-353 (setPrice; movePct guard against exchange.price_change_max_pct at :344) | Prices are posted data guarded by a registered max-move variable and already exposed via the public market endpoint; belongs on the mechanics page as 'prices are posted by the treasury role, moves capped at X%'. |
| Commerce products (price, token grant, recurrence) |  | Admin-defined products; settlement grants tokens from treasury stock, renewals refuse when product inactive | economics | server/index.ts:2664 (payment_products join: token_slug, token_amount, kind, recurring, active) | Every inbound payment type is a product row an admin defines; economic, public-facing, already in its right plane. |
| Role definitions (capabilities, minStage, seats) |  | 4 seeded roles; founders-circle holds full governance caps with minStage null | governance | server/seeds/roles-seed.json:6 (e.g. founders-circle carries proposal.decide, quest.consent, forum.moderate), table roles via server/index.ts:619-632 | Appointed-role capability grants are the second arm of the one gate and are data already; the mechanics page should render live roles+capabilities, not the seed. |

## A4. Tunable but founder-only (Ring 1) — 9

| key / name | label | current value | affects | where | why it is a mechanic |
|---|---|---|---|---|---|
| payments.currency | Deployment fiat currency | "usd" (createCheckout default and fiat_charges default) | economics | server/lib/payments.ts:84 and :459 | A real fork knob (non-US villages), but purchase limits are USD-denominated and changing currency mid-history would corrupt the 30d/annual sums — founder-level, never open governance. |
| gratitude.eligible_sender_rule | Sybil eligibility rule for breadth and pool payout | sender counts only if stage >= "member" OR >= 1 consented quest | both | server/index.ts:3941-3951 | A genuine game rule (who counts toward recognition breadth and pool shares) that the mechanics page must state, but loosening it reopens alt-farm inflation of real value — economy invariant 2.2 #9. |
| Anonymization scope (tombstone semantics) |  | value rows kept; identity scrubbed; admin audit rows retained id-only (Law 8968) | both | server/index.ts:1840-1950 | The scrub list is legal posture (data protection + accountability retention) — never open governance, and mostly not tunable at all; retention day-counts that ARE tunable already live in the registry (retention.*, map.contact_retention_days). |
| legalReview flags on funds-bearing modules (stays, exchange, commerce) |  | legalReview: true on all three | both | shared/modules.ts:206, 317, 348 | Which modules force the caution card is legal posture in code; ENABLING them is per-deployment founder work behind version-stamped acks, never a community vote. |
| exchange.tradingEnabled and library.creditSaleEnabled defaults |  | both false, 'OFF by default forever', require version-stamped legalAck/creditSaleAck | both | shared/modules.ts:322 and :270 | Gate B / L9 posture: a stale ack closes trading; these switches must stay behind the founder's caution card, out of any open-governance surface. |
| Exchange internal trading switch (tradingEnabled + legalAck) |  | tradingEnabled: false; enabling requires legalAck matching TRADING_CARD_VERSION "2026-07-27"; stale ack closes swapping | both | shared/modules.ts:322 (defaultConfig), server/index.ts:7854-7856 (tradingOpen), server/index.ts:3899-3929 (ack stamping) | Opening peer swap is a legal-posture decision per deployment behind a version-stamped caution card; display its on/off state publicly but never hand the switch to open governance. |
| Library credit sale switch (creditSaleEnabled + creditSaleAck) |  | creditSaleEnabled: false; requires creditSaleAck matching LIBRARY_CREDIT_CARD_VERSION "2026-07-27" | both | shared/modules.ts:270 (defaultConfig), server/lib/exchange.ts:146-148 (gate) | Selling library credits for fiat is the L9 caution-card opt-in with the same legal posture as trading; show the posture on the mechanics page, keep the switch founder-only. |
| Investor summary terms (projected IRR, structure, governance-weight claim) |  | Debt (secured notes), projected IRR 19.6%, most terms 'To be confirmed' | economics | server/index.ts:423-438 (DEFAULT_INVESTOR_SUMMARY; IRR 19.6% at :429; 'the more you contribute, the more governance weight you earn' at :433) | Published investment terms are legal posture, not open-governance tunables — and the governance-weight sentence makes a mechanics claim the mechanics page must not contradict. |
| FAQ economic/governance claims |  | Amora-specific copy asserting how Gratitude converts, dues, revenue share, and consent decision-making | both | server/index.ts:368-395 (DEFAULT_FAQS: fees stw-2/inv-7, revenue sharing pro-2, consent governance stw-1/inv-8) | Content, not mechanics — but it states economic promises ('Gratitude can convert to cash, equity, or currency') that can drift from the actual registered rules; the mechanics page should be the authority it defers to. |

## A5. Deliberately fixed — 58 (kept in code, with reasons)

| value | current | where | why it stays fixed |
|---|---|---|---|
| Allow-negative source whitelist | Set("stay_night", "payment_reversal") | server/lib/ledger.ts:144 | Deliberately static: the boot invariant that validates negative balances reads this same set, so runtime registration could race the check; extending it is meant to be a reviewed one-line keystone change. |
| Default recognition token slug | "gratitude" | server/lib/ledger.ts:49 | Renaming the slug would silently re-denominate ledger history; display names are already editable in the tokens table. |
| Pair-post deadlock retry policy | 3 attempts, 25ms*attempt + 0-25ms jitter | server/lib/ledger.ts:399-405 | Pure operational resilience tuning, not a game rule. |
| Never-listed token set | Set("library-credit") | server/lib/exchange.ts:101 | Static on purpose like the allow-negative list; the sanctioned exception is the L9 caution card, which opens purchase only and never swap. |
| Library-credit sale caution card version | "2026-07-27" | server/lib/exchange.ts:143 | The version stamp must ship with the card text it describes so a stale acceptance fails closed; making it editable would let config forge an acceptance. |
| Trading caution card version | "2026-07-27" | server/index.ts:258 | Same version-stamped-legal-card mechanism as the credit-sale card; bumping is intentionally a code+docs change that closes swapping until re-accepted. |
| Swap spread clamp (0-2000 bps) | Math.max(0, Math.min(2000, spreadBps)) | server/lib/exchange.ts:495 | Defense-in-depth bound mirroring the registered variable's max (exchange.swap_spread_bps, max 2000); the clamp is the safety rail, the variable is the dial. |
| Order-expiry floor for abandoned checkouts | ORDER_EXPIRY_FLOOR_HOURS = 25 (also passed to stays reaper at server/index.ts:2320-2323 and commerce reaper at 2349) | server/lib/exchange.ts:733 | The floor exists because a Stripe Checkout session stays payable ~24h; releasing earlier could take a member's money against a cancelled order — deliberately enforced in code rather than trusted to whoever edits exchange.order_expiry_hours. |
| Swap reaper stale window | 5 minutes | server/lib/exchange.ts:681 | Operational reconciliation timing for orders whose legs never posted; not a rule any village would tune. |
| Max quote quantity guard | 1,000,000,000 (quote) / 1,000,000 (buy qty) | server/lib/exchange.ts:497 and server/index.ts:7799 | Overflow/sanity guards, not policy; the real per-order ceiling is the registered exchange.swap_max_receive_per_order and payments.purchase_limit_per_order_usd. |
| Stripe signature replay tolerance | 300 seconds | server/lib/payments.ts:126 | Security constant from Stripe's v1 scheme; loosening it widens the replay window. |
| Webhook claim grace window | CLAIM_GRACE_MINUTES = 10 | server/lib/payments.ts:197 | Balances double-delivery races against healing crashed settlements; operational, with idempotent handlers absorbing any rerun. |
| Dispute/refund suspension policy | auto-suspend buyer on dispute or externally-initiated full refund; village-initiated refunds never suspend; partial refunds never reverse | server/lib/payments.ts:387-395 | Fraud-response posture whose edge cases (partial refunds, village-initiated refunds) were each hard-won bug fixes; tunability would reopen them, though the rule itself should be published on the mechanics page. |
| Legacy GAME_CONFIG.gratitude block | monthlyBudget 100, maxPerRecipientPerCycle 1, requireMessage true | shared/gameConfig.ts:230-233 | Appears unread anywhere (superseded by gratitude.* registered variables) — dead duplicate that risks drifting from the real values; candidate for deletion rather than exposure. |
| Fixed-price product minimum | 50 minor ($0.50) floor; default min donation 100 minor ($1.00) | server/index.ts:5884 and :5905 | Tracks Stripe's own minimum charge; per-product minimums are already admin data (min_amount_minor). |
| Product checkout per-IP rate cap | 10 per IP per hour | server/index.ts:5763 | Anonymous abuse guard on the payment door, same family as the hardcoded forum/upload limiters; the abuse.* variables cover the identity-probing endpoints where village size matters. |
| Stripe webhook per-IP flood cap | WEBHOOK_MAX_PER_MIN = 300 per IP per minute (in-memory, map cleared above 5000 entries) | server/index.ts:3130 | DB-amplification guard set far above real Stripe traffic; a 429 is retried by Stripe, so nothing is lost — pure operations. |
| Assistant global daily call cap | 600 calls/day, hardcoded at five call sites | server/index.ts:2247, call sites 4821/5661/6180/6459/8970 | API-cost runaway guard, not a game rule; if touched at all, deduplicate the literal into one constant. |
| Library fee defaults and escrow ceiling | cancelled/expired pay 0; default settle = computed wear (usage_fee_pct of value, ceil) + 0 damage; wear+damage hard-capped at escrow | server/lib/library.ts:404-413 | The percentages are registered variables; the escrow-as-ceiling and zero-fee-if-never-left rules protect the no-library-debt invariant ("liability beyond escrow is a human conversation") — publish them, don't tune them. |
| Stay-credit token registration | slug stay-credit, name "Stay Credits", kind credit, transferable:false re-asserted every boot | server/lib/stays.ts:33-43 | Boot unconditionally re-asserts transferable:false, which means the registered stay.credits_transferable variable (default false) is currently dead — flag the contradiction before anyone flips that variable expecting an effect. |
| Library-credit token registration | slug library-credit, name "Library Credits", transferable:false re-asserted every boot | server/lib/library.ts:42-52 | Non-transferability is the policy that keeps shelf-backed credits personal; identical boot re-assertion pattern to stay credits. |
| Nightly-posting runaway guard | 366 nights max catch-up | server/lib/stays.ts:189 | A year of catch-up means something else broke; operational circuit breaker. |
| Exit blocking-domain policy | loans/stays/exchange-orders/debts block resolution; positive balances, roles and warnings do not; positive balances sweep to sys:exit-settlement, never to treasury | server/lib/exit.ts:107-193 | Openstate-for-a-person semantics mirror module lifecycle; sweeping to a dedicated settlement account (not treasury) keeps departed value auditable — publish, don't tune. |
| Swap-resume justification minimum | resume note >= 20 characters | server/index.ts:8194 | "Narrowing the market is a hand, widening it writes a sentence" — governance friction whose exact character count is immaterial. |
| Product receipt sequence seed | COALESCE(MAX(receipt_no), 1000) + 1 | server/index.ts:2643 | Cosmetic receipt numbering start; the gapless FOR UPDATE discipline is the part that matters. |
| Earned-badge metric vocabulary | EARNED_METRICS = quests_consented, ledger_earned_total, gratitude_breadth; RECOGNITION_METRICS = gratitude_breadth | server/lib/badges.ts:29-31 | Adding a metric is code (each needs a settled-source query); badge definitions themselves (thresholds, stacking, capabilities) are already admin data in the badges table. |
| Capability union + ALL_CAPABILITIES list | 14 capability keys | shared/capabilities.ts:21-57 | The vocabulary of permissions is code (modules extend it); making keys data would fork the one-gate contract. |
| Gate order: admin → badgeDenies → role → badgeCapabilities → stage | fixed ordering (Gate E, S36: deny beats role) | shared/capabilities.ts:95-118 | The order of authority IS the policy; a tunable order would let a role trivially override a warning badge — publish it on the Game Mechanics page as constitution, never as a knob. |
| Stage ladder (12 stages, ids, order, earn rules) | visitor→sage; rules: default/account/granted/training-complete/membership/quests | shared/gameConfig.ts:215-228 | gameConfig is the fork-time white-label swap point (identity, per its header); the ladder's shape is constitutional structure per deployment — renaming/reshaping is a fork decision with a deploy, not a runtime dial. |
| GAME_CONFIG.gratitude block (monthlyBudget 100, maxPerRecipient 1, requireMessage true) | 100 / 1 / true | shared/gameConfig.ts:230-234 | Duplicate of the registered variables gratitude.base_budget / max_per_recipient_per_cycle / require_message which win at runtime — flag it as a shadow seed so the Game Mechanics page lists the variable, not this literal. |
| Role seat count clamp | seats clamped to 1..20 | server/index.ts:4650 | Sanity bound on the seats editor, not a game rule; the seat count itself is already admin-editable data. |
| Account role ladder (member / admin / founder) | three fixed account roles | server/index.ts:3764 | Template concept every fork inherits (S1/S2); operator authority tiers are platform structure, distinct from the data-driven village roles. |
| Only a founder may change account roles | actor.role must be founder | server/index.ts:3760-3762 | Admins run the village, founders run the admins — making this tunable would let an admin group vote itself founder powers. |
| Last-founder guard | demoting the last founder returns 409 | server/index.ts:3770-3774 | Bootstrap is one-shot and spent; a deployment must never strand itself without a master admin. |
| Founder cannot exit / be exited before demotion | 409 on both the self-service and admin-opened paths | server/index.ts:6695-6696 and 6721-6722 | Same stranding invariant as the last-founder guard, applied to the exit flow. |
| Consent actor gate (quest.consent via the ONE gate) | admin OR hasCapability('quest.consent') | server/index.ts:9838-9850 | Value release is human-gated through the single capability gate; WHO holds quest.consent is already tunable via roles/badges — the gate itself is structure. |
| No-self-consent witness rule | claim.userId === actor.userId refused, except the audited solo-founder window | server/index.ts:9866-9895 | Consent mints recognition from the faucet; without a witness rule a steward could claim, submit, and pay themselves — the only escape hatch is already the registered variable above. |
| Decision-thread gates (proposal.open to open, proposal.decide to record) | capability-gated via hasCapability | server/index.ts:4174 and 4437 | Mechanism, not parameter — the tunable part is which stage/role grants those capabilities (STAGE_UNLOCKS and role sets above). |
| Steward appointment via proposal.decide (F5) with three guards | non-admin deciders: no self-appointment (10684), no removals (10687), may only seat into roles whose every capability they already hold (10690-10699) | server/index.ts:10660-10699 | The subset guard closes the two-hop self-promotion (open unverified registration makes a second account cost one request); weakening any of the three guards converts a recording capability into capture. |
| Appointments respect role minStage | add refused below role.minStage | server/index.ts:10716-10723 | Enforcement mechanism; the minStage values themselves are the tunable (see role editor gap). |
| Restorative intake rate limit | 3 intakes per member per 24h | server/index.ts:6816 | Abuse guard on a private channel, not a game rule; other abuse guards are variables but this one bounds a sensitive human process and 3/day is already generous. |
| Exit resolve blocked by open economic state | tombstone refused while any blocking domain has open state | server/index.ts:6776-6782 (server/lib/exit.ts) | Exit never invents a settle path — each domain settles through its own terminals first; tunability would let value evaporate with the leaver. |
| Module lifecycle ladder and semantics | off < preview < members < public; absent row = off; preview 404s identically for non-admins | shared/modules.ts:13-21 (enforced server/lib/modules.ts:78-84) | The lifecycle vocabulary is platform structure; the per-module lifecycle VALUE is already an admin mechanic in module_settings. |
| Core modules undisableable (quests, gratitude, progression, profiles) | core → always served public | shared/modules.ts:28-29 + server/lib/modules.ts:81 | The game the platform is born playing; disabling progression or gratitude would orphan the capability gate and the economy. |
| Module hard dependencies (e.g. feed requires forum) | requires: ['forum']; missing dependency demotes to off at boot | shared/modules.ts:177 (reconcile server/lib/modules.ts:108-118) | Architectural fact (the feed is a lens over forum threads), not a policy choice. |
| Founder bootstrap rate limit and one-shot semantics | 5 attempts/IP/hour; shared password authenticates nothing after first founder exists | server/index.ts:3490-3519 | Security bootstrap, not a game rule; its expiry is the mechanism that retires the shared password. |
| Trading caution-card version | TRADING_CARD_VERSION = "2026-07-27" | server/index.ts:258 | Bumping the version is how amended legal terms force re-acceptance; it must stay code-owned or the re-read guarantee dies. |
| Library credit caution-card version | LIBRARY_CREDIT_CARD_VERSION = "2026-07-27" | server/lib/exchange.ts:143 | Same re-acceptance mechanism as the trading card; code-owned by design. |
| Tools Hub categories | governance, communication, documents, coordination | shared/modules.ts:372-379 (defaultConfig), read at server/index.ts:4921 | Navigation taxonomy only — admin-editable in its plane but not a rule of the game; does not need the public mechanics page. |
| Automation ready-queue cap (maxReadyQueue) | 15 (integer 1..100) | shared/modules.ts:223 (default 15, bounds 1-100 at :226); hardcoded fallback 15 duplicated at server/index.ts:6388 and :6454 | An operational throttle on AI-suggested tasks awaiting human publish, not a game rule; note the literal 15 fallback is duplicated in two server sites and can drift from the module default. |
| Automation publish target (forumCategory) and youtubeChannelId | forumCategory: "village-life", youtubeChannelId: "" | shared/modules.ts:223; read at server/index.ts:6572 and :2544 | Structural routing/integration wiring, admin-editable in its plane; not a mechanic to publish. |
| Map circles source | circlesSource: "platform" | shared/modules.ts:136 | Structural choice of where the org chart's circles come from; not a tunable game rule. |
| Funds-bearing legalReview flags (stays, exchange, commerce) | legalReview: true on stays, exchange, commerce | shared/modules.ts:206, :317, :348 | Which modules must show the caution card before enabling is platform safety policy, never per-village tunable. |
| sellsToken declaration (stays sells stay-credit) | sellsToken: "stay-credit" on stays only | shared/modules.ts:209 | Enforces economy invariant #3 (one selling module per token, boot-asserted); structural, not a knob. |
| Email config (pathway routing addresses, sender, assistant key slots) | All blank; keys now live in the write-only secrets store, sender falls back to EMAIL_FROM | server/index.ts:342-356 (DEFAULT_EMAIL_CONFIG), merged at :1976-1985, admin PUT at :8876 | Operational routing and integration wiring, not a game rule; keep off the mechanics page. |
| Visit types and costs | Community Call (Free), Land Tour (Details TBD), Immersion (Details TBD) | server/index.ts:408-421 (DEFAULT_VISIT_CONFIG) | Marketing content with cost strings, admin-editable in its doc; not a game mechanic. |
| Legacy GAME_CONFIG.gratitude block | monthlyBudget 100, maxPerRecipientPerCycle 1, requireMessage true | shared/gameConfig.ts:230-234 | Shadow duplicate of registered variables gratitude.base_budget / max_per_recipient_per_cycle / require_message — appears unread by live server paths; a divergence trap the synthesizer should flag for deletion rather than tuning. |
| Core module flags (quests, gratitude, progression, profiles) | core: true — cannot be disabled in v1, always served public | shared/modules.ts:69 (and :80, :98, :109) | Which modules are the undisableable spine is platform architecture; publish it as fact on the mechanics page, never as a knob. |

---

# Part B — regen-civics

## B1. The public mechanics snapshot — 36 entries (already on the GameMechanics page)

> Pattern notes from the auditor: THE MODEL BEING REPLICATED (end-to-end pattern): (1) STORE — MySQL table game_variables: category, subcategory, key, displayName, description, value DECIMAL, valueType (integer\|percentage\|multiplier\|currency\|decimal\|boolean), unit, minValue, maxValue, defaultValue, isActive, updatedBy. Seeded by SQL migrations (drizzle/0097, 0099, 0100 core; 0166 display-name/description/unit backfill; 0170+0173 evolution; 0173 gratitude budget; 0174 legacy-flag descriptions) and idempotent upsert scripts (scripts/seed-crowdpool-config.ts; a seed-bounty-valuation.ts is referenced by bountyValuation.ts:80). (2) READ — server/game/index.ts getGameVariable(key) [throws if missing; callers wrap with hardcoded fallbacks] and getGameVariables(keys) [silently skips missing], both SELECT ... WHERE key AND isActive=1 with Redis cache gv:<key> TTL 300s; invalidateGameVariable busts per-key. Several modules bypass this with raw SQL (songs.ts, regenGuide.ts, forum.ts, forumFeed.ts, orgRatings.ts, proposals.ts, evolution.ts, assembly.ts, governanceJobs, assemblyLifecycleJob) — no cache, no isActive filter, per-file duplicate readVar helpers with per-file fallbacks (source of the sensing_min_citizen_tier 0-vs-1 fallback drift). (3) WRITE — ONE shared path: applyVariableChange in server/lib/evolution.ts (bounds enforced from minValue/maxValue, append-only game_variable_history row with previousValue/newValue/changedBy/reason/proposalId, cache bust). Two callers: trpc game.updateVariable (adminProcedure, requires reason; server/routes/game.ts:75-95) and the Evolution Engine ratification dispatcher dispatchExecution (community proposal -> Hypha vote -> confirmed outcome -> variable_change payload applied by bot user 'bot:governance-engine'). Bounds changes go through applyBoundsChange with the same discipline. (4) PUBLIC SNAPSHOT — shared/gameMechanics.ts declares MECHANICS_VARIABLE_FALLBACKS (34 keys) + structural constants (TIER_BONUS, RITES_OF_PASSAGE_COUNT, PLAYER_STEWARD_QUEST/VOTE_COUNT from shared/questPools.ts; LUNAR_CYCLE_DAYS; SEEDS_REGEN_PER_USD); pure buildGameMechanicsSnapshot(vars, generatedAt) merges live values over fallbacks into a typed GameMechanicsSnapshot; served by public trpc game.getMechanics (server/routes/game.ts:26-29); consumed via client/src/hooks/useGameMechanics.ts (5-min staleTime) by GameMechanics.tsx, Game.tsx, ClaimSeeds.tsx. Unit-tested in server/gameMechanics.test.ts (live-over-fallback, fallback correctness, structural constants, key/fallback parity). (5) PUBLIC PAGE + ADMIN UI — /game-mechanics (client/src/pages/GameMechanics.tsx): trpc.game.listVariables is PUBLIC and returns ALL rows (value, bounds, description), so the page renders every registered variable with HelpTips from DB descriptions and slider bounds from minValue/maxValue; inline edit appears only for SUPER_ADMIN_EMAIL client-side, enforced by adminProcedure server-side, every edit requires a reason logged to history; there is also client/src/components/admin/AdminGameVariables.tsx using the same two procedures, and assembly RaiseModal.tsx lists variables when raising a variable_change proposal. The page adds: previous-season ghost values from seasonSnapshots (game.previousVariables), a server-backed bounty valuation simulator (bounties.simulateValuation calls the REAL engine), per-section mini-simulators that emit copy-paste proposal markdown, and 'Export as Proposal' deep-links into /assembly — closing the loop simulate -> propose -> ratify -> auto-apply. KEY GAP FOR THE INITIATIVE: the engine reads roughly 75 distinct keys; the typed snapshot covers 34, and of those 34 about 12 are not actually read by any server runtime (quest/forum scoring weights, harvest splits, harvest multipliers, trust_graph, composting rate, legacy gratitude multipliers) while the live economy (gratitude budget engine, bounty valuation/learning, trust weights, citizenship extra criteria, project ladder, all governance process dials) is absent from the snapshot and reaches the page only through the untyped listVariables dump. Concrete drift bugs found: harvest.split.projects/stewards (snapshot) vs harvest.split.bffs/orgs (DB) key mismatch; legacy gratitude.multiplier.* rendered as tier gratitude multipliers while the engine uses gratitude.budget_multiplier.*; bounty tier-name mismatch small/medium/large/epic (snapshot) vs trivial/small/medium/large (engine) with fallbacks shifted one tier; endorsement key bug (both branches of game.ts:196-198 use endorsement_from_player); crowdpool reminder fallback 2 vs seed 3; sensing gate fallback 0 vs 1 across forum.ts/forumFeed.ts. Dynamic key families to remember when building a registry: governance.claim_threshold_<tokenType>, bounty.tier.<tier>.base / .delivery, bounty.season_budget.<circle>. Recommended replication: put every engine-read key into MECHANICS_VARIABLE_FALLBACKS (the parity test then enforces it), route ALL reads through getGameVariable/getGameVariables (kill the raw-SQL readers and their divergent fallbacks), and keep the single applyVariableChange write path.

| key / name | label | current value | affects | where | why it is a mechanic |
|---|---|---|---|---|---|
| Quest/forum scoring weights (scoring.weights.quest_routine=10, forum_post=5) |  | 10 / 5 | economics | shared/gameMechanics.ts:36-37 | In the snapshot and admin-editable, but NO server code reads them at runtime (no recordScoreEvent call uses these keys), so they are currently display-only dials that need wiring to the scoring engine. |
| Citizenship tier percentile/season thresholds + grace period (7 keys) |  | co_creator 15/2, steward 50/3, sage 90/6, grace 30d | governance | shared/gameMechanics.ts:39-45; engine read server/lib/citizenship.ts:20-42 | Fully wired: snapshot, public page, admin edit, and the nightly tier engine all read the same game_variables keys. |
| Citizenship harvest multipliers (co_creator 1.5x, steward 2.0x, sage 3.0x) |  | 1.5 / 2.0 / 3.0 | economics | shared/gameMechanics.ts:46-48; seeded drizzle/0100_seed_citizenship_powers.sql:46-49 | Registered and shown publicly, but no server code reads them — the seasonal Harvest distribution engine is not implemented yet, so these are promises, not live mechanics. |
| Legacy gratitude tier multipliers (gratitude.multiplier.co_creator/steward/sage) |  | 1.2 / 1.5 / 2.0 | economics | shared/gameMechanics.ts:50-52; legacy note drizzle/0174_gratitude_var_descriptions.sql:8,33-36 | DRIFT BUG: the snapshot renders these LEGACY trust-weighting values as each tier's 'gratitude multiplier' on the public tier cards, while the live budget engine uses gratitude.budget_multiplier.* (1/2/3/5) — the same page shows two different numbers for the same concept. |
| Gratitude trust-graph weight + max bonus, trust composting rate |  | 0.1 / 0.5 / 5% | economics | shared/gameMechanics.ts:53-54,74 | In the snapshot and rendered on the public page, but no server runtime reads gratitude.trust_graph.* or trust.composting_rate anywhere — display-only until the trust-graph bonus and composting jobs are built. |
| Token claim thresholds (regen 1000, rgvoice 20, rcivics 1000, rcvoice 20) |  | 1000/20/1000/20 | both | shared/gameMechanics.ts:56-59; engine reads server/routes/governance.ts:355-358, server/routes/players.ts:630-633, server/routes/gratitude.ts:247-249 | The model working as designed: snapshot, engine gate, and public page all read the same keys (players.ts builds the key dynamically as governance.claim_threshold_<tokenType>). |
| Harvest split percentages (contributors/projects/stewards/treasury) |  | 30/20/20/30 | economics | shared/gameMechanics.ts:61-64 vs seeds drizzle/0099_seed_citizenship_trust_harvest.sql:76-79 | DRIFT BUG: the snapshot reads harvest.split.projects and harvest.split.stewards but the DB seeds harvest.split.bffs and harvest.split.orgs — two of four snapshot keys do not exist in the table, so admin edits to the real rows never reach the public page (values only coincide by luck), and no engine code reads any of them yet. |
| Bounty tier base rewards in snapshot (small 25, medium 75, large 250, epic 750) |  | 25/75/250/750 | economics | shared/gameMechanics.ts:66-69 vs engine server/db/bountyValuation.ts:30,82-89,158-170 | DRIFT BUG: the valuation engine's tiers are trivial/small/medium/large (defaults 25/75/250/750) — the snapshot exposes an 'epic' tier the engine never reads, omits 'trivial' which it does, and its per-tier fallbacks are shifted one tier relative to engine defaults. |
| Bounty rounding step (bounty.round_to=25) |  | 25 | economics | shared/gameMechanics.ts:70; engine server/db/bountyValuation.ts:132-135 | Correctly wired end to end: snapshot, valuation engine, and public simulator all read the same key. |
| Play adoption reward (plays.adoption_reward=500) |  | 500 | economics | shared/gameMechanics.ts:72; engine server/routes/plays.ts:227 | Wired end to end; engine falls back to the seeded value when the variable is missing. |
| Hymn submission winner reward (hymnSubmissionWinnerReward=3333) |  | 3333 | economics | shared/gameMechanics.ts:73; engine server/routes/songs.ts:37 | Wired, but songs.ts reads it via raw SQL bypassing getGameVariable (no cache, no isActive filter) and the key breaks the dot-namespace convention — worth normalizing to e.g. rewards.hymn_winner. |
| Gratitude budget engine variables (11 keys: base_budget 100, full_power_threshold 10, streak_bonus_per_cycle 0.03, streak_bonus_max 0.3, budget_multiplier.explorer/co_creator/steward/sage 1/2/3/5, pool_per_cycle 10000, max_payout_per_person 1000, claim_threshold 1000) |  | see list | economics | server/lib/gratitude-cycles.ts:129-158 | This is the LIVE token-emission economy and every key is MISSING from MECHANICS_VARIABLE_FALLBACKS — the typed snapshot exposes none of the real gratitude engine (the public page only shows them via the untyped listVariables path). |
| Extra citizenship tier criteria (8 keys: co_creator.min_fire_quest/min_rites/min_gratitude_sent, steward.min_epic_quests/min_endorsements_project/min_gratitude_received, sage.min_contributions/min_endorsements_total) |  | fallbacks: gratitude_sent 5, endorsements_project 1, gratitude_received 10, contributions 100, endorsements_total 10 | governance | server/lib/citizenship.ts:20-40, qualification logic :63-92 | The tier engine reads all 8 but they are MISSING from the snapshot, so the public Citizenship Tiers page understates the real promotion requirements; note min_fire_quest, min_rites and min_epic_quests are loaded but never used in the current qualification checks (dead reads). |
| Trust score formula weights (7 keys: trust.weight.endorsements_from_projects 4, endorsements_from_players 1, account_age_seasons 0.5, quests_completed 0.2, gratitude_received 0.3, flags_validated -5, contribution_percentile 0.01) |  | see list | economics | server/routes/batchJobs.ts:80-117 | The whole 7-input trust formula is variable-driven but MISSING from the snapshot, so the public mechanics page never explains how trust (a score multiplier) is computed. |
| Land project status ladder (7 keys: project.accepted_endorsements 1, active_endorsements 3, active_contributions 5, established_funded_campaigns 1, established_seasons 3, anchor_seasons 8, anchor_endorsements 20) |  | see list | governance | server/routes/batchJobs.ts:264-308 | Engine-read project progression thresholds (applied->accepted->active->established->anchor) entirely MISSING from the snapshot and public page. |
| Endorsement scoring weights (scoring.weights.endorsement_from_player, scoring.weights.endorsement_given) |  | read live, no code fallback | economics | server/routes/game.ts:196-201 | Engine reads them on every endorsement but they are MISSING from the snapshot; also note game.ts:196-198 has a bug where both player and project endorsements resolve to the same key. |
| Crowdpool contribution points (scoring.weights.crowdpool_contribution=20) |  | 20 | economics | server/routes/campaigns.ts:125-128,707-710; seeded scripts/seed-crowdpool-config.ts:31-39 | Engine-read recognition points (fires on fulfillment only, never mints tokens) — MISSING from the snapshot. |
| Crowdpool claim expiry windows (crowdpool.claim_expiry_days_item 14 / _shift 7 / _loan 14, crowdpool.reminder_days_before_expiry 3) |  | 14/7/14/3 | governance | server/routes/campaigns.ts:35-39; server/routes/batchJobs.ts:503; seeded scripts/seed-crowdpool-config.ts:41-79 | Engine-read but MISSING from the snapshot; small drift: the code fallback for reminder_days is 2 while the seed value is 3. |
| Bounty settlement hold (bounty.settlement_hold_hours) |  | variable; fallback 708h (29.5 days) | economics | server/db/bounties.ts:48-54 | Engine-read anti-fraud hold before bounty tokens become claimable — MISSING from the snapshot. |
| Bounty season budget cap (bounty.season_budget, plus dynamic bounty.season_budget.<circle>) |  | unset = no cap (Infinity) | economics | server/db/bounties.ts:56-62; server/db/bountyValuation.ts:243-254; server/routes/bounties.ts:924 | The hard ceiling on bounty emission per season, engine-read in three places including a dynamic per-circle key family — MISSING from the snapshot (the page shows it via valuationInfo instead). |
| Bounty proposal fraction (bounty.proposal_fraction) |  | fallback 0.15 | economics | server/db/bounties.ts:325; server/routes/bounties.ts:527 | The proposer's cut of a bounty (delivery x fraction), engine-read but MISSING from the snapshot. |
| Bounty tier delivery amounts (dynamic bounty.tier.<trivial\|small\|medium\|large>.delivery) |  | read live; fallback 0 | economics | server/db/bounties.ts:319-334 | A second per-tier amount family (delivery vs base) the engine reads dynamically — MISSING from the snapshot and easy to miss because the key is template-constructed. |
| Large-bounty citizenship floor (bounty.large_tier_min) |  | fallback 0 (everyone qualifies) | governance | server/db/bounties.ts:340-357 | Gates who may take large bounties by citizenship tier index — engine-read, MISSING from the snapshot. |
| Bounty valuation weights (bounty.tier.trivial.base 25, impact.low/normal/high 0.75/1.0/1.5, priority.boost 1.25, anchor.weight 0.25, bounty.max 2000) |  | see list | economics | server/db/bountyValuation.ts:82-89,158-193 | The full deterministic pricing formula's inputs including the hard per-bounty cap (bounty.max) are engine-read but MISSING from the snapshot; the page renders them only via the generic category='bounty' listVariables filter. |
| Bounty demand-learning dials (bounty.learning.window_days 45, unclaimed_days 14, raise_sensitivity 0.15, lower_sensitivity 0.05, factor_min 0.9, factor_max 1.4) |  | see list | economics | server/jobs/coordinationFlywheel.ts:388-394; defaults server/db/bountyValuation.ts:91-98 | These bound how far the self-learning engine can move prices (factor_min/max are the leash) — engine-read, MISSING from the snapshot. |
| Stale bounty-claim nudge/expire windows (coordination.stale_claim_nudge_days 7, coordination.stale_claim_expire_days 14) |  | 7 / 14 days | governance | server/jobs/coordinationFlywheel.ts:38-39,71-72 | Engine-read claim lifecycle timing, MISSING from the snapshot. |
| Assembly promotion + signal-advance dials (governance.promotion.min_thread_age_hours 48, min_unique_voices 3, cosigner_window_hours 24, signal_advance_points 12, signal_advance_avg 1.0) |  | see list | governance | server/routes/assembly.ts:172-175,270-275; server/routes/governance.ts:39-41,81-82,183 | The thresholds for a sensing thread to become a proposal and for signals to advance it — engine-read, MISSING from the snapshot. |
| Governance lifecycle cadence (governance.last_call_hours 48, minor_lane_quiet_days 7, resting_after_days 30, closing_soon_window_hours 48, sunset_renewal_warning_days 7, synthesis_refresh_cooldown_min 30) |  | see list | governance | server/jobs/assemblyLifecycleJob.ts:40,67,86; server/jobs/governanceJobs.ts:61,127; server/routes/assembly.ts:375,830,924 | The clocked heartbeat of the governance process — all engine-read, all MISSING from the snapshot. |
| Governance access gates (governance.signal_min_tier 0, governance.sensing_min_citizen_tier, governance.delegation_is_active 0) |  | 0 / seeded / 0 (delegation off) | governance | server/routes/assembly.ts:855; server/routes/forum.ts:924-926; server/routes/forumFeed.ts:168-169; server/routes/governance.ts:755 | Who may participate — engine-read, MISSING from the snapshot, and the sensing gate has a fallback drift bug: forum.ts defaults missing to 0 (open) while forumFeed.ts defaults to 1 (gated). |
| Governance content and load bounds (governance.storyteller_narrative_min_words 300 / max_words 600, governance.load.warning_threshold 15 / critical_threshold 30) |  | 300/600 words; 15/30 proposals per 30 days | governance | server/routes/governance.ts:589-590,935-937 | Engine-read, MISSING from the snapshot. |
| ReGen Guide AI bot dials (governance.guide.proactive_posts_per_week 5, governance.guide.devil_advocate_unanimity_pct) |  | 5 posts/week | governance | server/lib/regenGuide.ts:46-50,132 | Rate-limits an AI actor's participation in governance discourse — engine-read via raw SQL, MISSING from the snapshot. |
| Proposal signal threshold (proposals.signal_threshold) |  | fallback 25 | governance | server/routes/proposals.ts:130-134 | Signal-vote count that flips a proposal's status — engine-read via raw SQL, MISSING from the snapshot. |
| Org rating publication threshold (org_rating.min_raters) |  | fallback 5 | governance | server/routes/orgRatings.ts:90-98 | Minimum raters before an org's rating becomes public — engine-read, MISSING from the snapshot. |
| Referral reward variables (referral.reward.signup 5, first_quest 10, seasonal_rite 15, crowdpooling 25, second_degree 5; caps max_rewards_per_month 50, max_second_degree_per_month 10) |  | see list | economics | drizzle/0099_seed_citizenship_trust_harvest.sql:87-93 | Seeded, described, and publicly listed — but no server code reads any referral.* key, so the entire referral reward economy is a dormant promise. |
| Seasonal harvest pool + composting decay (harvest.pool_size 10000, composting.decay_rate 10%) |  | 10000 $ReGen / 10% | economics | drizzle/0097_seed_game_variables.sql:43,49; client-only reads client/src/pages/GameMechanics.tsx:598-599 | Seeded and rendered in the public simulator, but no server job implements harvest distribution or composting — dormant dials that the page presents as live mechanics. |
| Living Tree cosmetic dials (living_tree.trunk_max_width, longevity_threshold, flower_scale, fruit_min_points) |  | page fallbacks 120px / 10 seasons / 1.0x / 50 pts | economics | client/src/pages/GameMechanics.tsx:2343-2376 | Client-rendered visualization dials read from game_variables via listVariables; harmless as tunables and already in the proposal flow. |

## B2. Tunable today but NOT on the public page — 48

Read from game_variables (or equivalent) by the engine, but missing from
MECHANICS_VARIABLE_FALLBACKS, so the public page doesn't show them.

| key / name | label | current value | affects | where | why it is a mechanic |
|---|---|---|---|---|---|
| seeds.regen_per_usd | SEEDS -> $ReGen claim rate | 10 $ReGen per $1 USD (fallback SEEDS_REGEN_PER_USD=10 in shared/gameMechanics.ts:26) | economics | server/routes/seedsClaims.ts:17-23,149-150 | Read via getGameVariable('seeds.regen_per_usd') with hardcoded fallback; claim amount always derived server-side from authoritative seeds_contributions USD. |
| Trust formula input weights |  | trust.weight.* via getGameVariables, fallbacks 4 / 1 / 0.5 / 0.2 / 0.3 / -5 / 0.01 (seeded in drizzle/0099) | both | server/routes/batchJobs.ts:80-117 | Registered and seeded, but absent from MECHANICS_VARIABLE_FALLBACKS so the public mechanics page never shows the trust formula; note the job still counts gratitude from the retired gratitude_transactions table (batchJobs.ts:99-100). |
| Citizenship tier requirements (full set) |  | citizenship.* via getGameVariables, fallbacks: sage 90/6/100/10, steward 50/3/1/10, co_creator 15/5/2, grace 30d (seeded drizzle/0099) | governance | server/lib/citizenship.ts:20-92 | Registered tunables, but five of them (min_gratitude_sent, min_endorsements_project, min_gratitude_received, min_contributions, min_endorsements_total) are missing from the mechanics-page snapshot; three loaded keys (min_fire_quest, min_rites, min_epic_quests) are read but never checked. |
| Gratitude lunar-cycle economy parameters |  | base_budget 100, full_power_threshold 10, streak 0.03/cycle capped 0.30, budget multipliers 1/2/3/5, pool_per_cycle 10000, max_payout_per_person 1000, claim_threshold 1000 (fallbacks; seeded 0166/0173/0219) | economics | server/lib/gratitude-cycles.ts:123-158 | The live gratitude economy (a 10,000 $ReGen pool minted every lunar cycle) is fully variable-driven yet NONE of its keys are in MECHANICS_VARIABLE_FALLBACKS; the page instead shows gratitude.multiplier.* (1.2/1.5/2.0) and trust_graph keys that no engine code reads. |
| Bounty valuation weights |  | base trivial/small/medium/large 25/75/250/750, impact 0.75/1.0/1.5, priority 1.25, anchor 0.25, max 2000, round_to 25 (defaults mirror scripts/seed-bounty-valuation.ts) | economics | server/db/bountyValuation.ts:82-89,158-193 | Fully variable-driven deterministic engine; but the mechanics page reads a DIFFERENT tier vocabulary (small/medium/large/epic with fallbacks 25/75/250/750) so the page's 'small' fallback (25) is the engine's 'trivial', and bounty.tier.epic.base is neither seeded nor read by any engine. |
| Bounty demand-learning parameters |  | bounty.learning.* via variables: window 45d, unclaimed 14d, raise 0.15, lower 0.05, factor 0.9-1.4 | economics | server/jobs/coordinationFlywheel.ts:388-396 with defaults server/db/bountyValuation.ts:91-98 | Registered and seeded with bounds, but not on the mechanics page even though it is the self-adjusting price mechanism for labor. |
| Bounty settlement hold |  | bounty.settlement_hold_hours variable, fallback 708 hours (29.5 days) | economics | server/db/bounties.ts:48-54 | The reversal window on every bounty payout; registered but invisible on the mechanics page. |
| Bounty proposer fraction |  | bounty.proposal_fraction variable, fallback 0.15 | economics | server/routes/bounties.ts:527-528 and server/db/bounties.ts:319-334 | The proposer's cut of every bounty; registered but not in the mechanics snapshot. |
| Bounty season budget (global and per-circle) |  | bounty.season_budget / bounty.season_budget.<circle> variables; unset => Infinity (no cap) | economics | server/db/bountyValuation.ts:244-254 and server/db/bounties.ts:56-62,131-155 | The total-emission cap on bounty payouts, enforced in payRole; belongs on the mechanics page next to committed emission (bounties.ts:919-927 already computes the readout). |
| Stale bounty-claim nudge/expiry |  | coordination.stale_claim_nudge_days fallback 7, coordination.stale_claim_expire_days fallback 14 | economics | server/jobs/coordinationFlywheel.ts:38-39,71-72 | Registered work-allocation timing that releases claimed roles back to the pool; reasonable mechanics-page candidate. |
| Crowdpool claim expiry windows |  | crowdpool.claim_expiry_days_shift 7 / _loan 14 / _item 14 (variables with fallbacks); reminder crowdpool.reminder_days_before_expiry fallback 2 (batchJobs.ts:503) | economics | server/routes/campaigns.ts:35-39 | Crowdpooling's only numeric mechanics (it deliberately mints no tokens); registered but not surfaced on the mechanics page. |
| Crowdpool contribution score points |  | scoring.weights.crowdpool_contribution (seeded 20 in drizzle/0097) | economics | server/routes/campaigns.ts:125-128,707-710 | Properly wired through recordScoreEvent; note this and endorsements are the ONLY score-event writers found, see notes on quest/forum scoring drift. |
| Token claim thresholds |  | governance.claim_threshold_regen 1000, _rgvoice 20, _rcivics 1000, _rcvoice 20 (variables, seeded 0132, fallbacks match) | economics | server/routes/governance.ts:353-359, server/routes/players.ts:630-636, server/lib/gratitude-cycles.ts:157 | On the mechanics page and enforced at requestClaim; the pair-eligibility OR rule (either token of a DHO pair unlocks both) is code-only and worth documenting on the page. |
| Hymn winner reward |  | hymnSubmissionWinnerReward variable, fallback 3333 $ReGen | economics | server/routes/songs.ts:33-45 | Registered, on the mechanics page; reads game_variables by raw SQL rather than getGameVariable so it bypasses the 5-minute cache and the key is unnamespaced. |
| Play adoption reward |  | plays.adoption_reward variable, fallback 500 $ReGen (seeded drizzle/0140) | economics | server/routes/plays.ts:224-236 | Registered and on the mechanics page; credit path is idempotency-key-free but guarded by one-adoption-per-user. |
| Land project status progression thresholds |  | project.* variables, fallbacks: accepted 1 endorsement, active 3+5, established 1 campaign+3 seasons, anchor 8 seasons+20 endorsements (seeded drizzle/0099) | governance | server/routes/batchJobs.ts:263-319 | Registered tunables absent from the mechanics page; note drizzle/0097 seeded a parallel dead key family projects.status.* at different values (anchor 25/4) that nothing reads. |
| Display-only game variables with no engine |  | trust.composting_rate 5, gratitude.multiplier.* 1.2/1.5/2.0, gratitude.trust_graph.* 0.1/0.5, citizenship.*.harvest_multiplier 1.5/2/3, harvest.split.* 30/20/20/30, plus seeded-but-unread harvest.pool_size 10000, harvest.distribution_curve 1.5, composting.decay_rate 10, composting.minimum_floor 100 | economics | shared/gameMechanics.ts:49-64 vs drizzle/0097_seed_game_variables.sql:41-59 and drizzle/0099:76-79 | These appear on the public mechanics page (or in game_variables) but grep confirms no server code reads any of them at runtime: the harvest distribution, trust composting, and gratitude trust-graph engines do not exist yet, so the page advertises mechanics with no enforcement; harvest.split page keys (projects/stewards) also mismatch the seeded keys (bffs/orgs). |
| Headline scoring weights with no writer |  | scoring.weights.quest_routine 10 and scoring.weights.forum_post 5 shown on the page; only crowdpool_contribution and endorsement keys are ever recorded | economics | shared/gameMechanics.ts:36-37 vs server recordScoreEvent call sites (routes/campaigns.ts:125-128, routes/game.ts:197-201) | The two scoring numbers the mechanics page leads with have no recordScoreEvent call site anywhere in server/, so quest completions and forum posts currently add zero contribution score; either wire them or the page overstates the mechanic. |
| citizenship.co_creator.* | Co-Creator citizenship requirements (min_percentile 15, min_gratitude_sent 5, min_seasons 2) | 15 / 5 / 2 | governance | server/lib/citizenship.ts:87-89 (seeded drizzle/0099_seed_citizenship_trust_harvest.sql:7-11) | Read live from game_variables on every nightly tier run; community-governable via the shared applyVariableChange path. |
| citizenship.steward.* | Steward citizenship requirements (min_percentile 50, min_seasons 3, min_endorsements_project 1, min_gratitude_received 10) | 50 / 3 / 1 / 10 | governance | server/lib/citizenship.ts:77-84 | Same live-read pattern; already on the Game Mechanics snapshot for percentile and seasons. |
| citizenship.sage.* | Sage citizenship requirements (min_percentile 90, min_seasons 6, min_contributions 100, min_endorsements_total 10) | 90 / 6 / 100 / 10 | governance | server/lib/citizenship.ts:68-73 | Read live per run from game_variables. |
| citizenship.grace_period_days | Citizenship grace period before demotion | 30 days | governance | server/lib/citizenship.ts:42 | Registered variable driving the demotion grace window; exposed on the mechanics snapshot. |
| proposals.signal_threshold | Proposal signal threshold (upvotes to reach Hypha readiness) | 25 (live read, fallback 25) | governance | server/routes/proposals.ts:131-134 | Read live from game_variables on every signal vote. |
| governance.signal_advance_points, governance.signal_advance_avg | Assembly advance gates: net points and average floor | 12 points / 1.0 avg | governance | server/routes/assembly.ts:172-176, 269-276 (seeded drizzle/0164 with bounds 1-500 and 0-3) | Bounded registered variables re-checked server-side on moveToDecide. |
| governance.promotion.min_thread_age_hours, governance.promotion.min_unique_voices | Assembly readiness gates: thread age and unique voices | 48 hours / 3 voices | governance | server/routes/assembly.ts:172-173; server/routes/governance.ts:81-82 (seeded drizzle/0108) | Shared by Assembly advance gates and forum-thread promotion readiness; read live. |
| governance.promotion.cosigner_window_hours | Dual-key co-signer window | 24 hours | governance | server/routes/governance.ts:183 | Live-read window for the second citizen to co-sign a promotion before it expires. |
| governance.signal_min_tier | Signal tier gate (who may signal on Assembly proposals) | 0 = any signed-in member | governance | server/routes/assembly.ts:855-864 (seeded 0164, bounds 0-1) | Designed as a no-deploy config flip; NOTE it is currently broken because it compares Number(citizenshipTier) against the gate while the column stores strings (see notes). |
| governance.minor_lane_quiet_days | Minor-lane lazy-consent quiet window | 7 days | governance | server/jobs/assemblyLifecycleJob.ts:67-76; server/routes/assembly.ts:274 (seeded 0165, bounds 1-30) | Days a minor-lane proposal sits unopposed before auto-passing; core consent mechanic. |
| governance.last_call_hours | Last-call window before binding vote | 48 hours | governance | server/jobs/assemblyLifecycleJob.ts:40; server/routes/assembly.ts:275,712 (seeded 0165, bounds 0-168) | The final objection window; already governed with bounds. |
| governance.resting_after_days | Proposal resting threshold | 30 days | governance | server/jobs/assemblyLifecycleJob.ts:86 (seeded 0165, bounds 7-120) | Days without signals/replies before a forming proposal folds into the resting strip. |
| governance.synthesis_refresh_cooldown_min | Synthesis refresh cooldown | 30 minutes | governance | server/routes/assembly.ts:924 (seeded 0164, bounds 5-1440) | Governs how often the AI synthesis of a proposal conversation can be refreshed. |
| evolution.max_autonomy_tier | Evolution Engine autonomy tier | 1 (variable changes auto-apply; features never auto-ship) | governance | server/lib/evolution.ts:57-69 (seeded drizzle/0170, bounds 0-3) | The machine's leash is itself a community-governed variable; raising it is explicit meta-governance. |
| evolution.launch_window_hours | Feature launch window (hours a gates-passed PR waits before auto-merge) | 24 hours | governance | server/lib/evolution.ts:498 (seeded 0170, bounds 0-168) | Governed pause during which any Steward can halt a machine-built ship. |
| evolution.launch_require_approval | Human approval required before machine-built merge | 1 (on) | governance | server/lib/evolution.ts:514-515 (seeded drizzle/0173, bounds 0-1) | Rye's standing decision, seeded ON, votable off when trust in automated verification is earned. |
| evolution.circuit_breaker_failures | Circuit-breaker failure threshold (consecutive failed/rolled-back ships that freeze Rung 3) | 2 | governance | server/lib/evolution.ts:577 (seeded 0170, bounds 1-10) | Tunable within code-enforced bounds; cannot go below 1, and the trip action (tier drops to 1) is code, not config. |
| governance.default_decision_window_days etc. | Decision defaults (window 7d, reflection 24h, snapshot 6h, one-way-door min 72h, default sunset 365d) | 7 / 24 / 6 / 72 / 365 | governance | drizzle/0108_governance_mechanics_variables.sql:13-24 | Seeded as registered variables with display metadata; several are not yet read by server code (decision flow lives partly in the Gov app), so verify consumption before publishing them as live mechanics. |
| governance.closing_soon_window_hours | Closing-soon window for open decisions | 48 hours | governance | server/jobs/governanceJobs.ts:61 | Read live by the hourly cron that flips decisions to closing_soon. |
| governance.sunset_renewal_warning_days | Sunset renewal warning (auto-create renewal thread) | 7 days | governance | server/jobs/governanceJobs.ts:127 | Read live by the cron that opens renewal threads before a decision sunsets. |
| governance.storyteller_narrative_min_words / max_words | Storyteller narrative word bounds | 300-600 words | governance | server/routes/governance.ts:589-590 | Enforced live at publish time. |
| governance.storyteller_threshold_tokens | Storyteller auto-assignment token threshold | 100000 internal tokens | both | drizzle/0108:27 (job at server/jobs/governanceJobs.ts:80-119 assigns without re-checking) | Seeded tunable; the enforcement point is in the decision flow, the cron only picks the person. |
| governance.delegation_is_active, governance.delegation_max_hops | Delegation switches (delegation_is_active 0, delegation_max_hops 2) | off / 2 hops | governance | server/routes/governance.ts:755 (active flag); drizzle/0108:36-37 (hops) | Proxy voting is dark-launched behind a governed flag; hops cap is seeded but not yet read by code. |
| governance.load.warning_threshold / critical_threshold | Governance load thresholds (warning 15 / critical 30 decisions per 30 days) | 15 / 30 | governance | server/routes/governance.ts:935-936 | Community overload dashboard reads them live. |
| governance.guide.* | ReGen Guide participation dials (proactive posts/week 5, devil-advocate trigger 95%, is_active 1) | 5 / 95 / on | governance | server/lib/regenGuide.ts:46-48,132; drizzle/0108:40-42 | How much the AI guide may participate in governance is itself governed data. |
| governance.sensing_min_citizen_tier | Sensing entry tier | 0 = any signed-in member | governance | server/routes/forum.ts:922-932; server/routes/forumFeed.ts:168-170 (set to 0 by drizzle/0163) | Rye's explicit call (revisit if abused); same string-vs-number tier-compare defect applies in forumFeed. |
| governance.claim_threshold_* | Token claim thresholds to Hypha (regen 1000, rgvoice 20, rcivics 1000, rcvoice 20) | 1000 / 20 / 1000 / 20 | both | server/routes/governance.ts:353-359 (seeded migration 0132) | Gates when private-ledger balances become claimable on-chain voice/value; already on the public mechanics snapshot. |
| governance.claim_bundle_max_items, governance.claim_cooldown_days | Claim bundle max items (20) and claim cooldown (30 days) | 20 / 30 | both | drizzle/0108:31-33 | Seeded tunables for the Hypha bridge claim flow; verify live consumption in the claims route before surfacing. |
| Custom-game civilization modules (crew size 2-12 default 3/7, launchQuestCount 5, intro cap 3/day, verification rung multipliers, ritual spine toggles, federation toggles) |  | per-fork configurable via blueprint with zod bounds | both | shared/customGameBlueprint.ts:238-299 | For forked games this IS the config plane: every module is a bounded knob the client project sets at intake, validated by blueprintSchema before generation. |
| Custom-game public token governance mode |  | enum hypha-voting \| none, default hypha-voting | governance | shared/customGameBlueprint.ts:276 | Forks choose their public-token governance surface, but internal-credit ladders never mint public tokens without it (guard in the levers spec). |

## B3. Hardcoded values that should become variables — 33

| key / name | label | current value | affects | where | why it is a mechanic |
|---|---|---|---|---|---|
| trust.multiplier.min / trust.multiplier.max | Trust score normalization and clamp (score/10, clamped 0..2.0) | /10 then clamp [0, 2.0] | economics | server/routes/batchJobs.ts:119-120 | Hardcoded despite trust.multiplier.min/max/default already being seeded in game_variables (drizzle/0097:32-34 at 0.5/1.5/1.0) — the code ignores those rows and uses its own 0..2.0 clamp, so the seeded dials are dormant and the shown bounds are wrong. |
| tier_bonus.co_creator / tier_bonus.steward / tier_bonus.sage | Tier bonus payouts (TIER_BONUS: co_creator 77, steward 144, sage 233 RGVoice) | 77/144/233 | economics | shared/questPools.ts:60-64; re-exported shared/gameMechanics.ts:217-221 | Pure payout amounts with no code coupling beyond the tier detector — nothing structural prevents them being game_variables like every other reward. |
| path_tier.steward.quest_count / path_tier.steward.vote_count | Player Steward progression thresholds (PLAYER_STEWARD_QUEST_COUNT=33, PLAYER_STEWARD_VOTE_COUNT=144) | 33 quests / 144 votes | governance | shared/questPools.ts:49,54; re-exported shared/gameMechanics.ts:215-216 | These are pure numeric progression thresholds (not content-coupled) sitting as TS constants; every comparable threshold (citizenship.*) is already a game variable. |
| path_tier.sage.percentile_threshold / path_tier.sage.days_pct_required | Sage path-tier criteria (SAGE_PERCENTILE_THRESHOLD=80, SAGE_DAYS_PERCENTAGE_REQUIRED=80) | 80th percentile on 80% of season days | governance | shared/questPools.ts:70-71 | Hardcoded Sage criteria that are not in the snapshot at all and sit next to a DIFFERENT Sage number the public page does show (citizenship.sage.min_percentile=90) — two Sage mechanics with divergent unexplained thresholds. |
| read existing gratitude.budget_multiplier.* / gratitude.streak_bonus_* rows | Client simulator hardcoded tier multipliers and streak math (5.0/3.0/2.0/1.0; 3% per cycle, 30% cap) | Sage 5x, Steward 3x, Co-Creator 2x, Explorer 1x; 0.03/0.30 | economics | client/src/pages/GameMechanics.tsx:767-771 | The public simulator duplicates gratitude.budget_multiplier.* and streak-bonus values as literals instead of reading the live variables it renders elsewhere on the same page — they match today's seeds but will silently drift after any admin edit. |
| events.attendance_reward_regen | Event attendance $ReGen reward (duplicated hardcode) | 33 $ReGen per attendance, hardcoded in two independent code paths (admin mark + self check-in) | economics | server/db.ts:3112-3122 and server/routes/events.ts:935-952 | A real token emission not in game_variables and not on the mechanics page, duplicated in two files, and written to a separate email-keyed regenTokenLedger outside the user_token_ledger conservation path. |
| quests.reward.<questId> (or a quest_rewards DB table surfaced on the mechanics page) | Canonical per-quest reward table | ~45 quests, 33 to 444 $ReGen + 1 RVoice each (e.g. quest-8=222, present-parenting=444, ringing-cedars=33) | economics | shared/quest-rewards.ts:19-68 (paid via server/routes/hyphaBridge.ts:153-185) | The single largest emission schedule in the game is a hardcoded TS table; the Hypha bridge pays from it and nothing surfaces it on the mechanics page or in game_variables. |
| quests.multiplayer.reward.<questId> | Multiplayer crew quest rewards | reward.regen 144, rvoice 1 per crew quest (per-quest literals) | economics | shared/multiplayerQuests.ts:71 (and each quest's reward field) | Same class as QUEST_REWARDS: emission amounts embedded in content files, invisible to the mechanics page and to admin tuning. |
| quests.attested_bonus_fraction | Peer-attestation bonus multiplier (verification ladder rung 2) | 0.25 (bonus = round(quest.reward.regen * 0.25), the 1.25x of ADR-42) | economics | server/routes/questCrews.ts:324-327 | A pure emission multiplier hardcoded inline; belongs beside the other quest economics on the mechanics page. |
| tiers.bonus.co_creator / tiers.bonus.steward / tiers.bonus.sage | Tier bonus amounts (RGVoice) | co_creator 77, steward 144, sage 233 RGVoice | economics | shared/questPools.ts:60-64 (paid in server/lib/tierDetector.ts:217-232) | Shown on the mechanics page as structural constants but they are pure token amounts with no code coupling; making them game_variables makes them admin-editable without a deploy. |
| tiers.sage.percentile_threshold / tiers.sage.days_pct_required | Sage criterion thresholds | SAGE_PERCENTILE_THRESHOLD=80, SAGE_DAYS_PERCENTAGE_REQUIRED=80 | governance | shared/questPools.ts:70-71 | Sage gating numbers hardcoded in a shared module, not in game_variables and not on the mechanics page (which shows different sage numbers from citizenship.sage.*). |
| scoring.tier_percentile.<tier> | Contribution tier label percentile bands | Guardian>=95, Elder>=85, Cultivator>=70, Grower>=50, Sapling>=30, Sprout>=15, else Seedling | both | server/game/index.ts:136-144 | These bands drive currentTier on every profile and gate tier quests (quests.tier_steward_min etc. exist as variables at DIFFERENT values 70/85/95, a second drift); the function ignores the variables entirely. |
| trust.scale_divisor / trust.score_max | Trust score scaling and clamp | clamped = max(0, min(2.0, rawScore / 10)) | both | server/routes/batchJobs.ts:119-121 | The seven trust weights are game_variables but the divisor 10 and ceiling 2.0 that turn them into the stored trustScore are hardcoded, so tuning the weights has a hidden nonlinear interaction. |
| capitals.points_per_contribution / capitals.points_per_1000usd / capitals.points_per_quest | Capital score point formula | contributions: COUNT*10 + FLOOR(SUM(estimatedValue)/1000); quest completions: *5 points | economics | server/routes/batchJobs.ts:148-179 | Eight-capitals percentile scores are computed from three hardcoded weights embedded in SQL and JS; they shape a public reputation surface with no tunable or page presence. |
| gratitude.max_sends_per_hour | Gratitude send rate ceiling | 30 sends per hour per sender, hardcoded | economics | server/routes/gratitude.ts:74-86 | An anti-bot brake that also bounds how fast a sender can reach the full-power threshold; admin-tunable is fine, open governance of it is not (lowering it to 0 kills the economy). |
| bounty.learning.fast_claim_days / bounty.learning.min_sample | Demand-learning hardcoded companions | FAST_CLAIM_DAYS=2, MIN_SAMPLE=3, MAX_FACTOR_STEP=0.1 | economics | server/jobs/coordinationFlywheel.ts:43-44 and server/db/bountyValuation.ts:277 | Three of the learning loop's parameters bypassed the variable treatment their siblings got; MAX_FACTOR_STEP could reasonably stay code-owned as a stability rail, but fast-claim-days and min-sample are ordinary tuning dials. |
| ship.regen_per_quest_point | Ship quest points paid 1:1 as $ReGen | amount = action.points (implicit 1 point = 1 $ReGen); entry threshold SHIP_ENTRY_THRESHOLD_POINTS=150 (server/lib/ship-config.ts:145) | economics | server/routes/ship.ts:219-243 (points from DB table ship_quest_actions) | Per-action points are DB-editable but the 1:1 points-to-$ReGen conversion is implicit and hardcoded; a conversion-rate variable would let ship quests be tuned without repricing every action row. |
| claims.tier.<tier>.usd / claims.tier.<tier>.tokens | Historical contribution claim tier valuations | suggestedTierUsd / suggestedTierTokens / finalTierTokens are client- or admin-supplied integers with no server-side canonical tier table | economics | server/routes/claims.ts:47-49,282-339 | The only economic flow found where the amount originates client-side (admin review/ratify gates it, so no mint without consent); a canonical server-side tier table would make the tiers auditable and displayable. |
| Co-Creator fire-quest/rites and Steward epic-quest requirements: loaded but NOT enforced |  | 1 / 1 / 1 (seeded), never checked in the promotion loop | governance | server/lib/citizenship.ts:23-29 (keys citizenship.co_creator.min_fire_quest, .min_rites, citizenship.steward.min_epic_quests) | Registered and publicly listed as tier rules but the engine never evaluates them, so the public page would advertise rules the game does not apply; either wire the checks or retire the keys. |
| Citizenship power toggles (24 vars: citizenship.<tier>.can_submit_proposals/can_signal_vote/can_rate_producers/can_nominate_tiers/can_arbitrate/can_sponsor) |  | explorer all 0; co_creator submit+signal 1; steward +rate+nominate; sage all 1 | governance | drizzle/0100_seed_citizenship_powers.sql:1-45 | Seeded as if governed, but enforcement code (requireCoCreatorPlus) hardcodes 'tier !== explorer' instead of reading them; wiring these vars in would make proposal/voting rights genuinely community-tunable. |
| citizenship.<tier>.can_submit_proposals / can_signal_vote | Proposal submit/vote gate hardcoded to 'anything above explorer' | tier must not be 'explorer' | governance | server/routes/proposals.ts:19-27 (requireCoCreatorPlus, used by proposals.create, proposals.signalVote, assembly.raiseFromThread) | The real gate for who may raise and signal-vote proposals ignores the 24 seeded power toggles that claim to govern exactly this. |
| governance.vote_weight.<tier> | Voting weights by tier (visitor 1 / citizen 2 / contributor 3 / steward 5) seeded but votes hardcode weight 1 | seeded 1/2/3/5; code writes weight: 1 for everyone | governance | drizzle/0108_governance_mechanics_variables.sql:4-7 vs server/routes/govProposals.ts:144 | A publicly seeded weighted-voting mechanic that the vote-cast path silently ignores; either read the weights at cast time or remove the vars before publishing them on a mechanics page. |
| governance.launch_carryover_idle_days | Stalled-launch carry-over window (anyone can launch after idle days) | 7 days, hardcoded | governance | server/routes/assembly.ts:794-796 | A real governance rule (who may carry a ready proposal to the binding vote) hardcoded next to sibling windows that are all registered variables. |
| evolution.pause_min_tier | Steward+ gate for pause/rollback of machine ships | tier >= 2 or admin | governance | server/routes/assembly.ts:138-150 (isStewardPlus: Number(tier) >= 2, admins bypass) | Who can halt the Evolution Engine is a core check-and-balance; currently hardcoded AND broken (string tier vs numeric compare means only admins ever pass). |
| governance.straw_poll.consensus_pct, governance.straw_poll.min_votes | Straw poll promote-readiness heuristic (>=66% consensus AND >=5 votes) | 0.66 / 5, hardcoded | governance | server/routes/governance.ts:704 | An advisory but visible governance threshold (lights the ready-to-promote signal) with no registered variable behind it. |
| paths.player.steward.quest_count, paths.player.steward.vote_count | Player->Steward thresholds (33 quests AND 144 proposal votes) | 33 / 144 | governance | shared/questPools.ts:49,54 (enforced server/lib/tierDetector.ts:301-331) | Pure numeric thresholds for the path-tier ladder, currently compile-time constants re-exported to the mechanics page; nothing structural prevents them being governed. |
| paths.tier_bonus.co_creator/steward/sage | Tier bonus amounts in RGVoice (Co-Creator 77, Steward 144, Sage 233) | 77 / 144 / 233 | both | shared/questPools.ts:60-64 (credited server/lib/tierDetector.ts:127-164) | One-time voice-token grants for governance milestones; a pure number with an idempotency key, safe to govern within bounds (already displayed on the mechanics snapshot as a 'structural constant'). |
| paths.sage.percentile_threshold, paths.sage.days_pct_required | Sage criterion parameters (80th percentile on 80% of season days) | 80 / 80, unenforced | governance | shared/questPools.ts:70-71 (checkSage stubbed at server/lib/tierDetector.ts:533-548) | Declared thresholds for the cross-path Sage tier; register them before the daily-snapshot implementation lands so the rule ships governed. |
| paths.ally.co_creator.min_distinct_tool_users | Alliance-partner Co-Creator threshold (tool clicked by 11+ distinct users) | 11, hardcoded | governance | server/lib/tierDetector.ts:502 (and comment at 460) | A path-tier promotion criterion buried as a literal in the detector while sibling criteria are spec-listed thresholds. |
| paths.land_project.steward.min_seasons | Land-project Steward criterion (>=1 season completed AND game launched) | 1 season + non-null gameLaunchedAt | governance | server/lib/tierDetector.ts:430-433 | The season count is a bare literal; the launched-game requirement is structural and can stay. |
| tiers.percentile.<label> | Percentile tier labels (Guardian 95 / Elder 85 / Cultivator 70 / Grower 50 / Sapling 30 / Sprout 15) | 95/85/70/50/30/15 | governance | server/game/index.ts:136-144; duplicated as quest_unlock_tiers seed in drizzle/0099 (Cultivator 70, Elder 85, Guardian 95) | Two parallel sources (hardcoded function and DB table) for the same progression bands invite drift; unify into one governed source before publishing. |
| Quest unlock tiers (Cultivator 70, Elder 85, Guardian 95, requiresRitesComplete) |  | 70/85/95 + rites required | governance | drizzle/0099_seed_citizenship_trust_harvest.sql (quest_unlock_tiers INSERT) | Editable only by SQL today; no admin surface, no history, no bounds, unlike game_variables. |
| Sociocratic role compensation bands (13 roles, band 1-5, tokenAward e.g. 700,000 $ReGen / $7,000, hoursPerWeek) |  | per-role hardcoded strings in a client data file | both | client/src/data/gameRoles.ts:86-88 and siblings | Role pay for the Infinite Game's coordination roles is governance-salient and currently display-only client data disconnected from the roles DB table roleHolders reads; move to DB so the Game Mechanics page and the ledger share one source. |

## B4. Founder-only (Ring 1) — 11

| key / name | label | current value | affects | where | why it is a mechanic |
|---|---|---|---|---|---|
| SEEDS-to-$ReGen claim rate (seeds.regen_per_usd=10) |  | 10 ($ReGen per $1 USD) | economics | shared/gameMechanics.ts:26,76; engine server/routes/seedsClaims.ts:19 | Wired end to end, but it is effectively an exchange-rate/price peg with legal and treasury implications ($ReGen=$0.10 decision 2026-07-02) — tunable, but not a dial open governance should move. |
| Evolution Engine autonomy dials (evolution.max_autonomy_tier 1, launch_window_hours 24, circuit_breaker_failures 2, launch_require_approval 1) |  | 1 / 24 / 2 / 1 | governance | server/lib/evolution.ts:57-69,498,514,577; seeded drizzle/0170,0173; public read server/routes/assembly.ts:604-608 | Meta-governance: values are community-governable within bounds but the bounds themselves are code-owned and un-governable by design (the leash geometry) — expose read-only on the mechanics page, never as an open dial. |
| Ship real-world stats (ship.asset_value_usd, community_owned_usd, trees_planted, seeds_planted_base, voyages_sailed_base) |  | absent -> 0 / code baselines | economics | server/routes/ship.ts:406-416 | Admin-entered real-world claims (asset value, ownership %) piggybacking on game_variables — factual data entry, not a game rule anyone should vote on. |
| Super-admin edit gate email (SUPER_ADMIN_EMAIL='rieki.cordon@gmail.com') |  | rieki.cordon@gmail.com | governance | client/src/pages/GameMechanics.tsx:269-278 | Client-side visibility toggle for the inline edit button (real enforcement is server-side adminProcedure) — should move to config, never to open governance. |
| Ship real-money economics |  | KEEPER_PAY_USD 200, CREW_SPONSOR_GOAL_CENTS 210000, MAIDEN_YEAR_VOYAGE_TARGET 40, WINTER_HOST_SHARE 25%, FLEET_BUYBACK_PCT 10, multi-week discounts 5/10/15% | economics | server/lib/ship-config.ts:135,145,151,169-175 and shared/shipPricing.ts:21-26 | Real-world USD product pricing and revenue splits (church agreements, buybacks); tunable but must never be open-governed and does not belong on the game-mechanics page. |
| Free Voyage Giveaway entry weights and window |  | base 1, perReferral 5, referral cap 40, nomination 3, quest 2, ig/yt 1, ARV $2093, window 2026-07-27 to 09-06, milestones 40/60/75/85/95%, max 6 voyages | economics | shared/shipGiveaway.ts:16-45 and shared/shipFreeVoyage.ts:10-20 | Sweepstakes math is defined verbatim in the official rules; changing it mid-promotion is a legal problem, so it must stay code-owned and counsel-gated, never admin- or governance-editable. |
| Token contract addresses and 18-decimal conversion |  | Base contract addresses (env-overridable hardcoded fallbacks); balances divided by 10^18 | economics | server/blockchain.ts:9-16,104 | Deployment facts of the on-chain tokens; wrong values silently break claim matching and balance reads, so env/founder only. |
| Protected + token-guarded path list (what the machine may never modify) |  | ~28 protected globs + 4 token-guarded globs; drizzle migrations restricted to additive DDL | both | .github/assembly-protected-paths.json (enforced by scripts/check-protected-paths.mjs and server/lib/evolution.ts:88-95,226-236) | The Evolution Engine's hard boundary; the file lives under .github (itself protected) so editing it is a human-only PR by construction, and the guard fails closed if the file is unreadable. |
| Church steward payment rights (church_role_holders: canAcceptPayments/canMakePayments, grant/revoke) |  | fully data-driven per holder row; grant/revoke admin-only | both | server/lib/church-permissions.ts:26-68; server/routes/churchRoles.ts:58-118 | Deliberately data-driven (ADR-18) so migration to community governance is a config change, but who moves church money is a legal/fiduciary posture that should not be open-governable yet; the admin-only gate is the current founder control. |
| Hypha binding-vote venue (targetDhoSlug 'regen-games'; webhook accepts 'regen-games'/'regen-civics') |  | regen-games | governance | server/routes/assembly.ts:801; server/lib/hypha-bridge/types.ts:69-70 | Where ratification legally/technically happens is constitutional plumbing; changing it silently would redirect binding votes. |
| Custom game price ($20,000 investment confirmation) |  | $20,000, hardcoded in comments/scoring | economics | server/routes/customGameApplications.ts:52,85; shared/customGameBlueprint.ts:393 | Commercial pricing of the fork offering; a business decision, not community-governable, but should live in one config constant rather than three comment strings. |

## B5. Deliberately fixed — 13

| value | current | where | why it stays fixed |
|---|---|---|---|
| Rites of Passage count (RITES_OF_PASSAGE_COUNT=14) | 14 (derived from RITES_OF_PASSAGE_IDS) | shared/questPools.ts:31-42 | Derived from the actual quest-content ID list — changing the number without changing the content would break the milestone detector, so it is correctly a structural constant re-exported through the snapshot. |
| Lunar cycle length (LUNAR_CYCLE_DAYS=29.53) | 29.53 days | shared/gameMechanics.ts:21 | Astronomy, not policy — the gratitude cadence is defined as one synodic month and shared/lunar.ts computes real cycle bounds from it. |
| Reputation tier label bands (getTierFromPercentile: 95 Guardian, 85 Elder, 70 Cultivator, 50 Grower, 30 Sapling, 15 Sprout, else Seedling) | 95/85/70/50/30/15 | server/game/index.ts:136-144 | A display taxonomy driving currentTier labels, not any payout or power gate — fine as code, but should be documented on the mechanics page so the ladder is public. |
| Game variable cache TTL (VAR_CACHE_TTL=300s) | 300 seconds | server/game/index.ts:11 | Operational freshness constant, not a game rule; the admin write path busts the cache anyway. |
| Stale claim-bridge auto-cancel window (staleAfterHours=24) | 24 hours | server/routes/batchJobs.ts:338-343 | Operational timeout for abandoned Hypha claim bridges (refund path is idempotent and conservative); not a rule players plan around. |
| Evolution engine defaults when DB rows absent (autonomy tier 1, launch window 24h, breaker 2, approval required 1) | code fallbacks matching seeds | server/lib/evolution.ts:59,65,498,514,577 | Fail-safe fallbacks that default to the most conservative posture (tier 1, approval on) when the table is unreachable — the safety net must stay in code. |
| Player Steward criteria (quest and vote counts) | 33 quests, 144 proposal votes | shared/questPools.ts:49-54 (checked in server/lib/tierDetector.ts:301-331) | Already surfaced on the mechanics page via the snapshot; deliberately structural per the documented rule in shared/gameMechanics.ts:7-11, and numerically symbolic (33/144). |
| Stale Hypha claim-bridge auto-cancel window | 24 hours default (admin override 1-720h via cancelStaleClaims input) | server/routes/batchJobs.ts:338-341,841 | Operational refund timing with idempotent, conservation-safe refund logic; changing it does not change any amount. |
| Synthesis daily cost caps (per-user 10, global 50) | 10 / 50 per day, in-memory | server/routes/assembly.ts:107-108 | LLM cost-control bound (AI-AUTOMATION-RISKS Risk 3), an operational safety limit rather than a game rule; kill switch is env ASSEMBLY_SYNTHESIS_ENABLED. |
| Signal score range -3..+3 and 7-bucket histogram | -3..+3 | server/routes/assembly.ts:846, 48-51 (schema drizzle/0164 TINYINT) | Structural: baked into the DB schema, aggregate histogram shape, and every advance-gate calculation; changing it is a schema+UI migration, not a dial. |
| Straw poll caps (5 polls/thread, 2-6 options, closes 1-30 days default 7) | 5 / 2-6 / 7 | server/routes/governance.ts:634-643 | Spam/shape bounds on an informal instrument, not a binding rule. |
| Rites of Passage set and count (quests 0-13, 14 rites) | 14 quests, IDs 0-13 | shared/questPools.ts:31-42 | Structurally coupled to authored quest content; changing the number without new content breaks the Co-Creator milestone, so it moves by content release, not by dial. |
| Church role catalogue (single 'steward' enum) | ["steward"] | server/routes/churchRoles.ts:25 | Kept as an enum so adding a role variant is a small code change; renamed from priest/priestess by ADR-20; not a runtime dial. |

---

# Part C — Ring 0: the invariants that must NEVER be tunable (96, merged across both systems)

These belong ON the public page — stated as constitutional law, not sliders.
A governance system that can vote away conservation is not a governance
system; publishing these is what makes the tunable list trustworthy.

| invariant | why it can never be a dial |
|---|---|
| Per token, SUM(balance) over all accounts = 0 (ledger conservation), enforced fail-loud at boot | Value is only ever moved, never created by accident; this is the foundation the whole recognition economy's meaning rests on. |
| The exchange is BUY-ONLY: fiat flows in only, tokens are never sold for fiat | Legal/economic posture of the whole platform; making it tunable would let a config change create a securities-like market. |
| Recognition-kind tokens are never purchasable or swappable; Hypha-governed tokens never trade (read-only display) | Recognition is a signal, not a price — the gratitude.pool_token description encodes this same separation as a hard rule. |
| Faucet-issued tokens are never swappable (destination-based taint: faucet to sys:treasury is stocking, faucet to anything else taints) | Prevents laundering freshly issued tokens into the swap path. |
| Caps fail closed: 0 means zero, never unlimited (exchange/payments caps) | A misconfiguration must reduce exposure, not remove limits. (Watch: library.intake_member_cycle_cap and several 0-disables knobs deliberately invert this — the public page must state each knob's 0-semantics explicitly.) |
| The app never computes binding vote weight — governance.voice_weighting only changes DISPLAY/tally of informal sensing; formal decisions bind on Hypha | Making informal sensing binding via a config knob would silently create a parallel governance system. |
| All ledger movement goes through postTransfer/postTransferPair; token_balances is a recompute-only cache; no raw ledger writes | The conservation invariant is only checkable if there is exactly one write path. |
| Unknown variable keys throw (server/lib/variables.ts:37-41) — a typo must not silently read as 0 | Guarantees every mechanic shown on a public page corresponds to a real registered definition; do not weaken this to a default-returning lookup. |
| validateVariable min/max/choice bounds run server-side before any write (shared/gameVariables.ts:984-1014) | The registry's declared ranges ARE the outer safety envelope; admin-editable means editable within these bounds only. |
| Trading requires a version-stamped legal ack; stale ack (legalAckVersion !== cardVersion) closes swapping | Legal consent must not be inheritable through config; it is per-deployment and re-consented on change. |
| Scheduler jobs never close gratitude cycles — settlement releases value and is a human act | Cycle close is the moment value mints; automating it via a knob would remove the human witness from issuance. |
| Ledger conservation: per token, SUM(balance) over all accounts ≡ 0, re-proven at every boot | The negative faucet balances ARE issuance-to-date; a tunable here would make minting invisible (server/lib/ledger.ts:631-672). |
| token_balances is a cache: recompute from transfers, never increment | A wrong cache is fixed by recomputation; increments compound errors silently (ledger.ts:154-171). |
| Every ledger write carries a unique idempotency key; the UNIQUE index is the dedupe | Retries and webhook redeliveries must post exactly once without trusting flags. |
| All value movement goes through postTransfer/postTransferPair — no raw ledger writes, ever | Guards, overdraft checks and cap enforcement live inside the primitives; a bypass bypasses everything. |
| Transfer amounts are strictly positive integers between two distinct, existing accounts | Zero/negative/self transfers are how phantom value and hidden burns enter a ledger (ledger.ts:191-210). |
| Only faucet accounts may run negative; non-faucet negatives exist only via ALLOW_NEGATIVE_SOURCES with allowNegative set | A negative balance must be a truthful debt (grace night, chargeback), never a convenience (ledger.ts:334). |
| A paired post (swap) may never create debt — allowNegative is illegal inside postTransferPair | A swap that could overdraft is a mint with extra steps (ledger.ts:421-423). |
| Pairs are fixed at exactly TWO legs | An N-leg API is a router, and a router is an automated market maker wearing a helper function (ledger.ts:364-376). |
| Hypha-governed tokens have zero ledger rows — the platform never mints or moves equity/voice | One posted row would quietly make this database the source of truth for the cap table (ledger.ts:634-638). |
| Rounding always favors the treasury/pool: ceil what the member pays, floor what the member receives; swap payQty ceils toward the treasury | This — not the spread — is what makes every round trip non-extractive at every spread including zero (payments.ts:43-53, exchange.ts:481-505, index.ts:10290). |
| Fiat flows IN only; tokens are never sold back for money — the exchange is buy-only | Selling out turns village tokens into redeemable securities; there is deliberately no sell path anywhere. |
| Recognition-kind tokens are never purchasable or swappable | Gratitude is earned, full stop; a price on recognition destroys the signal (exchange.ts:114-116). |
| Faucet-issued tokens are never swappable — destination-based taint test, no override at any privilege level | A token the village can conjure as a reward must never become a claim on goods someone paid money for (exchange.ts:181-212). |
| Caps fail closed: 0 means ZERO, never unlimited | A missing or zeroed cap must refuse, not open the floodgates — platform-wide convention (exchange settings, mint cap, purchase limits). |
| One seller per token, across modules AND the exchange | Two sale doors for one token means two prices and an arbitrage the members pay for (exchange.ts:120-122, 293-306). |
| Trading and credit-sale open only under a current version-stamped legal card; a stale acceptance closes the market | Legal posture must be re-accepted when its text changes; a config flag alone could outlive the terms it agreed to. |
| Capability-bearing earned badges may never ride a recognition metric | Applause must never auto-mint permissions — bought reach would become power (badges.ts:119-121). |
| Only warning badges deny capabilities; self and hypha badges gate nothing | Self-declarations and external mirrors carrying power would let members or outside facts mint authority (badges.ts:108-113). |
| The Sybil eligibility filter applies to BOTH the breadth metric and the pool payout | Filtering the leaderboard but not the money leaves the treasury open to alt farms (gratitude-cycles.ts:114-141, index.ts:10270-10292). |
| Library escrow reconciliation: balance(sys:library-escrow) equals the sum of unsettled loans' escrow, to the credit, or boot refuses | A drained or double-released escrow must never normalize (library.ts:551-603). |
| settleLoan is the single terminal for every loan ending; one atomic claim decides the outcome forever, losers only repair | Nothing may pay twice or pay two different stories (library.ts:394-465). |
| The scheduler never closes gratitude cycles — settlement releases value and is a human act | Automated value release removes the human consent moment the economy is built on (CLAUDE.md spine rule, index.ts:2306-2307). |
| No self-consent on quest claims outside the solo-founder window | Consent mints recognition and advances stages; without a witness a steward could pay themselves (index.ts:9866-9895). |
| Webhooks fail closed: a missing signing secret or bad signature rejects the event | An unsigned settle event is an anonymous instruction to mint credits (payments.ts:238-250). |
| Exit deletes no value rows; conservation holds through departure; debts settle only through the domain that created them | Departure must be auditable forever and never a write-off path (exit.ts header, sweepBalances). |
| Automated authority may narrow the market and never widen it | Boot repairs delist tainted tokens loudly; nothing automated may list, resume, or re-open anything (exchange.ts:770-797). |
| The one-gate order of authority: admin → badgeDenies → role → badgeCapabilities → stage (shared/capabilities.ts:95-118), and that there is only ONE gate — no parallel permission mechanism anywhere. | A tunable order lets a role override a warning badge, and a second gate is where capture hides; the whole permission model rests on this ordering being constant. |
| A warning badge's deny beats role and stage grants; only admin outranks it (Gate E, S36). | A warning that a role trivially overrides is not a warning. (Per session memory: this shipped as default and still awaits Rye's explicit confirmation — but as code it must stay non-tunable either way.) |
| The last founder can never be demoted, and a founder must be demoted before exiting (server/index.ts:3772, 6695, 6721). | Bootstrap is spent after first use; a deployment stranded without a master admin has no recovery path. |
| No self-consent on quest claims outside the explicit, audited solo-founder window (server/index.ts:9866-9895). | Consent mints recognition from the faucet; a self-serve consent is a self-serve mint. |
| Non-admin appointers may never seat themselves, never remove holders, and never seat anyone into a role carrying capabilities they do not hold (server/index.ts:10683-10699). | Registration is open and unverified — the subset guard is the only thing between proposal.decide and two-account self-promotion. |
| Account deletion = anonymization; value rows (ledger, gratitude, claims, loans, orders, badge awards) are never deleted (server/index.ts:1820-1899). | Per-token SUM(balance) ≡ 0 must keep holding and settlements must keep explaining themselves; deleting value rows breaks the conservation proof. |
| Exit cannot resolve while any domain reports blocking open state, and exit never invents its own settle path (server/index.ts:6776). | Each domain's terminals are the only place its value settles; a tombstone over open state destroys owed value. |
| Restorative-intake content reaches only the intake role's holders — no forum thread, no event row, no exits-row content (server/index.ts:6807-6837). | A person must never be the subject of a consent decision in a general forum; this is the exit flow's hard rule as code. |
| Core modules (quests, gratitude, progression, profiles) are always public and cannot be disabled; a module missing a hard dependency serves as off. | The capability gate and the economy assume these substrates exist; half-served modules must degrade loudly, not silently. |
| Funds-bearing switches (exchange trading, library credit sale) stay behind version-stamped legal acknowledgments; a stale ack closes the feature. | Legal posture is per-deployment founder responsibility — amended terms must be re-read, and no governance process may vote the caution card away. |
| ALL_CAPABILITIES stays in lockstep with the Capability union (shared/capabilities.ts:38-57). | A capability missing from the value list is silently ungrantable by badges — a structural consistency requirement, not a knob. |
| Per-token ledger conservation: SUM(balance) over all accounts = 0, boot-asserted; token_balances is a cache that is recomputed, never incremented (server/lib/ledger.ts) | The entire recognition economy's honesty rests on it; a tunable conservation rule is a license to counterfeit. |
| Fiat flows IN only; the exchange is buy-only; the platform never sells a token holder's tokens back to fiat | Reversing the flow turns community tokens into securities-like instruments and changes the legal character of the whole platform. |
| Faucet-issuance taint: faucet → sys:treasury is stocking, faucet → anything else taints the token as non-swappable forever | Destination-based tainting is what stops issued tokens laundering into the swap market. |
| One capability gate with fixed authority order: admin → badgeDenies → roleCaps → badgeCaps → stage; a badge deny beats role and stage (shared/capabilities.ts:95-118) | The ordering IS the policy (Gate E); making the order tunable reintroduces the second permission mechanism the design forbids. |
| A token has at most one selling module (sellsToken uniqueness, boot-asserted) | Two sellers of one token means two prices and an arbitrage door inside the village. |
| Legal caution-card acceptances are server-stamped (authenticated actor + server clock) and version-matched; the client may never author acceptedBy/acceptedAt (server/index.ts:3899-3929) | Who consented to funds-bearing terms and when is a record about a person; forgeable consent is no consent. |
| All token movement goes through postTransfer/postTransferPair; no raw ledger writes | The single entry point is what makes conservation checkable at all. |
| Gratitude cycle settlement is a human act — scheduler jobs never close cycles or roll seasons | Releasing value automatically removes the human consent moment the economy is built on. |
| openStateCheck refuses turning a module off while economic state is outstanding (open loans, pending purchases, standing warnings) | A module switch must never strand value or erase live governance. |
| Funds-bearing modules refuse enabling while no per-admin identity with a real credential exists (shared-password posture) | Money-touching authority must be attributable to a named person. |
| Restorative exit content flows only to its recipients; records hold only an agreement pointer and status | A privacy rule that can be tuned off is not a privacy rule. |
| evolution.* bounds (the leash geometry) are code-owned and can never be changed by any governance or admin path | applyBoundsChange and validateExecutionPayload hard-reject evolution.* (server/lib/evolution.ts:185-187,239-243); if the machine could widen its own leash, the autonomy ladder is meaningless. |
| The circuit breaker that freezes feature auto-ship (drops autonomy tier to 1 after consecutive failures) is code-enforced and must never be tunable off | server/lib/evolution.ts:572-599 is the last-resort brake on self-modification; only the failure-count threshold is a bounded variable, never the mechanism. |
| Single write path for every game-variable change: bounds re-checked, append-only game_variable_history row with reason, cache busted | applyVariableChange (server/lib/evolution.ts:129-169) is shared by admin edits and ratified proposals; a second writer would let values move without provenance or bounds. |
| Exactly one applied execution per ratified proposal (UNIQUE proposalId + idempotent dispatch) | server/lib/evolution.ts:310-343 prevents a proposal from applying twice under concurrent webhooks — double-applying a variable or feature change corrupts the record. |
| The gratitude pool is a ceiling, not a promise: per-person caps and integer flooring mean withheld amounts are never minted and never roll forward | computePoolShares (server/lib/gratitude-cycles.ts:74-112) is the anti-collusion conservation rule; making the remainder recoverable reopens pool-draining. |
| Gratitude budget divides by max(recipients, full_power_threshold) — the use-it-or-lose-it formula shape is fixed even though its parameters are tunable | computePerPersonShare (server/lib/gratitude-cycles.ts:65-72); dividing by n alone lets a sender concentrate their full budget on one person. |
| Sending gratitude never mints tokens at send time; $ReGen only enters the ledger at cycle close from the capped pool | The flat-5-per-send model was deliberately removed (server/routes/game.ts:206-217, ADR-30); reintroducing send-time minting is uncapped emission. |
| Crowdpooling never mints platform tokens — contributions earn recognition points only, and only on fulfillment | Locked decision recorded in scripts/seed-crowdpool-config.ts:11-13 and server/routes/batchJobs.ts:428-429; crowdpool value is real goods, not token issuance. |
| Every bounty credit flows through payRole() and every reversal through reverseRole() — no other code path writes bounty rewards | server/db/bounties.ts:1-11; the double-pay guard, season budget cap, separation-of-duties hold, and settlement hold only protect payouts that go through the single path. |
| Ratified feature proposals can never touch protected paths, and the check fails closed if the protected-paths list is unreadable | server/lib/evolution.ts:216-236 + .github/assembly-protected-paths.json; this is the boundary between community-shippable surface and human-only core (ledger, auth, the evolution engine itself). |
| Bounds changes require finite min < max and the current value inside the new range; value changes outside bounds are rejected at raise AND at execution | Double validation (server/lib/evolution.ts:151-158,245-261,276-281) guarantees a ratified change can never leave a variable in an inconsistent state even if bounds moved between vote and apply. |
| Every snapshot key must have a numeric fallback and vice versa (enforced by test) | server/gameMechanics.test.ts:41-48 keeps MECHANICS_VARIABLE_KEYS and MECHANICS_VARIABLE_FALLBACKS in lockstep so a fresh environment renders sane numbers instead of zeros. |
| Self-endorsement is forbidden | server/routes/game.ts:179-181; endorsements feed trust and scoring, so self-dealing is an integrity rule, not a dial. |
| Admin authority for variable edits is enforced server-side (adminProcedure), never by the client-side email check | The SUPER_ADMIN_EMAIL constant in GameMechanics.tsx only toggles UI visibility; treating it as the gate would make the economy editable by anyone who edits client JS. |
| Private balance cache = SUM(user_token_ledger) recomputed inside one transaction on every credit/debit (server/db/tokens.ts:75-123) | Ledger conservation and self-healing drift repair; making the recompute skippable or the cache writable would allow balance forgery. |
| No client-supplied monetary amount is ever honored: SEEDS claims recompute USD from seeds_contributions (seedsClaims.ts:127-150), quest payouts resolve from the canonical table (hyphaBridge.ts:150-165), bounty payouts from bounty_roles.amount | This is the closed minting hole; any tunable that reintroduced client-trusted amounts reopens unauthenticated minting. |
| Every credit path carries a DB-level idempotency key or uniq-row guard (bounty pay/reverse, gratitude distributions, claim refunds, tier bonuses, ship quests) | Duplicate-credit impossibility must be enforced by the database, not by tunable application logic. |
| payRole/reverseRole is the single write path for bounty rewards (server/db/bounties.ts:1-12) | Separation-of-duties holds, budget caps, and settlement holds only apply if no second payout path can exist. |
| Token flow is one-way private -> public via the Hypha claim bridge; server writes touch only the private ledger (server/db/tokens.ts:13-18) | Reversing the direction or adding a server-side public mint would bypass the on-chain consent step entirely. |
| The gratitude pool is a ceiling, never a promise: capped and floored remainders are never minted and never roll forward (gratitude-cycles.ts:74-112) | This is the anti-collusion floor; making remainder-rollforward tunable lets a small group drain accumulated pools. |
| Crowdpooling never mints platform tokens (locked decision, campaigns.ts:703, batchJobs.ts:428-429) | Mixing real-goods pledges with token emission creates securities/consideration exposure; it is a legal posture, not a dial. |
| evolution.* bounds are code-owned and never governed (server/lib/evolution.ts:186,242); every governed variable change re-checks minValue/maxValue at raise AND execution | The leash on self-amending governance must not be adjustable by the thing it leashes. |
| Sweepstakes entry math and promotion window are fixed by the published official rules for the duration of a live promotion (shared/shipGiveaway.ts) | Changing disclosed odds mid-promotion violates sweepstakes law; only counsel-reviewed code changes between promotions. |
| Auto-approve of a SEEDS claim requires an authenticated user, full-entitlement acceptance, and no dispute; guests only ever get a pending row (seedsClaims.ts:163-169) | Credits must bind to a known account; loosening this reintroduces the email-lookup credit hole. |
| evolution.* variable bounds are code-owned and can never be changed by governance (applyBoundsChange and validateExecutionPayload both reject them) | The leash geometry of the machine must not be loosenable by the machine's own change pipeline; only the values inside the bounds are votable (server/lib/evolution.ts:185-187,239-243). |
| The Evolution Engine may never modify protected or token-guarded paths, regardless of a proposal's declared scopePaths; the CI guard fails closed and the scope list is fetched from the server, never from the branch under review | The machine must not write its own permission slip (.github/assembly-protected-paths.json, scripts/check-protected-paths.mjs, assembly.proposalScope). |
| The machine never writes token credit/claim/ledger code (tokenGuardedGlobs: server/db.ts, server/db/**, shared/tokens.ts, server/lib/tokenLedger.ts) | Ledger conservation and the sacred token model (STEERING section 5) cannot depend on machine-authored diffs. |
| Every variable change, by admin or by ratified proposal, goes through the single applyVariableChange path: bounds re-checked, game_variable_history written with proposal provenance, cache busted | One write path means the audit trail and bounds can never be bypassed (server/lib/evolution.ts:132-169). |
| Exactly one applied execution per proposal (UNIQUE governance_executions.proposalId, migration 0172), and executions/history are append-only | A ratified vote can apply once and only once, and the Record can never be rewritten. |
| Ratification confirmation lives outside the engine: a human admin relay or the Hypha on-chain webhook, never the platform deciding its own outcomes | ADR-8 / AI-AUTOMATION-RISKS Risk 7 keeps the binding human confirmation between vote and execution (server/lib/ratification.ts). |
| The circuit breaker (consecutive failed/rolled-back ships drop autonomy to tier 1) is code-enforced and its threshold cannot go below 1 | The freeze reflex must exist even if the community tunes everything else (evolution.ts:574-599, minValue 1 in 0170). |
| Individual Assembly signal scores are never exposed to anyone; only aggregates (sums, averages, counts, histograms) leave the server | Locked decision 4; protects members from vote-level social pressure (assembly.ts signalAggregates). |
| Dual-key promotion requires a second, distinct citizen; you cannot co-sign your own promotion | No single actor can move a thread into formal decision (governance.ts:214). |
| Church payment rights come only from church_role_holders rows, never hardcoded names or user IDs, and every server mutation re-checks them | ADR-18: rights must be grantable and revocable through governance without code changes, and the client is never trusted. |
| In forked custom games: an elder persona always carries human-steward review with standing veto; player memory is opt-in, default off, with the transparency surface shipped first; a recognition currency can never grant voice, carry a fiat peg, or release into itself | Encoded as z.literal / superRefine in shared/customGameBlueprint.ts:160-171,282,295-297 so no blueprint can configure them away. |
| Blueprint integrations hold provider NAMES only; API keys are refused by regex and never stored in ReGen Civics systems | Custom-games Decisions locked #1 (customGameBlueprint.ts:44-50). |
| Below autonomy tier 3, a ratified feature parks in 'paused' and NOTHING is created on GitHub; launch windows never advance | Rung 3 is dark until the community explicitly votes the tier up (evolution.ts:450-457,493). |

---

# Part D — What already exists vs. what has to be built

## Exists today

| capability | game-amora | regen-civics |
|---|---|---|
| Typed/bounded variables registry | YES (`shared/gameVariables.ts`, delta-only overrides, unknown keys throw) | Partial (`game_variables` key/value + `MECHANICS_VARIABLE_FALLBACKS`) |
| Admin editing UI | YES (Admin → Game Mechanics tab) | YES |
| PUBLIC mechanics page | **NO** | YES (`GameMechanics.tsx`: sliders, baseline-vs-proposed diff, shareable URL state, per-section "copy proposed changes" markdown) |
| Proposal generation from the page | NO | YES (markdown generator, Hypha-ready) |
| Hypha bridge (pre-filled forms, bridge table, lifecycle) | NO (deliberately read-only toward Hypha: `shared/hypha.ts` deep-links only) | YES (`server/lib/hypha-bridge/`: intents registry, bridge page, title-marker matching) |
| On-chain verification of outcomes | NO | YES (Alchemy webhook receiver on Base: ProposalCreated / ProposalExecuted, signature-verified, receipts back to source) |
| Auto-apply passed proposals to settings | NO | **NO** (the missing last mile in BOTH systems) |
| Founder enters their DHO URL | YES (`hypha.org_url` variable) | Hardcoded per-deployment |

## To build (the shape of the work, given the above)

1. **game-amora public page** — port the regen-civics GameMechanics page
   pattern onto the typed registry (easier than regen's: the registry already
   carries labels, descriptions, bounds, categories — the page is mostly a
   renderer). Include Ring 0 invariants and Ring 1 items as read-only.
2. **Registry hygiene both sides** — promote the "make-variable" rows
   (A2: 13, B3: 33), add regen's B2 keys to its snapshot, and tag every
   variable with its ring (one new field on the def).
3. **Port the Hypha Bridge module into game-amora** as `server/lib/hypha-bridge/`
   with ONE new intent: `mechanics-change` (no payout — a policy agreement,
   not a payment). The bridge carries a machine-readable change-set
   `{key, from, to}[]` in metadata plus the title marker for matching.
4. **The apply step** (new in both): on ProposalExecuted-passed for a
   mechanics-change bridge, validate each key is Ring 2 + value within bounds
   + registry still knows the key, then write through the existing variable
   store, record an entry in a new `mechanics_changes` ledger (who proposed,
   proposal id, tx hash, vote link, applied-at), and post a Pulse line.
5. **Founder DHO onboarding** — already half-exists in game-amora
   (`hypha.org_url`); add the on-chain side: DAO contract address discovery +
   per-deployment webhook registration (see open question 3 on who listens).

---

# Part E — Open questions (answer before building)

1. **Ring assignment authority.** The three-ring model needs an owner per
   variable. Is the ring a PLATFORM decision (fixed in code per variable), or
   does each village's founder choose which Ring-2 dials their community may
   govern (a per-variable "governable" flag the founder sets)? Recommendation:
   platform sets a ceiling (some things can never be Ring 2), founder opts
   variables into open governance underneath it.
2. **Which chain and which DAO product, exactly?** The working bridge verifies
   **Base (EVM) via Alchemy**. But regen-civics docs also reference EOSIO-era
   Hypha ($RCivics on EOSIO in BLOCKCHAIN_INTEGRATIONS.md). Is every village
   DHO — Amora's included — on app.hypha.earth on Base, with the same
   ProposalCreated/ProposalExecuted event shapes the webhook already parses?
   If any target DHO is still EOSIO-Hypha, the verification layer is a second
   implementation, not a port.
3. **Who runs the listener?** Two architectures: (a) every fork registers its
   own Alchemy webhook (sovereign, but every founder needs an Alchemy account
   and a signing key — real onboarding friction), or (b) the ReGen hub runs
   ONE listener service for all forks and forwards verified outcomes to each
   village over a signed callback (fits the existing feedback-hub
   "queue-and-forget, hub is a listener" pattern; villages stay servable when
   the hub is down because apply-on-callback is idempotent and re-sendable).
   Recommendation: (b), with (a) as an escape hatch. Which do you want?
4. **Who may propose?** Any signed-in member? A stage floor? The existing
   `governance.hypha_threshold` variable (earned recognition to qualify)?
   And is proposing rate-limited? Recommendation: reuse
   `governance.hypha_threshold` + a per-member weekly cap, both themselves
   Ring 2.
5. **Apply timing.** Instant-on-verification, or at the next cycle boundary?
   Mid-cycle changes to pool/budget variables have real hazards (the sticky
   cycle-split work exists precisely because mid-flight basis changes corrupt
   settlements). Recommendation: economic variables apply at next cycle
   close; non-economic (copy, toggles) apply instantly. Confirm.
6. **Does passage on Hypha bind automatically, or does a founder ratify?**
   Auto-apply is the stated goal; is there an emergency brake (founder can
   freeze auto-apply, loudly, with an audit trail)? Recommendation: yes —
   a Ring-1 "governance.auto_apply_enabled" kill switch.
7. **Batch proposals.** One proposal = one variable change, or a change-set
   (several dials moved together, e.g. a whole harvest-split rebalance that
   must sum to 100)? The harvest split literally cannot be changed one key at
   a time without passing through invalid states. Recommendation: change-SETS
   with an atomic apply. Confirm.
8. **Identity linking.** For "return and say it passed" attribution: is the
   proposer's Hypha/wallet identity linked to their game account (game-amora
   already has wallet verification via signed nonce)? Without it anyone can
   claim anyone's passed proposal. Require verified wallet for one-click
   bridge, allow copy-paste flow without?
9. **What do custom-game forks inherit?** Ring assignments, the mechanics
   page, and the bridge presumably ship with the platform; does the HUB also
   want a directory view ("this village's constitution") via the
   /api/platform/info handshake?
10. **Scope check on "all users of the tool".** The public page shows values
    to EVERYONE including anonymous visitors (like regen-civics)? Or
    members-only? Anonymous read is the regen precedent and the transparency
    point — confirm, since some variables (abuse-guard caps) arguably help
    attackers; those are Ring 1 and would stay OFF the page.

# Part F — Ways to improve the overall idea

1. **Publish the constitution, not just the dials.** The page's most radical
   feature is the Ring 0 list — the things NO vote can change. That is what
   makes the tunable list credible to outsiders and funders.
2. **Change-sets with invariant checks, not single-key edits** — e.g. the
   harvest split rows must sum to 100; a proposal is validated as a WHOLE
   against per-set constraints at creation time, not apply time.
3. **A mechanics ledger, mirror of the token ledger.** Every applied change:
   who proposed, in-game sensing results, Hypha proposal id, tx hash,
   basescan link, when applied, previous value. Renders as a public
   "Amendment history" on the page. Delta-only storage means history is
   currently THROWN AWAY on revert-to-default — the ledger fixes that.
4. **In-game sensing BEFORE the chain.** You already have
   `governance.sensing_days` and voice weighting in game-amora, and forum
   integration in regen-civics: let a proposal collect in-game
   support/perspectives first, and only offer the "take it to Hypha" button
   once it clears a visible threshold. Cuts chain noise, creates deliberation.
5. **Simulator-first proposing (regen already half-has this).** The regen
   page's slider+diff pattern generalizes: the proposal IS the simulator
   state. For game-amora, wire live figures (current pool, member count,
   recognition distribution) so movers see projected impact ("pool 500→800 ≈
   +2.1 per active member at current breadth") before proposing.
6. **Bounds are the safety envelope — publish them.** min/max in the registry
   are Ring 0. A proposal outside bounds is unproposable at form time, not
   rejected at apply time. (game-amora's registry already has bounds;
   regen-civics's mostly doesn't — porting the typed registry there is part
   of this work.)
7. **Cooldowns per variable.** A dial that moved last cycle can't move again
   for N cycles (N itself Ring 2). Prevents thrash and vote-fatigue attacks.
8. **Expiring experiments.** Let a proposal carry an optional sunset: "raise
   pool to 1500 for 3 cycles, then auto-revert unless re-passed." Communities
   experiment safely; the mechanics ledger records the experiment.
9. **One listener service at the hub** (see question 3): forks get
   verification without Alchemy accounts, and the hub gets an ecosystem-wide
   view of governance activity — the beginnings of the village directory.
10. **The proposal carries its own receipt path.** Put the bridgeKey marker in
    the Hypha proposal title (regen's existing `[rc:key]` pattern) so
    verification is deterministic; "return to say it passed" becomes a
    fallback for copy-paste proposals, with on-chain check before apply.
11. **Templates/presets for forks.** A custom game picks a starting
    constitution ("tight-knit village", "open network", "high-trust co-op") —
    a named bundle of Ring-2 defaults; the blueprint system in regen-civics
    (customGameBlueprint) is the natural home.
12. **Per-variable provenance on the page.** Show default vs current, who
    changed it last and under which proposal — every number becomes a story,
    which is exactly the pedagogical point of a public mechanics page.
13. **Don't move module-config mechanics into the registry blindly** (A3):
    structural JSON (forum categories) isn't a dial. Instead give the public
    page pluggable "sections" that read each plane read-only, and only
    promote to the registry what governance should MOVE.
14. **Test the loop against a Hypha sandbox DHO before shipping** — the QA
    harness pattern (scratch DB + smoke) extends naturally: a fake Alchemy
    payload injector + a sandbox DHO gives an end-to-end governance drill.
