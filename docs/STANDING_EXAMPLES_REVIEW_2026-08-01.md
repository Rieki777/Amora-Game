# Standing Examples — Adversarial Review, 2026-08-01

> Verdict: **NOT READY TO SHIP.** 28 findings confirmed by independent
> adversarial verification (29 raised, 1 refuted): 5 blockers, 10 majors, 13 minors.
> The set's own tests (9/9) and provers pass because they exercise only the
> guarded paths — the confirmed findings live on the paths they do not walk.
> Reviewed at working-tree state ~13:51 2026-08-01; the authoring session was
> still editing, so some findings may already be mid-fix.

The pattern behind most of it: the four rules are right, and the guard
machinery exists (isExampleRow, EXAMPLE_REFUSAL_BODY, isExampleUser), but
coverage is partial — several economic routes, both scheduled jobs, the
store-db replaceAll path and the gratitude engine never consult the flag.

## [BLOCKER] POST /api/stays/checkout has no example guard: Stripe session and open-state block from a demo room

Only /api/stays/request is guarded (index.ts:5481). /api/stays/checkout (index.ts:5499-5537) never calls isExampleRow on the accommodation. The seed gives every example room active stay-credit AND usd prices for both audiences (examples-seed.json:411-445, inserted active=1 by examples.ts:434-440), so priceFor (stays.ts:128-140) returns valid rates and the route inserts a 'pending' stay_purchases row and opens a Stripe Checkout session for an example room — directly violating 'never a Stripe object'. staysOpenState (stays.ts:346-359) counts pending stay_purchases, so that row blocks setModuleLifecycle('off') for up to ~25h until releaseAbandonedStayPurchases reaps it; if the member pays, the webhook settles and mints real stay credits sold against a room that does not exist. The seed's own _note (examples-seed.json:401, 'no ledger, no Stripe. Booking an example is refused') and docs/STANDING_EXAMPLES.md's guard list are both contradicted by this route. Fix: the same isExampleRow(pool, 'accommodations', accommodationId) refusal as :5481, before the stay_purchases INSERT.

**Fix:** In server/index.ts, inside POST /api/stays/checkout, add the same refusal used at line 5481 immediately after parsing accommodationId (before the priceFor calls and stay_purchases INSERT): `if (await isExampleRow(getPool(), "accommodations", String(accommodationId ?? ""))) { return res.status(409).json(EXAMPLE_REFUSAL_BODY); }`. Both symbols are already imported (index.ts:220-221). Also add "stay checkout" to the Guards list in docs/STANDING_EXAMPLES.md (line 12) and cover it in server/lib/examples.test.ts alongside the existing stay-request refusal case.

## [BLOCKER] Example quests are claimable; consent path mints recognition and stay credits from an example row

POST /api/game/quests/:id/claim (index.ts:10293-10338) has no isExampleRow guard, and QUEST_SELECT (server/repos/quests.ts:62-63) does not even select is_example, so no downstream code can see the flag. The three example quests seed with status 'open' and gratitude ranges (examples-seed.json:135-180) into the core quests module at boot on every fresh fork (seedExamplesAtBoot, index.ts:1179-1194). A member claims ex-quest-swale, submits (10340), and a steward consents (10394) — which by its own comment (index.ts:10400-10403) 'mints recognition from the faucet, grants stay credits and advances stages': a real ledger row born from an example, the exact breach rule 1 names. Retirement (first real quest via /api/admin/quests, 10254) then deletes the quest out from under the in-flight claim, stranding it. Fix: refuse claim (and submit) when isExampleRow(pool, 'quests', id).

**Fix:** In server/index.ts, at the top of POST /api/game/quests/:id/claim (after the quest lookup at ~10297) add: if (await isExampleRow(getPool(), "quests", quest.id)) return res.status(409).json(EXAMPLE_REFUSAL_BODY); — both symbols are already imported (lines 220-221). Add the same guard to POST /api/game/quests/:id/submit (keyed on req.params.id) to cover claims already in flight before deploy. Optionally, for defense in depth and labelling: add is_example to QUEST_SELECT and an isExample field to rowToQuest (server/repos/quests.ts) and render an ExamplesBanner (moduleId="quests") on the quests page so members see the rows as examples. Add a regression case to server/lib/examples.test.ts asserting claim against ex-quest-* returns 409 example_immutable.

## [BLOCKER] replaceAll on tools/circles/roles rewrites example rows with is_example=0, making them permanent unretirable 'real' content

dbCollection.replaceAll (store-db.ts:126-140) does DELETE-all + re-INSERT using only the spec column list, and the specs for rolesRepo (server/index.ts:659-672), circlesRepo (736-751) and toolsRepo (754-776) do not include is_example. The caches these specs feed are reloaded after seeding (so example rows ARE in cache, minus their flag), and every replaceAll write path then re-inserts them with the column's DEFAULT 0. Trigger sites: the tools-link-check scheduler job (index.ts:2637-2651) calls toolsRepo.replaceAll(all) DAILY with no admin action — seeded example tools have last_checked_at NULL so they are due immediately, meaning example tools are laundered to is_example=0 within ~24h of seeding; also PUT /api/admin/circles/:id (4932), DELETE /api/admin/circles/:id (4944), PUT /api/admin/roles/:id (4963), and the tools order/toggle/edit/delete routes (5371, 5384, 5394, 5410). Once laundered: retireExamples' DELETE ... WHERE is_example = 1 (server/lib/examples.ts:649) matches nothing, so the first real publish stamps the permanent tombstone while the example rows keep rendering as real village content forever; POST /api/admin/modules/:id/examples/clear answers alreadyRetired/0 and can never remove them; isExampleRow guards stop refusing mutations (rule 1 'inert' breaks); and hasRealContent counts them as village-authored. This is the write-direction twin of doc bug 1 — wireExampleCaches fixed reads only. Fix needs is_example in the three column specs (or replaceAll preserving unknown columns) plus a guard keeping example rows out of replaceAll payloads.

**Fix:** Add { js: "isExample", db: "is_example", kind: "bool" } to the three column specs (rolesRepo, circlesRepo, toolsRepo in server/index.ts) so the flag round-trips through load()/replaceAll()/insert(). CAUTION: toDb() (store-db.ts:76-77) maps an absent value to NULL, and the column is NOT NULL — so either make the bool kind coerce null/undefined to 0, or set isExample: false explicitly in the create routes (POST /api/admin/circles, roles seeding, POST /api/admin/tools). Then example rows keep is_example=1 through every replaceAll, retireExamples' WHERE is_example = 1 works again, and hasRealContent stops counting them. Optionally also make mutation routes refuse edits/deletes targeting rows with isExample set (tools/circles/roles currently have no isExampleRow guards at all, unlike forum/library/badges/stays/commerce/exchange). Add a regression test: seed tools examples, run the link-check job body, then assert SELECT COUNT(*) FROM tools WHERE is_example = 1 is still 3 and that retireExamples removes them.

## [BLOCKER] forum/feed retirement is not scoped to the retiring module — the promised id-scoping does not exist in retireExamples

The EXAMPLE_TABLES comment (examples.ts:43-46) says feed's forum_threads entry 'is scoped by the seeded ids rather than the whole table; see retireExamples()', but retireExamples (639-651) runs an unscoped DELETE FROM forum_threads WHERE is_example = 1 for BOTH modules. Concrete failures: (a) the very first real forum thread (POST /api/forum/threads, index.ts:4516) retires 'forum' and thereby deletes the feed's 3 example posts while feed's example_state stays seeded/not-retired — feed keeps its 'showingExamples' banner (withRows survives via the ex-event health_events row) over posts that no longer exist, violating rule 3 (retire per module) and rule 2 (honest labelling); (b) POST /api/admin/modules/feed/examples/clear (index.ts:4182) wipes the forum's 3 example threads too, and because feed's table list omits forum_replies, the forum's example replies survive as is_example=1 orphans pointing at deleted threads (no FK on forum_replies, 0019_forum.sql:39-51) — refreshRowPresence then counts those orphans and keeps 'forum' in modulesWithExamples over an empty page. Retirement for feed must be scoped to the seeded feed post ids (kind='post' AND is_example=1, or the ex-feed-* ids), and forum's to its own threads, or the two modules must retire together atomically.

**Fix:** Implement the scoping the EXAMPLE_TABLES comment already promises, in retireExamples (server/lib/examples.ts). Replace the bare table list with an optional per-module WHERE predicate, e.g. extend EXAMPLE_TABLES entries to { table, extraWhere? } or add a SCOPED map: for feed, delete forum_threads rows with `is_example = 1 AND id LIKE 'ex-feed-%'` (do NOT scope by kind='post' — ex-feed-3 is seeded with kind='announcement'; the seeded-id prefix or an explicit id list from examples-seed.json is the reliable key); for forum, delete forum_threads with `is_example = 1 AND id LIKE 'ex-thread-%'` and forum_replies via `thread_id IN (forum's example thread ids)` or `id LIKE 'ex-reply-%'`. Alternatively (simpler, also honest): retire forum and feed atomically as a pair — when either retires, stamp both tombstones and delete both modules' tables including forum_replies — since they share a physical table. Whichever route, also: (a) run refreshRowPresence afterwards as now, which then yields correct withRows; (b) add a regression test in server/lib/examples.test.ts covering: first real forum thread in a non-feed category leaves feed's 3 posts intact and feed still labelled, and admin clear of feed leaves forum's threads+replies intact (no orphaned is_example replies).

## [BLOCKER] Example identities are valid gratitude recipients: real budget, ledger credit, notification, and pool payout to a phantom

sendGratitude (server/lib/gratitude.ts:85-88) looks up the recipient via members.byEmail/byId with no isExample check, and the repo (server/repos/users.ts) never filters example rows. The identity emails are fixed and public in every fork (ex-user-mira@examples.invalid, examples.ts:266), so any signed-in member can send gratitude to 'Mira (example)'. Consequences: (1) the sender's real budget is spent and recognition is issued from the faucet into memberAccount('ex-user-mira') — a real ledger row credited to a phantom (gratitude.ts:147-161); (2) server/index.ts:10661-10669 then notify()s the example identity — the exact 'no notifications addressed TO example identities' rule — and the daily-digest path will attempt email delivery to @examples.invalid; (3) at cycle close (index.ts:10771+) the pool distributes real pool tokens proportionally to recognition received, so the phantom can receive real value, and its name renders in the public distributions report (index.ts:10734-10756) and gratitude journal; (4) retirement's raw `DELETE FROM users WHERE is_example = 1` (examples.ts:677) then deletes the owner row while the non-zero recognition account persists — stranded value, bypassing the settle-first posture anonymizeMember enforces. The contract (examples.ts:257, seed _identities note) explicitly claims exclusion from 'gratitude eligibility'; it is not implemented on the recipient side. Fix: refuse in sendGratitude when recipient.isExample (one check covers hearts too).

**Fix:** In sendGratitude (server/lib/gratitude.ts), immediately after the recipient lookup at lines 85-88, refuse example recipients using the existing helper: `import { isExampleUser } from "./examples";` then `if (isExampleUser(recipient)) return { ok: false, status: 409, error: "This is a standing example — appreciation flows to real members." };` (recipient.isExample is already mapped by usersRepo.rowToMember, so the check is one line and covers both the direct-send route and any future caller; the heart route's thread-level guard at index.ts:4342 stays as defense-in-depth). For robustness, also (a) filter `!u.isExample` in the distributions report's nameOf source (index.ts:10734) as index.ts:12116 already does for the member list, and (b) either have settleCycle callers exclude example toIds or have retireExamples assert/settle the phantom ledger accounts before `DELETE FROM users WHERE is_example = 1` (examples.ts:677) so value can never be stranded. Add a loop-test case asserting the 409 for toEmail ex-user-mira@examples.invalid.

## [MAJOR] replaceAll on tools/circles/roles silently strips is_example — daily link-check job launders example tools into real rows

dbCollection.replaceAll (store-db.ts:126-140) is DELETE-all + re-INSERT of only the spec'd columns, and none of the toolsRepo/circlesRepo/rolesRepo specs include is_example (index.ts:659-672, 736-751, 754-776). Any replaceAll therefore rewrites the example rows with is_example reset to DEFAULT 0. The tools-link-check job (index.ts:2637-2662) does toolsRepo.replaceAll(all) DAILY, so within ~24h of tools examples appearing the flags are wiped automatically; PUT/DELETE /api/admin/tools (index.ts:5371, 5384, 5394, 5410), PUT/DELETE /api/admin/circles/:id (4932, 4944) and PUT /api/admin/roles/:id (4963) do the same on any edit of ANY row. Aftermath: isExampleRow returns false (guards dead), retireExamples' DELETE WHERE is_example=1 (examples.ts:649) matches nothing so the platform content masquerades as village content forever, hasRealContent flips true, withRows drops the module so the banner disappears while the rows remain, and — for roles — example roles carrying capabilities become indistinguishable, assignable real roles. Fix: add is_example to the three column specs (and preserve it through the repo records), or make example rows invisible to all()/replaceAll.

**Fix:** Add { js: "isExample", db: "is_example", kind: "bool" } to the toolsRepo, circlesRepo, and rolesRepo column specs in server/index.ts so load() carries the flag and replaceAll writes it back. In the PUT handlers that merge req.body over the cached row (e.g. PUT /api/admin/tools/:id does { ...all[idx], ...req.body, id: all[idx].id }), also pin isExample: all[idx].isExample the same way id is pinned, so a client body cannot launder or forge the flag. Alternatively (or additionally), have the DELETE step of replaceAll for these tables preserve is_example=1 rows, but carrying the column through the specs is the smaller, spec-consistent fix. Add a regression test in server/lib/examples.test.ts: seed examples, run a replaceAll-driven path (e.g. the link-check body or an admin PUT), and assert COUNT(*) WHERE is_example=1 is unchanged.

## [MAJOR] Admin manual stay purchase mints real stay credits against example-room prices, unguarded

POST /api/admin/stays/purchases/manual (index.ts:5731-5762) takes any accommodationId, reads priceFor — which the example rooms' seeded active prices satisfy — then inserts a 'paid' stay_purchases row referencing the example accommodation, records a fiat charge, and calls mintStayCredits (a real ledger post, index.ts:5752-5755). No isExampleRow check anywhere. The admin UI lists example rooms alongside real ones (GET /api/admin/stays, 5540-5573, no distinction), so recording a walk-in payment against 'The garden cabin (example)' is one dropdown mistake away and creates real minted value keyed to a demo room. Fix: the same accommodations guard as :5481 before the INSERT.

**Fix:** In the POST /api/admin/stays/purchases/manual handler (server/index.ts, after resolving the guest and before reading priceFor), add the same guard used at :5481: `if (await isExampleRow(getPool(), "accommodations", String(accommodationId ?? ""))) return res.status(409).json(EXAMPLE_REFUSAL_BODY);`. Consider applying the identical guard to POST /api/stays/checkout (index.ts:5499-5537), which is also unguarded and would open a Stripe session against an example room's posted USD price.

## [MAJOR] sendGratitude has no example-recipient refusal; isExampleUser and NOT_EXAMPLE have zero call sites

sendGratitude resolves the recipient via members.byId/byEmail (gratitude.ts:85-88) with no example check, and the users repo maps isExample (repos/users.ts:119) but never filters it. examples.ts:743-748 exports isExampleUser and NOT_EXAMPLE promising exclusion from 'the member directory, launch counts, gratitude eligibility, the Sybil helper' — grep shows ZERO consumers; the only implemented exclusions are inline: admin roster (index.ts:12116), members_total snapshot (health.ts:54), badge engine (badges.ts:317). Consequence: POST /api/game/gratitude/send (index.ts:10654) with toEmail 'ex-user-mira@examples.invalid' (the pattern is examples.ts:270; author ids are public on every thread payload, index.ts:4450) posts a REAL ledger leg of recognition to mem:ex-user-mira — value issued to a non-person, spent from the sender's real budget, later counted by settlement. When the last module retires, DELETE FROM users WHERE is_example=1 (examples.ts:677) removes the identity but not its ledger account, stranding the balance and orphaning the gratitude_log rows. Fix: refuse in sendGratitude when the resolved recipient isExample, and actually apply NOT_EXAMPLE/isExampleUser at the documented exclusion sites.

**Fix:** In sendGratitude (server/lib/gratitude.ts), after resolving the recipient (~line 88), refuse example recipients — either `if (isExampleUser(recipient)) return { ok: false, status: 409, error: ... }` or, matching the codebase's preferred column-truth pattern, `await isExampleRow(deps.pool, "users", recipient.id)`. This one guard covers both callers (direct send and hearts). Additionally honor the documented exclusions: filter isExample in eligibleSenderIds (server/index.ts:4239) and drop example recipients in settleCycle or at the distribution loop (index.ts:10824), and use NOT_EXAMPLE/isExampleUser at the existing inline sites so the exported helpers gain their promised consumers. Optionally add a retirement sweep for ledger accounts/gratitude_log rows referencing ex-user-* ids as defense in depth.

## [MAJOR] Listing the example-listed token self-destructs: upsertSettings keeps is_example=1 and the retire trigger then deletes the admin's real listing

The exchange example is a token_exchange_settings row for the REAL token 'credits' with is_example=1 (examples-seed.json:730-746). The documented retire trigger is PUT /api/admin/exchange/tokens/:slug (index.ts:8614). But upsertSettings (exchange.ts:245-286) upserts via ON DUPLICATE KEY UPDATE without touching is_example, so when the admin lists 'credits' for real they UPDATE the example row in place — it stays is_example=1 — and then onRealItemPublished('exchange') (index.ts:8637) fires retireExamples, whose DELETE FROM token_exchange_settings WHERE is_example=1 (examples.ts:649) deletes the very listing the admin just configured (plus their currency_prices history row if it was the example one). The route races retirement to return 'success' with settings that are about to vanish. Until that PUT, the buy guard (8157) also means the flagship token is permanently unbuyable with no admin-visible reason. Relatedly, prepareSwap (index.ts:8234-8289) has NO is_example check at all — swap inertness currently rests solely on the seed keeping swappable=false. Fix: upsertSettings should clear is_example on write (making the row real) — retirement then only removes other modules' rows — and prepareSwap should mirror the buy guard.

**Fix:** In upsertSettings (server/lib/exchange.ts:267-273), make a write claim the row as real: add is_example to the INSERT column list with value 0 and append is_example=0 (or is_example=VALUES(is_example)) to the ON DUPLICATE KEY UPDATE clause. Retirement's DELETE ... WHERE is_example=1 then removes only untouched example rows. Optionally do the same for setPrice's currency_prices insert for symmetry (it already defaults to 0). Separately, mirror the buy guard in prepareSwap (server/index.ts:8234-8289): before or alongside the settings checks, refuse with EXAMPLE_REFUSAL_BODY if isExampleRow(pool, "token_exchange_settings", payToken/receiveToken, "token_slug") — so example inertness for swaps does not depend on the seed data keeping swappable=false. Add a DB-backed test: seed exchange examples, PUT the example-listed slug, assert the settings row survives retirement with is_example=0 and that the response settings match a subsequent GET.

## [MAJOR] Admin's first real exchange listing self-destructs when it targets the example token 'credits'

The seed's exchange listing occupies token_exchange_settings PK 'credits' (server/seeds/examples-seed.json, listings[0].tokenSlug). PUT /api/admin/exchange/tokens/:slug (index.ts:8614) — which is the module's DECLARED retirement trigger per the seed's _trigger — has no isExampleRow guard (the only guard is on the buy route, index.ts:8157). upsertSettings (server/lib/exchange.ts:245-286) does INSERT ... ON DUPLICATE KEY UPDATE that never touches is_example, so listing 'credits' updates the example row in place with the flag still 1; then index.ts:8637 fires onRealItemPublished('exchange') whose fire-and-forget retireExamples deletes token_exchange_settings WHERE is_example = 1 — i.e., the settings row the admin just saved. The 200 response even returns settingsFor(slug) racing the void retirement, so the admin sees success while the token is silently delisted. (Second attempt works because the tombstone is now set and the upsert creates a fresh is_example=0 row — which makes the first-attempt failure look like flaky data.) The route needs either an is_example=0 reset in the upsert or retirement BEFORE the write, like the doc's 'after a real row commits' pattern done consciously for the shared-PK case.

**Fix:** Make the real write claim the row before retirement can delete it: add is_example to upsertSettings' statement — include it in the column list with value 0 and add "is_example=VALUES(is_example)" (or "is_example=0") to the ON DUPLICATE KEY UPDATE clause in server/lib/exchange.ts:267-273. An admin PUT then converts the example row into a real row, and the subsequent retireExamples DELETE ... WHERE is_example=1 no longer matches it (it still correctly removes the example currency_prices rows; setPrice already writes is_example=0 rows). This preserves the "retire AFTER a real row commits" doctrine — retiring before the upsert would tombstone examples on a failed write. Optionally also expose isExample in rowToSettings so the admin exchange UI can label the example listing.

## [MAJOR] health examples can never seed after the first gratitude cycle close — health_snapshots counts as 'real content' but is written un-gated infrastructure

hasRealContent (examples.ts:601-618) iterates EXAMPLE_TABLES.health = ['regen_entries','health_snapshots']; health_snapshots is not in NOT_EVIDENCE_OF_REAL_CONTENT (examples.ts:75) and health has no REAL_CONTENT_CHECK entry. But snapshotCycle runs inside every gratitude cycle close and is explicitly 'NOT module-gated: collection is infrastructure, display is the module' (index.ts:10870-10881). So any village that has settled one lunation has real health_snapshots rows regardless of the health module ever being on, and enabling health afterwards silently seeds nothing (stamp seeded:false) — the dashboard shows '1 of 3 lunations' and an empty regen ledger, the exact empty-module problem the feature exists to fix. This is the same defect class as the health_events/feed bug the doc records as bug 2 (a bookkeeping spine table read as village-authored content), fixed for feed and missed for health. health_snapshots should be excluded from the evidence set (or health given a custom check on regen_entries only).

**Fix:** In server/lib/examples.ts, give health a REAL_CONTENT_CHECK mirroring feed's: real content = COUNT(*) FROM regen_entries WHERE is_example = 0 > 0 (health_snapshots is infrastructure-written and never evidence of village-authored content). Additionally, inside the health case of seedExamples (examples.ts:476-485), guard the snapshot-example block: skip seeding example snapshot cycles when any is_example=0 row already exists in health_snapshots, so fabricated lunations are never interleaved with 1-2 real ones (regen_entries examples always seed; with 3+ real lunations the (cycle_number, metric_key) UNIQUE key + INSERT IGNORE already makes collisions no-ops, but the explicit guard keeps partial-history cases clean). Add a regression test in server/lib/examples.test.ts: insert a real (is_example=0) health_snapshots row, call seedExamples(pool, "health", seed) without force, and assert regen_entries examples were seeded while no example snapshot rows were written.

## [MAJOR] progression has no retirement trigger — onRealItemPublished('progression') is wired nowhere

The seed declares progression's trigger as 'A real role is seeded or edited into existence', but a grep across server/ finds onRealItemPublished call sites for 14 modules and none for 'progression'. PUT /api/admin/roles/:id (index.ts:4949-4965), the only role-mutation route, fires nothing, so the 3 example roles never retire on real role activity — only the admin clear endpoint removes them, and (compounded by the replaceAll laundering finding) that same route strips their is_example flag first, after which even admin-clear deletes nothing. Rule 3 ('the first real item published in a module deletes that module's examples permanently') is unimplemented for this module.

**Fix:** Three changes in concert: (1) Add { js: "isExample", db: "is_example", kind: "bool" } to the rolesRepo column list (server/index.ts:659-672) so replaceAll round-trips the flag and stops laundering example roles into real ones. (2) In PUT /api/admin/roles/:id (index.ts:4949), refuse edits to example rows like the sibling routes do: if (await isExampleRow(getPool(), "roles", req.params.id)) return res.status(403).json(EXAMPLE_REFUSAL_BODY). (3) After a successful replaceAll in that route, fire onRealItemPublished(getPool(), "progression", adminActor(req)?.id ?? null) — this implements the "edited into existence" half of the declared trigger. Optionally, also fire onRealItemPublished for "progression" after the boot roles seed in ensureDataFiles (index.ts:1074-1085) to cover the "seeded into existence" half for the admin-clear-then-reboot recovery path. Mirror the same column-list + guard + trigger treatment for circlesRepo/PUT /api/admin/circles/:id if the sibling replaceAll-laundering finding is confirmed for the map module.

## [MAJOR] No ExamplesBanner on member-facing pages whose modules hold examples: quests, map, progression, exchange

The 9-page wire set (scripts/wire-examples-banner.mjs:11-21: library, forum, stays, tools, badges, commerce, network, feed, health) omits five modules that DO seed rows: quests, map, progression, exchange, automation. Grep confirms Quests.tsx, VillageMap.tsx, Circles.tsx, Roles.tsx and Wallet.tsx contain zero example handling. gratitude/profiles seed nothing (correctly bannerless) and automation is admin-facing (defensible), but the rest are member/public pages: a fresh fork's ENTIRE quest board is three unlabeled example quests (quests is core, seeds at boot into an empty table), the public map shows three example circles, Roles shows three example roles, and the exchange page shows a purchasable-looking 'credits' listing with a posted price. The doc's own build-note #1 (docs/STANDING_EXAMPLES.md:124-131) concedes name-based labelling vanishes and 'the client must read is_example and render a chip' — yet quests/map/roles/exchange content carries no author name at all, so on these pages there is NO signal whatsoever. Amplifier: POST /api/game/quests/:id/claim (server/index.ts:10293-10338) has no isExampleRow guard, so a member can claim, submit, and have consented an unlabeled example quest — consent mints real recognition and stay credits against demo content (contrast the seven guarded routes at index.ts:4342/5481/6094/7419/7817/7967/8157). Either banner these pages or document why each omission is safe; the quest claim path additionally needs the guard.

**Fix:** 1) Add the inert guard to the quest claim route (server/index.ts, after the quest fetch at ~10297): `if (await isExampleRow(getPool(), "quests", req.params.id)) return res.status(409).json(EXAMPLE_REFUSAL_BODY);` — this single guard closes claim, submit, and consent since the chain requires a claim. 2) Wire ExamplesBanner into the member-facing pages: Quests.tsx (moduleId "quests", noun "quest"), VillageMap.tsx + Circles.tsx ("map", "circle"), Roles.tsx ("progression", "role"), Wallet.tsx exchange section ("exchange", "listing") — GET /api/examples already returns these module ids, so it is client wiring only. 3) Document the automation omission (admin-facing) and the refuted fresh-fork ordering in docs/STANDING_EXAMPLES.md, and add quest-claim to the doc's Guards list once implemented. 4) Optional hardening: have quest consent (not just admin quest create at index.ts:10254) treated as evidence of real use, or at minimum note that consenting cannot occur on examples once the claim guard lands.

## [MAJOR] Banner promises examples 'cannot be replied to' but the forum reply route has no example guard

ExamplesBanner.tsx:62-64 states 'They cannot be borrowed, booked, bought or replied to.' and docs/STANDING_EXAMPLES.md:108-109 claims 'Every mutation route that can address an example row calls it [assertNotExample].' Neither is true for replies: POST /api/forum/threads/:id/replies (server/index.ts:4556-4596) inserts with no isExampleRow check. Two of the three seeded threads (ex-thread-water, ex-thread-event) are unlocked, so a real member's reply lands on an example thread, mutates the example row (reply_count + last_reply_at, index.ts:4590-4593), and — because replies do not trigger retirement (forum's trigger is thread creation) — sits there until someone posts a real thread, at which point retireExamples deletes the parent thread (examples.ts:649) and the member's real (is_example=0) reply becomes an invisible orphan: their words silently destroyed. POST /api/forum/threads/:id/subscribe (index.ts:4664) similarly allows subscribing to example threads, leaving orphan subscription rows after retirement. Notification fan-out is incidentally safe (forum.ts:100,162 skip empty-passwordHash targets). Either add the reply/subscribe guards to match the banner, or soften the banner copy — right now the client promises a refusal the server does not perform.

**Fix:** In POST /api/forum/threads/:id/replies (server/index.ts, after the forumThreadById lookup at ~4566) and POST /api/forum/threads/:id/subscribe (~4667), add the same guard the sibling heart route uses at index.ts:4342: `if (await isExampleRow(getPool(), "forum_threads", req.params.id)) return res.status(409).json(EXAMPLE_REFUSAL_BODY);` (both helpers are already imported at index.ts:221). Consider the same for POST /api/forum/threads/:id/report. Client-side, Forum.tsx already renders the 409's message inline per the docs' pattern; optionally also hide the reply composer when the thread is an example (requires exposing is_example from forumThreadById). Finally, fix the doc drift: STANDING_EXAMPLES.md names assertNotExample(row) but the shipped helper is isExampleRow + EXAMPLE_REFUSAL_BODY.

## [MAJOR] Feed/forum retirement scoping comment is false — cross-module deletion leaves the banner claiming examples on an emptied page

examples.ts:43-46 claims feed's forum_threads entry 'is scoped by the seeded ids rather than the whole table; see retireExamples()' — but retireExamples (examples.ts:639-650) has no such scoping: every table in EXAMPLE_TABLES gets a plain `DELETE ... WHERE is_example = 1`. Because forum and feed share forum_threads (EXAMPLE_TABLES at :51-52), the first real FORUM thread deletes the feed's three example posts with no feed tombstone, and the first real FEED post deletes the forum's example threads while forum_replies example rows survive (feed's list omits forum_replies). Those surviving orphan replies keep 'forum' in withRows (refreshRowPresence :174-189 counts forum_replies first), so GET /api/examples keeps listing forum and the Forum page keeps rendering 'These are standing examples' over a page showing none — precisely the banner-over-nothing failure the build notes say was fixed for gratitude/profiles (doc :164-167). Client honesty depends on the withRows accounting being truthful; the unscoped shared-table deletes break it in both directions.

**Fix:** Make retirement match the comment's promise by scoping the shared forum_threads table per module in retireExamples (server/lib/examples.ts). Feed's example rows are distinguishable: they are seeded with kind from the feed block (kind='post', title NULL) while forum's are threads with titles. Concretely: replace the table-name-only EXAMPLE_TABLES entries with optional per-entry scope predicates, e.g. EXAMPLE_TABLES.forum = [{table:"forum_replies"},{table:"forum_threads", where:"kind <> 'post'"}] and EXAMPLE_TABLES.feed = [{table:"forum_threads", where:"kind = 'post'"},{table:"health_events"}], and have both retireExamples and refreshRowPresence/hasRealContent apply the same scope so withRows accounting stays truthful. Alternatively store the seeded ids per module in example_state (or a sibling table) at seed time and delete by id list. Either way, also update the false comment at examples.ts:43-46 to describe the actual mechanism, and add a regression test: seed forum+feed, retire one, assert the other module's example rows and banner survive intact and no orphaned forum_replies remain.

## [MINOR] network-sync job iterates the example peer: outbound HTTP to example.org every 6h and mutation of example rows

syncPeers selects peer_instances WHERE status='active' with no is_example filter (network.ts:83-85), and the seeded example peer is status 'active' with baseUrl https://example.org (examples-seed.json:686-693). Every 6 hours (index.ts:2739-2743) — and immediately on adding any real peer (index.ts:6400) — each fork sends /api/platform/info and /api/network/published requests to a third-party domain it does not control, then writes last_error onto the example row (network.ts:117-120), or 'paused' if example.org ever answers JSON with a mismatched identity. A scheduled job repeatedly addressing and mutating example rows is exactly the bypass class the inertness rule forbids. Fix: add AND is_example = 0 to the syncPeers SELECT (or seed the peer with a non-active status).

**Fix:** Add the filter the seed already promises: in server/lib/network.ts syncPeers, change the query to "SELECT * FROM peer_instances WHERE status = 'active' AND is_example = 0" (the column exists per drizzle/0046_standing_examples.sql:36). Prefer this over seeding the peer non-active, since the example's status is displayed in the UI and 'active' is the intended look; filtering in syncPeers also keeps the last_error/paused writes from ever touching example rows.

## [MINOR] Replying to an example thread is unguarded; retirement then orphans the member's real reply

POST /api/forum/threads/:id/replies (index.ts:4556-4596) has no isExampleRow check: a real member's reply INSERTs with is_example=0 attached to the example thread, bumps the example row's reply_count, and onReplyCreated fires notifications (including toward the example author, whose notifications can never be read). docs/STANDING_EXAMPLES.md lists reply-liveness as an OPEN question for Rye, but rule 1 as shipped says every mutation is refused — code and contract disagree. Concrete damage: when forum examples retire, retireExamples deletes forum_threads/forum_replies WHERE is_example=1 only; the drizzle schema has no foreign keys (grep FOREIGN KEY|REFERENCES over drizzle/ finds none), so the member's real reply survives as an orphan pointing at a deleted thread — their words silently disappear from every view. Subscribe (4664) and report (4674, whose soft-report auto-hide can hide an example thread) are similarly unguarded. Fix: guard the reply route with the EXAMPLE_REFUSAL (and subscribe/report if rule 1 stands), or have Rye resolve open question 1 and update the contract.

**Fix:** In server/index.ts, after fetching the thread in the reply route (~line 4567), add the same guard as the heart route: if (await isExampleRow(getPool(), "forum_threads", req.params.id)) return res.status(409).json(EXAMPLE_REFUSAL_BODY);. Apply the same guard to subscribe (4664) and report (4674) — or at minimum add AND is_example = 0 to the soft-report auto-hide UPDATE. Alternatively, if Rye resolves open question 1 in favor of live replies, retirement must handle real replies on example threads (e.g., skip deleting threads holding is_example=0 replies, or delete those replies knowingly) and STANDING_EXAMPLES.md rule 1 must be amended to match.

## [MINOR] Example library items inflate the supply-vs-backing check; assorted admin mutations address example rows unguarded

supplyVsBacking's backing figure sums credit_value over ALL non-written-off library_items with no is_example filter (library.ts:632-635), so the three example items add phantom backing and can keep the over-issuance red flag grey while real circulating credits exceed real shelves — an example row participating in an economic invariant readout (the seed note at examples.ts:412-413 even acknowledges coverage rises). Fix: AND is_example = 0. Same unguarded-admin-mutation pattern, non-economic but violating rule 1's 'every mutation refused': PUT /api/admin/library/items/:id can write off an example item (index.ts:7610-7631), PUT /api/admin/badges/:id edits example badges (7893), PUT /api/admin/products/:id edits example products (6255), PUT /api/admin/network/share/:id flips example shared_items (6369-6376), POST /api/admin/syntheses/:id/publish publishes an EXAMPLE synthesis as a real forum thread while mutating the example recording/synthesis rows (6896-6960), call-task accept/dismiss acts on example tasks (6964-6973), and regen retract retracts example entries (7268-7295). None post ledger/Stripe state, but each lets example rows drift from the seeded shape and confuses retirement semantics.

**Fix:** In supplyVsBacking (server/lib/library.ts:633) change the backing query to "SELECT COALESCE(SUM(credit_value),0) AS s FROM library_items WHERE status <> 'written_off' AND is_example = 0". Then add the existing guard idiom — if (await isExampleRow(getPool(), "<table>", req.params.id)) return res.status(409).json(EXAMPLE_REFUSAL_BODY); — to the seven unguarded admin routes in server/index.ts: PUT /api/admin/library/items/:id (library_items, ~7612), PUT /api/admin/badges/:id (badges, ~7896), PUT /api/admin/products/:id (payment_products, ~6256), PUT /api/admin/network/share/:id (shared_items, ~6372), POST /api/admin/syntheses/:id/publish (call_syntheses, ~6900 — this one is highest priority since it creates a real forum thread from example content and the seed note already claims the refusal exists), POST /api/admin/call-tasks/:id/accept|dismiss (call_tasks, ~6966), POST /api/admin/health/regen/:id/retract (regen_entries, ~7272). Cover with a test in server/lib/examples.test.ts asserting each route 409s with code example_immutable on a seeded example id, and that supplyVsBacking backing excludes example rows.

## [MINOR] The 'decided-without-seeding' stamp writes seeded_at NULL — it records nothing and the decision is not sticky

When hasRealContent is true, seedExamples calls stamp(p, moduleId, { seeded: false }) (examples.ts:305-309), but stamp (586-594) then inserts/updates example_state with seeded_at = NULL, so isSeeded() stays false. Two consequences: (a) contrary to the comment at 597-600 ('marked decided-without-seeding so the check runs once, not on every boot'), the has-real-rows check reruns on every boot for every such module; (b) the decision is not durable — a village that later deletes ALL its real rows in a module (delete every tool, every badge) gets examples seeded over it on the next boot/enable, which cuts against rule 3's 'a village that has spoken for itself is never talked over again' (the doc's letter only tombstones retirement, but the code comment clearly intended a durable decision). Either write a real marker (e.g. a decided_at column or seeded_at with zero rows) or fix the comment and accept re-checking.

**Fix:** Change examples.ts:307 from stamp(p, moduleId, { seeded: false }) to stamp(p, moduleId, { seeded: true }). This follows the existing precedent in the same file: gratitude and profiles are deliberately stamped seeded while creating zero rows (comment at 155-161), and modulesWithExamples() (163-167) additionally requires withRows.has(id), so a seeded-with-zero-rows module never shows a false 'showing examples' banner. Side effects are benign: a later real publish triggers onRealItemPublished -> retireExamples, which deletes zero example rows and stamps the permanent retired_at tombstone, making the decision fully durable. Then simplify stamp's signature (the seeded:false branch becomes dead) and, if desired, note in scripts/check-examples.mjs that seeded_at may mean 'decided' rather than 'rows written'. Alternative (if boot-time re-checking is actually wanted so a village that empties a module DOES get examples): delete line 307 entirely and rewrite the comment at 597-600 to say the check reruns each boot — but do not keep the current stamp, which writes NULL and records nothing either way.

## [MINOR] Feed's two definitions of 'real feed content' disagree: seeding checks kind='post' anywhere, retirement triggers on the feed category only

REAL_CONTENT_CHECK.feed (examples.ts:83-90) counts non-example forum_threads with kind = 'post' in ANY category, while the retirement hook fires only when thread.category === stringVar('feed.category_slug') (index.ts:4517-4519). POST /api/forum/threads accepts kind='post' with any forum category (index.ts:4468-4473), so a real micropost outside the feed category permanently suppresses future feed seeding without ever retiring standing feed examples. The doc specifies the check as 'a non-example thread in the feed's own category' — the implementation should filter on the category slug (matching the trigger), not on kind alone.

**Fix:** In server/lib/examples.ts, make REAL_CONTENT_CHECK.feed category-scoped to match both the retirement trigger (server/index.ts:4517) and the feed lens query (server/index.ts:4256): import stringVar from server/lib/variables.ts (same directory, no cycle) and change the query to `SELECT COUNT(*) n FROM forum_threads WHERE is_example = 0 AND category = ?` with [stringVar("feed.category_slug")]. Drop the kind filter — the lens displays every kind in the category, and the retirement hook already treats any kind in that category as real feed content, so category-only is the single consistent definition. Optionally note in the comment that re-pointing feed.category_slug changes what counts as real feed content, which is exactly the lens semantics.

## [MINOR] retireExamples stamps the permanent tombstone even when every DELETE failed, leaving examples stuck with no removal path

Each per-table DELETE in retireExamples is wrapped in its own try/catch that logs and continues (examples.ts:641-654), and the retired_at tombstone insert (662-668) then runs unconditionally. A transient DB error during the deletes (deadlock, timeout, connection blip) yields retired_at set with example rows still in the tables; every subsequent call — including POST /api/admin/modules/:id/examples/clear, which short-circuits on isRetired (index.ts:4185-4187) and retireExamples' own isRetired guard (636) — is a permanent no-op, so the rows are unremovable without manual SQL while the module reports examplesRetired=true. 'Deliberately forgiving' is right for the caller's 201, but the tombstone should only be stamped when the deletes succeeded (or the clear endpoint should be allowed to re-run the deletes for an already-retired module).

**Fix:** In retireExamples, track whether any per-table DELETE failed for a reason other than table/column absence (mirror scripts/seed-examples.mjs: treat ER_NO_SUCH_TABLE and ER_BAD_FIELD_ERROR as benign, anything else as a real failure). If a real failure occurred, skip the example_state tombstone INSERT, the state.set, and the identity cleanup, log loudly, and return the partial removed count — the caller's 201 is unaffected (onRealItemPublished is already fire-and-forget) and the next retirement trigger or admin clear retries the deletes. Alternatively (or additionally), let POST /api/admin/modules/:id/examples/clear re-run the delete loop even when isRetired, keeping the tombstone semantics one-way for seeding but giving admins a repair path for leftover rows.

## [MINOR] Real recordings ingested via the Riverside webhook or YouTube RSS job never retire automation examples

onRealItemPublished('automation') fires only on the manual POST /api/admin/recordings route (index.ts:6743). The Riverside webhook (index.ts:6668, ingestRecording + putTranscript) and the YouTube RSS scheduler job (index.ts:2753-2758) both create real recordings without any retirement hook, so a village whose recordings arrive through its live integrations shows the example recording/synthesis alongside genuinely real ones indefinitely — against rule 3's 'retires the moment the village publishes anything real'. (It matches the seed's declared _trigger literally, so if this is intentional the declared-trigger doc should say webhook/RSS ingestion does not count; note ingestRecording also fires the manual-route hook even when r.fresh is false, i.e. a duplicate ingest that created nothing.)

**Fix:** In server/index.ts, fire the retirement hook on fresh integration ingests: in the Riverside webhook (after the putTranscript block around line 6709) add `if (r.fresh) onRealItemPublished(getPool(), "automation", null);` and in the recording-rss job loop (around line 2757) add the same guarded call when `r.fresh` is true. Optionally tighten the manual route at 6743 to match (it is always fresh today, so no behavior change). Alternatively, if webhook/RSS ingestion is deliberately excluded, update the header contract in server/lib/examples.ts and the _trigger note in server/seeds/examples-seed.json:582 to state explicitly that only manual admin ingestion retires automation examples and that integration-fed villages must use POST /api/admin/modules/automation/examples/clear.

## [MINOR] Example identities count as 'living members' for the solo-founder self-consent window

server/index.ts:10414 computes livingMembers as `(await members.all()).filter((u) => u.email).length` — all three example identities carry emails (@examples.invalid), so once any NEEDS_IDENTITIES module (forum/feed/commerce/health/network/exchange) has seeded, the count is inflated by 3. quest.self_consent_until_members defaults to 6 (shared/gameVariables.ts:224-230), so a founder with just two real co-members already loses the self-consent exception while demo content is up — the window effectively shrinks from 6 real people to 3. Contradicts the stated contract that the identities are excluded from 'launch counts... and every metric that counts humans' (examples.ts:257-258, 742-745; seed _identities note). health.ts:54 shows the correct pattern (is_example = 0). Filter !u.isExample here (the adjacent comment 'tombstoned members do not count' suggests this filter was intended to be stricter than a truthy email check anyway).

**Fix:** In server/index.ts:10414, replace the truthy-email filter with the health.ts:54 counting rule so both example identities AND tombstones are excluded: `const livingMembers = (await members.all()).filter((u: any) => !u.isExample && u.email && !String(u.email).endsWith("@anonymized.invalid")).length;` — isExample is already mapped on MemberRecord (server/repos/users.ts:119) and tombstone emails are always `deleted-<id>@anonymized.invalid` (server/index.ts:2126). This makes the code match both the adjacent comment ("tombstoned members do not count toward the size") and the example-identity exclusion contract.

## [MINOR] Sybil eligibility helper does not exclude example identities despite the documented claim

eligibleSenderIds() (server/index.ts:4239-4249) — explicitly 'the Sybil eligibility rule... in ONE place', consumed by the badges engine and health snapshots — iterates members.all() with no isExample filter. All three example identities pass the stage test (stageGranted 'member'/'member'/'co-creator' in examples-seed.json) and land in the eligible set. examples.ts:257 and the seed's _identities note both claim exclusion from 'the Sybil helper'; it is not implemented. Practical exposure is currently low (the identities cannot log in, so they never appear as senders in gratitude_log), but the stated invariant is false and the helper is the single point every breadth metric trusts — add `if (u.isExample) continue;`.

**Fix:** In eligibleSenderIds() (server/index.ts, inside the for loop at ~line 4244), skip example identities before the eligibility test: add `if (u.isExample) continue;` as the first statement of the loop body (or use the existing isExampleUser() helper from server/lib/examples.ts:743 for consistency). This makes the implementation match the exclusion documented at server/lib/examples.ts:255-258 and examples-seed.json _identities._note.

## [MINOR] Health cycle snapshots count example content: decisions_opened_cycle and events_total_cycle

members_total correctly excludes examples (health.ts:54), but two sibling metrics in the same snapshotCycle() do not: decisions_opened_cycle (health.ts:99-106) counts forum_threads kind='decision' with no is_example filter — the seeded ex-thread-decision (created_at = seed time, so inside the current cycle's window) counts as a real decision and its example author inflates distinctAuthors, feeding the F13 authorship-concentration read; events_total_cycle / byKind (health.ts:59-66) counts the seeded example health_events row ('ex-event-1'). When the first real cycle closes on a demo fork, its frozen snapshot permanently records example activity as village activity — snapshots are INSERT IGNORE and never recomputed (health.ts:35-39), so retirement cannot fix the row afterward. Add `AND is_example = 0` to both reads (both tables carry the column per drizzle/0046_standing_examples.sql:20,31).

**Fix:** In server/lib/health.ts snapshotCycle(): add "AND is_example = 0" to the two example-blind reads — (a) the events query at lines 59-62: "SELECT kind, COUNT(*) AS n FROM health_events WHERE at >= ? AND at < ? AND is_example = 0 GROUP BY kind"; (b) the decisions query at lines 99-103: "... FROM forum_threads WHERE kind = 'decision' AND is_example = 0 AND created_at >= ? AND created_at < ?". For consistency, consider the same filter on the members_active_cycle read (lines 68-71) as cheap insurance even though seeded example events currently carry no actor_user_id, and on the all-time governanceReads queries (health.ts:304-313), which count the example decision while it stands (those are live reads, so they self-correct on retirement — lower priority). Snapshot rows already frozen with example content on existing deployments cannot be repaired automatically per the never-recompute rule; if any demo fork has closed a cycle with examples standing, a deliberate one-off correction (delete the affected snap-N-decisions_opened_cycle / snap-N-events_total_cycle rows before the next close, or accept the row) needs a human decision.

## [MINOR] Public GET /api/examples announces modules that are only enabled in preview/members lifecycle

GET /api/examples (server/index.ts:5848-5850) is deliberately unauthenticated, justified by 'the banner it drives is the honest label on content a visitor can already see' (comment at :5839-5847). But seeding runs whenever lifecycle leaves 'off' — including into 'preview' (admin-only) and 'members' (index.ts:4160-4173) — and modulesWithExamples() (examples.ts:163-167) has no lifecycle filter. So an admin previewing, say, stays or commerce broadcasts that module's id to any anonymous visitor, whose own page requests would be refused by requireModule. The leak is metadata-only (module ids, never rows), but it contradicts the endpoint's stated rationale and quietly reveals a village's not-yet-public module experiments. Intersect the response with modules whose effective lifecycle is visible to the caller (or at least exclude 'preview').

**Fix:** In the GET /api/examples handler (server/index.ts:5848), intersect modulesWithExamples() with modules the caller may know about, mirroring the /api/platform/info floor: always include core modules and those with LIFECYCLE_RANK[effectiveLifecycle(id)] >= LIFECYCLE_RANK.public for anonymous callers (or >= members, matching platform/info's existing public floor); include preview modules only when `await isAdmin(req)` — the admin previewing a module still needs the banner, so preview must not be dropped unconditionally. Note the client fetch in client/src/components/ExamplesBanner.tsx:22 is a bare fetch with no Authorization header, so if a caller-aware filter is chosen, that fetch must attach the session credential or admins will lose the banner on preview pages. Minimal safe version: filter out any module whose effectiveLifecycle is 'off' or 'preview' (fixes both the preview leak and the off-after-seeding case) and accept that preview pages show unlabelled examples to admins, or thread auth through as above.

## [MINOR] Example identity is bootstrappable into a hidden founder, breaking the 'can never log in' contract

POST /api/admin/bootstrap (server/index.ts:3776-3796, response at :3843) finds the target by email with no isExample check. On an unbootstrapped fork (or via BREAK_GLASS_ADMIN_EMAIL), supplying ADMIN_PASSWORD plus ex-user-mira@examples.invalid promotes the is_example=1 row to founder and — because its passwordHash is empty — returns the set-password claimUrl directly in the HTTP response, so the operator sets a password and signs in as 'Mira (example)'. That account is then invisible on the admin roster (index.ts:12116 filters !u.isExample) — a founder no admin screen shows — and a later example retirement hard-DELETEs it (examples.ts:677: DELETE FROM users WHERE is_example = 1), destroying a founder account outside anonymizeMember's settle-first checks. The seed and examples.ts:258-259 promise the identities 'can never log in'; the ordinary login/forgot-password paths do hold (index.ts:3720, 3877 — empty hash refuses), but bootstrap does not. Refuse bootstrap (and set-password) for isExample rows. Requires the bootstrap credential, hence minor.

**Fix:** In server/index.ts, inside POST /api/admin/bootstrap right after `let user = all.find(...)` (line 3776), refuse example rows before any mutation: `if (user?.isExample) return res.status(409).json(EXAMPLE_REFUSAL_BODY);` (or a plain 409 "That address belongs to a standing example identity, which can never sign in"). Add the same guard defensively in POST /api/auth/set-password after `members.byId(claim.userId)` (line 3925-3926) so a token minted before the guard cannot be redeemed. Optionally also reject any bootstrap email ending in `@examples.invalid`, and consider a regression test in server/lib/examples.test.ts asserting bootstrap with ex-user-mira@examples.invalid leaves role = 'member' and returns no claimUrl.

## [MINOR] forgetExamplesCache() has no callers, so the banner mislabels the member's first real content in-session

ExamplesBanner.tsx:35-38 exports forgetExamplesCache() with the comment 'Call after publishing something real, so the banner leaves with the examples' — but no file in client/src ever calls it (grep: only the definition). The module-level cache (:15) lives for the whole SPA session, so after a member publishes the first real thread/post/tool and the server retires the examples, the page still shows 'These are standing examples... Nobody here made them' above content that now includes the member's own real item, until a hard reload. That is the inverse honesty failure — real content labeled as example. Wire the publish paths on the nine bannered pages (e.g. Forum.tsx thread create, Feed.tsx post create) to call forgetExamplesCache() and re-fetch.

**Fix:** Wiring forgetExamplesCache() into the nine publish-success handlers is necessary but NOT sufficient: useShowingExamples holds `showing` in useState with effect deps [moduleId], so clearing the module cache alone will not re-render an already-mounted banner. Do this instead, all in ExamplesBanner.tsx: (a) add a module-level Set<() => void> of listeners; useShowingExamples subscribes on mount and re-runs its fetch when notified; (b) make forgetExamplesCache() null the cache AND notify listeners; (c) call forgetExamplesCache() from each create-success handler (Forum.tsx post(), Feed.tsx post create, Library/Network/Stay/ToolsHub/VillageHealth/Badges/Contribute equivalents). One timing caveat: onRealItemPublished is fire-and-forget server-side (void retireExamples), so an instant client re-fetch of /api/examples can race the retirement; either optimistically set showing=false for that moduleId in forgetExamplesCache(moduleId?) (retirement is one-way, so optimism is safe) or delay the re-fetch by ~1s. The optimistic per-module variant is the simplest correct fix.
