# Defects that survived two independent refutation passes

108 raised by thirteen audit lenses; 74 survived the first pass; **72 survived a second, stricter pass** that required a reproduction against current line numbers rather than an argument. That pass also CORRECTED severities: no criticals remain (they were either fixed or downgraded as unreachable).

Execution order and batching: see AUDIT_2026-07-30_FIXPLAN.md.


## HIGH

### client/src/components/SwapCard.tsx:171
- **Fix** (small): Add role="alert" to the error <p> at SwapCard.tsx:171 and role="status" to the receipt <p> at :164-165, then make the same one-attribute change at the sibling message sites (Wallet.tsx:78-79, Library.tsx:74-75, Stay.tsx:99-100, Badges.tsx:73 — notice/success gets role="status", failure gets role="alert"). No new component, no layout or copy change.
- **Risk:** A live region inserted into the DOM at the same instant its text appears is announced by most current screen reader/browser pairs but not all; the fully robust form is an always-mounted empty <div aria-live="polite"> whose text is swapped, which is a larger edit. role="alert" is assertive and interrupts whatever is being read, so it must stay on these request-completion messages only and never on anything that re-renders per keystroke. Sighted members notice nothing. No migration, no server change, no invariant touched.
- **Reproduction:** Reproduced against current code. Wallet.tsx:145 renders SwapCard when swap is enabled; the Confirm button (SwapCard.tsx:277) calls confirm() (:123), which POSTs /api/exchange/swap. server/index.ts:7454-7460 returns 409 {code:"QUOTE_STALE", quote} before any exchange_orders insert; post() rethrows with code attached (SwapCard.tsx:110); the catch at :143-145 does setQuote(e.body.quote) and setError("The price moved while you were deciding — this is the trade as it stands now."); that string renders at SwapCard.tsx:171 as `{error && <p className="text-sm text-amber-800 bg-amber-50 rounded-lg px-4 py-2.5 mb-4">{error}</p>}` — no role="alert", no aria-live — and busy clears at :150 so Confirm re-

### server/index.ts:1940
- **Fix** (small): Add a `sender` field to DEFAULT_EMAIL_CONFIG (server/index.ts:335) and change line 1940 to `from: opts.from ?? cfg.sender?.trim() || process.env.EMAIL_FROM || "Amora Site <notifications@amora.cr>"`, validating the value matches either `addr@dom.tld` or `Name <addr@dom.tld>` before use; whitelist `sender` in the PUT /api/admin/email-config body mapping (~8291) so an admin can set it without a deploy, and append the EMAIL_FROM row plus the "set your sender address" step to the docs/FORK_RUNBOOK.md env table in the same session.
- **Risk:** The sender address becomes admin-editable, so a malformed or wrong-domain value silently kills all email exactly as today — hence the shape validation, and the underlying swallow at 1949-1955 (separate finding) still hides a rejected send. Existing deployments are unaffected because the blank default falls through to the current literal; keeping that literal as the last-resort fallback holds the brand ratchet at its baseline 25 for server/index.ts (dropping it entirely is also allowed, since counts may only decrease). No migration: dbDocument merges DEFAULT_EMAIL_CONFIG over the stored doc, so the new key appears on read without touching existing rows.
- **Reproduction:** Reproduced end to end against current code. server/index.ts:1940 hardcodes `from: opts.from ?? "Amora Site <notifications@amora.cr>"`, and no caller ever supplies `from`: the eight direct call sites (2964, 2972, 3222, 3252, 4294, 8770, 8778 — 4294 passes only replyTo) all omit it, and the notification spine cannot pass it at all because NotifyDeps.sendEmail (server/lib/notify.ts:103) types only {to, subject, html} and server/index.ts:1593 forwards it verbatim. No override exists in any config plane: DEFAULT_EMAIL_CONFIG (335-344) and getEmailConfig (1849-1858) carry no sender field, the admin PUT /api/admin/email-config (8286-8309) whitelists only the four pathway addresses plus the two secr

### server/index.ts:3280
- **Fix** (small): Add POST /api/auth/forgot-password immediately before the set-password route: rate-limit with the existing overLimit helper, look the member up by email, and when one exists with a passwordHash mint makeSetPasswordToken(user.id) and email `${notifyDeps.origin()}/set-password?token=…` via sendResendEmail — always answering the same 200 body so it is not an account-enumeration oracle — then add a "Forgot password?" link to client/src/pages/Login.tsx after line 118. The SetPassword page and /api/auth/set-password already handle the token, so no new token machinery is needed.
- **Risk:** Three things the fix must respect. (1) client/src/pages/SetPassword.tsx:35 hard-redirects to /admin on success; a member resetting a password would land on the admin refusal screen, so send non-admins to /profile. (2) The route makes set-password tokens member-reachable, and readSetPasswordToken (938) has no single-use marker and /api/auth/set-password does not bump tokenVersion — the reset handler should bump tokenVersion (or that replay defect should land first) so a leaked link cannot be replayed within its hour. (3) It makes email deliverability load-bearing for account recovery: on a fork whose Resend sender domain is unverified the send fails silently after a 200 and the member gets nothing, so log the failure loudly and keep the response text honest ("if an account exists, a link is on its way") rather than promising delivery.
- **Reproduction:** Reproduced end to end. Login (client/src/pages/Login.tsx:47-130) offers only email/password plus "Create Account" — no reset affordance. Wrong password returns 401 "Invalid credentials" (server/index.ts:3157); Register returns 409 "Email already exists" (3121). The only routes that write passwordHash are register (3128), the legacy SHA256 re-hash inside login which already requires the correct password (3162), /api/auth/set-password (3294), and anonymizeMember which blanks it (1801). The only minter of a set-password token, makeSetPasswordToken (932), is called at exactly 3219 and 3249, both inside POST /api/admin/bootstrap (3178) — which 403s "Already bootstrapped" for any email but BREAK_G

### server/index.ts:3301
- **Fix** (small): Add one admin route beside /api/admin/users/:id/revoke-sessions that calls the existing makeSetPasswordToken(target.id) and emails the /set-password?token=... link through sendResendEmail exactly as bootstrap does at 3221-3228, plus a recordEvent audit row. Reuses both existing primitives, no schema or migration.
- **Risk:** The route is an admin-to-member impersonation lever: set-password returns a live session token at 3296, so whoever holds the link is logged in as the target. It must refuse a founder target unless the actor is a founder (mirroring 3316's actor rule) or a plain admin escalates by resetting the founder's password; it must email the link only and never return claimUrl in the response the way bootstrap deliberately does at 3276; and it must write a kind:"audit" health_event since this is the one admin action that can produce member-attributed activity. Separately, set-password (3294) does not bump tokenVersion, so a reset leaves pre-existing sessions alive - fine for the lockout case, insufficient for the stolen-password case, and a distinct change.
- **Reproduction:** Reproduces exactly against current code. The entire credential surface is three routes: /api/auth/register (3115, refuses an existing email at 3120 with 409, so it cannot re-credential), /api/auth/login (3145, verifyPassword at 3156 is the only way in), and /api/auth/set-password (3281, requires a token at 3289). makeSetPasswordToken (932) has exactly two call sites, 3219 and 3249, both inside POST /api/admin/bootstrap (3178-3277), which is closed to her twice over: 3203 returns 403 "Already bootstrapped" for any email other than BREAK_GLASS_ADMIN_EMAIL once an admin/founder exists, and even on the break-glass email the mint at 3219 sits behind `if (!user.passwordHash)` so a member who HAS a

### client/src/pages/VillageMap.tsx:305
- **Fix** (small): On line 305 lift the overlay above the tab bar (`z-50` -> `z-[70]`, per the MobileFab.tsx:24 convention), and on line 306 give the panel mobile bottom clearance so no control lands in the bar band: `p-6 pb-[calc(1.5rem+4rem+env(safe-area-inset-bottom,0px))] md:pb-6`.
- **Risk:** The scrim now covers the tab bar, so while the sheet is open a tap on the bar closes the sheet (the overlay's onClick={onClose}) instead of navigating — standard modal behaviour, but a change members would notice. z-[70] also places this sheet above the FAB (z-[60]); that is the correct order for a modal but it is a new entry in the layering ladder documented in MobileFab.tsx:24, which should be updated in the same edit so the convention comment stays true. Desktop (md:) rendering is unchanged since both new values are mobile-only or md-reset.
- **Reproduction:** Reproduced. /map (App.tsx:174) renders CircleAccordion on mobile (VillageMap.tsx:82-84); a role row (:230) opens NodeCard (:85-87). NodeCard's overlay is `fixed inset-0 z-50 flex items-end md:items-center` (:305) and its panel is `p-6 max-h-[80vh] overflow-y-auto` with no bottom inset (:306), so the panel's last child sits ~24px above the viewport bottom. That last child is the module's primary action: the vacant-seat CTA (:342-345), then the Raise my hand / Cancel row (:337-339), then the status line (:380) — and identically the contact Send/Cancel row (:358-360, :364-367). MobileTabBar is rendered from Layout.tsx:598 and portaled with createPortal(bar, document.body) (MobileTabBar.tsx:100)

### server/lib/library.ts:308
- **Fix** (small): Two small pieces: (1) wrap library.ts:308-327 in try/catch that runs the same compensating DELETE/UPDATE as the !r.ok branch before rethrowing, so a thrown escrow post never leaves an unfunded loan; (2) add a loud repair in assertLibraryInvariants (library.ts:538) that runs BEFORE the assert — for each unsettled live loan with escrow_credits>0 where `await ledgerEntryExists(pool, `loan:${id}:escrow`)` is false, UPDATE it to status='cancelled', escrow_credits=0, settled_at=NOW(), return the item to 'available', and console.error + recordEvent — then assert. Only loans whose escrow leg is provably absent get repaired; a genuine drained escrow (leg present, balance short) must still refuse boot.
- **Risk:** The repair cancels a member's reservation and puts the item back on the shelf — a visible behaviour change, so it must be loud (admin notify/event), never silent. Do not route it through settleLoan as-is: settleLoan would compute release = escrow_credits and try to move credits out of an escrow that holds nothing, driving sys:library-escrow negative (refused, non-faucet) — zero escrow_credits in the same UPDATE that sets settled_at so the single-terminal claim stays honest. No migration needed. The ledgerEntryExists test must be exact-key, not a looser LIKE, or a real drained escrow would be silently normalised — the one thing library.ts:20-22 forbids.
- **Reproduction:** Reproduced. reserveItem commits the loan row (escrow_credits=N, status='reserved', settled_at NULL) at library.ts:300, then posts escrow in a separate transaction at library.ts:308-318. postTransfer rethrows every non-ER_DUP_ENTRY error (ledger.ts:346-348), and the compensating DELETE/UPDATE at library.ts:323-324 is inside `if (!r.ok)` (line 319) with no try/catch around 308-327 — so a thrown escrow post (or a container kill between the two commits) leaves a live loan expecting N credits while sys:library-escrow holds 0. 'reserved' is in LIVE_LOAN_STATUSES (library.ts:83), so escrowReconciliation (library.ts:528-535) returns expected=N, actual=0, and assertLibraryInvariants throws at library

### server/lib/library.ts:308
- **Fix** (small): Wrap the escrow postTransfer at library.ts:308-318 in try/catch that runs the same compensating cleanup already at 323-324 (DELETE the loan, set the item back to 'available') and then rethrows, so a caught exception cannot leave an orphan. Then make the orphan harmless if the process dies outright: in settleLoan, before line 414, read `const deposited = await ledgerEntryExists(pool, `loan:${input.loanId}:escrow`)` and use `deposited ? loan.escrowCredits : 0` as the escrow ceiling for both fee and release.
- **Risk:** Visible behaviour change: settling a loan whose deposit never landed now releases 0 instead of N. That is correct (the member was never charged) but the member-facing "released" number changes, and it must stay inside settleLoan's "a re-settle only REPAIRS" invariant — the ledgerEntryExists call is a read, so the repair path stays idempotent. Useful side effect worth noting: with the guard in place, cancelling an orphan clears settled_at and drops `expected` by N, so reconciliation heals itself and boot recovers without hand-editing rows; without it an orphan still requires manual DB cleanup. The reserveItem catch must not swallow the original error — rethrow after compensating, since the compensation itself can fail if the DB is genuinely gone. No migration needed.
- **Reproduction:** Reproduces end to end. server/index.ts:6560 calls reserveItem; library.ts:296-300 commits the library_loans row with escrow_credits=N, and the escrow leg is a separate autocommit at library.ts:308-318. ledger.ts:346-348 rethrows every non-ER_DUP_ENTRY error and postTransfer has no deadlock retry (only postTransferPair does, ledger.ts:399-408), so a deadlock/lock-timeout/recycled connection propagates out of reserveItem and skips the compensating cleanup, which lives only inside the if (!r.ok) branch at library.ts:319-325. Express 4 (package.json:56) does not catch async-handler rejections and errors.ts:115-117 logs the unhandled rejection without exiting, so the loan row survives with no led

### server/index.ts:5842
- **Fix** (small): Immediately after the lifecycle check at server/index.ts:5842-5844, require a shared secret before any write: add "riverside_webhook_secret" to SECRET_KEYS in server/lib/secrets.ts and compare req.headers["x-riverside-secret"] against secretValue("riverside_webhook_secret") with a timing-safe equal, returning the same inert 200 {received:true, discarded:"..."} when the secret is unconfigured or mismatched (fail closed, so an unsigned delivery never writes) — and reuse the Stripe handler's per-IP bucket pattern (server/index.ts:2857) so a flood of fresh ids gets a 429.
- **Risk:** Fail-closed means any deployment already pointing a live Riverside webhook at this URL silently stops ingesting until a founder pastes the secret in admin settings — surface it on the automation admin card the way webhookSecretConfigured() is surfaced for Stripe (server/index.ts:5110, 8324). It also breaks the asserted behaviour in server/loop.e2e.test.ts:2443-2447, which must be updated to send the header. Keep the 200-and-discard shape rather than a 401 to preserve the stated invariant that this webhook never 404s/errors into a provider retry storm, and register the new key via the existing secrets store rather than a new env-only mechanism (ARCHITECTURE.md:670).
- **Reproduction:** Reproduced end to end. server/index.ts:5841 registers POST /api/webhooks/riverside after express.json (2877) and outside the requireModule("automation") mounts (5831-5834). Its only guard is the lifecycle rank check at 5842-5844; at preview or above it falls through to 5845-5846, which validate nothing but truthy id and non-blank title, then call ingestRecording (5847, writing INSERT INTO recordings at server/lib/recordings.ts:85-102) and putTranscript (5852, writing INSERT INTO transcripts at server/lib/recordings.ts:115-125) with attacker-supplied values. No header is read in the handler: no signature, no secretValue(...), no shared secret. No rate bucket either — the Stripe webhook's buck

### server/index.ts:6916
- **Fix** (small): In the /api/badges/match handler, add the gate its own contract specifies before querying: `const viewer = await authedUser(req); if (!(await isAdmin(req)) && !(viewer && hasCapability("quest.consent", await capabilityCtx(viewer)))) return res.status(viewer ? 403 : 401).json({ error: "Matching members is for stewards" });` and change line 6934 to emit `name: firstName(r.name)` so even privileged callers get the platform-standard first name (id + handle still disambiguate).
- **Risk:** Behaviour change: /api/badges/match starts returning 401/403 to anonymous and ordinary members. Grep finds zero consumers in client/, scripts/, or server/ (the MemberMatchPanel the doc describes was never built), so no UI breaks today — but the village-map concierge integration named in docs/modules/badges.md:128 must send a steward token when it is built. firstName() collapses members sharing a first name in match output; id and handle remain for identification. No migration. Respects the one-capability-gate invariant by using hasCapability/capabilityCtx rather than a new ad-hoc check.
- **Reproduction:** Reproduces for /api/badges/match only. server/index.ts:6824 mounts requireModule("badges") as the sole middleware; server/lib/modules.ts:181 lets anonymous callers through when the lifecycle is 'public' (a documented, admin-settable state for a non-core module). The handler at server/index.ts:6916 never calls authedUser/isAdmin/hasCapability, both branches SELECT u.name (6923, 6930) from the MySQL users table (drizzle/0001_init.sql:13, a single free-text name filled verbatim at register, index.ts:3116/3126), and 6934 emits it raw with the stable userId, LIMIT 100. So `curl '/api/badges/match?skill=spanish'` with no token returns up to 100 members' full names. This contradicts the recorded co

### game-amora/server/index.ts:9434
- **Fix** (trivial): In the wall handler, drop heart rows before the window: `const wall = log.filter((g) => g.kind !== "heart").slice(-60).reverse()...` — filter BEFORE `.slice(-60)` so the wall still shows 60 entries. (`kind` is already on the entry: server/repos/gratitude.ts:53.)
- **Risk:** Visible behaviour change for members too: any hearts currently rendering on /gratitude disappear from the wall. That matches the documented intended default (feed.hearts_on_wall: false, docs/modules/gratitude-feed.md:126), but a deployment whose wall is mostly hearts will look suddenly emptier — worth a line in the module doc or a follow-up that registers the variable properly rather than hardcoding. No migration and no economy impact: gratitude_log rows are untouched, so /api/game/gratitude/me, the per-kind caps in server/lib/gratitude.ts:100-119, heart_count recomputation (server/index.ts:3612), gratitude-cycles settlement (server/lib/gratitude-cycles.ts:158) and health metrics all read the log directly, not the wall. Existing tests are safe — loop.e2e.test.ts:368 asserts a kind='gratitude' send and :507 only asserts status 200.
- **Reproduction:** Reproduces exactly. server/index.ts:3608 derives the heart message as `❤ on "<first 80 chars of thread.title ?? thread.body>"`; server/lib/gratitude.ts:129 and server/repos/gratitude.ts:72 persist it verbatim into gratitude_log.message; server/index.ts:9432 serves it with `_req` — no authedUser, no requireModule (no /api/game middleware exists; gratitude is core so effectiveLifecycle is always "public"), and no `kind` filter (line 9437 maps `message: g.message` for every row). server/loop.e2e.test.ts:368 calls the wall with no token and gets 200 with message text, proving anonymous reachability today. The suppression the design called for was never built: docs/modules/gratitude-feed.md:126 s

### server/index.ts:1737
- **Fix** (trivial): Widen the de-attribution UPDATE at server/index.ts:1737 so it also scrubs the denormalized text, e.g. `UPDATE notifications SET actor_user_id = NULL, title = 'A message from a departed member', body = NULL WHERE actor_user_id = ?`, and update the docstring bullet at ~1716 to say de-attributed AND text-scrubbed.
- **Risk:** Members would notice: an unread restorative intake or gratitude note in a steward's inbox loses its readable title and body, so the intake steward's only working copy of an open rupture disappears at resolve time (ARCHITECTURE.md:454-456 keeps only a pointer on the exits row) — acceptable under the deletion right, but the exit flow should settle intakes before resolving. The blanket scrub also flattens non-identifying titles (e.g. quest/thread names) on rows that merely had the departed member as actor; scoping by type would preserve those at the cost of a longer statement. No schema change needed, and no migration is included, so notifications belonging to members anonymized before the fix stay unscrubbed unless a runOnce backfill is added (it cannot be keyed by actor_user_id, since that is already NULL — it would have to key off type/title patterns, which is why doing it now matters).
- **Reproduction:** Reproduces end-to-end on current code. POST /api/exit/restorative-intake (server/index.ts:6284) inserts a notification for each intake-role holder with title `A private intake from ${user.name}` (6307) and body = message.slice(0,2000) (6308); insertNotification persists both verbatim (server/lib/notify.ts:120-123). anonymizeMember (1722) then only DELETEs the departed member's own inbox (1736) and nulls actor_user_id on the rest (1737) — no statement in the function touches notifications.title or notifications.body. GET /api/notifications (8099) -> notificationsFor (server/lib/notify.ts:230-250) returns title (245) and body (246) unchanged, so the steward still reads the departed member's fu

### server/index.ts:10212
- **Fix** (trivial): Make a failed boot actually fatal: replace `startServer().catch(console.error);` with a handler that logs and then exits — `startServer().catch((e) => { console.error("[startup] refusing to serve:", e); setTimeout(() => process.exit(1), 2000).unref(); });` — mirroring errors.ts:122's 2-second alert grace, which is safely under the scheduler's 15-second first tick. Optionally also move `startScheduler(getPool())` from line 2286 to immediately before `server.listen` at 10207, so arming the timer is the last thing boot does.
- **Risk:** Behaviour change operators will notice: railway.toml's `restartPolicyMaxRetries = 3` now applies, so three consecutive failed boots stop the deployment outright instead of leaving a half-up container — a visible outage in place of a silent zombie (that is the intent, but it is new). The 2-second exit grace must stay well below the scheduler's 15-second first tick, or the fix reintroduces the very tick it prevents. If startScheduler is also moved, keep it after every registerJob call (all ten are above line 2286) so the registry is complete when the tick arms; job cadence is unaffected because dueness lives in the scheduled_jobs table, not in boot timing. No migration.
- **Reproduction:** Reproduced end to end. startScheduler(getPool()) at server/index.ts:2286 arms a ref'd 15s first tick (server/lib/scheduler.ts:76 — only the 5-min interval on line 77 is unref'd at line 79). loadModuleSettings runs at 2291, so the jobs' effectiveLifecycle guards see real on-states. assertLibraryInvariants at 2324 then throws from server/lib/library.ts:541-543 ("refusing to serve"), so server.listen at 10207 is never reached — but server/index.ts:10212 is startServer().catch(console.error), which neither exits nor re-throws (and being caught, errors.ts:115's unhandledRejection handler never sees it; only errors.ts:118-124's uncaughtException path exits). 15 seconds later the tick runs every du

### server/lib/stays.ts:315
- **Fix** (small): Add a releaseAbandonedStayPurchases(pool, hours) beside staysOpenState that sets status='cancelled' for provider='stripe' AND status='pending' AND created_at < NOW() - INTERVAL hours HOUR, skipping (and reporting) any row where ledgerEntryExists(pool, `ord:${id}:leg1`) — exactly the two rules exchange.ts:702-711 states — using numberVar('exchange.order_expiry_hours') clamped by the existing ORDER_EXPIRY_FLOOR_HOURS of 25; call it from the stay-nightly job at server/index.ts:2106, before the `effectiveLifecycle("stays") === "off"` early return so a demoted module cannot re-wedge the sweep.
- **Risk:** No migration ('cancelled' is already in the enum at drizzle/0021_stays_and_payments.sql:61). Members would notice only that an abandoned purchase stops showing as pending. Two invariants to respect: never cancel a row whose ledger leg exists (that is a settle failure, not an abandonment — notify admins instead), and keep the window above Stripe's ~24h session life so a member still on the payment page is never cancelled out from under their money. If a cancelled session does later complete, the settle handler at index.ts:2652 sets status='paid' unconditionally and still mints, so no money is stranded.
- **Reproduction:** Reproduced end to end. index.ts:4743 inserts stay_purchases status='pending' before createCheckout; the only paths off 'pending' are the two webhook handlers (index.ts:2652 settle, 2695/2715 reversal), which require a Stripe event that never fires for an abandoned session, and payments.ts handles no checkout.session.expired (only lines 281/336). The one admin route, POST /api/admin/stays/purchases/:id/refund, hard-refuses at index.ts:4981 ('Only paid purchases refund'). No sweep exists: releaseAbandonedFiatOrders (exchange.ts:718, called index.ts:2129) covers exchange_orders only, and its own comment at index.ts:2126-2128 states this exact failure mode ('blocks disabling the module AND block

### client/src/components/Layout.tsx:57
- **Fix** (medium): Make the shell read the config the client already serves: add `logo`, `heartLogo` to the overlay's `images` and a `siteUrl` (plus optional `eventsUrl`) to `project` in `DEFAULT_BRAND` (server/index.ts:425-433) — `/api/game/config` already passes `m.project`/`m.images` wholesale so they flow for free — then in Layout.tsx call `fetchConfigCached()` (client/src/lib/gameApi.ts:47) and drive the logo src+alt (57, 408), the tagline (411) and the copyright name (581) from `project.name`/`project.tagline`/`images.*`, and render the five outbound links (237, 383, 542, 552, 562) only when `project.siteUrl` is set so a fork with no external site shows no link rather than a link to Amora. Seed Amora's current asset paths and amora.cr URLs into its own overlay in the same change so tenant one looks identical, and refresh scripts/brand-refs-baseline.json.
- **Risk:** Three things to respect. (1) The logo becomes async, so the header must reserve the 64px box (and the footer the 90px box) with a same-height placeholder or every page shifts on first paint — a visible CLS regression on the most-seen surface. (2) The overlay's blank-inherits contract (server/index.ts:421-424) means Amora's gameConfig defaults must carry its current asset paths and amora.cr URLs, or tenant one loses its logo and its nav "Main Site" button — a change its own members would notice immediately. (3) `alt="Amora"` is hardcoded inside AmoraLogo.tsx:52 and :79, so the name has to be threaded into those components or the fix leaves the wrong village name for screen readers. Separately worth its own ticket: fix `stripComments` in scripts/check-brand-refs.mjs so `https://` in a URL is not treated as a comment — until then the guard cannot see brand references in any href anywhere in the repo, and the baseline must be re-derived after that fix (counts will rise, so re-baseline deliberately rather than let CI mask it).
- **Reproduction:** Reproduces exactly as claimed, at current line numbers. CHAIN. Entry: any route — 44 of the 46 files in client/src/pages wrap their tree in `<Layout>` (e.g. C:/Users/taren/Desktop/Amora/game-amora/client/src/pages/Home.tsx:1 import, :85 `<Layout>`). Layout is the shell. 1) The fork sets its identity and the wizard reports success. Admin.tsx:201 registers the "Make This Yours" tab; SetupWizard (client/src/pages/Admin.tsx:5904) Identity section collects `project.name` and `project.tagline` (Admin.tsx:6053-6054 `brandField("project","name",…)` / `brandField("project","tagline",…)`); saveBrand PUTs to `/api/admin/brand` (Admin.tsx:6006-6011) and toasts "Saved" (Admin.tsx:6016). Server persists i

### server/db/migrate.ts:79
- **Fix** (medium): Make the file's progress durable so a re-run resumes instead of replaying: add a `statements_done int NOT NULL DEFAULT 0` column to `_migrations_applied` (idempotently, via an `information_schema` check inside `applyPending` before line 66), upsert the row's counter after each statement in the loop at line 79, and start the loop at `parts.slice(statementsDone)` — treating a file as complete only when the counter reaches `parts.length`. Keep the fail-loud throw at `server/index.ts:2069` exactly as it is; this only changes what the *next* boot replays.
- **Risk:** Three things to respect. (1) The ledger table itself gains a column, and its own `CREATE TABLE IF NOT EXISTS` at lines 60-65 will not add it to deployments that already have the table — the ALTER must be written idempotently against `information_schema.columns`, because a failure there is the same brick one level up. (2) Resume-at-offset makes "a shipped migration file is never edited" a hard invariant rather than a convention; if a file's statement order changes after partial application, the resume skips the wrong statements. Worth stating in the file header and in the ARCHITECTURE.md migration trap list. (3) `server/db/testDb.ts:57` and `migrationStatus` (lines 94-108) read the same table, and `server/db/schema.ts:216` declares it — all three need the new column or the drizzle type and the scratch-schema harness drift from production. Members notice nothing; this is boot-path only.
- **Reproduction:** Reproduced against current code. Chain, entry point first: 1. `server/index.ts:2066-2069` (in `startServer`) is the only production entry: `const conn = await dbConnect(url); const result = await applyPending(conn, ...); if (result.failed) throw new Error(`migration failed: ${result.failed}`);` — a migration failure kills boot. `docs/ARCHITECTURE.md:69-72` states this is intentional ("any migration failure -> the process throws and never serves"), and nothing runs before this block (the crash handlers at 2051-2055 only report). 2. `server/db/migrate.ts:44-46` — the connection is plain: `mysql.createConnection({ uri: url, multipleStatements: false, timezone: "Z" })`. No transaction is opened 


## MEDIUM

### client/src/components/Layout.tsx:81
- **Fix** (small): On the button at line 81 add `type="button"`, `onClick={() => setPathsOpen((v) => !v)}`, `aria-expanded={pathsOpen}`, `aria-haspopup="true"` and `aria-controls="paths-menu"`, and put `id="paths-menu"` on the motion.div at 87; leave the wrapper's hover handlers untouched so mouse behaviour is unchanged.
- **Risk:** One noticeable behaviour change for mouse users: clicking "Your Path" while the menu is already hover-open now collapses it, and because the pointer is still inside the wrapper no fresh mouseenter fires, so it stays closed until the pointer leaves and re-enters. If that matters, pair the toggle with an onKeyDown Escape-to-close and keep onClick as a plain open, or track hover and click opens separately. Also worth adding a document-level click/Escape close (dropdownRef at 27 is declared and currently used for nothing else), but that is beyond the minimal fix. No migration, no invariant touched — the four hrefs in the `paths` array (16-21) already match the routes at App.tsx:166-169.
- **Reproduction:** Reproduces against current code. Layout.tsx:69 gates the desktop nav to >=1280px; the dropdown wrapper's only state triggers are onMouseEnter (78) / onMouseLeave (79) and the file contains no onFocus, onKeyDown or focus-within anywhere; the button at 81 has no onClick and none of aria-expanded/haspopup/controls, so Enter and Space fire a click with no listener; the menu body is gated by `{pathsOpen && (` at 86, so the four Links at 94-109 never mount and are not in the tab order — the next Tab reaches Circles (115). The keyboard-correct twin (button with onClick at 281) is inside the xl:hidden mobile block at 271, so it is display:none and unfocusable at that width — no code path covers a ke

### C:/Users/taren/Desktop/Amora/game-amora/client/src/pages/GratitudeWall.tsx:102
- **Fix** (trivial): Add an accessible name to the number input at lines 102-109, derived from live config rather than hardcoded: aria-label={`Amount of ${currency.toLowerCase()} to send`} (currency is already in scope from the useState at line 20). Optionally give the email input and textarea real <label htmlFor> associations in the same edit, but they at least have placeholder-derived names today, so the amount field is the only one that must change.
- **Risk:** Essentially none. aria-label is invisible to sighted users, changes no layout, no state, and no submitted payload; the form still posts {toEmail, amount, message} unchanged. Two things to respect: derive the string from the `currency` state variable so a fork that renames the currency gets the right label (hardcoding "Gratitude" would violate white-label discipline, though it would not trip scripts/check-brand-refs.mjs, whose BANNED list at lines 35-40 is village names only); and treat this as the one field, not a licence to sweep all 117 controls in the same change — docs/AUDIT_2026-07-30_IMPROVEMENTS.md:102 explicitly warns that a 117-file sweep is unreviewable and should burn down behind a jsx-a11y baseline ratchet instead.
- **Reproduction:** Reproduces exactly at the cited location. /gratitude mounts unguarded at App.tsx:204 (gratitude is a core always-public module), the authenticated branch at GratitudeWall.tsx:81 renders the form, and the amount input at GratitudeWall.tsx:102-109 carries only type/min/required/value/onChange/className — no id, aria-label, aria-labelledby, title, or placeholder, and its wrapper at line 93 is a plain div, not a label. Accessible-name computation returns empty, so it is announced as a bare spin button (WCAG 4.1.2 Level A). The systemic count is also accurate: 120 <label> elements in pages/ + components/ (excluding components/ui/), only 3 with htmlFor (Login.tsx:74, Login.tsx:92, Profile.tsx:168)

### client/src/pages/Register.tsx:218
- **Fix** (trivial): In the class string at Register.tsx:218 and the identical one at Login.tsx:126, replace `border-amber text-amber ... hover:bg-amber/5` with `border-teal-deep bg-white text-teal-deep ... hover:bg-teal-deep/5` — adding the explicit `bg-white` fill removes the dependence on the gradient and puts #157f7d on #ffffff at 4.81:1 for the label and the border (4.66:1 in the hover state), clearing both 1.4.3 and 1.4.11. Do NOT reach for `text-gold`: index.css:323 aliases it to `--color-amber`, and `--color-gold` #a06b1c is only 3.82:1 on this page's surface.
- **Risk:** Visible design change members will notice: the two auth pages lose their only amber accent and read as all-teal, so it is worth a brand sign-off rather than a silent edit. It is also a deliberately narrow fix — it leaves the other ~155 `text-amber` sites alone, including Layout.tsx's 25 `hover:text-amber` nav links, which are #ecb163 on #157f7d at 2.53:1 and fail the same rule; those belong in the token-collapse work already scoped at docs/AUDIT_2026-07-30_IMPROVEMENTS.md:104, not here. No migration, no invariant touched; `client/` is in the brand ratchet zone but neither token is a brand-name string, so `node scripts/check-brand-refs.mjs` is unaffected.
- **Reproduction:** Reproduces exactly. App.tsx:196 routes /register to Register; Register.tsx:78 sets the page surface to `bg-gradient-to-br from-teal-deep/5 to-amber/5` over `--background: #f2f2f2` (index.css:100), i.e. ~#e8ecec; Register.tsx:218 renders the alternate-action link as `border-2 border-amber text-amber font-semibold ... hover:bg-amber/5` with no background fill, so glyphs and the 2px border both paint amber onto that near-white surface. `amber` = #ecb163, declared at index.css:78 inside the `@theme inline` block opened at line 26 (and again explicitly as `.text-amber` at index.css:282-284) — both cited lines say what the finding claims. #ecb163 (L=0.5016) on #e8ecec (L=0.8320) is 1.60:1, and 1.9

### client/src/components/Layout.tsx:70
- **Fix** (small): Replace the `text-white/50|60|70` resting tints on all 14px header, mobile-drawer, and footer text in Layout.tsx with solid `text-white` (the only value that clears 4.5:1 on #157f7d, at 4.812), and move the de-emphasis/hover signal off contrast to `hover:underline`, since `hover:text-white` becomes a no-op once the resting state is opaque.
- **Risk:** Two member-visible consequences. (1) The nav and footer lose their intentional visual hierarchy — all links read at equal weight — so a replacement hover/active affordance must ship in the same change or hover feedback disappears entirely. (2) The footer's existing `hover:text-amber` is itself only 2.529:1 on #157f7d, so hovering a 'fixed' link drops it back below AA; the honest fix pairs solid white with a non-color hover rather than amber. Also, solid white clears AA by only 0.31, so this fix does not tolerate any future change to `--color-teal-deep` (index.css:72), which is a brand-overlay value — a village forking the repo with a lighter primary re-breaks it. Out of scope but adjacent: 107 total `text-white/50|60|70|80` occurrences across client/src (GameDashboard, InvestorSummary, MobileTabBar, SeasonBanner, Admin, CoCreatorsGuide, the three Journey pages, MasterPlan, ProjectHistory), each needing its own background checked since many sit on gradients or images.
- **Reproduction:** Reproduced against current code. Layout.tsx:53 sets `bg-teal-deep` for the header and Layout.tsx:402 for the footer; index.css:72 defines `--color-teal-deep: #157f7d` and index.css:234-235 binds `.bg-teal-deep` to it. There is no global override — no `nav a`/`footer a` color rule, no `prefers-contrast` block, and the only forced white is `.btn-amora` (index.css:331), unrelated. The 14px (`text-sm`) links then render at: Layout.tsx:70, 81, 115, 118, 121, 125, 133, 141, 149, 157, 165, 173, 181, 189, 196, 199 (desktop nav, `text-white/70`); 274, 282, 305, 308, 311, 315, 320, 325, 330, 335, 340, 345, 350, 355, 359, 362, 367, 378 (mobile drawer, `text-white/70`, on `bg-teal-deep/95` at 271 — the 

### client/src/contexts/AuthContext.tsx:61
- **Fix** (trivial): Narrow both wipe paths to genuine rejection: change the else at :61 to `else if (res.status === 401 || res.status === 403) { localStorage.removeItem("amora-auth-token"); setToken(null); }` and delete the removeItem/setToken(null) pair from the catch at :67-68, leaving only the console.error — the token stays on the device so the next reload with signal re-validates and signs her back in.
- **Risk:** Creates a transient state members can notice: token present in localStorage but user===null, so pages gated on `user` render signed-out while gameFetch (gameApi.ts:103-112, which reads localStorage directly) still sends the Authorization header. That resolves on the next reload, but if any view offers "log in" while requests are already authenticated it will look inconsistent — worth showing a "couldn't reach the village, retry" state instead of the logged-out view, or retrying validateToken once. No migration, and the tokenVersion revocation invariant is respected because a revoked token still returns 401 and is still cleared.
- **Reproduction:** Reproduces on current code. AuthContext.tsx:43-48 loads the stored token on mount and calls validateToken; :55-57 fetches /api/profile; :61-63 (else branch) removes the token for ANY non-ok status, and :65-68 (catch) removes it for any rejected fetch. Only 401/403 means the token is bad. A dropped mobile connection mid-request (fetch rejects with TypeError), or a 502/503/500/429 while the container restarts, therefore deletes the member's only persisted credential. It is the only credential: gameApi.ts:4 and :90-92 read the same single localStorage key, there is no cookie session, no refresh endpoint, and the only auth routes are register (server/index.ts:3115), login (:3145) and set-passwor

### client/src/pages/Admin.tsx:225
- **Fix** (medium): Add a QuestsTab component that lists GET /api/quests and calls POST/PUT/DELETE /api/admin/quests (same authHeaders(password) pattern as QuestClaimsTab), register it in the "The Game" nav group at Admin.tsx:225 and in the tab switch at Admin.tsx:6708, and repoint the wizard row at Admin.tsx:5999 to that tab with honest hint copy ("seeded starter quests — rewrite, add or remove them here").
- **Risk:** Deleting a quest is destructive and currently unguarded: server/index.ts:9149-9152 removes the row with no check for outstanding quest_claims (consumed by server/lib/badges.ts:268 and server/lib/health.ts:91), so a delete button should refuse or warn while claims are in claimed/submitted state, in the spirit of the openStateCheck "settle first" invariant. Editing a title is safe — server ids are the join key (Quests.tsx:64-69) — but the change is live to members with no deploy. New client-side labels must stay brand-clean: client/ is a ratchet zone for scripts/check-brand-refs.mjs. PLATFORM_FOUNDATION.md:49 and docs/FORK_RUNBOOK.md:39 should be reconciled in the same session per CLAUDE.md.
- **Reproduction:** Reproduced. Fresh fork: server/index.ts:2787-2795 seeds server/seeds/quests-seed.json into an empty quests table, including Amora-branded copy (quests-seed.json:8 "member of the Amora Family for life"). Quests is a core always-public module (CLAUDE.md invariant), so Quests.tsx:113 fetches /api/quests (server/index.ts:9116) and renders it. Admin CRUD exists at server/index.ts:9121 (POST), 9140 (PUT), 9149 (DELETE) behind isAdmin — the finding's 8972-9004 has shifted, real range is 9121-9152 — and `grep -rn "admin/quests" client/src/` still returns nothing. No admin surface: CONTENT_SECTIONS (Admin.tsx:15-22) covers journeys/circles/roles only; "The Game" nav (Admin.tsx:222-243) has quest-clai

### server/index.ts:3294
- **Fix** (small): Bind the claim token to the account's password state: have makeSetPasswordToken take the user's current passwordHash and include a fingerprint of it (e.g. sha256(user.passwordHash || "")) in the signed payload, then in /api/auth/set-password after loading the user at 3291 reject with 401 when that fingerprint no longer matches the account's current hash — writing the new hash at 3294 then makes the token dead on first use. In the same update at 3294 also bump `u.tokenVersion = (u.tokenVersion ?? 0) + 1` (and encode the session at 3296 from the bumped value) so setting a password revokes prior sessions.
- **Risk:** makeSetPasswordToken gains a parameter, so both mint sites (3219 and 3249) must pass the account's current hash. Claim links already in flight when the change deploys carry no fingerprint and would be rejected — recovery is re-running the break-glass bootstrap, which line 3216-3221 already exists to do, but it is a user-visible "link invalid" for anyone mid-claim. The tokenVersion bump signs out any existing session for that account: no observable change today because claim-pending accounts have no sessions, but it becomes the semantics the future reset flow inherits (which is the correct behaviour). Do not add a separate used-tokens table for this — the fingerprint keeps the token stateless, matching how the route is written.
- **Reproduction:** Reproduces against current code. readSetPasswordToken (server/index.ts:938-953) validates only the HMAC (943-946), purpose/userId (948) and exp (949, 60-min TTL at 931) — it makes no DB call and there is no nonce/single-use store for it (the codebase's only nonce table, wallet_challenges in server/lib/base-reads.ts:128-166, does consume its nonce, so the pattern exists but was not applied). POST /api/auth/set-password (3281) redeems at 3289, writes the hash at 3294 (`members.update(user.id, (u: any) => { u.passwordHash = hash; })`), and issues a session at 3296 with `fresh.tokenVersion ?? 0` — no used-marker, no tokenVersion bump, no binding to the passwordHash present at mint time, so the i

### client/src/pages/VillageMap.tsx:114
- **Fix** (trivial): On line 114 change `role="img"` to `role="group"` (keeping `aria-label="Village map"`), since `group` has no presentational-children characteristic and still accepts an accessible name — the circle and role-seat buttons at 138-152 and 164-177 then reach the accessibility tree unchanged.
- **Risk:** Members using a screen reader will now hear the map's interior instead of one summary graphic: the loose `<text>` labels at 153-159 and 196-198 become announced content, so the desktop map gets noticeably more verbose (circle name may be heard twice — once as the `<text>` node, once as the button's aria-label). Nothing changes for mouse or sighted-keyboard users; no migration, no invariant touched. If the verbosity matters, follow up by adding aria-hidden to the decorative `<text>` nodes rather than restoring role=\"img\". Severity downgraded from the claimed high: the map module ships OFF by default (shared/modules.ts:116-119) and the same data and actions are available as real buttons in CircleAccordion (VillageMap.tsx:219-239) at viewports under 768px.
- **Reproduction:** Reproduces against current code. App.tsx:174 routes /map to VillageMap; VillageMap.tsx:79-84 renders MapCanvas inside `hidden md:block` while the accessible accordion sibling is `md:hidden` (display:none, pruned) at desktop widths; MapCanvas still opens with `<svg … role=\"img\" aria-label=\"Village map\">` at VillageMap.tsx:114, and every interactive node lives inside it — `circle role=\"button\" tabIndex={0} aria-label` at 138-152 and `g role=\"button\" tabIndex={0} aria-label` at 164-177. The evidence says what is claimed: WAI-ARIA 1.2 §5.2.9 Presentational Children — \"The DOM descendants are presentational. User agents SHOULD NOT expose descendants of this element through the platform a

### client/src/pages/Wallet.tsx:32
- **Fix** (small): In load() (lines 32-37) add a three-way status state — `const [status, setStatus] = useState<"loading"|"ready"|"failed">("loading")` — throwing on `!r.ok` and setting "failed" in the .catch, "ready" on success; then at line 87 branch on status first, showing a short "Couldn't load your balances — retry" line (calling load()) while status is "failed" and a neutral placeholder while "loading", reserving the "Nothing yet" copy for status === "ready" with zero balances. Apply the same status check to the listings empty-state at line 107 so it does not claim an empty exchange on a failed fetch.
- **Risk:** Members will notice new copy on the wallet: a placeholder instead of an instant empty state on first paint, and a retry line where the page previously said "Nothing yet". Keep the failed state scoped to the balances and listings panels — do not let it suppress or false-error the swap, notSwappable, halted or receipts sections, which already key off optional `data?.swap` / `data?.mine` fields. No migration, no server change, no ledger or capability invariant touched. Only Wallet.tsx is in scope; the same `r.ok ? r.json() : null` shape appears 25 times across client/src, so this fix intentionally leaves the rest of the client inconsistent rather than widening the change.
- **Reproduction:** Reproduces exactly against current code. client/src/pages/Wallet.tsx:32-37 is still `.then(r => r.ok ? r.json() : null).then(setData).catch(() => {})`, and `data` (line 27) is the only payload state — `error` (line 29) is written solely by buy() at line 59, and there is no loading flag anywhere in the file. Both failure modes reach the same render: a network timeout is swallowed by .catch at line 36, and a 500 (server/index.ts:7138 runs ~10 unguarded awaited DB calls; any rejection lands in the terminal handler at server/index.ts:10193 returning `{error:"Internal server error"}` with status 500) makes r.ok false. Either way data stays null, so line 62 `data?.mine?.balances ?? {}` yields {}, 

### client/src/contexts/AuthContext.tsx:106
- **Fix** (small): Add app.post("/api/auth/logout") beside revoke-sessions (server/index.ts:3300) that requires authedUser(req) and bumps only the caller's own tokenVersion, and have logout() await that call before clearing localStorage.
- **Risk:** tokenVersion is an all-sessions-or-nothing lever (no per-session record exists), so every logout becomes a global sign-out — logging out on a shared tablet also signs the member out on her phone. That is a behaviour change members will notice; the alternative is a separate "Sign out everywhere" control on Profile. True per-session revocation would require a schema change. Add a loop-test step asserting a replayed post-logout token 401s.
- **Reproduction:** Reproduced end to end in current code. logout() at client/src/contexts/AuthContext.tsx:106-110 is three client-only statements with no fetch, and a repo-wide grep for logout/sign-out across server/ returns zero hits — no /api/auth/logout route exists (auth routes are only register 3115, login 3145, set-password 3281). authedUser() (server/index.ts:1148-1161) accepts any HMAC-valid token whose v matches the row (1159); decodeToken only expires it via TOKEN_TTL_MS = 30 days (919, 333); no session table exists in drizzle/. The only tokenVersion bumps are POST /api/admin/users/:id/revoke-sessions (3301) behind isAdmin (3302) and anonymizeMember (1802), whose only member-reachable caller is POST 

### client/src/pages/GratitudeWall.tsx:90
- **Fix** (small): Separate "budget unknown" from "budget is zero": widen the state at :19 to `GameMe | null | undefined` starting `undefined` (undefined = not loaded / load failed), then at :85-91 show the "unlocks as you progress" caption only when `budget && budget.total <= 0` and otherwise show "We couldn't load your budget — reload", and drop `!budget` from the disable list at :131 so the server's own 403/400 (gratitude.ts:90-95) speaks instead of a false statement about her standing.
- **Risk:** Copy change members will notice on the core gratitude page. Enabling Send while the budget is unknown means an over-budget click surfaces a server error after clicking, which cuts against the intent recorded in the comment at :122-123 — so it must be paired with the explicit "couldn't load" caption, not shipped alone. No migration, no ledger or capability invariant touched; the server-side budget guards stay the authority.
- **Reproduction:** Reproduces. fetchGameMe (client/src/lib/gameApi.ts:144-153) returns null on both !res.ok and throw, so a failed /api/game/me is indistinguishable from GratitudeWall's initial state (GratitudeWall.tsx:19, set at :30 with no catch or loaded flag). `user` is populated by a separate /api/profile fetch (AuthContext.tsx:53-72), so the signed-in form branch at :81 still renders; `budget` is undefined at :62, which selects the else-arm caption at :90 and trips `!budget` in the disable predicate at :131. The caption is provably false in that branch: gratitude.base_budget defaults to 100 (shared/gameVariables.ts:60-66) and only the `visitor` stage has gratitudeMultiplier 0 (shared/gameConfig.ts:197) w

### client/src/modules/ModuleProvider.tsx:61
- **Fix** (small): Make the manifest fetch depend on the session: in ModuleProvider, `const { token } = useAuth();` and change line 61 to `useEffect(refresh, [token])`, so login, register and logout each refetch /api/modules. To avoid a null→token flash on reload, also initialize AuthContext's token state synchronously from localStorage (AuthContext.tsx:40, `useState(() => localStorage.getItem("amora-auth-token"))`) instead of only in the effect at AuthContext.tsx:43-51.
- **Risk:** Couples ModuleProvider to AuthContext — it must stay nested inside AuthProvider (it is, App.tsx:217-218), or useAuth throws (AuthContext.tsx:139-141). If the AuthContext token stays effect-initialized, the effect fires twice on every load for signed-in members (one extra anonymous /api/modules request) and the anonymous response sets loaded=true first, which can flash <NotFound /> on /feed, /wallet, /library, /stay before the authed manifest lands — hence the synchronous token initializer in the fix. No migration or server change; the server-side gate at server/index.ts:3369-3374 is unchanged, so nothing new becomes reachable.
- **Reproduction:** Reproduced end to end. ModuleProvider (client/src/modules/ModuleProvider.tsx:61) fetches /api/modules once per page load via useEffect(refresh, []), reading the token from localStorage at that moment (ModuleProvider.tsx:47-48). For a first-time visitor that is null, and the server filters members-lifecycle modules out of an unauthenticated manifest (server/index.ts:3369-3374: `if (lc === "members") return authed;`, asserted by server/loop.e2e.test.ts:962-968). Login is pure SPA: pages/Login.tsx:21-22 does `await login(...); navigate("/profile")` with no reload, and AuthContext.tsx:85-87 only writes localStorage plus React state. Nothing ever calls refresh — grep for `refresh` across client/s

### client/src/components/Layout.tsx:496
- **Fix** (small): Add `const commerceModule = useModule("commerce")` and `const networkModule = useModule("network")` after the existing hooks (line 37), then add three footer entries: an ungated "Leaving Well" -> /exit-policy and a `networkModule &&` "Village Network" -> /network before the Governance column's </ul> (line 496), and a `commerceModule &&` "Contribute" -> /contribute in Resources before line 533.
- **Risk:** Add them to the footer, not the header: the comment at Layout.tsx:61-68 records that the desktop nav is already ~1250px wide and caused sideways scrolling below xl, so two more header links reopen that bug. The /contribute and /network links must stay module-gated — both pages render <NotFound /> when their module is off (Contribute.tsx:57 `if (modules.loaded && !commerce) return <NotFound />`, same shape in Network.tsx), so an ungated link would be a visible dead end on the default config where all optional modules ship off. Members will notice three new footer links; no migration, no invariant touched.
- **Reproduction:** Reproduced against current code. App.tsx:163-164 and :182 mount /network, /contribute and /exit-policy, and nothing in the app links to them. Layout.tsx desktop nav (69-244) enumerates /, the four paths (16-21), /circles, /roles, /quests, the nine module-gated links (/feed, /stay, /wallet, /badges, /library, /village-health, /forum, /map, /tools), /work-with-us, /how-we-create, admin-only /journey-to-launch (204), /profile|/login and external amora.cr — none of the three. The mobile drawer (273-390) mirrors that exact set. All five footer columns (Your Journey 418-459, Governance 465-496, Resources 502-533, Connect 539-575) omit them. config/mobileNav.ts TAB_SLOTS and FAB_ACTIONS omit them. 

### server/lib/exchange.ts:646
- **Fix** (small): In `swappableBalance`, add the commerce leg to `held`: also sum `token_ledger.amount` for rows with `to_account = memberAccount(userId)`, `token_type = slug`, `source = 'product_grant'`, `at > NOW() - INTERVAL ? DAY`, add it to the `exchange_orders` total, and fold that query's `MAX(at)` into the `clearsAt` calculation. Every token-granting product is forced onto the Stripe path (server/index.ts:5418), so every `product_grant` row is genuinely card-bought — no over-restriction, and the read stays valid on the `PoolConnection` that `capGuard` passes.
- **Risk:** Visible behaviour change for honest buyers: anyone who buys a token pack with a card now hits `RECENT_PURCHASE_HOLD` on swaps for `exchange.swap_fiat_hold_days` (45 by default), and `heldFromRecentPurchase`/`holdClearsAt` on the quote surface start reporting non-zero for pack buyers. Recurring token grants post one ledger row per period, so each renewal starts its own hold window — correct, but it means a monthly token subscription is permanently partly held. No migration; keep the function accepting `Pool | PoolConnection` so the in-transaction `capGuard` read stays serialized behind the same treasury lock, and keep the `Math.max(0, balance - held)` floor so an already-clawed-back grant cannot produce a negative swappable figure.
- **Reproduction:** Reproduced end to end against current code. (1) `tradingProblem` (server/lib/exchange.ts:108-124) blocks a token from being listed swappable only for never-listed/recognition/hypha/`moduleSoldTokens()`, and `moduleSoldTokens()` (exchange.ts:91-93) is the STATIC `MODULES.filter(m => m.sellsToken)` set — it knows nothing about `payment_products`. The product-create route validates only `purchaseProblem(tokenSlug)` (server/index.ts:5416), the weaker check, so any token listed `swappable` can also be sold as a commerce `token_pack`. No route cross-checks the two. (2) `POST /api/products/:id/checkout` (index.ts:5279) inserts only into `product_purchases` (index.ts:5347); the Stripe settle posts `

### server/index.ts:3764
- **Fix** (trivial): Make the Pulse text kind-aware at server/index.ts:3764: use the title when there is one, and for kind === 'post' emit a content-free line (e.g. `${firstName(user.name)} posted in the feed`) instead of `(thread.title ?? thread.body).slice(0, 60)`, keeping actorUserId/entityType/entityRef so click-through still works.
- **Risk:** Forward-only: health_events rows already written keep the leaked snippet, so a village that already ran at forum='members' needs the existing admin pulse-delete route (server/index.ts:10131) or a one-off UPDATE to redact history. Members will also notice micropost Pulse lines lose their text preview and become generic. Respect the invariant that module code emits activity only through moduleActivity (docs/ARCHITECTURE.md:306) — change the string, not the emission path.
- **Reproduction:** Reproduced end to end against current code. POST /api/forum/threads (server/index.ts:3714) allows kind='post' with no title (3731) and stores title NULL by construction (3748). The Pulse line at 3764 is `${firstName(user.name)} started \"${(thread.title ?? thread.body).slice(0, 60)}\"`, so a micropost emits its first 60 body characters. moduleActivity (server/lib/modules.ts:310) only returns early BELOW 'members', so at lifecycle 'members' it calls recordEvent (311), which defaults audience to 'public' (server/lib/events.ts:45). GET /api/game/pulse (server/index.ts:9977) reads recentEvents(pool,'public',…) with no authedUser, no requireModule and no /api/game middleware anywhere, and returns

### server/index.ts:1883
- **Fix** (small): In applyAcceptReward, before the members.update at server/index.ts:1874, when amount > 0 post the credit through the ledger — `const credit = await postTransfer(getPool(), { from: RECOGNITION_FAUCET, to: memberAccount(match.id), amount, source: "work_with_us_accepted", sourceRef: entry.id, description: "Work With Us proposal accepted", idempotencyKey: `work_with_us_accepted:${entry.id}` })` — bail out returning false if `!credit.ok`, then replace line 1883 with `u.recognitionBalance = credit.toBalance;` (keep the contribution row, and skip the transfer entirely when amount is 0 since postTransfer refuses non-positive amounts).
- **Risk:** Members already credited by the buggy path will see their balance snap to the ledger-true value (losing the phantom amount) the first time this or any other credit recomputes it — the same correction the next gratitude received would have applied anyway, but it is a visible drop. Also honour the amount>0 guard so an admin setting acceptGratitude to 0 still logs the contribution and pulse instead of the ledger refusing and the whole reward returning false; and `source`/`sourceRef` should be new values, not reused from another flow, so the ledger entry reads honestly in /api/game/ledger.
- **Reproduction:** Reproduced end to end. PUT /api/admin/submissions/:id/status (server/index.ts:3016, admin-gated 3017) calls applyAcceptReward at 3027 for a work-with-us submission moving to "accepted". applyAcceptReward (server/index.ts:1866) reads amount = getWorkWithUs().acceptGratitude (default 100, server/index.ts:448), pushes a contribution row with recognitionEarned: amount (1874-1882), and at 1883 does `u.recognitionBalance = (u.recognitionBalance ?? 0) + amount;`. The whole function (1866-1886) contains no postTransfer, no RECOGNITION_FAUCET, and no idempotencyKey. Every other recognition credit posts to the ledger and then RECOMPUTES the cache: server/lib/gratitude.ts:145-158 (postTransfer with ide

### server/index.ts:9882
- **Fix** (small): At server/index.ts:9869 change `_req` to `req` and resolve `const viewer = await authedUser(req); const admin = await isAdmin(req);`, then at line 9882 emit `holders: (admin || viewer) ? holders.filter(...).map(...) : []` plus an additive `holderCount: holders.filter((h) => h.roleId === r.id).length` so anonymous callers still see structure and seat counts but no names or userIds. Do not add a new capability — CLAUDE.md:74 forbids gating outside shared/capabilities.ts, and a plain login check is exactly the bar docs/modules/village-map.md:125 already states.
- **Risk:** The four Admin.tsx fetches of `${API_BASE}/roles` (client/src/pages/Admin.tsx:2701, 2989, 3172, 4813) send no auth headers, so the same change must add `headers: authHeaders(password)` to all four or the admin role-appointment panel (2701) and the exit panel (4813) will silently render every seat as vacant. Members with map.viewPeople are unaffected since they are authenticated; genuinely anonymous visitors lose holder names on any surface that reads /api/roles (none today outside Admin). Adding holderCount is additive, so client/src/pages/VillageMap.tsx and the static client/src/pages/Roles.tsx need no change. Respect the core-module invariant: do not try to fix this by mounting requireModule("progression"), which returns "public" for core modules and would be a no-op.
- **Reproduction:** Reproduces. server/index.ts:9869 registers `app.get("/api/roles", async (_req, res) => {` — the request object is `_req` and nothing in the handler reads auth, session, capability or any variable. No middleware covers the path: the only API-level `app.use` registrations are express.json (2877), the /api/admin audit hook (2886), CORS (2906) and the per-module requireModule mounts (3491, 3629, 4062-4065, 4456, 4646, 5256, 5499, 5831, 6320, 6510, 6824, 7127) — none matches /api/roles. shared/modules.ts:102 does list "/api/roles" under progression.apiPrefixes, but apiPrefixes is declarative only (referenced nowhere under server/), and progression is core:true so server/lib/modules.ts:81 (`if (de

### C:/Users/taren/Desktop/Amora/game-amora/server/index.ts:8722
- **Fix** (trivial): Insert the codebase's standard guard as the first statement of the handler at line 8722, before the submissions write: `if (await overLimit(`investor-docs:${clientIp(req)}`, 3, 60 * 60 * 1000)) return res.status(429).json({ error: "Too many requests. Please try again shortly." });` — optionally also accept and honour the same `hp` honeypot that /api/forms/submit uses at line 8927.
- **Risk:** overLimit is the shared DB-backed limiter, so the bucket is per-IP across all instances: several genuine investors behind one corporate NAT egress IP requesting the packet within the hour would get a 429. Pick the cap generously (3/hour is fine; 1/hour is not). Adding the honeypot also requires the InvestorJourney form to send `hp`, or bots and humans are treated identically — the rate limit alone needs no client change.
- **Reproduction:** PARTIALLY CONFIRMED — the abuse vector reproduces; the access-control headline does not. Reproduction of the part that is real (all lines current): 1. server/index.ts:8721 `app.post("/api/investor-docs/request", async (req, res) => {` — no middleware between the path and the handler (contrast 8639, `app.post("/api/admin/investor-docs/upload", adminOnly, upload.single("file"), ...)`). 2. 8722-8725 is the entire admission check: `const { name, email, accredited } = req.body ?? {};` then `if (!name || !email || typeof accredited !== "boolean") return res.status(400)`. No overLimit, no honeypot, no authedUser call anywhere in the handler. 3. 8727-8735 unconditionally appends to the shared store 

### server/index.ts:7031
- **Fix** (small): After the badgeProblem check at server/index.ts:7031, refuse a kind change when awards exist: if merged.kind !== existing.kind, SELECT COUNT(*) FROM badge_awards WHERE badge_id = ? and return 409 ("this badge has N awards — revoke them or create a new badge") since kind is the authority under which each award was made; and add a recordEvent({kind:"audit", text:`badge:edit:${req.params.id}`, actorUserId: adminActor(req)?.id ?? null}) so definition edits leave a trail like every award does.
- **Risk:** Stewards who legitimately want to reclassify a badge must revoke its awards first or create a new badge — a visible behaviour change on the admin badges screen (client/src/pages/Admin.tsx), which should surface the 409 message rather than swallow it. No schema change, no migration; existing loop.e2e assertions are unaffected because none of them flips kind. The guard must not block harmless same-kind edits (name, icon, description, active) or it will break normal badge editing.
- **Reproduction:** Reproduced. PUT /api/admin/badges/:id (server/index.ts:7014, isAdmin-only) merges a caller-supplied kind (7022) and capabilities (7023), validates only the POST-merge shape with badgeProblem (7030) — so the self-badge capability ban at server/lib/badges.ts:108-110 does not fire once kind is flipped to "granted" — then issues one UPDATE against `badges` alone (7032-7036) with no touch of badge_awards, no recordEvent and no notify. badgeGrantsFor (server/lib/badges.ts:146-149) joins badge_awards to badges with no kind or awarded_by filter, so every pre-existing self-claim row (created by POST /api/badges/:id/claim at 6937-6946 with awardedBy = the member) now yields the new capabilities; capab

### server/index.ts:9230
- **Fix** (small): At server/index.ts:9230 widen the gate from `isAdmin` alone to admin OR `hasCapability("quest.consent", await capabilityCtx(user))`, adding a guard that refuses a claim whose `claim.userId` is the actor (no self-consent); apply the same widening to GET /api/admin/quest-claims at line 9222 so a steward can see the claims they are now allowed to consent.
- **Risk:** Real authority expansion members will notice: consent mints Gratitude from sys:gratitude-pool (idempotency key `quest_consent:${claim.id}`, line 9321), grants stay credits, and advances stages — so every steward-circle appointee becomes a faucet operator, and the self-consent guard is load-bearing, not optional. Second, the audit spine at line 2886 attributes via `adminActor(req)`, which only `isAdmin` populates, so a steward's consent would leave no audit row unless the handler records one explicitly. Third, the declines branch (9232-9250) and the notification's `actorUserId: adminActor(req)?.id` need the same non-admin actor threaded through. Fixing only the report side instead (dropping the keys) would break roles-seed.json and the admin role picker, so it is not the smaller option.
- **Reproduction:** Reproduced end to end. GRANT: server/seeds/roles-seed.json:14 gives steward-circle ["quest.consent","quest.propose"]; server/index.ts:1340 feeds roleCapabilities into capabilityCtx; shared/capabilities.ts:112 (`if (ctx.roleCapabilities.includes(cap)) return true`) makes hasCapability("quest.consent") true. REPORT: server/index.ts:9689 (GET /api/game/progression) and 9400 (GET /api/game/me) both return `capabilities: ALL_CAPABILITIES.filter(c => hasCapability(c, ctx))`, client/src/components/ProfileJourney.tsx:63-67 renders each as a chip titled "A capability your stage or a role grants you", and GET /api/admin/members/:id/capabilities (server/index.ts:9787) reports source="role". ENFORCE: th

### server/index.ts:9869
- **Fix** (small): At server/index.ts:9869 mirror what /api/map already does: take `req` instead of `_req`, compute `viewPeople = (await isAdmin(req)) || (!!viewer && hasCapability("map.viewPeople", await capabilityCtx(viewer)))`, then emit `holders: viewPeople ? [...] : []` plus a `holderCount` so anonymous callers still get seat counts. map.viewPeople has a `guest` stage floor (shared/capabilities.ts:69), so any signed-in account still sees names and the badge-deny path still applies — the one gate extended, not bypassed.
- **Risk:** No member-visible change today since the route has zero consumers (Roles.tsx is static, no tests or smoke scripts touch it), but it is a wire-format change: `holders` becomes `[]` for anonymous and a new `holderCount` appears, which would break any unknown external/embed consumer. Do not also drop `capabilities` in the same edit without checking — that is the "shape" half of the payload and role capability lists are surfaced by /api/admin/roles as well.
- **Reproduction:** Reproduced end to end. server/index.ts:9869 registers `app.get("/api/roles", async (_req, res) => {` — the handler binds `_req`, so it never calls authedUser, isAdmin, capabilityCtx or hasCapability. Lines 9870-9872 load `await members.all()` and build `nameOf` via `firstName(...)`; lines 9878 and 9881 unconditionally emit `capabilities: r.capabilities ?? []` and `holders: holders.filter((h) => h.roleId === r.id).map((h) => ({ userId: h.userId, name: nameOf(h.userId) }))`. No middleware covers the path: the only app.use entries are the /api/admin audit hook (2886), CORS (2906), eleven requireModule mounts (3491-7128, none matching /api/roles), static (10146) and the error handler (10193); th

### server/index.ts:9912
- **Fix** (small): Hoist the ctx built inline at 9901 into a variable, then add a third non-admin guard inside the 9912 block: refuse with 403 unless every capability in the target role's `capabilities` list is one the actor themselves holds via hasCapability on that ctx. That blocks both giving a sock a capability the appointer never held and the two-hop re-seating of the appointer.
- **Risk:** Behaviour change a village would notice: a decider whose proposal.decide comes only from a badge or a narrow role can no longer seat anyone into founders-circle. On the seeded config nothing changes — every other seeded role's capability set is a subset of founders-circle's — and all appointment calls in server/loop.e2e.test.ts (398, 410, 1255) use the admin/founder token, so tests are unaffected. It does not stop an existing founders-circle holder from minting equal-power sock holders; that legitimacy problem needs the separate second-signoff/decisionRef work parked at docs/AUDIT_2026-07-30_IMPROVEMENTS.md:106. Respect the gate's order of authority (CLAUDE.md:74) — implement the check with hasCapability, not a parallel permission path, and keep removals admin-only.
- **Reproduction:** Reproduces on current code. POST /api/admin/roles/:id/holders (server/index.ts:9898) admits any holder of proposal.decide (9901-9904; the /api/admin middleware at 2886 is audit-only). Its non-admin guards (9912-9919) test only identity ("userId !== actorUser.id") and "action !== remove" — never whether the target role's capabilities are ones the appointer holds. POST /api/auth/register (3115) is unauthenticated, unthrottled, unverified and returns the new id and token via publicUser (1478), so a second account costs one request. founders-circle has "minStage": null (roles-seed.json:7) so the ladder check at 9935 is skipped, and its capabilities (roles-seed.json:6) include proposal.decide and

### client/index.html:34
- **Fix** (small): In the same commit, add a neutral platform favicon and OG image under client/public/assets/images/ and repoint lines 34-35 and 20/31 at those local paths, so no deployment fetches assets from a third-party host. Then delete the two things a platform cannot know — the canonical link (:13) and the JSON-LD Organization block (:54-71) — instead of hardcoding one village's identity, and neutralize the static title/description/og:site_name wording (PageTitle already repaints the tab from live config).
- **Risk:** The local favicon and OG image files must land in the same commit as the href change, or the tab icon and every WhatsApp/X link preview 404 rather than merely being wrong. Dropping the canonical link removes an SEO signal that is currently correct for play.amora.cr, so Amora's own deployment loses it (visible in Search Console) until its fork re-adds it. Neutralizing the static title/description changes the pre-hydration tab text and all crawler/preview text for Amora, since PageTitle only repaints after /api/game/config returns. Finally run `node scripts/check-brand-refs.mjs --update-baseline` to lock the lower count into scripts/brand-refs-baseline.json (a decrease passes the guard, but the baseline should record the win so it cannot creep back).
- **Reproduction:** Reproduced. Entry: server/index.ts:10181 app.get("*") -> res.sendFile(dist/public/index.html) at 10183, a raw byte-for-byte send with no templating. The served file (client/index.html:34-35) declares `<link rel="icon">` and `apple-touch-icon` pointing at https://amora.cr/wp-content/uploads/2025/11/favicon.png, so every page load in every deployment issues a cross-origin GET to Amora's WordPress host. No override exists: grep of client/src for `rel="icon"` / `querySelector("link` returns zero hits, and client/public/ holds only assets/fonts and assets/images with no favicon file to substitute. The rest of the head is equally unreachable by the brand overlay (the app_config "brand" doc): :13 c

### server/index.ts:4133
- **Fix** (trivial): Mirror the four sibling slug sites: append `.slice(0, 64) || `circle-${Date.now().toString(36)}`` to the slug expression on 4133, and reduce the guard on 4134 to `if (!String(name ?? "").trim()) return res.status(400).json({ error: "A name is required" });`. The slice(0,64) is part of the correct fix because circles.id is varchar(64) (drizzle/0018_village_map.sql:7) while name allows 120 chars, so a long ASCII name currently overflows the PK as well.
- **Risk:** Non-Latin names now get an opaque generated id (e.g. circle-m9x2k1) instead of a readable slug, and the admin UI prints c.id next to the name (Admin.tsx:3070), so stewards will see it; it is cosmetic because quest routing resolves through name/aliases (PUT handler 4160-4170), not id. With no rename path in the UI the generated id is permanent, so it must stay unique — Date.now() in base36 keeps the 4135 duplicate check meaningful. No migration needed; the alias-maps-to-exactly-one-circle invariant and the 4135 409 collision check are untouched.
- **Reproduction:** Reproduced end to end. Admin.tsx:2978 defines the create-circle form state as {name, purpose} with no slug field (inputs at 3090/3092); Admin.tsx:3002-3007 POSTs exactly that body to /api/admin/circles; server/index.ts:4133 derives slug = String(id ?? name ?? "").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"") which I executed against "Круг воды", "水の円" and "دائرة الماء" — all yield "" — and server/index.ts:4134 then returns 400 "A name (and slug) is required"; Admin.tsx:3009 surfaces it as a toast. There is no rename path in the UI (circle rows at Admin.tsx:3066-3086 expose only alias-add), so the create-then-rename workaround needs a hand-rolled PUT. Two corrections to the 

### C:/Users/taren/Desktop/Amora/game-amora/server/index.ts:5483
- **Fix** (trivial): Replace the `=== "off"` test with a rank test so anything below `members` 404s: `if (LIFECYCLE_RANK[effectiveLifecycle("network")] < LIFECYCLE_RANK.members) return res.status(404).json({ error: "Not found" });` — LIFECYCLE_RANK is already imported at index.ts:212 and this is the same idiom already used at index.ts:6041 and server/lib/modules.ts:310.
- **Risk:** Downgraded from the claimed high because /api/platform/info (index.ts:5046, deliberately public per S56) already returns instanceId, name, version and `modules: [{id:"network", lifecycle:"preview"}]` to anonymous callers, so the existence/identity disclosure is pre-existing by design elsewhere; the incremental leak is item content that the publish form labels "visible to every peer village" (Network.tsx:157). The fix itself carries one noticeable behaviour change: a village sitting at `preview` that had already added a peer stops being readable by that peer — peerSharedItems swallows fetch failures, so the peer silently shows nothing rather than erroring. It does not close the platform/info catalog disclosure, which is a separate route. Keep the existing `{ error: "Not found" }` body so preview and off stay indistinguishable on this route. The e2e suite exercises only off and public, so it stays green — add a preview assertion beside loop.e2e.test.ts:2815 if you want the invariant pinned.
- **Reproduction:** Reproduces. server/index.ts:5482 registers GET /api/network/published before the gate at :5499 (app.use("/api/network", requireModule("network"))), so Express serves it ungated. Its only check is :5483 `if (effectiveLifecycle("network") === "off") return res.status(404)` — at `preview` that passes, and `network` is requires:[] and non-core (shared/modules.ts:351-360) so effectiveLifecycle returns "preview" unchanged. The handler never inspects the request (`_req` is unused) and no global auth middleware precedes it (between express.json at :2877 and the route there is only the /api/admin audit hook at :2886 and CORS at :2906), so an anonymous curl gets 200 with instanceId, project name, PLAT

### server/index.ts:10146
- **Fix** (small): At server/index.ts:10146, add `compression` as a dependency and register `app.use(compression())` immediately before the static handler — that alone cuts the wire payload from 1.55 MB to ~380 KB. Then, in a separate mount ahead of it, serve only the hashed directory with a long cache: `app.use("/assets", express.static(path.join(staticPath, "assets"), { maxAge: "1y", immutable: true }))`, leaving the existing bare `express.static(staticPath)` to keep index.html at max-age=0.
- **Risk:** Do NOT put `maxAge`/`immutable` on the existing bare `express.static(staticPath)` call: serve-static's `index` option defaults to ["index.html"], so `/` is served by that same handler and a year-long immutable HTML cache would pin members to a dead bundle hash — exactly the stale-index white screen the comment at server/index.ts:10155-10175 was written to prevent. Hence the separate /assets mount. Adding the `compression` dependency puts a new package under the blocking `pnpm audit --prod --audit-level high` gate described at docs/ARCHITECTURE.md:766-778, so the gate must be re-run. Compression also drops Content-Length in favour of chunked transfer and weakens ETags, which can surface in any test or monitor that asserts on those headers; the loop e2e test runs the built dist/index.js, so build before testing.
- **Reproduction:** Reproduced end to end. (1) server/index.ts:10146 is `app.use(express.static(staticPath));` with no options; serve-static 1.16.2 -> send 0.19.0 index.js:861-869 builds `'public, max-age=' + Math.floor(this._maxage / 1000)` with _maxage defaulting to 0, so every content-hashed asset ships `Cache-Control: public, max-age=0` and is revalidated each visit. (2) No compression exists: the only app.use middlewares before the static handler are express.json (2877), the admin limiter (2886) and a header setter (2906) plus route-scoped requireModule guards; the sole repo hit for "compression" is the sharp log line at server/index.ts:8572; no compression/express-static-gzip in package.json; no Dockerfil

### server/index.ts:5059
- **Fix** (small): At server/index.ts:5059 raise the filter from "not off" to "at least members": `MODULES.filter((m) => m.core || LIFECYCLE_RANK[effectiveLifecycle(m.id)] >= LIFECYCLE_RANK.members)` — LIFECYCLE_RANK is already imported at index.ts:212, and the mapped `lifecycle` line then never emits "preview". Since the endpoint is unauthenticated it cannot be viewer-scoped like /api/modules; hiding preview outright is the only consistent option (members-level modules stay listed, whose existence 401 already discloses).
- **Risk:** scripts/smoke-all-modules.mjs:167-179 asserts the handshake ids equal admin `served !== "off"`; that assertion must be tightened to rank >= members in the same change, or the smoke run goes red whenever a module sits at preview (notably the FORCE_PREVIEW path in scripts/enable-all-modules.mjs:125). Behaviour change a future village directory would notice: a peer's preview modules stop appearing in its handshake — which is the intent, but it means the handshake is no longer a complete "what is served" list and its docstring (index.ts:5038-5045) and docs/ARCHITECTURE.md:551-556 ("the served module list") should say "at members or above".
- **Reproduction:** Reproduces on current code. GET /api/platform/info is registered at server/index.ts:5046 as `app.get("/api/platform/info", async (_req, res) => {` — the handler never inspects the request (`_req`) and calls no isAdmin/authedUser; the only middleware ahead of it are express.json (2877), the /api/admin non-GET audit hook (2886, different prefix) and CORS (2906), so it is genuinely unauthenticated. At 5059-5062 it emits `MODULES.filter((m) => m.core || effectiveLifecycle(m.id) !== "off").map((m) => ({ id: m.id, lifecycle: m.core ? "public" : effectiveLifecycle(m.id) }))`. A module stored at `preview` via PUT /api/admin/modules/:id/lifecycle (index.ts:3437) returns "preview" from effectiveLifecy

### client/src/pages/Admin.tsx:4382
- **Fix** (small): Add a small numeric "Days" input to the award form (extend the `award` state at Admin.tsx:4183 with `days: ""`, render it next to the note input at 4379) and, in the Award onClick at 4382, send `expiresAt: award.days ? new Date(Date.now() + Number(award.days) * 86400000).toISOString() : undefined` — the route at server/index.ts:7055-7065 already parses and stores it, so no server, schema or migration change is needed.
- **Risk:** Member-visible behaviour change: the dormant `badge-expiry-sweep` job starts firing real "restrictions lifted" notifications once any award carries an expiry. Blank days still means permanent, so this fixes reachability, not the missing default — going further (a `badges.warning_default_days` default applied server-side) means registering a new key in shared/gameVariables.ts, which is the fail-loud plane and a bigger change. Existing awards keep `expires_at = NULL` until re-issued (`upsertAward`'s ON DUPLICATE sets `expires_at = VALUES(expires_at)`, server/lib/badges.ts:204, so a re-award with days set does back-fill them — and equally, a re-award with the field blank silently clears an expiry someone set earlier). Respect the Gate E ordering in shared/capabilities.ts: do not add a second expiry check anywhere; lazy expiry stays the WHERE clause.
- **Reproduction:** Reproduced against current code. The chain, step by step with live line numbers: 1. Admin creates a warning badge with denies — `POST /api/admin/badges` accepts `denies` (server/index.ts:6993, 6998, 7006) and `badgeProblem` only rejects denies on non-warning kinds (server/lib/badges.ts:111). (Correction to the claim: there is NO seeded "Timeout" badge — `server/seeds/` holds only circles/content/quests/roles-seed.json, no badges-seed.json — so the steward must create the warning badge first. Cosmetic error; the rest holds.) 2. The award form is the only UI path. State is `useState({ badgeId, userId, note })` (client/src/pages/Admin.tsx:4183); the form has exactly three controls — badge selec

### server/db/pool.ts:32
- **Fix** (small): In getPool(), after createPool and before `return _pool`, pin the session on every pooled connection: `_pool.on("connection", (c) => { c.query("SET time_zone = '+00:00'"); });` — use the numeric offset, never the name 'UTC', which fails on servers without the timezone tables loaded. Add the same statement after connect in server/db/migrate.ts:45 and to the two testDb.ts connections so the discipline docs/ARCHITECTURE.md:702-705 already claims is actually complete, and extend server/db/harness.test.ts:59-69 to assert `SELECT @@session.time_zone` (or compare a MySQL-generated NOW() row against Date.now()), since the current string round-trip cannot catch this.
- **Risk:** On a deployment whose MySQL is currently non-UTC, previously app-written TIMESTAMP values (badge_awards.expires_at, wallet_challenges.expires_at) were stored shifted by the offset; pinning the session to UTC makes those historical rows read back shifted by that offset one time, while DB-generated rows start reading correctly. On a UTC MySQL — Railway, i.e. the Amora deployment — the change is a no-op. Second risk: the init query runs on every pooled connection, so it must not throw; '+00:00' is always accepted, whereas 'UTC' would throw on a server without tz tables and take the pool down. Third: fixing this changes scheduler cadence and rate-limit behaviour on any fork that was silently running jobs every tick or with the abuse guard disabled — that is the intended correction, but it is a visible behaviour change there.
- **Reproduction:** Reproduces. server/db/pool.ts:26-32 sets mysql2 timezone:'Z' and nothing ever pins the MySQL session time_zone (no `time_zone` occurrence in server/ or drizzle/). mysql2's parseDateTime('Z') is `new Date(str + 'Z')` (node_modules/mysql2/lib/packets/packet.js:725-733) and writeDate emits the UTC wall clock (packet.js:936-947), while every column in drizzle/ is TIMESTAMP, which MySQL converts to/from the SESSION zone on every read and write. So the driver's assumption holds only if the session is UTC. Display chain as claimed: notifications.created_at is DB-generated (drizzle/0017_notification_spine.sql:24; the insert at server/lib/notify.ts:123 supplies no value), read at notify.ts:232 and to

### server/index.ts:2933
- **Fix** (trivial): Delete lines 2933, 2943 and 2944 and replace them with a single `await submissionsRepo.insert(entry);`, exactly as the sibling raise-hand route already does at index.ts:4224 — insert() is one INSERT plus a cache append (store-db.ts:142-146), so there is no read-modify-write across awaits.
- **Risk:** No member-visible behaviour change: the response shape at :2980 is unchanged, and load() re-sorts by submitted_at, id (index.ts:536) so append order does not matter across restarts. Two things to respect: insert() runs a single un-transacted statement on the pool rather than replaceAll's DELETE+re-INSERT, so rows are no longer re-normalised through toDb on every write (strictly better, but it means a pre-existing malformed row is no longer silently rewritten); and this only removes the most frequent and the only unauthenticated participant — the remaining replaceAll sites on submissions (:1678, :1755, :3009, :3030, :8735) still race with each other, so do not record the table as race-free.
- **Reproduction:** Reproduced. server/index.ts:2933 takes a synchronous snapshot copy of the cache (store-db.ts:122-124), :2943 pushes, :2944 calls replaceAll, which does DELETE FROM submissions + per-row INSERT and only assigns cache = [...rows] AFTER commit (store-db.ts:126-133). Every await inside replaceAll yields the event loop while the cache still lacks the new entry, so a second handler's :2933 read returns the pre-write array, and its replaceAll deletes the first entry and re-inserts the stale array. No PK conflict, nothing throws, both handlers return success at :2980. The window is one full transaction plus one INSERT round-trip per existing row, so it widens as the table grows. insert() (store-db.t

### server/index.ts:9945
- **Fix** (small): Serialize the role_holders read-modify-write: keep a module-level promise chain (e.g. `let roleHolderWrites = Promise.resolve()`) and run only lines 9945-9971 (snapshot, mutate, replaceAll) inside it, moving the addActivity/notify calls to AFTER the replaceAll so the SMTP send never sits inside the critical section. Apply the same wrapper to the other role_holders writer at index.ts:1757-1758. Reordering alone is not enough — replaceAll yields at getConnection/beginTransaction before it assigns `cache = [...rows]` (store-db.ts:127-133), so a second handler could still snapshot the pre-write cache; the lock is what closes it. (Equivalent alternative: give the collection per-row primitives — the add branch can already use the existing `roleHoldersRepo.insert` (store-db.ts:142), and add a delete-by-id for the remove branch — which removes the DELETE-all entirely.)
- **Risk:** Concurrent appointments now queue behind each other; imperceptible once the mail send is outside the lock, but if addActivity/notify are left inside, appointments would block for the duration of an SMTP round trip. Moving notify after the write means the dedupeKey at 9965 (`holders[holders.length - 1]?.id`) must be captured before the array is handed to replaceAll, or it will read the wrong id. A notification/pulse event must no longer be emitted for an appointment whose write later throws — emitting after the write actually fixes an existing inconsistency, but it is a behaviour change (a failed replaceAll previously still left a "You were appointed" notification). No migration; no schema change; the invariant to respect is that role_holders is the input to the one capability gate, so the lock must cover both branches, not just add.
- **Reproduction:** Reproduces on current code. server/index.ts:9945 takes a synchronous cache snapshot (loadRoleHolders -> roleHoldersRepo.all -> store-db.ts:123 `return [...cache]`), then awaits two DB round trips at 9956 (addActivity -> recordEvent) and 9957 (notify -> insertNotification, which itself awaits an SMTP send via maybeEmailImmediate), and only at 9971 calls roleHoldersRepo.replaceAll(holders), which is DELETE FROM role_holders + re-INSERT of the stale array (store-db.ts:126-140) and then overwrites the cache. No mutex or queue exists anywhere in server/. Two overlapping requests both snapshot the same pre-state; the later replaceAll erases the earlier appointment from both DB and cache, permanent

### server/index.ts:1883
- **Fix** (small): In applyAcceptReward, when amount > 0, post the credit through the ledger before the members.update — `const credit = await postTransfer(getPool(), { from: RECOGNITION_FAUCET, to: memberAccount(match.id), amount, source: "proposal_accepted", sourceRef: entry.id, description: "Work With Us proposal accepted", idempotencyKey: `proposal_accepted:${entry.id}` })`, return false if `!credit.ok` — and replace line 1883 with `u.recognitionBalance = credit.toBalance` (keep the contribution row unconditional so an acceptGratitude of 0 still logs, since postTransfer refuses zero-amount posts). RECOGNITION_FAUCET and memberAccount are already imported at index.ts:19-29.
- **Risk:** Three things to respect: guard on amount > 0, because postTransfer rejects zero-amount legs and an unguarded call would make a village with acceptGratitude=0 lose the contribution row entirely; these credits now appear in a member's /api/game/ledger and drive the gratitude faucet's negative total (correct per S7, conservation still nets to zero, but the issuance becomes visible where it previously was not); and the fix is not retroactive — any proposal already accepted keeps its unbacked cached balance until the next credit resets it, so a one-time backfill or resync is a separate decision.
- **Reproduction:** Reproduces on current code. PUT /api/admin/submissions/:id/status (index.ts:3026-3027) calls applyAcceptReward (index.ts:1866), which at index.ts:1883 does `u.recognitionBalance = (u.recognitionBalance ?? 0) + amount` (amount defaults to 100, DEFAULT_WORK_WITH_US.acceptGratitude at index.ts:448) and never calls postTransfer — the only write to recognitionBalance in the repo that is not `= r.toBalance` from a ledger post (contrast index.ts:9314-9323, index.ts:7916-7931, gratitude.ts:145-158). members.update persists it to users.recognition_balance (repos/users.ts:54,131). /api/game/ledger then reports inSync:false (index.ts:9734-9745, `summed === user.recognitionBalance` where summed comes fr

### client/src/pages/VillageMap.tsx:305
- **Fix** (small): In NodeCard, give the inner panel (line 306) role="dialog" aria-modal="true" aria-labelledby pointing at an id added to the h3 at line 309, plus tabIndex={-1} and a ref; add one useEffect that saves document.activeElement, focuses the panel on mount and restores that element on unmount, and put onKeyDown={(e) => e.key === "Escape" && onClose()} on the overlay at line 305; add aria-label="Close" to the icon-only button at line 312. (The larger-but-cleaner alternative is to render it through the already-present Radix wrapper in client/src/components/ui/dialog.tsx, which also gives real Tab containment.)
- **Risk:** The Escape handler only fires while focus is inside the overlay, so the focus-move-on-open is load-bearing, not optional — shipping one without the other leaves Escape dead. Moving focus is a visible change for mouse users (a focus ring lands on the card), so keep focus-visible styling on the panel rather than a raw outline. Focus restore must cover both entry points, the SVG node and the ConciergeBar match button at line 436 — capturing document.activeElement at mount handles both. The minimal fix still does not contain Tab inside the card; only the Radix route does. If you take the Radix route, preserve the mobile bottom-sheet geometry (items-end / rounded-t-2xl / max-h-[80vh]) and the separate MobileTabBar overlap defect recorded at docs/AUDIT_2026-07-30_DEFECTS.md:109. No migration, no server change, no capability-gate or ledger invariant touched, and no brand-ratchet impact.
- **Reproduction:** Reproduces exactly against current code. App.tsx:174 routes /map to VillageMap; VillageMap.tsx:79-81 renders MapCanvas on desktop; VillageMap.tsx:138-152 makes each circle a focusable pseudo-button (role="button" tabIndex={0}, onKeyDown Enter/Space -> onSelect) so the keyboard entry point in the claim is real and intentional; VillageMap.tsx:85-87 renders {selected && <NodeCard …/>} as a DOM sibling AFTER the map wrappers; VillageMap.tsx:304-306 is the overlay `<div className="fixed inset-0 z-50 flex items-end md:items-center justify-center bg-black/30" onClick={onClose}>` with no role="dialog", no aria-modal, no aria-labelledby and no onKeyDown; NodeCard (lines 256-302) declares five useStat

### server/lib/gratitude.ts:42
- **Fix** (small): Add two aggregate methods to `gratitudeLogRepo` (server/repos/gratitude.ts:45) — `spentInCycle(fromId, cycleId)` returning `COALESCE(SUM(amount),0)` (served by the existing `gratitude_from_cycle_idx`) and `countPair(fromId, toId, cycleId, kind)` returning `COUNT(*)` — and call them from `budgetFor` (gratitude.ts:42) and the cap check (gratitude.ts:100), leaving `all()` only for the read-only wall/journal/export routes; add one additive migration `ALTER TABLE gratitude_log ADD KEY gratitude_context_idx (context_ref, kind)` so the `heart_count` recompute at index.ts:3613 stops scanning too.
- **Risk:** The SUM must reproduce the current JS semantics exactly: gratitude.ts:43-45 sums `amount` across ALL kinds (hearts included, since `feed.heart_amount` can be > 0) with no kind filter, while the cap count at :101-103 does filter kind — swapping those two conditions silently changes every member's spendable budget. The refusal order and exact guard strings at gratitude.ts:90-119 are asserted by the order-dependent `server/loop.e2e.test.ts`, so keep them byte-identical and rebuild before running it. The index is a live ALTER on the busiest table (the boot runner is fail-loud, `server/db/migrate.ts`), so keep it additive and obey the house rule about `--` comments never ending in `;`.
- **Reproduction:** Reproduced against current code, and it is worse than the finding says. Chain, heart-tap path: 1. `server/index.ts:3596` — `app.post("/api/feed/threads/:id/heart")`. No `overLimit()` guard on this route (the only rate limiter is the `overLimit` helper at index.ts:1983, unused here). 2. `server/index.ts:3601` — calls `sendGratitude(gratitudeDeps, {... kind: "heart" ...})`. `gratitudeDeps.log` is `gratitudeRepo` (index.ts:1577 wiring `gratitudeLogRepo(getPool())` from index.ts:524) — no cache layer anywhere. 3. `server/lib/gratitude.ts:89` — `const budget = await budgetFor(deps, user)` → `server/lib/gratitude.ts:42` `const log = await deps.log.all()`, then filters in JS at :43-45. **Full scan 

### server/index.ts:3116
- **Fix** (trivial): Insert a throttle as the first statement of the handler, before the existsByEmail check so it bounds the oracle too: `if (await overLimit(`register:${clientIp(req)}`, 10, 60 * 60 * 1000)) return res.status(429).json({ error: "Too many attempts. Try again in a few minutes." });` — same shape as login at 3148, and clientIp/overLimit are already in scope.
- **Risk:** A single shared NAT (village wifi during an onboarding gathering, a co-working space) now shares one bucket, so the 11th genuine signup from that IP within the hour gets a 429 — pick the cap with that in mind rather than copying login's 10/15min. overLimit fails open on database trouble (1998-2001), so this cannot take registration down. It only bounds the oracle per-IP; an attacker with a large IP pool can still probe, so this is mitigation, not elimination — fully closing it means returning an identical response for taken and fresh addresses and notifying the existing account by email, which is a larger UX change and a separate decision.
- **Reproduction:** Reproduced. server/index.ts:3115 POST /api/auth/register runs 3116-3141 with no overLimit() call (grep of all 21 overLimit sites shows none inside the handler), no honeypot, no auth; the only pre-route middlewares are express.json (2877), the /api/admin gate (2886) and CORS (2906), so nothing throttles it globally and no express-rate-limit exists in server/. Line 3120-3122 returns a distinct 409 "Email already exists" from a case-insensitive SELECT 1 (server/repos/users.ts:174) versus the 200 at 3141, giving an unauthenticated account-existence oracle on addresses the handle system (846-855) exists to keep off public surfaces; it is the only such oracle, since there is no public members rout

### C:/Users/taren/Desktop/Amora/game-amora/server/index.ts:3148
- **Fix** (small): Split overLimit into a check-only helper plus a recordHit(bucket) next to it (index.ts:1983), then at 3148 pre-check two buckets without inserting — a looser `login-ip:<clientIp>` (e.g. 30/15min) and a new `login-acct:<normalized email>` (10/15min) — and call recordHit on both only when the credential check at 3155-3157 fails, so a correct sign-in never consumes anyone's budget.
- **Risk:** The per-account bucket creates a targeted-DoS surface: anyone who knows a member's address can 429 that one account for 15 minutes, so keep the cap generous and never record a hit on the success path. Raising the IP cap loosens the per-IP brute-force ceiling — the per-account bucket is what compensates, so the two changes must land together. The email must be normalized the same way members.byEmail normalizes it, or case variants get separate buckets and the account limit is trivially bypassed. Login rate_hits rows roughly double, already bounded by the opportunistic sweep at index.ts:1993. No migration needed; keep overLimit's fail-open-on-DB-error behaviour (1998-2000) intact so a guard-table outage cannot take login down.
- **Reproduction:** Reproduces exactly against current code. server/index.ts:3148 is the login route's sole guard: overLimit(`login:${clientIp(req)}`, 10, 15*60*1000), evaluated before req.body is read (3151) and before credentials are checked (3155). grep -rn "login:" server/ returns that one line only — no per-account bucket exists anywhere. overLimit (1983-1993) INSERTs a rate_hits row whenever the bucket is under max, unconditionally on the auth outcome, and the handler (3151-3167) never removes it on success, so successful sign-ins consume the same 10-slot budget as failures. clientIp (2025-2038) with the default TRUSTED_PROXY_HOPS=1 returns the address the edge observed, so every device behind one NAT/Sta

### server/db/pool.ts:32
- **Fix** (trivial): Pin the session zone on every pooled connection right after createPool: `_pool.on("connection", (c) => { c.query("SET time_zone = '+00:00'"); });` — the SET queues ahead of any user query on that connection, so NOW()/CURRENT_TIMESTAMP land in the same UTC frame as the driver's Date rendering. Apply the same one-liner to the standalone connections in server/db/migrate.ts:45 and server/db/testDb.ts:48/56 so invariant 7 is actually whole.
- **Risk:** On an already-running non-UTC deployment the stored TIMESTAMP values are correct absolute instants (TIMESTAMP normalises to UTC internally), so no migration is needed and reads simply start rendering true UTC. Two things to respect: (a) any wall-clock value a member previously saw will appear to jump by the offset once, and rate-limit windows and job cadences start behaving as written — hourly jobs stop running every 5 minutes, which will look like jobs "stopped" in scheduled_jobs.last_result; (b) the one DATETIME column in the schema, payments_log.handled_at (drizzle/0038_payments_log_handled.sql:17), does not self-normalise, so rows written before and after the change will differ by the offset — check how it is written before shipping if that column is ever compared or displayed.
- **Reproduction:** Reproduced. server/db/pool.ts:28 pins the driver to timezone:"Z" so mysql2 renders JS Date params as UTC wall clock, but nothing anywhere issues SET time_zone (grep for SET time_zone / SET SESSION / SET @@ / initSql / pool.on("connection") across server/, drizzle/, scripts/ hits comments only), so NOW()/CURRENT_TIMESTAMP still evaluate in the MySQL session zone. Two load-bearing comparisons mix the frames in SQL. (1) overLimit, server/index.ts:1983-2002: line 1986 builds `since` as a JS Date, line 1988 compares `at > ?`, line 1992 writes `at` with CURRENT_TIMESTAMP(3) into a `timestamp(3)` column (drizzle/0014_authority_flip.sql:11). MySQL interprets the bound UTC literal in the session zone

### server/lib/base-reads.ts:52
- **Fix** (small): Make `rpcClient()` async and, for any host that is not the deliberately-whitelisted loopback literal (`127.0.0.1`/`localhost`), return null unless `await guardOutboundUrl(url)` from `./toolcheck` passes; also pass `fetchOptions: { redirect: "error" }` to `http()` so viem cannot hop off the vetted host to a private range. The single caller at `base-reads.ts:84` is already inside an async try block, so it only needs `await`, and a refusal falls through the existing catch to last-known-or-null rather than crashing.
- **Risk:** Three things to respect. (1) The loopback exemption must survive: refusing it breaks `server/loop.e2e.test.ts:2087` (the acceptance path points the platform at `http://127.0.0.1:3782`) and local anvil/hardhat dev — so do not simply route this through `guardedFetchJson`, which is https-only and would reject both. (2) `redirect: "error"` is a behaviour change an admin could notice if their RPC provider legitimately 3xx-redirects, and a self-hoster running a node at a private https address (e.g. `https://10.0.0.5`) would now be refused; both surface as the documented "shows nothing rather than a wrong number" path (NULL ON RPC FAILURE, NEVER ZERO — `base-reads.ts:13-16`), never as a zero balance, so the economy invariant holds. (3) Residual gap accepted by precedent: range-check-without-pinning still leaves the DNS-rebinding window, which `toolcheck.ts:249-254` already records as accepted for admin-entered, range-checked URLs. Closing that too means a `fetchFn`/custom transport delegating to the pinned dialer (viem supports `fetchFn`, confirmed at `viem/_esm/clients/transports/http.js:24,32-34`) — worth doing if you want parity with `network.ts`, but it is a larger change than the fix above.
- **Reproduction:** Reproduced, with one sub-claim corrected. The chain, current line numbers: (1) `server/index.ts:9789` `app.put("/api/admin/variables/:key")` gated by `isAdmin` only → `setVariable` (`server/lib/variables.ts:84-90`) → `validateVariable` (`shared/gameVariables.ts:873`), whose `_url` rule at 887-893 accepts any `/^https:\/\/\S+$/` with NO range check, and separately whitelists `^http:\/\/(127\.0\.0\.1|localhost)`. (2) `server/index.ts:6480` `app.get("/api/wallet")` — `authedUser` only; grep for `app.use("/api/wallet"` returns nothing, so there is no `requireModule()` gate. (3) `server/index.ts:6484-6497`: with `tokens.show_economics_section` true and `user.walletVerifiedAt` set, every load call

### server/index.ts:9323
- **Fix** (small): Refuse a non-positive award before anything is written — after `const granted = requested;` (9273) return 400 when `granted <= 0` ("consent releases value; the amount must be at least 1") — and make the cache write conditional at 9323: `if (credit.ok) { ... u.recognitionBalance = credit.toBalance ... }`, else log the refusal and respond with an error instead of 200, exactly as server/lib/gratitude.ts:154 and server/index.ts:7928 already do.
- **Risk:** Behaviour change an admin would notice: a village that today "consents at 0" to mark work acknowledged without recognition (possible under capped/unlimited, or a quest advertising "0") will now get a 400 and must use Decline or a stay-credit-only quest instead. Respect the invariant that the balance column is recomputed from the ledger, never assigned from a failed post; if you also want the claim not to flip when the credit fails, that is a larger change (the flip at 9303 and the transfer at 9314 are not in one transaction) and should not be attempted as part of this fix.
- **Reproduction:** Reproduced end to end. POST /api/admin/quest-claims/:id/consent (server/index.ts:9229) computes `granted = Math.max(0, Number(amount) || 0)` at 9265/9273; with `quest.consent_cap_mode` = "capped" only `requested > ceiling` is refused (9289) and "unlimited" has no check at all, so 0 passes (and even the default "posted" passes when the quest advertises a reward starting at 0, since parseRewardRange returns min:0/valid:true — shared/questRewards.ts:41-44). The claim flips to status "consented", amount 0 at 9303-9307, BEFORE value moves. postTransfer at 9314 then fails in validateLeg (server/lib/ledger.ts:197 "amount must be a positive integer") and returns `{ok:false, toBalance:0}` (ledger.ts:

### client/src/pages/Quests.tsx:200
- **Fix** (trivial): On Quests.tsx:200 change `sticky top-[64px]` to `sticky top-24` (6rem = 96px = the real header height: 64px logo + py-4's 32px), so the filter bar parks flush below the nav instead of 32px behind it.
- **Risk:** Purely visual, no behaviour or data change: the bar stops 32px lower, so members see the whole Circle row and get the full 24px tap target. The one durable risk is that 96px stays a hand-copied constant — any future change to Layout.tsx:57's `height={64}` or the nav's `py-4` silently re-opens the same bug, and CoCreatorsGuide.tsx:270 already hard-codes its own `top-0` guess. If you want it to stop drifting, derive both from one shared header-height token rather than patching the number here.
- **Reproduction:** Reproduced in a browser against the current code. Chain: Quests.tsx:145 renders inside <Layout>; Layout.tsx:53 nav is `sticky top-0 z-50` with an opaque `bg-teal-deep`; Layout.tsx:54 is `container py-4` (16+16); Layout.tsx:57 is `<AmoraLogo height={64}/>` which AmoraLogo.tsx:52 applies as `style={{height:'64px'}}` — unconditional, no responsive variant, and index.css:192-217 gives .container horizontal padding only, so the header is exactly 96px tall at every width. Quests.tsx:200 is `sticky top-[64px] z-30`, so it parks 32px inside the header at a lower z-index. Measured with getBoundingClientRect/elementFromPoint after scrollTo(0,1200) on a faithful repro of these exact declarations: navHe

### server/lib/library.ts:562
- **Fix** (small): In supplyVsBacking, add the sold-stock provenance to `outstanding`: keep `shelfBacked = -balanceOf(pool, LIBRARY_MINT, LIBRARY_CREDIT)` and add `sold = Math.max(0, -balanceOf(pool, MINT_FAUCET, LIBRARY_CREDIT) - balanceOf(pool, TREASURY, LIBRARY_CREDIT))` (minted-to-treasury minus stock still sitting unsold in the treasury), then `outstanding = shelfBacked + sold`. Import MINT_FAUCET and TREASURY from ./ledger (library.ts:30 already imports balanceOf from there) and return `shelfBacked`/`sold` alongside the existing fields so the provenance separation the L9 card promises stays visible.
- **Risk:** Behaviour change stewards will notice: any village that already opened L9 and sold credits flips this panel from grey to red on the next admin load — intended, but it will read as a new alarm on unchanged data. Two correctness traps in the fix itself: sys:mint is shared across every token, so the balance must be read per token_type (balanceOf already is); and treasury-held stock must be subtracted or unsold inventory is counted as circulating, which would flag a village that stocked conservatively and sold nothing. Keeping the existing {outstanding, backing, flagged} keys means no client edit is required; adding fields without updating Admin.tsx:4502-4508 just leaves the new provenance breakdown unrendered.
- **Reproduction:** Reproduced. supplyVsBacking (server/lib/library.ts:562) measures circulating credits as -balanceOf(sys:library-mint), which only ever sees the intake faucet (library.ts:162/198/233/312). With the L9 card accepted (exchange.ts:145-149 creditSaleOpen, index.ts:3466-3486 stamps the ack), purchaseProblem stops refusing library-credit (exchange.ts:152-165), so PUT /api/admin/exchange/tokens/library-credit lists it purchasable (index.ts:7690-7714, re-proved at boot via repairTaintedListings exchange.ts:769-770). Stocking posts sys:mint -> sys:treasury (index.ts:7809-7826, MINT_FAUCET = ledger.ts:56) and the sale posts sys:treasury -> mem:{buyer} (settleExchangeOrder, exchange.ts:398-412). Neither 

### server/index.ts:5483
- **Fix** (trivial): Replace the off-only guard with the rank check already used elsewhere in the file: `if (LIFECYCLE_RANK[effectiveLifecycle("network")] < LIFECYCLE_RANK.members) return res.status(404).json({ error: "Not found" });` — LIFECYCLE_RANK is already imported at server/index.ts:212 and this is the same shape as line 6041. That keeps the deliberate unauthenticated publication at members/public while preview honours the framework's admin-only 404, and the doc comment at 5476-5481 ("whenever the module is on at all") should be amended to "at members or above" in the same edit.
- **Risk:** Behaviour change a peering village would notice: a village trialling federation at preview stops answering peers, so any peer that already added it records a 404 in peer_instances.last_error on every 6-hour sweep (network.ts:115-121) until the village moves to members/public — arguably correct, but it turns a silent state into a visible peer error. No migration and no invariant conflict; the loop e2e test only exercises off and public for this route (server/loop.e2e.test.ts:2815, 2821, 2842) so it still passes, which also means the preview case stays untested unless an assertion is added. The sibling gap this fix does NOT close is that /api/admin/network/share and /api/admin/network/peers remain ungated by requireModule, so items can still be drafted and peers added while the module reads off.
- **Reproduction:** Reproduced end to end against current code. server/index.ts:5482 registers GET /api/network/published 17 lines BEFORE the gate at server/index.ts:5499 (app.use("/api/network", requireModule("network"))), and its sole guard at 5483 is `effectiveLifecycle("network") === "off"`, so 'preview' passes. requireModule would have 404'd non-admins at preview (server/lib/modules.ts:174-176), and that promise is shown to the steward verbatim in the lifecycle picker: client/src/pages/Admin.tsx:2816 `preview: "Admins only — invisible to everyone else."` Publishing during preview is reachable: there is no app.use("/api/admin/network", requireModule(...)) mount (unlike /api/admin/forum, /api/admin/map, /api

### client/src/components/NotifyPrefsPanel.tsx:156
- **Fix** (small): At line 156 replace `.then((r) => r.blob())` with `.then((r) => { if (!r.ok) throw new Error("export failed"); return r.blob(); })`, and change the empty `.catch(() => {})` at line 165 to `.catch(() => setError("Could not build your export — please sign in again and retry."))`; because the only renderer of `error` is line 204 inside the `confirming` branch, also add a `{error && <p className="text-xs text-red-600">{error}</p>}` just after the anchor closes at line 170.
- **Risk:** The `error` state is shared with the delete-account flow, so a stale export error would also appear inside the delete confirmation card unless it is rendered in its own slot or cleared when `setConfirming(true)` runs (line 174). Members who previously got a silent no-op now see a visible failure message — intended, but a noticeable behaviour change. No server, API-contract or migration impact.
- **Reproduction:** Reproduced against current code. Profile.tsx:142 renders NotifyPrefsPanel; the panel renders (past the `if (!prefs) return null;` gate at NotifyPrefsPanel.tsx:100) whenever the token was valid at mount. The export handler at NotifyPrefsPanel.tsx:155-165 is verbatim as claimed: `.then((r) => r.blob())` with no `r.ok` check, and `.catch(() => {})`. server/index.ts:10068 returns `res.status(401).json({ error: "Unauthorized" })`, and fetch resolves for 4xx, so that 24-byte body is blobbed and saved as `my-data.json` by lines 158-162. The token dies while the panel is mounted via three current paths: the 30-day TTL check at server/index.ts:919 (TOKEN_TTL_MS at :333), or the tokenVersion mismatch 

### server/index.ts:3070
- **Fix** (trivial): Add one helper next to the route — `const csvCell = (s: string) => `"${(/^[=+\-@\t\r]/.test(s) ? "'" + s : s).replace(/"/g, '""')}"`` — and use it for both the data cells at :3070 (`return csvCell(strValue);`) and the header cells at :3059 (`headers.map(csvCell)`), which today do not even double embedded quotes.
- **Risk:** Cosmetic, visible change: any exported value that legitimately begins with `-` or `+` (negative amounts, `+31…` phone numbers) gains a leading apostrophe and lands as text rather than a number in the spreadsheet. Nothing in the repo re-parses this CSV, so no code depends on the old shape; no migration, no invariant touched. Keep the prefix outside the quote-doubling order shown above, otherwise the apostrophe escapes wrongly for values containing quotes.
- **Reproduction:** Reproduces on current code. server/index.ts:2920 POST /api/forms/submit is public (only a presence check at :2922, honeypot at :2925, 6/10min IP rate limit at :2927) and stores req.body.data verbatim (:2937 `data,`, persisted at :2944) with no key whitelist and no value sanitising — I checked express.json at :2877 and the /api/admin middleware at :2886, there is no sanitising layer. The export at :3036 turns those values into cells at :3069-3070 as `"${strValue.replace(/\"/g,'\"\"')}\"` — quote-doubling only, no neutralisation of leading = + - @ tab CR — and attacker-supplied `data` keys become the header row at :3059 without even quote-doubling. Sent as text/csv attachment at :3078-3080. Ex

### server/index.ts:2317
- **Fix** (small): Add a commerce counterpart to releaseAbandonedFiatOrders and register it as an hourly job right after the commerce openStateCheck closure (index.ts:2317): UPDATE product_purchases SET status = 'cancelled' WHERE status = 'pending' AND created_at < NOW() - INTERVAL ? HOUR, reusing exchange.ts's ORDER_EXPIRY_FLOOR_HOURS = 25 floor so a member still sitting on the Stripe page is never cancelled out from under them, and skipping any row that already has a fiat_charges entry for `<id>#%`. No migration is needed — 'cancelled' is already in the enum at drizzle/0032_payment_products.sql:49.
- **Risk:** Two things to respect. (1) The 25-hour floor is load-bearing: a Stripe Checkout session stays payable for ~24h, so a shorter window would cancel an order while the payer is still on the payment page — money taken against a cancelled order. Enforce the floor in code, not in a game variable. (2) Manual/zeffy pending rows are steward-reconciled by hand and can legitimately sit for days; auto-cancelling them changes what stewards see in the admin purchases list. Either scope the reaper to provider = 'stripe' (the only rows with no manual escape hatch, and the minimal fix for the reported defect), or keep it kind-agnostic but verify /confirm still works on a cancelled row — settleProductPurchase's guard at :2492 only refuses 'reversed', so a later hand-confirm would still settle. No behaviour a member would notice either way; no migration.
- **Reproduction:** Reproduced end to end. POST /api/products/checkout inserts a pending product_purchases row at server/index.ts:5347 BEFORE createCheckout (:5362), so a closed tab or a Stripe throw leaves it pending. Only two status writes exist in the whole server tree — 'paid' (:2491) and 'reversed' (:2584, reachable only via the Stripe reversal handler, which needs a real charge) — and there is no DELETE and no registered reaper job (all ten registerJob calls are at :2098-2270; none touches commerce). The 'cancelled' value is already declared in the enum at drizzle/0032_payment_products.sql:49 and never written. The one admin route that could clear a row, POST /api/admin/products/purchases/:id/confirm (:54

### client/src/modules/ModuleProvider.tsx:58
- **Fix** (small): In the catch at ModuleProvider.tsx:58, stop claiming the catalog is known on failure: retry refresh a few times with backoff (e.g. 1s/2s/4s via setTimeout) and leave loaded false while the manifest is unknown, so a network blip self-heals instead of latching an empty catalog. Optionally add a `failed` flag to ModulesState so pages can later render "couldn't load" instead of NotFound, but the one-site change is the catch handler.
- **Risk:** Behaviour change members can see: with loaded staying false, the ten gated pages render their normal shell (empty, since their own loaders are keyed on the module) instead of NotFound while the catalog is unknown — for a genuinely disabled module that briefly looks like an empty page rather than a 404. Server-side requireModule() still enforces access, so nothing leaks. Also guard against setState after unmount and cap the retries so a hard outage doesn't poll /api/modules forever.
- **Reproduction:** Reproduced end to end. App.tsx:218 mounts ModuleProvider; ModuleProvider.tsx:61 runs refresh exactly once; the fetch at :48-49 never checks r.ok and pipes straight into r.json(), so a timeout, dropped connection or 5xx HTML body all reject; the catch at :58 commits `{...EMPTY, loaded: true}`, i.e. modules: [] WITH loaded true — indistinguishable from every module being switched off. useModule() (:72) then returns undefined for every id, and the page gates fire: Wallet.tsx:45 (module read at :24), Library.tsx:41, Forum.tsx:31, Stay.tsx:48, Feed.tsx:75, Contribute.tsx:57, plus Badges.tsx:44, Network.tsx:74, ToolsHub.tsx:61, VillageHealth.tsx:51, VillageMap.tsx:55 — all `if (modules.loaded && !

### server/index.ts:9582
- **Fix** (small): Make the split basis sticky per cycle: in the recipient loop (9556-9594) persist the whole computed split first and post each transfer from the persisted `credited`, adding a repo write that leaves `credited`/`received*`/`distinct_senders` untouched when a row for (cycleId, userId) already exists (drop those columns from the ON DUPLICATE KEY UPDATE list in server/repos/gratitude.ts:162-164, or add an addIfAbsent). A retry then re-posts the identical amounts under the identical idempotency keys and converges instead of re-splitting a pool that is already partly out the door.
- **Risk:** Two things to respect. (1) Rows would now exist for a cycle that is not yet closed; server/lib/badges.ts:286 reads `MAX(distinct_senders) FROM gratitude_distributions` with no closed-cycle filter, so a partially failed close could grant a breadth badge before the cycle actually settles — either write the rows inside the same step that marks the cycle closed, or add the closed filter to that query. The public report at index.ts:9479 already filters `status === "closed"`, so nothing leaks there. (2) Visible behaviour change: an admin who edits gratitude.pool_per_cycle after a failed close will find the original pool size honoured on the retry. That is the correct invariant but worth stating in the route comment. No migration needed.
- **Reproduction:** Reproduced. server/index.ts:9548 computes `eligible` from live members + live consented-quest counts (index.ts:3500-3510) every call — no per-cycle snapshot. 9554/9561 derive each share from that live basis. 9575-9577 returns 500 mid-loop, before cyclesRepo.upsert at 9596, so earlier recipients are paid and the cycle stays unclosed; dueCycles re-selects it (gratitude-cycles.ts:201 filters only on closed). On the retry the eligible set has drifted, so totalReceived and every credited change. For already-paid members the idempotency key gratitude_pool:{cycleNumber}:{userId} (9573) hits ER_DUP_ENTRY and ledger.ts:316-322 returns ok:true/duplicate:true with NO amount comparison, so the ledger ke

### server/index.ts:1303
- **Fix** (trivial): Make the write an append: hoist the pushed object into a `const event = {...}` and replace line 1303 with `await stageEventsRepo.insert(event);`, deleting the now-unused `stageEventsRepo.all()` read at line 1283 — `insert()` already exists on DbCollection (server/repos/store-db.ts:140) and is the append-only pattern used at :4148, :4224, :4579.
- **Risk:** No migration and no member-visible behaviour change other than older history now being retained (and it also removes the concurrent lost-update). The in-memory cache becomes unbounded in principle, though in practice it is bounded at roughly 11 rows per member since only forward crossings are recorded; if a deployment ever wants a bound it must become an archive/rollup, not a DELETE. Duplicate-id risk from the `stage-${Date.now()}-${random}` key is unchanged: a PK collision now throws from insert() instead of rolling back the replaceAll transaction, still surfacing as a 500.
- **Reproduction:** Reproduced end to end. `PUT /api/admin/players/:id/stage` (server/index.ts:10007) calls recordStageEvent at :10019, and the quest-consent route calls it at :9360. recordStageEvent (:1281) reads the entire collection at :1283, pushes one row at :1294, and at :1303 calls `stageEventsRepo.replaceAll(events.slice(-2000))`, which in server/repos/store-db.ts:124-139 issues `DELETE FROM stage_events` and re-inserts only the rows passed, also resetting the memory cache (`cache = [...rows]`). Rows past 2000 are permanently destroyed. The lost data is only readable from stage_events: `/api/game/progression` builds `history` from `stageEventsRepo.all().filter(userId)` (:9683, :9691-9694) and `/api/prof

### server/index.ts:6377
- **Fix** (small): Stop trusting the body: write the registry unit unconditionally — replace String(unit ?? def.unit).slice(0, 32) with def.unit at server/index.ts:6377 (and use def.unit in the activity string at 6381), then delete the Unit input from the admin form (client/src/pages/Admin.tsx:4757-4759) and the unit field from its form state/POST body (4658, 4678) so the UI stops implying a choice the server ignores. With every row canonical by construction, MAX(unit) in server/lib/health.ts:283 becomes correct and needs no change.
- **Risk:** Rows already written with a non-canonical unit stay mixed, so MAX(unit) keeps mislabelling historical totals until they are backfilled — a one-shot UPDATE regen_entries SET unit = <registry unit per metric_key> is needed for the existing ledger, and that touches append-only-flavoured rows (the 0040 retract-don't-delete contract in server/index.ts:6386-6400 is about values and deletion, not the denormalized unit label, but the correction should be recorded). Removing the Unit input is a visible admin-form change stewards will notice, and any fork whose health-config regen metric intentionally used a per-entry unit loses that freedom.
- **Reproduction:** Reproduced end to end. The admin form (client/src/pages/Admin.tsx:4757-4759) offers a free-text Unit input whose only hint of the canonical unit is placeholder={selected?.unit ?? ""}, and posts it as unit: form.unit || undefined (Admin.tsx:4678). The route POST /api/admin/health/regen (server/index.ts:6362) validates only metricKey against REGEN_METRICS (6367) and writes the unit verbatim: String(unit ?? def.unit).slice(0, 32) (server/index.ts:6377). Grep confirms the only two references to def.unit in server/index.ts are that INSERT and the activity string at 6381 — no comparison, no whitelist, no normalization anywhere. regenTotals (server/lib/health.ts:281-288, SQL at 283) then does "SELE

### server/index.ts:1677
- **Fix** (small): In runRetentionSweep, before `await submissionsRepo.replaceAll(keep)` (index.ts:1678), diff `before` against `keep` and for each dropped row unlink `path.join(UPLOADS_DIR, path.basename(String(row.data?.attachment)))` when that value is a non-empty string not still referenced by any kept row, each in its own try/catch so a missing file cannot abort the sweep.
- **Risk:** The attachment becomes unrecoverable at the same instant the row does — irreversible, with no grace window, so a village that wanted the file longer than the row must raise retention.submissions_days. Two invariants the fix must respect: run the stored value through path.basename (it originates from a request body, and brand images plus investor-vault docs share UPLOADS_DIR, so a `../` or a non-proposal name must not be able to delete a neighbouring file), and skip any filename a kept row still references. Fixing only the sweep leaves the identical gap on anonymizeMember (index.ts:1743-1755), which is the path a member's own deletion request takes.
- **Reproduction:** Reproduced. Public unauthenticated POST /api/work-with-us/attachment (server/index.ts:8520) writes proposal-<ms>-<rand><ext> into UPLOADS_DIR (8504-8511) and returns the filename; the client stores it as data.attachment (WorkWithUs.tsx:227) and posts the form to /api/forms/submit, which persists the whole object as the submission's JSON data (index.ts:2934-2944). Admin.tsx:782-785 renders that value as the only reference to the file. runRetentionSweep() (index.ts:1668-1701) filters handled rows older than retention.submissions_days and calls submissionsRepo.replaceAll(keep) at 1678 — the function contains no fs call at all, and the only unlinkSync in the server is the investor-docs delete at

### server/lib/feedback.ts:107
- **Fix** (small): Import guardedFetchJson from "./toolcheck" and replace the bare fetch (and its AbortController/timer scaffolding) with `await guardedFetchJson(hubUrl, 10_000, { method: "POST", body: payload })`, then mark the rows relayed on return; the existing catch already turns any throw into the log-and-requeue path, so a non-2xx or a refused URL keeps the queue-and-forget semantics.
- **Risk:** guardedFetchJson is https-only and refuses private/loopback/CGNAT addresses, so an operator self-hosting a hub at http://hub.internal or on a VPC/docker-internal address would see the relay stop working — silently, with only a log line (no data loss: rows stay queued with relayed_at NULL). That is the intended tightening but it is a behaviour change for existing self-hosted forks, so it needs a FORK_RUNBOOK.md line stating FEEDBACK_HUB_URL must be https and publicly resolvable (CLAUDE.md requires the runbook updated in the same session for env-var semantics). Secondary: guardedFetchJson throws on non-2xx rather than returning a status, so the specific "[feedback] hub answered NNN" log collapses into the generic "relay skipped: NNN" message; keep the distinction if the hub-side debugging value matters. The relay must stay non-fatal — do not let a refused URL escape relayFeedback, since "the hub is a listener, not a dependency" (feedback.ts:16).
- **Reproduction:** Reproduces against current code. server/index.ts:2145 registers the unattended 15-minute "feedback-relay" job; line 2146 gates only on platform.feedback_relay (ON by default per docs/FORK_RUNBOOK.md:32); line 2147 reads hubUrl straight from process.env.FEEDBACK_HUB_URL with no scheme or range validation; relayFeedback then selects up to 50 rows carrying the full 8000-char member `detail` (feedback.ts:84-86) and POSTs them at feedback.ts:107 via bare global `fetch`. Bare fetch skips the https-only check and private-range check that guardOutboundUrl performs (toolcheck.ts:262-270) and lets undici follow redirects with a fresh DNS resolution per hop, instead of the by-hand per-hop re-guard in d


## LOW

### server/index.ts:4065
- **Fix** (trivial): Add one line beside the other map mounts, immediately after server/index.ts:4065: app.use("/api/assistant/coordinate", requireModule("map")); — mount the exact path, not the /api/assistant prefix, so the unrelated proposal-assistant routes at 8348/8374/8381 stay ungated.
- **Risk:** Behaviour change on forks with map off: the concierge goes from 200 to a 404 {error:"module_disabled",module:"map"} instead of the current {error:"The concierge is off"} shape, and at map lifecycle "preview" it becomes admin-only. No legitimate client is affected — client/src/pages/VillageMap.tsx:397 is the only caller and that page's own /api/map fetch already 404s when the module is off. The loop e2e is safe: it sets map to public at server/loop.e2e.test.ts:1163 before the concierge calls at 1225. Respect the invariant that the mount must be registered before the handler (4065 precedes 4334).
- **Reproduction:** The structural claim reproduces exactly: server/index.ts:4062-4065 mounts requireModule("map") on /api/map, /api/circles, /api/admin/circles and /api/admin/map only, and the concierge is registered at server/index.ts:4334 on /api/assistant/coordinate — outside every one of those prefixes, with no other app.use covering /api/assistant (only express.json, the /api/admin audit hook at 2886, and CORS at 2906 are global). Its only guards are authedUser (4335) and boolVar("map.concierge_enabled"), whose default is "true" (shared/gameVariables.ts:356-362). server/lib/modules.ts makes an absent module_settings row OFF, so on a fresh fork the map module is off while this route still serves: it reads 

### client/src/index.css:503
- **Fix** (trivial): Change the guard at index.css:503 from `@media (max-width: 767px)` to `@media (pointer: coarse)` so the 16px floor keys on the touch device rather than on viewport width — matching the coarse-pointer block already at line 520. If you want to exclude touchscreen laptops, use `@media (pointer: coarse) and (hover: none)` instead.
- **Risk:** Visible layout change members will notice on iPad and other coarse-pointer devices at 768px and up: every input, textarea and select jumps 14px to 16px, so dense screens (Admin tables, the Setup Wizard, filter selects like Forum.tsx:113 and 118) get taller and wider controls and some may wrap. Plain `pointer: coarse` also catches Windows touchscreen laptops at desktop widths, which is why the `and (hover: none)` variant is the more conservative choice. No migration, no server behaviour, no invariant touched.
- **Reproduction:** Reproduced. client/index.html:6 permits zoom (maximum-scale=5), so iOS focus-zoom is live. The only 16px floor in the whole stylesheet is client/src/index.css:503-509, gated on @media (max-width: 767px) — grep for "767" returns that single hit and client/src has exactly one CSS file. The controls named in the finding sit at 14px above that breakpoint: client/src/pages/Forum.tsx:377-379 is a raw textarea with className "w-full text-sm …" (14px at every width, no responsive variant), and client/src/pages/VillageMap.tsx:416-422 is the concierge input with the same "text-sm" class, with the "Ask" button as its flex sibling at line 424. The shadcn primitives fail from the other side: client/src/c

### client/src/pages/Forum.tsx:158
- **Fix** (trivial): Using the Edit tool only (never shell redirection or Set-Content), replace each mojibake run with the intended character: em dash at lines 4 and 158, ellipsis at 114, 240 and 378, left arrow at 235 and 248 — and drop the stray U+0090 control char that trails both arrows. Same one-character-per-site fix applies to scripts/smoke-all-modules.mjs:43,187.
- **Risk:** Two traps in the fix itself. (1) Forum.tsx starts with a UTF-8 BOM (ef bb bf) — an Edit-tool replacement preserves it, but any rewrite of the whole file through PowerShell will either strip it or re-corrupt every non-ASCII character, reproducing the defect. (2) The visible corruption is three distinct source characters, not one, so a single blind find-and-replace on "â€”" leaves the ellipses and arrows garbled. Members will notice the text change on /forum, which is the intent. No migration, no behaviour change, no invariant touched.
- **Reproduction:** Reproduced at byte level and in the shipped bundle. client/src/pages/Forum.tsx carries literal double-encoded characters on 7 lines: em dash (c3 a2 e2 82 ac e2 80 9d) at 4 and 158, ellipsis (c3 a2 e2 82 ac c2 a6) at 114, 240, 378, and left arrow (c3 a2 e2 80 a0 c2 90) at 235 and 248. Not a tool-rendering artifact — the same file shows a clean U+2026 at line 130 and a clean c2 b7 at line 261, and shared/modules.ts:142 has a clean em dash, so the decoder is fine and Forum.tsx is the outlier. Render path is unguarded: App.tsx:183-184 routes /forum and /forum/:id; Forum.tsx:27-32 gates only on module presence; line 240 (Loading…) and line 248 (← All threads) are on the unconditional path of ever

### server/index.ts:9746
- **Fix** (trivial): Swap both reads to the live merged config: at server/index.ts:9746 use `currency: mergedConfig().currency.name`, and at server/index.ts:9647 use `${mergedConfig().currency.nameLower}`. mergedConfig() is synchronous and already backed by the boot-loaded brandRepo cache (server/index.ts:632, 736), so no await or signature change is needed at either call site.
- **Risk:** Behaviour change members would notice, in the intended direction: the /profile ledger caption and future cycle-close pulse lines start using the village's own word. Respect the event-spine invariant that pulse text is persisted at write time — do not backfill health_events, so pulse rows written before the fix keep saying "gratitude" and the two words will coexist in the Pulse history. No migration. Does not affect the brand ratchet baseline (GAME_CONFIG/mergedConfig are not banned tokens). Leaves the Quests.tsx and other client-copy hardcodes untouched, so /quests still says "Gratitude" — including the non-marketing quest-card reward label at Quests.tsx:314 — which is worth tracking separately, ideally by adding the currency term to BANNED in scripts/check-brand-refs.mjs with a fresh baseline.
- **Reproduction:** Reproduced. Setting the currency name in Admin > Setup Wizard > Identity (client/src/pages/Admin.tsx:6059) writes the brand doc; mergedConfig() (server/index.ts:1179) overlays it and is what /api/game/config (server/index.ts:9032) and the gratitude send route (server/index.ts:9414) read — so /gratitude correctly says "Seeds" (GratitudeWall.tsx:33). Two neighbouring sites read the platform default instead: (1) server/index.ts:9746 returns `currency: GAME_CONFIG.currency.name` from GET /api/game/ledger (handler opens 9727), and the client renders that string verbatim on /profile — client/src/components/ProfileJourney.tsx:156 `Every movement of {ledger.currency ?? "recognition"} on your account

### client/src/pages/Quests.tsx:200
- **Fix** (trivial): Change `sticky top-[64px]` to `sticky top-24` on Quests.tsx:200 so the offset matches the nav's real 96px height; separately, at Quests.tsx:332 branch the empty state on `quests.length === 0` to say the village has no quests posted yet (with the Propose a Quest link) and keep the filter wording only when a filter is actually narrowing a non-empty list.
- **Risk:** `top-24` is still a hard-coded mirror of the nav height — if Layout's logo height or `py-4` ever changes, the offset silently drifts again (a `--nav-h` CSS var set in index.css and used by both would be the durable version). The empty-state rewording must stay neutral: with the fetch failure at Quests.tsx:116 swallowed, "no quests yet" would present an API outage as an empty village, so do not claim the village posted nothing — say the board could not be loaded or is empty without asserting why.
- **Reproduction:** Half of the finding reproduces, half does not. CONFIRMED by measurement in a running dev build: Layout.tsx:53-58 renders the nav as `sticky top-0 z-50 bg-teal-deep` with `container py-4` around a 64px logo, measured height 96px at both 1280px and 375px; Quests.tsx:200 sticks the filter bar at `top-[64px] z-30`, so scrolled I measured navBottom=96, filterTop=64, overlap=32. The first circle chip measures top=80/bottom=104 and the "Circle:" label top=82, so 16 of the chip's 24px and the label are behind the opaque higher-z nav — only ~8px of the primary filter row is visible and clickable. REFUTED: the headline scenario. A new village never has zero quests — server/index.ts:2786-2789 (`const e

### server/index.ts:4780
- **Fix** (small): In GET /api/admin/stays, before the res.json at index.ts:4780, count non-ended/non-cancelled stays per accommodation_id from the already-loaded `stays` array and attach `activeStays` plus `overCapacity: activeStays > a.capacity` to each accommodation row; then render capacity and the flag in the rooms panel (Admin.tsx:3512) and beside the room name in the stays table (Admin.tsx:3566).
- **Risk:** Purely additive response fields, no migration, no change to activation or nightly posting (keep it a flag — a hard block would contradict docs/modules/stays.md:164 and belongs to Phase 4). The real risk is noise: because the admin UI offers no capacity input (Admin.tsx:3545-3551, roomForm hardcodes 1), every room is capacity 1, so a village with a genuine multi-bed dorm would see a permanently-lit flag and learn to ignore it. Add a capacity field to the add-room form and room editor in the same change, or the flag is worse than nothing.
- **Reproduction:** Reproduced. capacity is written (server/index.ts:4790), editable (4800), and read into AccommodationRow (server/lib/stays.ts:99,116) — and then never used for any decision or display anywhere in server/, shared/, drizzle/, scripts/, or client/. POST /api/stays/request (index.ts:4693-4714) gates only on auth, stay.guest_booking_enabled, the 5/day limiter, and accommodation existence; arriveOn is stored (4707-4708) but never compared to any other stay. POST /api/admin/stays/:id/activate (4836-4864) gates only on isAdmin, row existence, status not ended/cancelled, and a posted credit rate, then flips status to active (4851). GET /api/admin/stays (4762-4781) returns accommodations + stays with o

