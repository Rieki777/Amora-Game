# Round E, 2026-08-10: the map on a phone

Rye's round, from Safari on a phone. Eleven items. This is the CODE half; the
art half was deliberately held back (see **Deferred**).

**State:** uncommitted in the shared worktree, artifact `v0.8-roundE`, all gates
green. Nothing pushed, nothing deployed.

## Lane note, read first

This landed while the other lane was live in the same worktree. Their D7
vocabulary work committed as `238784e` and `096a649` mid-round, and their
`BUILD_VERSION` moved twice underneath (`roundD` to `roundD1` to `roundD2`).

Nothing collided, because every edit here went through an anchor script rather
than a wholesale write. `patch_e9_version.py` aborted rather than guess when
the label moved, which is the protocol doing its job, and now anchors the
FAMILY instead of the exact predecessor.

Verify this round survived, from the shared checkout:

```
grep -c PLATE_LEASH DISTRICT_LEASH PLATE_COST nearestSeal TOAST_MAX  -> 2 2 4 3 2
grep -c "sr.onchange=()=>logEdit" grounds-v0.html                    -> 0
grep -o "BUILD_VERSION='[^']*'" grounds-v0.html                      -> v0.8-roundE
```

If the artifact is ever overwritten, re-run `patch_e1` through `patch_e9` in
order against the CURRENT file on the branch. They are re-runnable per step.

## How to verify this round, and the way to get it wrong

Round E's ARTIFACT and its TWO SUITES are a matched pair. `verify_features.js`
and `verify_badges.js` assert the new intent, so main's copies fail against a
round E artifact **by design**: `no two marks overlap anywhere` and `crowded
rings collapse to a counted seal` are the two rules this round replaces, and
main's fan test crashes on `.dataset` of undefined hunting for a ring a
neighbour collapsed. Those failures are the signal, not a problem.

```bash
git archive origin/main docs/prototypes | tar -x -C /tmp/pinned
cp docs/prototypes/qa/verify_{features,badges}.js /tmp/pinned/docs/prototypes/qa/
cd /tmp/pinned/docs/prototypes
GROUNDS_FILE="file:///.../ga-map/docs/prototypes/grounds-v0.html" node qa/verify_features.js
```

**PIN BOTH SIDES OR NEITHER.** Pinning the artifact to a SHA and reading the
suite off the shared working tree cost a false bug report this round: a
`window.SCENE` term that existed only in another lane's uncommitted mid-edit
state was reported as a defect on main, where it has never existed. Half-pinning
looks rigorous, which is what makes it expensive. One command closes it:
`git show origin/main:<suite> | grep -c "<marker>"`.

Two more that came out of the same exchange:

- **Grep for the syntax that would EXECUTE, not for the name.** `window.SCENE`
  matched the docstring *explaining* the trap and nearly produced a second false
  alarm. `typeof window.SCENE\|window\.SCENE &&` cannot match prose. Comments
  here name the bugs they prevent, so this recurs as the comments improve.
- **Prove which file a gate read.** The export carries the artifact's version, so
  `map_scene.version == v0.8-roundE` is what makes a schema green provably about
  this round rather than about a stale file in the working directory.

## What changed

| # | script | what |
|---|---|---|
| 1 | `patch_e1_plates.py` | The plate leash. Every escape a plate had was upward and uncapped, five passes deep with a four-step dodge inside each; a plate in a cluster ratcheted off its building into the column at the top of the screen. A plate now tries a short list of offsets near its own crown and DOES NOT DRAW if none is clear. Building names are budgeted per frame, nearest the middle of the screen first: 7 in a pocket, 16 on a desk. |
| 2 | `patch_e2_ring.py` | Rings stop fighting. The three passes that let neighbours collapse each other's rings are gone, and a tap no longer throws the ring wider. A ring is solved against ONE building, its own. Seals grew 22px to 28 (16 to 22 at mid zoom). |
| 3 | `patch_e3_toasts.py` | The toast lane moves to the bottom in a pocket, capped at three alive at once, held to a readable width. The top band was the vitals bar, the plates, the open card and a toast outranking all of them. |
| 4 | `patch_e4_scalelog.py` | `sr.onchange` was assigned twice and the second clobbered the first, so every size edit logged a `to` with no `from` and a no-op drag still wrote an edit. |
| 5 | `patch_e5_taps.py` | The bill for #2. Allowing overlap cost tap correctness: 2 marks in 34 answered a neighbour's door. The plane now resolves a tap by DISTANCE, not paint order. Back to 34/34. |
| 6 | `patch_e6_districts.py` | A district names a region, not a roof, and at the far zoom it is the only wayfinding on screen, so it gets a wider table and never goes silent. |
| 7 | `patch_e7_leastbad.py` | Placement is priced, not filtered: mark 8, plate 4, icon 2. A plate that must draw takes the cheapest spot instead of the rawest. |
| 8 | `patch_e8_districtroom.py` | Two fixes the gate found: the table was still too small, and `marksHit` was not measuring the boxes the gate measures (`.banner` is `translate(-50%,-100%)`, so its `left` is the centre and its `top` is the BOTTOM edge). Rectangles now. |
| 10 | `patch_e10_catchment.py` | **The map lane caught this and they were right.** #5 asserted 34/34 marks answer their own tap, which is true and insufficient: nearest-centre resolution gives two centres `d` apart a catchment of `d/2`, and a fingertip is 30 to 45 px. Two marks 13 px apart resolved perfectly at their exact centres and opened the wrong door under a thumb. Marks of DIFFERENT buildings now keep a floor, bought with a few degrees of ring rotation. The radius never changes and nothing collapses, so no neighbour places anyone's marks; the whole ring turns at once, so each kind keeps its seat relative to the others. |
| 11 | `patch_e11_floor.py` | The floor is the seal's WIDTH (28, catchment 14, covering the ink), not the 22 derived when seals were 22 px. **Do not raise it without re-running `qa/_probe_floor.js`.** |
| 9 | `patch_e9_version.py` | `v0.8-roundE`. Family unchanged, so the importer still admits it. Run last. |

## Measured, pocket viewport (390x844, `hasTouch` alone)

`qa/_shot_e_pocket.js`, and the drift figure is from the plate's ANCHOR, the
point directly above the roof, not from the building centre.

| | before | after |
|---|---|---|
| building-name drift, worst | **251 px** | **40 px** (the leash bound) |
| building-name drift, median | 24 px | 13 px |
| names drawn (pocket, z 1.35) | 12 | 7 (budget) |
| rings collapsed by neighbours | 7 of 19 | **0 of 19** |
| marks answering their own tap | 32/34 | **34/34** |
| closest two doors of different buildings | 13.0 px (catchment 6.5) | **28.3 px** (catchment 14.2, ink 14) |
| district names at z 0.62 | 3, one clipped under the vitals bar | 5, over their regions |

## Gates

All green: `check_blocks`, `verify_doors`, `verify_features`, `verify_badges`,
`verify_loom`, `verify_skin_bridge`, `verify_vocab_bridge`, schema round-trip.

Three assertions in `verify_features.js` encoded the OLD intent and were
rewritten rather than relaxed, because "no two marks overlap anywhere" is a
rule this round deliberately removed:

- `no two marks overlap anywhere on the land` became **every mark stays on its
  own building's ring** (`reach` = the widest ring the layout can hand out) plus
  **two marks on one ring never touch**, which is the 44 px tap target and a
  correctness rule rather than a tidiness one.
- `every mark answers its own tap` now asks `nearestSeal()` instead of
  `elementFromPoint`, because stacking stopped being the mechanism.
- `crowded rings collapse to a counted seal` became its inverse, **no ring is
  collapsed by its neighbours**.

**Then the map lane caught that ONE CAMERA PROVES ONE CAMERA**, which is the
more important of their two catches. The thumb check ran at z 1.7, and that is
the easy end: `off` comes from `_crownOff`, which shrinks with the LOD scale, so
pulling back tightens every ring AND converges the buildings at once. Swept:

| z | ink | doors | unreliable | worst gap |
|---|---|---|---|---|
| 1.00 - 1.20 | 22 | 50 | **4 (8%)** | 12.3 px |
| 1.35 | 22 | 50 | 0 | 24.9 px |
| 1.45 - 1.60 | 28 | 50 | **3 (6%)** | 18.8 px |
| 1.70 - 2.00 | 28 | 50 | 0 | 28.3 px |

**No rotation budget clears the whole range**, and that is not a tuning failure.
Measured across (budget, step) tables from 18/3 to 42/3, the best clears only
from z 1.7 up. At z 1.0 there are 50 doors in a converging area and the screen
does not hold 50 exclusion zones. A zoom gate would hide 50 doors to fix 4, so
it was not taken.

The second band exists for a reason worth keeping: `.bmid` takes a seal from
22 px to 28 at z 1.45 while the ring geometry does not change, so **the ink
grows before the ring does**. Moving that boundary up is the cheap lever if the
budget ever needs to come down.

So the swept check is a RATCHET, like the brand refs: the top of the range must
stay perfect, the known shortfall is recorded rather than hidden, and anything
that makes it meaningfully worse fails.

  D2 A1: from z 1.7 up every door has room for a thumb (1.7:0 2:0)
  D2 A1: the crowded low-zoom bands stay within budget (worst z 1: 4 of 50, budget 4)

One assertion was ADDED, and it is the one a passing tap test hides:
**every mark has room for a thumb**, the minimum centre-to-centre distance
across DIFFERENT buildings, against the seal's own ink width. A tap test asks
whether a click at an exact centre resolves; this asks how much room a finger
has. If it ever fails, do NOT raise `MARK_FLOOR`. The solver is greedy and a
higher floor has measured WORSE (floor 28 reached 20.8 px on nine-degree
rotation steps, against 23.1 px for floor 22, because a ring that keeps hunting
past a good-enough rotation settles somewhere that costs a later ring more than
it gained). Re-run `qa/_probe_floor.js` and widen `RING_ROT` instead.

`verify_badges.js` fan test used to hunt for a ring a NEIGHBOUR had collapsed,
which no longer happens. It now MAKES an intrinsic cluster (shrink a building
carrying several kinds until its own ring stops fitting) and fans that, which
is deterministic and tests the feature on its own terms.

## Deferred to the art round, by Rye's call

Rye chose code first, art second, so these are specified and not built:

1. **Badge icons regenerated** in the house hand. Size is done; the art is not.
2. **Under construction as its own sprite set.** Today it is an SVG scaffold
   overlaid on the finished sprite (`.scaffold`, `.ph2`). Two prerequisites:
   `sprites/` is EMPTY (the art lives embedded as data URIs, so run
   `sync_sprites_from_artifact.py` first), and `gen_sprites.py` is
   text-to-image ONLY, so "the finished building as context" needs an
   image-to-image mode added.
3. **Seats as role plus human.** Rye chose BOTH: a circle-within-circle glyph on
   the map, and a generated character per open role in the sheet a tap opens.
   The dashed rim that reads as "open seat" today is `.bseal.r-open .rim`.
4. **The four pocket-bar icons** are emoji (`🏞 ☰ ⚑`), not art.

`GEMINI_API_KEY` is NOT in `ga-map/.env`. It is in
`C:\Users\taren\Downloads\regen-civics-clean\.env` (new `AQ.` prefix, so an
`AIza` grep misses it).

## Open

- **The streaks** ("lines coming from some of the icons", over the water). Not
  reproduced: it wants a real iOS Safari session, and the one candidate already
  on the board is the surround plate's healed capture boundary in the far
  south-west. Needs a repro note: which zoom, and does it move with the land or
  stay put on the screen.
- **`plateBudget()` is a guess.** 7 in a pocket, 16 on a desk. It is one line,
  and it is the dial to turn if Rye wants more names or fewer.
- **The help icon** (item 11: comments that sandwich out with deep links) is
  untouched. The addresses it needs already exist: `#/place/<key>?item=<kind>:<id>`.
