# Build-out session — Amora Living Map (paste this whole file as your prompt)

You are continuing a multi-session build of the Living Map for the game-amora platform. Everything is committed in the connected `Amora` folder. Stage files into the sandbox, edit there, commit back with device tools, and update the Cowork artifact `amora-living-map` after each milestone.

## Read first, in order

1. `game-amora/docs/LIVING_MAP_PLAN.md` — the spec. Decisions **D1–D11** LOCKED (D9 lens-not-ledger, D10 forum conversations on structure cards, D11 per-layer graphics agency + land-only plate — all added 2026-08-08 evening; read §4.3c). §5.2 schema is the export contract; §5.3 Stage 3b, §4.3b, and the D1 paint stack are BUILT (see below).
2. `game-amora/docs/prototypes/grounds-v0.html` — the live prototype **v1.0**, ONE self-contained file (~3.9 MB). Waves 1–3 of `FIXES_TO_MAKE_2026-08-08.md` + `FEATURE_GEOMETRY_LAYER.md` are SHIPPED: all 11 defects + held-key sweep; painted mode = deterministic filter over the harmonized satellite (brush/palette sliders, generated plate retired to six donor floats); STYLE_NATURAL re-rolls (10 families, zero architecture) + waterfall/pool families (30 total); the geometry layer Stages A–C (25 migrated features with real phases, honest vector floor, ✎ Draw editor with snap/measure/undo-stable ids, footprints with hit/drag/remove knock-ons, flows via drawn routes); persistence (autosave + beforeunload + restore-as-import), slug keys, `qa/check-schema.js`. Three `<script>` blocks: scene data + georef; engine/render (the two plate blobs live at `/*PLATE_HOOK*/` and `/*PAINT_HOOK*/`); HUD/build/inspect/resolver/curation/Maia (sprites at `/*SPRITES_DATA*/`).
3. `game-amora/docs/prototypes/QA_TEST_PROMPT.md` — the v1.0 QA matrix (28 sections incl. geometry + persistence). Rye may paste a bug report — fix those FIRST. Harness: `qa/run.js` (+ `check-schema.js`).

## Current state (v0.9, Playwright-verified 2026-08-08, zero page errors)

Everything from v0.8 (QA-fix pass; inspect card with audited edits; boundary editor + stranded flags + WGS84 GeoJSON export; deterministic quest resolver with visible reasoning; Flows lens + §4.3b panel strips; §5.2-shaped export), plus the paint stack:

- **Painted plate (D1+D11)**: Gemini 3 Pro Image translated the satellite foundation into an oil-painted **pure-terrain** plate — land only, NO buildings painted (v2; sprites carry all built form, so scenery can never contradict build-mode edits). Same frame → georef stays true; embedded 2400×1600 JPEG ~914 KB; provenance recorded in the export's art_manifest. Terrain row in the ✦ Theme panel: **Satellite (default) | Painted | Vector**; boundary/roads/mist/Vision render over any mode; wash applies only to satellite; minimap follows; vector repaint is deterministic (local seeded stream).
- **Sprite set**: 28 painterly sprites, one per icon family, in Rye's locked art direction — **futuristic solarpunk-elven, grown-not-built** (flowing living roofs, sculpted timber ribs, curved glass, brass inlay; NOT medieval/rustic — AoE was inspiration only, this is our own theme). Generated by `gen_sprites.py` (magenta-keyed to transparency, ~176px, ~60–85 KB each), embedded via `embed_sprites.py`. **Painted** is a 4th icon-style chip; families without an approved sprite fall back to their SVG emblem, honestly; blueprints stay emblematic.
- **Curation grid** (✦ Sprites in the build footer): 28 cells, per-family approve (live map-wide swap) and ↻ re-roll marking (accumulates a `gen_sprites.py fam1 fam2…` command in toasts — re-rolls run in the pipeline, never in the browser; the key stays out of the HTML). Approvals + re-roll list export inside `map_scene.art_manifest`.
- Pipeline beside the prototype: `fetch_sat.py`, `gen_plate.py` (legacy masterplan path), `embed_plate.py`, **`embed_paint.py`** (painted plate → PAINT_HOOK), **`gen_sprites.py`** + **`embed_sprites.py`**, `sprites/` (the 28 keyed PNGs, skip-existing for cheap re-rolls), `amora-plate-v1.png` (plate source).

Masterplan: `game-amora/AMORA MASTER PLAN V7 (1).pdf`. Pin 9.2320128,-83.8343203 → world (1520,800), 1.08 m/unit (`GEOREF`, mirrored from fetch_sat.py; `worldToLatLon` verified round-trip).

## Environment

- **GEMINI_API_KEY is live** and sits in `game-amora/.env` (build key added 2026-08-08; probe with a models-list call first — `gemini-3-pro-image` is the workhorse). Never put the key in the HTML or any committed doc besides `.env`.
- **Image-approval workflow (Rye's standing rule): deliver EXAMPLES for approval before running any bulk image suite.** One or two samples → user approves → then the batch. Style direction is locked (futuristic solarpunk-elven, bright high-key, vivid, magenta background for keying); subjects live in `gen_sprites.py` FAMILIES.
- Verify EVERYTHING with Playwright (chromium preinstalled): pageerror + console listeners, screenshot each feature, LOOK at the screenshots, iterate to zero errors. That is the bar.

## Work queue, priority order

1. **QA fixes** — everything in Rye's pasted QA report, blockers first.
1b. **Geometry Stage D** (optional per spec): computed vitals from geometry — Canopy ha from forest zones; zone-aware quests.
1c. **Open Rye decisions to fold in when answered**: public roads locked? · Fix-12 phase mapping confirmation · partial-stranding flag · zone vocabulary.
2. **The Talk tab (D10, §4.3c)** — conversations on the cards, lens-not-ledger:
   * `SCENE.threads` sample data (labeled sample) shaped like the production forum rows (forumPosts/forumReplies lineage) with a `structures:[keys]` multi-address array — the join, not a copy.
   * Portal panel gains **Talk**: threads addressed here (most recent first), inline reply composer, "start the conversation" (creates a thread with this structure pre-picked, never auto-spawned), honest empty state.
   * Multi-address picker at thread creation (checkbox the places it lives); generalize `resolveQuestAddress` → `resolveAddress(kind)` so unpicked threads resolve deterministically (explicit picks → author role home → circle home → lexicon → commons pool at Community Center); extend the resolver demo card to threads.
   * Audience: each sample thread carries an audience field; the map renders member-only threads as counts-not-content when simulating the visitor portal.
   * Integration: new replies ripple the pulse + banner glint (rate-limited); reply-at-your-structure joins the attention cycle; deep links `#structurekey` open the camera there (both doors get handles).
   * Export: `forum_threads` + `thread_structures` in §5.2/forum-schema shape — the standard-format-elsewhere rule made literal.
3. **Graphics section (D11)**: unify the Theme panel rows — Terrain + Structures (built) plus **Ambient life (Full | Calm | Off — reduced-motion story)** and **Label density (names+counts | names only)**; note per-member persistence as the production plan.
4. **Sprite polish**: per-family footprint scale in ARCHMAP (a tower ≠ the great hall), seeded per-instance variation (mirror flip + slight hue shift from the structure's hash — five springs stop being quintuplets, deterministically); re-roll families Rye marks in the curation grid (samples-first rule).
5. **Memory layer surfacing**: origin story shown in the portal panel Overview; photo-timeline placeholder with honest empty state.
6. **Maia × new surfaces**: directives for Flows/Painted lenses, stranded/needs-steward/new-reply items in the attention cycle, resolver as a Maia answer.
7. **G0 execution prep**: hand the export JSON to the real repo — migration + `server/seeds/` from `amora-scene.json` (the founder's hand becomes ground truth).
8. After each milestone: commit `grounds-v0.html` to `docs/prototypes/` + update artifact `amora-living-map`.

## Guardrails

**The lens rule (D9) governs every new surface: the map addresses, never stores — anything shown here must live canonically in a module, readable in another standard format; delete the map and no data dies.** Mirror, not fantasy (no unlabeled invented data; sample vitals stay labeled "sample"). Deterministic charm (seeded ambient; resolver pure and zero-token; terrain repaint idempotent). Labels always win. ONE self-contained HTML, ≤ 8 MB (currently 4.4). Key only in `.env`. Structures thin, facts narrow, counts computed. Samples before suites for all image generation. AoE-grade bar — and since v0.9 the bar is our own: screenshot wide/close/night after every change and look at them.

Report at the end: shipped, verified (zero-error statement), blocked (why), and the exact next queue.
