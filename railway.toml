[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile=false && npm run build"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[build.nixpacksPlan.variables]
NODE_ENV = "development"
